/**
 * Why `APP4-S01` verifies a contact.
 *
 * `SUBMISSION` — the purpose that precedes customer identity. `STEP_UP`
 * re-proves possession for a sensitive action and belongs to whatever surface
 * performs one; this screen never issues it, and pinning the value here rather
 * than at the call site is what stops it from becoming a prop someone widens.
 */
import { IssueVerificationChallengeBodyPurpose } from '@embroidery/api-client';

export const VERIFICATION_PURPOSE = IssueVerificationChallengeBodyPurpose.SUBMISSION;
