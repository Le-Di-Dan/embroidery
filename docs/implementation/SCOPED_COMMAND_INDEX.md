# Scoped Command Index

Canonical discovery index for every command that used to be a root
`package.json` alias. Locked by `GOV-Q01-C1` (2026-08-04).

Root `package.json` is **not** a command registry. It carries only
repository-global orchestration, the global quality controls and shared
infrastructure/database lifecycle — 30 scripts. Everything else lives with its
owner and is invoked directly. This file is how you find it.

## 1. How to read this

| Field | Meaning |
|---|---|
| **Command ID** | Stable semantic id. Cite it in checkpoint prompts and reports. |
| **Former root alias** | The `package.json` key removed by `GOV-Q01-C1`. Never re-add it. |
| **Owner** | The workspace, tool file, historical checkpoint or release activity that owns the command. |
| **Direct invocation** | Exactly what to run. |
| **Usage scope** | The impact reason that justifies running it. |
| **Status** | `ACTIVE_SCOPED`, `HISTORICAL_SCOPED`, `RETIRED_AGGREGATE` or `BROKEN_LEGACY_REFERENCE`. |

Statuses:

- **`ACTIVE_SCOPED`** — current, run when its usage scope is met.
- **`HISTORICAL_SCOPED`** — owned by a delivered checkpoint. Still runnable and
  still authoritative over its own inputs, but run **only** when the current
  change touches those inputs, and say why in the report.
- **`RETIRED_AGGREGATE`** — a command that combined unrelated work. It is
  **not** a recommended entry point. Its owner commands are listed instead. It
  may be reconstructed only by an explicitly authorized release/regression plan
  (`VALIDATION_GOVERNANCE.md` §7).
- **`BROKEN_LEGACY_REFERENCE`** — the former alias pointed at a target that does
  not exist. None at the time of writing.

**This index is documentation.** There is no command runner, dispatcher, JSON
registry, generated script or CLI behind it, and none may be added. Do not add a
root script merely to make a command discoverable — add or update a row here.

### 1.1 APP3 checkpoint gates carry a frozen APP3-era baseline

Recorded by `APP5-X01` (2026-08-17), closing
`FU-APP5-B01-APP3-SURFACE-GATE-01`. **Source reconciliation only — no gate was
run and no gate logic was changed.**

Every APP3 checkpoint gate asserts, alongside its own rules, that three
*repository-global* artifacts are unchanged since its checkpoint shipped:

```text
tools/app3-accepted-surface.mjs   last recorded world = APP3-S06
                                  → 37 paths / 42 operations / 84 schemas
EXPECTED_MIGRATIONS = 34          (tools/check-app3-*.mjs, 17 declarations)
ROOT_SCRIPTS        = 30
```

The current repository is **58 paths / 63 operations / 132 schemas** and **35
migrations** (`0035_add_app5_intake_provenance`, owned by `APP5-DB01`). The
surface table has no entry after `APP3-S06`, so it cannot describe any world
APP4 or APP5 left behind: those assertions fail *by construction* on the current
tree, on every branch, whatever the change under review. `ROOT_SCRIPTS = 30` is
still correct.

**44 of the 54 APP3 gate commands in §3/§3.1 reach one of those assertions**
(directly or through a chained module) and are therefore reclassified
`ACTIVE_SCOPED` → `HISTORICAL_SCOPED` here. The ten that do not —
`CMD-CHECK-APP3-A01`, `-A02`, `-A03`, `-S04`, `-S08`, `-S09`, `-CLOSURE`,
`-F01-FONT-ASSETS`, `-W01A-OUTPUT`, `-W01B-GRAMMAR` — keep `ACTIVE_SCOPED`.

Consequences, binding on any later checkpoint:

- A frozen-count failure from one of these gates is **not** evidence of a defect
  in the change under review, and must never be reported as one. `APP5`'s
  pre-E01 audit verified this directly: `node tools/check-app3-s01.mjs` produced
  the identical five failures on a **pristine** tree.
- These gates are **not** current acceptance evidence for any non-APP3 phase and
  must not be listed as required commands for one.
- Their APP3-specific rules remain authoritative over APP3 inputs. Run one only
  when the change touches those inputs — and read past the frozen-artifact
  failures rather than "fixing" them by editing counts.
- Repairing the baseline (teaching the surface authority about post-APP3 worlds,
  or removing the global assertions from checkpoint-scoped gates) is **APP3 /
  tooling maintenance**. It is deliberately not done here: rewriting 44 gates to
  make old counts green is exactly the change that would destroy the evidence
  those gates exist to carry.

Some §3 usage-scope cells still quote the frozen numbers in prose (for example
`CMD-CHECK-APP3-S10` and `-S11`, "an unchanged OpenAPI artifact of 37 paths / 42
operations / 84 schemas"). Those sentences are accurate as a record of what the
gate asserts, and are left as written; this subsection is what says the assertion
is historical.

## 2. Root `package.json` — what stays

Exactly 30 scripts, in three groups. A `sonar` script may be added only when
`FU-GOV-Q01-SONAR-COMMAND-01` produces a portable, token-safe invocation.

| Group | Scripts |
|---|---|
| Repository-wide orchestration | `dev`, `build`, `clean` |
| Global quality controls | `format`, `format:check`, `lint` (SonarQube: see `VALIDATION_GOVERNANCE.md` §6) |
| Shared Docker/infrastructure lifecycle | `docker:dev:config`, `docker:dev:build`, `docker:dev:up`, `docker:dev:ps`, `docker:dev:logs`, `docker:dev:down`, `docker:debug:up`, `docker:debug:down`, `docker:quality:up`, `docker:quality:down`, `docker:clean:volumes` |
| Shared database lifecycle and operations | `db:up`, `db:down`, `db:logs`, `db:generate`, `db:generate:custom`, `db:migrate`, `db:status`, `db:reset`, `db:backup`, `db:restore`, `db:pitr:rehearse`, `db:retention`, `db:anonymize` |

`docker:quality:up` / `docker:quality:down` stay because they manage shared
**SonarQube infrastructure**, not a validation alias.

## 3. Index — 71 removed aliases

| Command ID | Former root alias | Owner | Category | Direct invocation | Source / target | Usage scope | Status |
|---|---|---|---|---|---|---|---|
| `CMD-TYPECHECK-ALL` | `typecheck` | release/regression activity | aggregate | `pnpm --filter <workspace> typecheck` | `turbo run typecheck` across 21 workspaces | Only under an authorized release/regression plan; otherwise typecheck the workspace you changed. | `RETIRED_AGGREGATE` |
| `CMD-TEST-ALL` | `test` | release/regression activity | aggregate | `pnpm --filter <workspace> test · node --test "tools/*.test.mjs"` | `turbo run test` across 21 workspaces plus the tools suite | Only under an authorized release/regression plan. Its two halves are separate owners and must be run as such. | `RETIRED_AGGREGATE` |
| `CMD-TEST-COVERAGE-ALL` | `test:coverage` | release/regression activity | aggregate | `pnpm --filter <workspace> test:coverage` | `turbo run test:coverage` across 21 workspaces | Produces the lcov inputs `sonar-project.properties` names; run under a Sonar or release activity. | `RETIRED_AGGREGATE` |
| `CMD-QUALITY-E2E` | `quality:e2e` | release/regression activity | aggregate | `pnpm --filter @embroidery/e2e-testing check:e2e · pnpm --filter @embroidery/e2e-testing e2e:full` | `check:e2e` + the full Playwright matrix | The browser tier. Only under an authorized release/regression plan; never a default acceptance criterion. | `RETIRED_AGGREGATE` |
| `CMD-TEST-OBJECT-STORAGE-CONTRACT` | `test:object-storage:contract` | `@embroidery/object-storage` | contract test | `pnpm --filter @embroidery/object-storage test:contract` | object-storage adapter contract suite | Any change to the object-storage abstraction or an adapter. | `ACTIVE_SCOPED` |
| `CMD-TEST-ASSET-INTAKE-INTEGRATION` | `test:asset-intake:integration` | `@embroidery/api` | integration test | `pnpm --filter @embroidery/api exec jest --config jest.asset-intake.config.mjs --testPathPatterns=asset-intake(-lifecycle)?[.]integration` | apps/api asset-intake integration specs | Asset intake API, policy or lifecycle changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-ASSET-INTAKE-API` | `test:asset-intake:api` | `@embroidery/api` | API test | `pnpm --filter @embroidery/api exec jest --config jest.asset-intake.config.mjs --testPathPatterns=asset-intake-api` | apps/api asset-intake API specs | Asset intake contract or controller changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-ASSET-INTAKE-GATEWAY` | `test:asset-intake:gateway` | repository tool | gateway seam test | `node --test tools/nginx-upload-seam.test.mjs tools/nginx-upload-render.test.mjs` | tools/nginx-upload-seam.test.mjs · tools/nginx-upload-render.test.mjs | Gateway upload routing or template changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-CATALOG-DRAFT-INTEGRATION` | `test:catalog-draft:integration` | `@embroidery/api` | integration test | `pnpm --filter @embroidery/api exec jest --testPathPatterns=catalog-draft[.]integration` | apps/api catalog-draft integration specs | Catalog draft persistence or domain changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-CATALOG-DRAFT-API` | `test:catalog-draft:api` | `@embroidery/api` | API test | `pnpm --filter @embroidery/api exec jest --testPathPatterns=catalog-draft-api` | apps/api catalog-draft API specs | Catalog draft contract or controller changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-CATALOG-DRAFT-GATEWAY` | `test:catalog-draft:gateway` | repository tool | gateway seam test | `node --test tools/nginx-catalog-draft-seam.test.mjs` | tools/nginx-catalog-draft-seam.test.mjs | Gateway admin-catalog routing changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP7-B01-UNIT` | `@embroidery/api` | Docker-free unit and contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns="product-sku|admin-sku" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/catalog/domain/product-sku.*.spec.ts, presentation/schemas/admin-sku.request.spec.ts, presentation/admin-sku.contract.spec.ts | Any change to the SKU order-eligibility rule, the SKU error contract, the two request bodies, or the published SKU operations. Docker-free: the contract half reads the committed OpenAPI artifact and generated client directly. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP7-B01-INTEGRATION` | `@embroidery/api` | real-database integration tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns="catalog-sku(-eligibility|-race)?[.]integration" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/catalog/tests/integration/catalog-sku*.integration.spec.ts | Any change to Admin SKU authoring, the variant-lock concurrency arbiter or the order-eligible invariant. Creates and drops disposable PostgreSQL databases; the race suite spawns independently pooled actors and needs a reachable PostgreSQL. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP7-B01-API` | `@embroidery/api` | API test | `pnpm --filter @embroidery/api exec jest --testPathPatterns=catalog-sku-api --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/catalog-sku-api.integration.spec.ts | Admin SKU route, guard-chain, envelope or refusal-status changes. Boots the real `AppModule` against a disposable database. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP8-B01-UNIT` | `@embroidery/api` | Docker-free unit and contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=admin-sku-stock --testPathIgnorePatterns="/node_modules/|admin-sku-stock-api"` | apps/api/src/modules/inventory/presentation/schemas/admin-sku-stock.request.spec.ts, apps/api/src/modules/inventory/presentation/admin-sku-stock.contract.spec.ts | Any change to the stock adjustment body, the SKU stock path parameter, or the three published Admin stock operations. The contract half builds the real OpenAPI document in process — so it also proves `AdminSkuStockModule`, and through it `InventoryModule`, is reachable from the composition root — and runs **before** the generation slot is spent. The pattern excludes `admin-sku-stock-api` on purpose: a bare `admin-sku-stock` also matches the live suite and would silently start a database, which is the opposite of Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP8-B01-API` | `@embroidery/api` | API test | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=admin-sku-stock-api --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/admin-sku-stock-api.integration.spec.ts | Any change to the Admin stock routes, the Gap A anchor path, the audited adjustment or the guard chain: lazy `sku_stocks` creation and its idempotence, the honest zero for a never-counted SKU, the 404 for a SKU the FK rejects, one ADJUSTMENT ledger entry plus one `sku_stock.adjusted` audit row per commit, the mandatory reason, the negative-stock refusal leaving neither row, and availability against a live reservation. Boots the real `AppModule` against a disposable PostgreSQL database; counts rows directly, which is the only way to prove a refused adjustment committed nothing. | `ACTIVE_SCOPED` |
| `CMD-TEST-INVENTORY-PERSISTENCE-INTEGRATION` | `@embroidery/api` | inventory repository suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=inventory-persistence[.]integration --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/inventory/tests/integration/inventory-persistence.integration.spec.ts, inventory-fixture.ts | Any change to `SkuStockRepository` or its shared CTX-INV fixture — `APP8-B01` ran it because it added `occurredAt` to `listLedger`'s projection, which this suite directly exercises. Creates and drops a disposable PostgreSQL database. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP7-B02-UNIT` | `@embroidery/api` | Docker-free unit and contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns="admin-order[.](projection[.]spec|request[.]spec|contract[.]spec)" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/order/application/admin/admin-order.projection.spec.ts, presentation/schemas/admin-order.request.spec.ts, presentation/admin-order.contract.spec.ts | Any change to the order-line subject discriminator, the Admin order queue filter set, the order id parameter, or the two published Admin order operations. The contract half builds the real OpenAPI document in process, so it runs **before** the generation slot is spent. Docker-free: no database, no container, no network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP7-B02-INTEGRATION` | `@embroidery/api` | real-database integration tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns="admin-order-(queue|detail)[.]integration" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/order/tests/integration/admin-order-context.ts, admin-order-queue.integration.spec.ts, admin-order-detail.integration.spec.ts | Any change to the Admin order queue or detail: the keyset page and its equal-timestamp boundary, the status filter, the Catalog and COP detail projections, the frozen-read rule (live product rename, SKU reprice and deactivation must not move the response), the payment-surface exclusion, or the Admin guard chain. Seeds through the canonical AGG-15 fixture and creates orders through `OrderRepository.createFromAcceptedQuotation` — the `APP7-W01-C1` implementation — never through the W01 worker. Creates and drops disposable PostgreSQL databases. | `ACTIVE_SCOPED` |
| `CMD-TEST-PUBLIC-CATALOG-INTEGRATION` | `test:public-catalog:integration` | `@embroidery/api` | integration test | `pnpm --filter @embroidery/api exec jest --testPathPatterns=public-catalog[.]integration` | apps/api public-catalog integration specs | Public catalog query or repository changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-PUBLIC-CATALOG-API` | `test:public-catalog:api` | `@embroidery/api` | API test | `pnpm --filter @embroidery/api exec jest --testPathPatterns=public-catalog-api` | apps/api public-catalog API specs | Public catalog contract or controller changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-PUBLIC-CATALOG-GATEWAY` | `test:public-catalog:gateway` | repository tool | gateway seam test | `node --test tools/nginx-public-catalog-seam.test.mjs` | tools/nginx-public-catalog-seam.test.mjs | Gateway public-catalog routing changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-PUBLIC-MEDIA-INTEGRATION` | `test:public-media:integration` | `@embroidery/api` | integration test | `pnpm --filter @embroidery/api exec jest --config jest.public-media.config.mjs` | apps/api public-media integration specs | Public media delivery or derivative changes. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B02` | repository tool | checkpoint gate (APP3-B02) | `node tools/check-app3-b02.mjs` | tools/check-app3-b02.mjs, tools/check-app3-b02-contract.mjs | Any change to the public Side-background route, its contextual eligibility query, the stream lifecycle, the header or cache policy, the provider/persisted size reconciliation, the manifest delivery block or the shared route helper. Chains B01, B01N, W01B and G04 (and through them W01A, G06, G07, P02, G05, P01, F01, DB01, G03–G01). Asserts the **negations** — no second query, no transaction across the stream, no buffering, no ETag or storage identity, no upload/presign/download, no generic Asset or derivative route, no migration, no root script. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-APP3-B02-CONTRACT` | repository tool | checkpoint gate (APP3-B02 published surface) | `node tools/check-app3-b02-contract.mjs` | tools/check-app3-b02-contract.mjs | The published half — one anonymous binary operation, the public schemas, the manifest delivery block, the route helper and the governance counts — runnable alone. Already chained by `CMD-CHECK-APP3-B02`. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B02` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b02.test.mjs` | tools/check-app3-b02.test.mjs | Any edit to either B02 checker or the sources they parse. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B02-STREAM` | `@embroidery/api` | Docker-free B02 unit and contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=side-background --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/catalog/**/*side-background*.spec.ts | Any change to the delivery service, the route/operation-id contract, the path validation or the API↔contracts composer equivalence. Docker-free: the object store and the repository are substituted, so the ordering and reconciliation properties are asserted directly. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B02-INTEGRATION` | `@embroidery/api` | B02 live-stack delivery suite | `pnpm --filter @embroidery/api exec jest --config jest.public-media.config.mjs --testPathPatterns=side-background` | apps/api/test/integration/public-media-side-background.integration.spec.ts | Any change to the delivery route, the eligibility query, the manifest quartet or the revocation semantics. Starts a **disposable** MinIO and a disposable PostgreSQL with every migration; needs a Docker daemon. Reuses the accepted `APP2-T01` public-media harness rather than adding a second one. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-P03` | repository tool | checkpoint gate (APP3-P03) | `node tools/check-app3-p03.mjs` | tools/check-app3-p03.mjs | Any change to the Zod→OpenAPI conversion authority, the DTO publication registry, the document augmentation that applies it, any `createZodDto` consumer, or the published request-body schemas and generated client types. Chains B02, B01 and B01N (and through them the whole accepted APP3 gate set). Asserts the **negations** — no empty published body, no hand-decorated DTO, no silent `{}` fallback, no unregistered consumer, no new dependency, no route/operation delta, no root script. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-APP3-P03-CONTRACT` | repository tool | checkpoint gate (APP3-P03 published surface) | `node tools/check-app3-p03-contract.mjs` | tools/check-app3-p03-contract.mjs | The published half — every `createZodDto` consumer registered, none hand-decorated, no empty JSON request body at any depth, the placement body intact and the generated client typing real fields — runnable alone. Already chained by `CMD-CHECK-APP3-P03`. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-P03` | repository tool | checkpoint gate tests | `node --test tools/check-app3-p03.test.mjs` | tools/check-app3-p03.test.mjs | Any edit to the P03 checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-P03-CONTRACT` | `@embroidery/api` | Docker-free platform DTO/OpenAPI tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns="zod-dto|zod-openapi" --testPathIgnorePatterns=/node_modules/` | apps/api/src/platform/openapi/*.spec.ts, apps/api/src/platform/validation/zod-dto-registry.spec.ts | Any change to the conversion vocabulary, the registry, the augmentation, or the repository-wide publication contract. Docker-free: it reads the committed OpenAPI artifact and generated client directly. | `ACTIVE_SCOPED` |
| `CMD-TEST-PUBLIC-MEDIA-GATEWAY` | `test:public-media:gateway` | repository tool | gateway seam test | `node --test tools/nginx-public-media-seam.test.mjs` | tools/nginx-public-media-seam.test.mjs | Gateway public-media routing changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-ASSET-PROCESSING-INTEGRATION` | `test:asset-processing:integration` | `@embroidery/worker` | integration test | `pnpm --filter @embroidery/worker exec jest --testPathPatterns=asset-inspection-.*[.]integration --testPathIgnorePatterns=/node_modules/` | apps/worker asset-inspection integration specs | Asset inspection handler or derivative changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-ASSET-PROCESSING-WORKER-SMOKE` | `test:asset-processing:worker-smoke` | `@embroidery/worker` | smoke test | `pnpm --filter @embroidery/worker build && pnpm --filter @embroidery/worker exec jest --testPathPatterns=asset-processing-worker.smoke --testPathIgnorePatterns=/node_modules/` | apps/worker asset-processing smoke spec (requires a prior worker build) | Worker asset-processing composition changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-WORKER-RUNTIME-INTEGRATION` | `test:worker-runtime:integration` | `@embroidery/persistence + @embroidery/worker` | integration test | `pnpm --filter @embroidery/persistence exec jest --testPathPatterns=worker-job-queue && pnpm --filter @embroidery/worker exec jest --testPathPatterns=worker-runtime-.*integration` | persistence job-queue specs and worker runtime integration specs | Job queue, claim protocol or worker runtime changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-WORKER-RUNTIME-SMOKE` | `test:worker-runtime:smoke` | `@embroidery/worker` | smoke test | `pnpm --filter @embroidery/worker build && pnpm --filter @embroidery/worker exec jest --testPathPatterns=worker-smoke --testPathIgnorePatterns=/node_modules/` | apps/worker smoke spec (requires a prior worker build) | Worker bootstrap or composition changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-WORKER-RUNTIME-SIGNAL-SMOKE` | `test:worker-runtime:signal-smoke` | `@embroidery/worker` | smoke test | `pnpm --filter @embroidery/worker exec jest --testPathPatterns=worker-signal-smoke --testPathIgnorePatterns=/node_modules/` | apps/worker signal-handling smoke spec | Worker shutdown/signal handling changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-WORKER-RUNTIME-UNCOOPERATIVE-TIMEOUT` | `test:worker-runtime:uncooperative-timeout` | `@embroidery/worker` | integration test | `pnpm --filter @embroidery/worker exec jest --testPathPatterns=worker-(un)?cooperative-timeout` | apps/worker cooperative/uncooperative timeout specs | Handler timeout policy changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-WORKER-RUNTIME-FATAL-PROCESS` | `test:worker-runtime:fatal-process` | `@embroidery/worker` | integration test | `pnpm --filter @embroidery/worker exec jest --testPathPatterns=worker-fatal-timeout.process --testPathIgnorePatterns=/node_modules/` | apps/worker fatal-shutdown process spec | Worker fatal-shutdown changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-STORAGE-BOOTSTRAP-API` | `test:storage-bootstrap:api` | `@embroidery/api` | integration test | `pnpm --filter @embroidery/api exec jest --testPathPatterns="start-api|object-storage-bootstrap|build-openapi-document"` | apps/api bootstrap and OpenAPI-document specs | API startup, storage bootstrap or OpenAPI document changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-STORAGE-BOOTSTRAP-WORKER` | `test:storage-bootstrap:worker` | `@embroidery/worker` | integration test | `pnpm --filter @embroidery/worker build && pnpm --filter @embroidery/worker exec jest --testPathPatterns=storage-bootstrap-worker --testPathIgnorePatterns=/node_modules/` | apps/worker storage-bootstrap spec (requires a prior worker build) | Worker storage bootstrap changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-STORAGE-BOOTSTRAP-COMPOSITION` | `test:storage-bootstrap:composition` | `@embroidery/api + @embroidery/worker` | integration test | `pnpm --filter @embroidery/api build && pnpm --filter @embroidery/worker build && pnpm --filter @embroidery/worker exec jest --testPathPatterns=storage-bootstrap-composition --testPathIgnorePatterns=/node_modules/` | cross-app storage-bootstrap composition spec (requires both builds) | Shared storage bootstrap contract changes. | `ACTIVE_SCOPED` |
| `CMD-E2E` | `e2e` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e` | default Playwright run | Browser tier. | `ACTIVE_SCOPED` |
| `CMD-E2E-SMOKE` | `e2e:smoke` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:smoke` | Playwright smoke project | Milestone smoke. | `ACTIVE_SCOPED` |
| `CMD-E2E-FULL` | `e2e:full` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:full` | full Playwright matrix | Release/regression. | `ACTIVE_SCOPED` |
| `CMD-E2E-APP1` | `e2e:app1` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:app1` | APP1 Playwright project | APP1 staff-access journeys. | `ACTIVE_SCOPED` |
| `CMD-E2E-APP5-E01` | `e2e:app5:e01` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:app5:e01` | APP5-E01 cross-layer acceptance project (`specs/app5/e01.acceptance.spec.ts`) | The APP5 custom-request journeys: navigation entry, customer-owned submission through verification/upload/inspection, the catalog branch on a real `APP5-B07` variant and `APP3-B07` session, Admin queue → detail → `APP5-B06` evidence → `APP5-B05` moderation, and the grant-scoped status. Starts the full topology on `*.localhost` hostnames (a trustworthy origin, as production is) and carries this run's object storage into the in-process worker. Requires built `apps/api`, `apps/worker`, `apps/storefront` and `apps/admin`. | `ACTIVE_SCOPED` |
| `CMD-E2E-HEADED` | `e2e:headed` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:headed` | headed Playwright run | Local debugging. | `ACTIVE_SCOPED` |
| `CMD-E2E-DEBUG` | `e2e:debug` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:debug` | Playwright inspector | Local debugging. | `ACTIVE_SCOPED` |
| `CMD-E2E-REPORT` | `e2e:report` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:report` | Playwright HTML report | After a browser run. | `ACTIVE_SCOPED` |
| `CMD-E2E-INSTALL` | `e2e:install` | `@embroidery/e2e-testing` | E2E | `pnpm --filter @embroidery/e2e-testing e2e:install` | Playwright browser install | First run or version bump. | `ACTIVE_SCOPED` |
| `CMD-CHECK-E2E` | `check:e2e` | `@embroidery/e2e-testing` | boundary check | `pnpm --filter @embroidery/e2e-testing check:e2e` | E2E package static boundary check | Browser-free; run with E2E changes. | `ACTIVE_SCOPED` |
| `CMD-OPENAPI-GENERATE` | `openapi:generate` | `@embroidery/api` | contract artifact | `pnpm --filter @embroidery/api openapi:generate` | regenerates packages/contracts/openapi/openapi.generated.json | Any OpenAPI operation change. | `ACTIVE_SCOPED` |
| `CMD-OPENAPI-CHECK` | `check:openapi` | `@embroidery/api` | contract drift gate | `pnpm --filter @embroidery/api openapi:check` | fails when the committed OpenAPI artifact drifts | Any OpenAPI operation change. | `ACTIVE_SCOPED` |
| `CMD-API-CLIENT-GENERATE` | `api-client:generate` | `@embroidery/api-client` | contract artifact | `pnpm --filter @embroidery/api-client generate` | Orval regeneration into packages/api-client/src/generated | After an OpenAPI change. | `ACTIVE_SCOPED` |
| `CMD-API-CLIENT-CHECK` | `check:api-client` | `@embroidery/api-client` | contract drift gate | `pnpm --filter @embroidery/api-client check:generated` | fails when the generated client drifts | After an OpenAPI change. | `ACTIVE_SCOPED` |
| `CMD-DB-MANIFEST-CHECK` | `db:check:manifest` | repository tool | database gate | `node tools/db-manifest-check.mjs` | tools/db-manifest-check.mjs | Schema or migration changes. | `ACTIVE_SCOPED` |
| `CMD-CHECK-FILE-SIZE` | `check:file-size` | repository tool | repository gate | `node tools/check-file-size.mjs` | tools/check-file-size.mjs | Any touched production/test source file. | `ACTIVE_SCOPED` |
| `CMD-CHECK-REPORT-SECRETS` | `check:secrets` | repository tool | repository gate | `node tools/check-report-secrets.mjs` | tools/check-report-secrets.mjs | Any new or edited completion report. | `ACTIVE_SCOPED` |
| `CMD-CHECK-STYLES` | `check:styles` | repository tool | frontend gate | `node tools/check-styling-boundaries.mjs` | tools/check-styling-boundaries.mjs | Any SCSS or styling change. | `ACTIVE_SCOPED` |
| `CMD-CHECK-FIGMA-DESIGN-INDEX` | `check:figma-design-index` | repository tool | design gate | `node tools/check-figma-design-index.mjs` | tools/check-figma-design-index.mjs | Any design or frontend UI checkpoint, and any registry edit. | `ACTIVE_SCOPED` |
| `CMD-CHECK-FRONTEND-BOUNDARIES` | `check:frontend-boundaries` | repository tool | frontend gate | `node tools/check-frontend-test-boundaries.mjs` | tools/check-frontend-test-boundaries.mjs | Frontend test-harness changes. | `ACTIVE_SCOPED` |
| `CMD-CHECK-FRONTEND-BUILD-BOUNDARY` | `check:frontend-build-boundary` | repository tool | post-build gate | `node tools/check-frontend-build-boundary.mjs` | tools/check-frontend-build-boundary.mjs (inspects .next; needs a prior build) | Build/CI stage after a frontend build. | `ACTIVE_SCOPED` |
| `CMD-CHECK-E2E-BOUNDARIES` | `check:e2e-boundaries` | repository tool | boundary gate | `node tools/check-e2e-boundaries.mjs` | tools/check-e2e-boundaries.mjs | E2E package structure changes. | `ACTIVE_SCOPED` |
| `CMD-CHECK-SPIKE-BOUNDARIES` | `check:spike-boundaries` | repository tool | boundary gate | `node tools/check-spike-boundaries.mjs` | tools/check-spike-boundaries.mjs (`--build` variant inspects dist) | Spike isolation; any production manifest change. | `ACTIVE_SCOPED` |
| `CMD-CHECK-LIFECYCLE` | `check:lifecycle` | repository tool | lifecycle gate | `node tools/check-lifecycle-consistency.mjs` | tools/check-lifecycle-consistency.mjs | Any LC-nn transition table or lifecycle policy change. | `ACTIVE_SCOPED` |
| `CMD-CHECK-PAGINATION-AUTHORITY` | `check:pagination-authority` | historical checkpoint | checkpoint gate (APP2-B04-C1) | `node tools/check-pagination-authority.mjs` | tools/check-pagination-authority.mjs | Public catalog pagination authority changes. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-STOREFRONT-ROUTE-AUTHORITY` | `check:storefront-route-authority` | historical checkpoint | checkpoint gate (APP2-S01-G01) | `node tools/check-storefront-route-authority.mjs` | tools/check-storefront-route-authority.mjs | Storefront route authority changes. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-STOREFRONT-PRODUCT-DETAIL-AUTHORITY` | `check:storefront-product-detail-authority` | historical checkpoint | checkpoint gate (APP2-S02) | `node tools/check-storefront-product-detail-authority.mjs` | tools/check-storefront-product-detail-authority.mjs | Product detail authority changes. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-STOREFRONT-PRODUCT-DETAIL-CORRECTION` | `check:storefront-product-detail-correction` | historical checkpoint | checkpoint gate (APP2-S02-C1) | `node tools/check-storefront-product-detail-correction.mjs` | tools/check-storefront-product-detail-correction.mjs | Product detail correction authority changes. | `HISTORICAL_SCOPED` |
| `CMD-SPIKE-EDITOR-BUILD` | `spike:editor:build` | `@embroidery-spike/design-studio` | spike operation | `pnpm --filter @embroidery-spike/design-studio spike:build` | design-studio spike build | Spike work only. | `ACTIVE_SCOPED` |
| `CMD-SPIKE-EDITOR-TEST` | `spike:editor:test` | `@embroidery-spike/design-studio` | spike test | `pnpm --filter @embroidery-spike/design-studio spike:test` | design-studio spike tests | Any `design-document`, `design-engine` or adapter change. | `ACTIVE_SCOPED` |
| `CMD-SPIKE-EDITOR-CHECK` | `spike:editor:check` | `@embroidery-spike/design-studio` | spike gate | `pnpm --filter @embroidery-spike/design-studio spike:check` | design-studio spike static check | Any change touching the spike boundary. | `ACTIVE_SCOPED` |
| `CMD-SPIKE-EDITOR-BENCHMARK` | `spike:editor:benchmark` | `@embroidery-spike/design-studio` | benchmark | `pnpm --filter @embroidery-spike/design-studio spike:bench` | APP0-R01 S/M/L performance scenes | Deliberate performance measurement (APP3 S02/S03/S07/S11/E01). | `ACTIVE_SCOPED` |
| `CMD-SPIKE-EDITOR-BENCHMARK-LINUX` | `spike:editor:benchmark:linux` | `@embroidery-spike/design-studio` | benchmark | `pnpm --filter @embroidery-spike/design-studio spike:bench --container` | containerised benchmark run | Cross-platform performance comparison. | `ACTIVE_SCOPED` |
| `CMD-SPIKE-EDITOR-BUNDLE` | `spike:editor:bundle` | `@embroidery-spike/design-studio` | spike operation | `pnpm --filter @embroidery-spike/design-studio spike:bundle` | design-studio spike bundle analysis | Spike work only. | `ACTIVE_SCOPED` |
| `CMD-SPIKE-EDITOR-ASSETS` | `spike:editor:assets` | `@embroidery-spike/design-studio` | spike operation | `pnpm --filter @embroidery-spike/design-studio spike:assets` | design-studio spike asset preparation | Spike work only. | `ACTIVE_SCOPED` |
| `CMD-SMOKE-APP1-BOOTSTRAP` | `smoke:app1-bootstrap` | historical checkpoint | smoke (APP1) | `node tools/smoke-app1-bootstrap-compose.mjs` | tools/smoke-app1-bootstrap-compose.mjs | Staff bootstrap or compose changes. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-PUBLICATION-PRODUCTION` | `smoke:app2-publication` | historical checkpoint | smoke (APP2) | `node tools/smoke-app2-publication-production.mjs` | tools/smoke-app2-publication-production.mjs | Publication path on the production topology. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-T01-PUBLIC-MEDIA` | `smoke:app2-t01-public-media` | historical checkpoint | smoke (APP2-T01) | `node tools/smoke-app2-t01-public-media.mjs` | tools/smoke-app2-t01-public-media.mjs | Public media delivery, dev topology. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-T01-PUBLIC-MEDIA-PRODUCTION` | `smoke:app2-t01-public-media:production` | historical checkpoint | smoke (APP2-T01-C1) | `node tools/smoke-app2-t01-public-media-production.mjs` | tools/smoke-app2-t01-public-media-production.mjs | Public media delivery, production topology. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-B04-PUBLIC-CATALOG` | `smoke:app2-b04-public-catalog` | historical checkpoint | smoke (APP2-B04) | `node tools/smoke-app2-b04-public-catalog.mjs` | tools/smoke-app2-b04-public-catalog.mjs | Public catalog queries, dev topology. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-B04-PUBLIC-CATALOG-PRODUCTION` | `smoke:app2-b04-public-catalog:production` | historical checkpoint | smoke (APP2-B04) | `node tools/smoke-app2-b04-public-catalog-production.mjs` | tools/smoke-app2-b04-public-catalog-production.mjs | Public catalog queries, production topology. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-S01-DISCOVER-PRODUCTION` | `smoke:app2-s01-discover:production` | historical checkpoint | smoke (APP2-S01) | `node tools/smoke-app2-s01-discover-production.mjs` | tools/smoke-app2-s01-discover-production.mjs | Discover feed on the production topology. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-S02-DETAIL-PRODUCTION` | `smoke:app2-s02-detail:production` | historical checkpoint | smoke (APP2-S02) | `node tools/smoke-app2-s02-detail-production.mjs` | tools/smoke-app2-s02-detail-production.mjs | Product detail on the production topology. | `HISTORICAL_SCOPED` |
| `CMD-SMOKE-APP2-E01-PUBLICATION-PRODUCTION` | `smoke:app2-e01-publication:production` | historical checkpoint | smoke (APP2-E01) | `node tools/smoke-app2-e01-publication-production.mjs` | tools/smoke-app2-e01-publication-production.mjs | Cross-layer publication journey. | `HISTORICAL_SCOPED` |
| `CMD-BENCH-DB9` | `bench:db9` | `@embroidery/api` | benchmark | `pnpm --filter @embroidery/api bench` | DB9 query benchmark harness | Deliberate database performance measurement. | `ACTIVE_SCOPED` |
| `CMD-EXPLAIN-Q01-PUBLIC-CATALOG` | `explain:q01` | repository tool | diagnostic | `node tools/explain-q01-public-catalog.mjs` | tools/explain-q01-public-catalog.mjs | Q-01 plan inspection during pagination/index work. | `ACTIVE_SCOPED` |

## 3.1 Commands added after `GOV-Q01-C1`

These were never root aliases and never may be — they are listed here because
this file, not `package.json`, is where a command is discovered. The `Former
root alias` column is dropped rather than filled with a fiction.

The `APP3-G01`…`G03` gates belong here too: `GOV-Q01` removed their aliases one
commit *before* this index existed, so the §3 sweep of 71 rows never saw them
and they have been undiscoverable since. `APP3-G04` indexes them rather than
leaving the hole open.

| Command ID | Owner | Category | Direct invocation | Source / target | Usage scope | Status |
|---|---|---|---|---|---|---|
| `CMD-CHECK-APP3-G01` | repository tool | checkpoint gate (APP3-G01) | `node tools/check-app3-g01.mjs` | tools/check-app3-g01.mjs | Any edit to APP3 placement authority, §6.4, IMP-D041 or the placement schema. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-APP3-G02` | repository tool | checkpoint gate (APP3-G02) | `node tools/check-app3-g02.mjs` | tools/check-app3-g02.mjs | Any edit to Template lifecycle authority, §6.5, IMP-D042, LC-24/LC-04 or the Template schema. Chains G01. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-G02` | repository tool | checkpoint gate tests | `node --test tools/check-app3-g02.test.mjs` | tools/check-app3-g02.test.mjs | Any edit to `check-app3-g02.mjs` or the inputs it parses. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-G03` | repository tool | checkpoint gate (APP3-G03) | `node tools/check-app3-g03.mjs` | tools/check-app3-g03.mjs, tools/check-app3-g03-security.mjs | Any edit to anonymous Session authority, §6.6, IMP-D043, LC-07 or the Session schema. Chains G02. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-G03` | repository tool | checkpoint gate tests | `node --test tools/check-app3-g03.test.mjs` | tools/check-app3-g03.test.mjs | Any edit to the G03 checkers or the inputs they parse. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-G04` | repository tool | checkpoint gate (APP3-G04) | `node tools/check-app3-g04.mjs` | tools/check-app3-g04.mjs, tools/check-app3-g04-media.mjs | Any edit to editor-media authority, §6.7, IMP-D044, the asset/derivative schema or the APP3 asset contract. Chains G03. Reports its schema mode: `REQUIRED_SCHEMA_CONTRIBUTION_PENDING` before `APP3-DB01`, `DERIVATIVE_METADATA_IMPLEMENTED` after it. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-G04` | repository tool | checkpoint gate tests | `node --test tools/check-app3-g04.test.mjs` | tools/check-app3-g04.test.mjs | Any edit to the G04 checkers or the inputs they parse. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-DB01` | repository tool | checkpoint gate (APP3-DB01) | `node tools/check-app3-db01.mjs` | tools/check-app3-db01.mjs, tools/check-app3-db01-placement.mjs | Any edit to migration 0034, the placement or asset-derivative schema, §6.8, or the APP3 database disposition. Chains G04. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-DB01` | repository tool | checkpoint gate tests | `node --test tools/check-app3-db01.test.mjs` | tools/check-app3-db01.test.mjs | Any edit to the DB01 checkers or the inputs they parse. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-DB01-INTEGRATION` | `@embroidery/database` | integration test | `pnpm --filter @embroidery/database exec jest --testPathPatterns=app3-` | app3-placement-authority / app3-derivative-metadata / app3-placement-upgrade integration specs | Any change to placement or derivative schema, migration 0034, or the guard triggers. Needs a reachable PostgreSQL; creates and drops its own disposable databases. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-F01-FONT-ASSETS` | repository tool | checkpoint gate (APP3-F01) | `node tools/check-app3-f01-font-assets.mjs` | tools/check-app3-f01-font-assets.mjs | Any change to the controlled Inter v4.1 assets, their provenance/coverage manifests or licence, §6.9, or the `APP3-P01` blocked status. Hashes the committed binaries and re-derives the mandated Vietnamese repertoire; does **not** parse WOFF2 — glyph coverage was measured once by pinned FontTools and is recorded in the evidence. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-F01-FONT-ASSETS` | repository tool | checkpoint gate tests | `node --test tools/check-app3-f01-font-assets.test.mjs` | tools/check-app3-f01-font-assets.test.mjs | Any edit to the F01 checker or the evidence it parses. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-P01` | repository tool | checkpoint gate (APP3-P01) | `node tools/check-app3-p01.mjs` | tools/check-app3-p01.mjs | Any change to `packages/design-document`, §6.10, the G04 complexity constants, the quantization precision, the browser/server export split or the controlled font registry. Chains F01 and DB01. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-P01` | repository tool | checkpoint gate tests | `node --test tools/check-app3-p01.test.mjs` | tools/check-app3-p01.test.mjs | Any edit to the P01 checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-DESIGN-DOCUMENT` | `@embroidery/design-document` | package unit tests | `pnpm --filter @embroidery/design-document test` | packages/design-document/src/**/*.spec.ts | Any change to the Design Document schema, validation, quantization, canonicalization, hashing, migration or font registry. Pure unit tests; no database, no network. | `ACTIVE_SCOPED` |
| `CMD-GENERATE-DESIGN-DOCUMENT-SCHEMA` | `schema:generate` | `@embroidery/design-document` | generated artifact | `pnpm --filter @embroidery/design-document schema:generate` | packages/design-document/src/schema/generated/design-document.schema.json | Any change to the Design Document TypeScript types. Regenerates the structural JSON Schema the API publishes as OpenAPI components (`APP3-B08-C1`). The artifact is generated, never hand-edited; the generator is dev-only and runs at author time, so nothing ships it. | `ACTIVE_SCOPED` |
| `CMD-CHECK-DESIGN-DOCUMENT-SCHEMA` | `schema:check` | `@embroidery/design-document` | drift gate | `pnpm --filter @embroidery/design-document schema:check` | packages/design-document/src/schema/generated/design-document.schema.json | Fails when the committed schema no longer matches what the current TypeScript types generate. Run after any Design Document type change, and before publishing an OpenAPI artifact that references the components. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-G05` | repository tool | checkpoint gate (APP3-G05) | `node tools/check-app3-g05.mjs` | tools/check-app3-g05.mjs, tools/check-app3-g05-transform.mjs | Any edit to `IMP-D045`, §6.11, the geometry coordinate/transform/bounds/containment rulings, the `pxPerMm` authority, the placement modes, or the `APP3-P02` disposition. Chains P01. Because a v1 document records no pivot marker, every fact here is asserted as an exact string, not a substring. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-G05` | repository tool | checkpoint gate tests | `node --test tools/check-app3-g05.test.mjs` | tools/check-app3-g05.test.mjs | Any edit to the G05 checkers or the authority they parse. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-P02` | repository tool | checkpoint gate (APP3-P02) | `node tools/check-app3-p02.mjs` | tools/check-app3-p02.mjs | Any change to `packages/design-engine`, §6.12, or the IMP-D045 semantics it implements. Chains G05 (and through it P01, F01, DB01, G04–G01). Asserts the *semantics* — pivot, composition order, stroke envelope, transform order — because a renamed-but-wrong function still exports the right name. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-P02` | repository tool | checkpoint gate tests | `node --test tools/check-app3-p02.test.mjs` | tools/check-app3-p02.test.mjs | Any edit to the P02 checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-DESIGN-ENGINE` | `@embroidery/design-engine` | package unit tests | `pnpm --filter @embroidery/design-engine test` | packages/design-engine/src/**/*.spec.ts | Any change to matrices, effective transforms, envelopes, bounds, placement reconciliation, px↔mm conversion or containment. Pure unit tests; no database, no network, no renderer. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B01` | repository tool | checkpoint gate (APP3-B01) | `node tools/check-app3-b01.mjs` | tools/check-app3-b01.mjs | Any change to the placement operations, the public manifest shape, the side-background eligibility predicate, `product_sides.background_asset_id`, the Catalog placement module or §6.14. Chains P02 (and through it G05, P01, F01, DB01, G04–G01). Asserts the manifest's **absences** — no background asset id, storage key, retirement or mutation field — against the committed OpenAPI document. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B01` | `@embroidery/api` | placement unit and contract tests | `pnpm --filter @embroidery/api exec jest src/modules/catalog/domain/product-placement.policy.spec.ts src/modules/catalog/application/product-placement.plan.spec.ts src/modules/catalog/application/product-placement.projection.spec.ts src/modules/catalog/presentation/product-placement.contract.spec.ts` | apps/api/src/modules/catalog/**/*placement*.spec.ts | Any change to the placement policy, plan, projection or HTTP contract. No database and no Docker; the contract suite reads the committed OpenAPI artifact. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B01-INTEGRATION` | `@embroidery/api` | placement live-database suites | `pnpm --filter @embroidery/api exec jest --runInBand test/integration/product-placement.integration.spec.ts test/integration/product-placement-manifest.integration.spec.ts test/integration/product-placement-concurrency.integration.spec.ts` | apps/api/test/integration/product-placement*.integration.spec.ts | Any change to the placement repository, transaction, retirement/supersession behaviour, the `products.updated_at` concurrency contract or the `studioEligible` derivation. Provisions a **disposable** PostgreSQL database and applies every migration; needs a running dev PostgreSQL container, no object storage. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-G06` | repository tool | checkpoint gate (APP3-G06) | `node tools/check-app3-g06.mjs` | tools/check-app3-g06.mjs, tools/check-app3-g06-events.mjs | Any edit to `IMP-D046`, §6.16, the `asset.normalization.requested` contract, the association→profile mapping, the W01A/W01B split, the `APP3-G07` sanitizer disposition or the B02/B03/B06 blocker map. Chains B01 (and through it P02, G05, P01, F01, DB01, G04–G01). Asserts the **negations** — no second queue or sweep, no persisted profile, no profile in the payload, no sanitizer selected, no API or worker implementation — as hard as the rulings. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-G06` | repository tool | checkpoint gate tests | `node --test tools/check-app3-g06.test.mjs` | tools/check-app3-g06.test.mjs | Any edit to the G06 checkers or the authority documents they parse. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-W01A` | repository tool | checkpoint gate (APP3-W01A) | `node tools/check-app3-w01a.mjs` | tools/check-app3-w01a.mjs | Any change to the normalization consumer, its payload, the association→profile derivation, the raster policy or limits, the quartet measurement, or the atomic READY completion. Chains G06 (and through it B01, P02, G05, P01, F01, DB01, G04–G01). Asserts the **negations** — no payload profile, no association scan, no source-metadata substitution, no SVG branch, no scheduler, no migration, no HTTP operation. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-W01A` | repository tool | checkpoint gate tests | `node --test tools/check-app3-w01a.test.mjs` | tools/check-app3-w01a.test.mjs | Any edit to the W01A checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-W01A-OUTPUT` | repository tool | checkpoint gate (APP3-W01A output contract) | `node tools/check-app3-w01a-output.mjs` | tools/check-app3-w01a-output.mjs, tools/check-app3-w01a-files.mjs | The output half — frozen output policy, the measured quartet and the atomic claim-guarded completion — runnable alone. Already chained by `CMD-CHECK-APP3-W01A`; run directly when only the produced derivative is in question. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01A-UNIT` | `@embroidery/worker` | normalization unit tests | `pnpm --filter @embroidery/worker exec jest src/jobs/asset-normalization` | apps/worker/src/jobs/asset-normalization/**/*.spec.ts | Any change to the payload contract, the frozen policy, profile derivation or media/limit admission. Docker-free; the limit cases decode real images through Sharp. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01A-INTEGRATION` | `@embroidery/worker` | normalization live-stack suites | `pnpm --filter @embroidery/worker exec jest --runInBand --testPathPatterns=asset-normalization.*[.]integration --testPathIgnorePatterns=/node_modules/` | apps/worker/src/jobs/asset-normalization/tests/*.integration.spec.ts | Any change to the normalization repository, the object protocol, idempotency or cleanup. Starts a **disposable** MinIO and a disposable PostgreSQL with every migration; needs a Docker daemon. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B01N` | repository tool | checkpoint gate (APP3-B01N) | `node tools/check-app3-b01n.mjs` | tools/check-app3-b01n.mjs | Any change to the shared normalization event contract, the placement producer rule, the append's position in the replace transaction, or the frozen OpenAPI/generated-client artifacts. Chains W01A (and through it G06, B01, P02, G05, P01, F01, DB01, G04–G01). Asserts the **negations** — no second declaration of the event string, no API↔worker import, no profile in the payload, no append on a read, a rename or a retirement, no deletion of a replaced Asset's derivative, no HTTP surface. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B01N` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b01n.test.mjs` | tools/check-app3-b01n.test.mjs | Any edit to the B01N checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B01N-UNIT` | `@embroidery/api` | producer unit tests | `pnpm --filter @embroidery/api exec jest src/modules/catalog/application/product-placement` | apps/api/src/modules/catalog/application/product-placement*.spec.ts | Any change to the intent derivation from the placement plan or to what the recorder appends. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B01N-INTEGRATION` | `@embroidery/api` | producer live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=product-placement-normalization[.]integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/product-placement-normalization.integration.spec.ts | Any change to the append's transaction position, the committed payload, or the no-append rules. Starts a **disposable** PostgreSQL with every migration; needs a Docker daemon. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B01N-CONSUMER` | `@embroidery/worker` | shared-contract compatibility | `pnpm --filter @embroidery/worker exec jest src/jobs/asset-normalization/domain/shared-event-contract` | apps/worker/src/jobs/asset-normalization/domain/shared-event-contract.spec.ts | Any change to the shared event contract or to the consumer's aliases for it. Proves the consumer still accepts what the shared builder produces. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-G07` | repository tool | checkpoint gate (APP3-G07) | `node tools/check-app3-g07.mjs` | tools/check-app3-g07.mjs, tools/check-app3-g07-policy.mjs | Any change to `IMP-D047`, the sanitizer or jsdom version evidence, the element/attribute allowlists, the URL/CSS/font refusals, the value, transform or path grammar, the `viewBox` metadata rule, the complexity limits, the thirteen-step pipeline or the canonical output form. Chains B01N (and through it W01A, G06, B01, P02, G05, P01, F01, DB01, G04–G01). Asserts the **negations** — no dependency installed, no lockfile importer, no sanitizer source, no schema column, no derivative kind, no migration, no HTTP surface, no root script. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-G07` | repository tool | checkpoint gate tests | `node --test tools/check-app3-g07.test.mjs` | tools/check-app3-g07.test.mjs | Any edit to the G07 checker or the documents it parses. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B07` | repository tool | checkpoint gate (APP3-B07) | `node tools/check-app3-b07.mjs` | tools/check-app3-b07.mjs | Any change to Session bootstrap/clone/resume: the discriminated request contract, the 30-day TTL, P01/P02 validation, Studio-eligibility scope resolution, clone scope equality and lineage, secret issuance and cookie attributes, the guarded rotation, or the Catalog provider-only read boundary. Chains B06A (and through it W01C, G08, G03). Asserts the **negations** — no clone→blank fallback, no live Template reference, no TTL extension or revision advance on resume, no Design import of the controller-bearing placement module, no second placement query, no Asset/upload/event/autosave, no migration, no external dependency, no root script. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B07` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b07.test.mjs` | tools/check-app3-b07.test.mjs | Any edit to the B07 checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B06B` | repository tool | checkpoint gate (APP3-B06B) | `node tools/check-app3-b06b.mjs` | tools/check-app3-b06b.mjs | Any change to the anonymous Session raster intake: the shared multipart parser or validated-file reader, either intake lane, the Session upload policy or result codec, the durable transaction, the association write, the intake controller, or the published multipart contract. Chains B07 and through it the whole APP3 gate graph. Asserts the **negations** — no presign, upload-intent or completion operation, no whole-file buffering, no storage call inside the durable transaction, no destructive reclaim of an object belonging to another request, no second normalization event, no re-implemented cookie/HMAC/origin/rate-limit logic, no migration, no external dependency, no root script, no worker change. Does **not** read the completion report. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B06B` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b06b.test.mjs` | tools/check-app3-b06b.test.mjs | Any edit to the B06B checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B08` | repository tool | checkpoint gate (APP3-B08) | `node tools/check-app3-b08.mjs` | tools/check-app3-b08.mjs | Any change to Design Session autosave: the request contract, the P01/P02 save pipeline, the session-scoped media allowlist, the placement resolution, the CAS, or the published operation. Chains B06B and through it the whole APP3 gate graph. Asserts the **negations** — no second operation, no expiry extension, no cookie issuance or secret rotation, no autosave idempotency record, no Outbox event, no submission or customer identity, no local copy of the document schema, no object-storage read to validate a document, no autosave cadence in backend authority, and no migration, dependency, root script or worker change. Does **not** read the completion report. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B08` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b08.test.mjs` | tools/check-app3-b08.test.mjs | Any edit to the B08 checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B08-API` | `@embroidery/api` | Docker-free autosave contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-session-autosave --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-session-autosave.spec.ts | The request DTO and its metatype binding, the P01/P02 save pipeline verdicts, media eligibility including the fail-closed allowlist, and the CAS call shape. Docker-free: collaborators are doubles, so what is proved is the decision rather than the SQL. Also matches the live suite by name — narrow to `design-session-autosave.spec` for the unit half alone. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B08-INTEGRATION` | `@embroidery/api` | autosave live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=design-session-autosave.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/design-session-autosave.integration.spec.ts, apps/api/test/support/design-session-autosave-context.ts | The composed pipeline against disposable PostgreSQL: canonical persistence, revision +1, unchanged expiry and secret digest, 409 on a stale revision with nothing written, the authorization refusals, every document and media rejection, B07 resume interoperability, and a **ten-iteration** same-revision two-writer race proving exactly one winner per iteration. No Docker container: autosave performs no object-storage I/O, so the offline storage placeholders suffice. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-P04-CONTRACT` | `@embroidery/api` | shared Session response contract | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-session-response.contract` | apps/api/src/openapi/design-session-response.contract.spec.ts | Any change to the published response of a public Design Session operation. Builds the real OpenAPI document in process — no database, no container — and proves all three operations answer with the one shared snapshot component, that its document resolves to the generated `DesignDocument`, that scope and lineage stay optional because runtime omits them, and that no credential, storage identity or internal publication marker reaches the contract. Runs before the single generation slot, so a defect costs nothing. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B06B-INTEGRATION` | `@embroidery/api` | Session raster intake live-stack suite | `pnpm --filter @embroidery/api exec jest --config jest.design-session-asset.config.mjs` | apps/api/test/integration/design-session-asset-intake.integration.spec.ts, apps/api/test/support/design-session-asset-context.ts | Any change to the Session upload lane, the durable transaction, the association write, the revision CAS or the normalization event contract. The `APP3-B06B-C1` durability proof against a disposable PostgreSQL and a disposable MinIO, driven through real HTTP multipart: one association and one of each event per upload, replay convergence, the stale-revision conflict, rollback leaving nothing partial, the persisted pair `W01C` retries against, and no secret, credential, raw byte or public URL in the durable trace. Starts a MinIO container; needs a Docker daemon, which is why it has its own config and is excluded from `pnpm test`. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B07-API` | `@embroidery/api` | Docker-free B07 contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns="design-session-bootstrap|catalog-placement-boundary" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-session-bootstrap.spec.ts, apps/api/test/architecture/catalog-placement-boundary.spec.ts | The request union and DTO-metatype proofs, the P01 blank-document authority, secret/cookie behaviour, and the eleven Catalog read-boundary assertions. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B03` | `@embroidery/api` | mechanical gate | `node tools/check-app3-b03.mjs` | tools/check-app3-b03.mjs | Any change to the Admin Design Template surface, its header-only create semantics, its keyset list, or the boundary that keeps the document save, the version, the association and the normalization producer in `APP3-B03A`. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B03` | `@embroidery/api` | gate regressions | `node --test tools/check-app3-b03.test.mjs` | tools/check-app3-b03.test.mjs | Any change to `check-app3-b03.mjs`. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the checker refuses it. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B03-API` | `@embroidery/api` | Docker-free B03 contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-template-admin.spec --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-template-admin.spec.ts | Any change to the Admin Design Template request contract, projection, scope authority or published responses. Reads the committed OpenAPI artifact and the generated client, so it must run after generation. The pattern ends at `.spec` on purpose: a bare `design-template-admin` also matches `design-template-admin.integration.spec.ts` and would silently start a database, which is the opposite of Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B03-INTEGRATION` | `@embroidery/api` | Admin Template live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=design-template-admin.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/design-template-admin.integration.spec.ts | Any change to Design Template persistence, the Admin reads or the create transaction. Starts a disposable PostgreSQL database and runs the whole HTTP stack; no MinIO, because `APP3-B03` performs no object-storage I/O. It counts rows in `design_template_versions`, `design_template_assets` and `outbox_events` as deltas, which is the only way to prove the create wrote nothing beyond the header. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B03A` | `@embroidery/api` | mechanical gate | `node tools/check-app3-b03a.mjs` | tools/check-app3-b03a.mjs | Any change to the Design Template draft save, its compare-and-set, its P01 document authority, its `TEMPLATE_SOURCE` media allowlist, or the `DESIGN_TEMPLATE_ASSET` normalization producer it owns. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B03A` | `@embroidery/api` | gate regressions | `node --test tools/check-app3-b03a.test.mjs` | tools/check-app3-b03a.test.mjs | Any change to `check-app3-b03a.mjs`. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the checker refuses it. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B03A-API` | `@embroidery/api` | Docker-free B03A contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-template-save.spec --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-template-save.spec.ts | Any change to the save request contract, the document authority, the media allowlist or the event payload. Reads the committed OpenAPI artifact and the generated client, so it must run after generation. The pattern ends at `.spec` on purpose: a bare `design-template-save` also matches the integration suite and would silently start a database. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B03A-INTEGRATION` | `@embroidery/api` | draft-save live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=design-template-save.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/design-template-save.integration.spec.ts | Any change to draft-version persistence, the compare-and-set, association reconciliation or the normalization event. Starts a disposable PostgreSQL database and runs the whole HTTP stack; no MinIO, because `APP3-B03A` performs no object-storage I/O. Contains the mandatory ten-iteration compare-and-set race, which is the only proof that two writers at the same expected version leave exactly one winner and one 409. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B04` | `@embroidery/api` | mechanical gate | `node tools/check-app3-b04.mjs` | tools/check-app3-b04.mjs | Any change to the Design Template lifecycle: the GRD-T01 publish guard, the compare-and-set transitions, the set-once `published_at`, the archive reason, or the boundary that keeps restore in `APP3-B04A`. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B04` | `@embroidery/api` | gate regressions | `node --test tools/check-app3-b04.test.mjs` | tools/check-app3-b04.test.mjs | Any change to `check-app3-b04.mjs`. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the checker refuses it. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B04-API` | `@embroidery/api` | Docker-free B04 contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-template-lifecycle.spec --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-template-lifecycle.spec.ts | Any change to the lifecycle request contract, the publication guard composition or the audit vocabulary. Reads the committed OpenAPI artifact, so it must run after generation. The pattern ends at `.spec` on purpose: a bare `design-template-lifecycle` also matches the integration suite and would silently start a database. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B04-INTEGRATION` | `@embroidery/api` | lifecycle live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=design-template-lifecycle.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/design-template-lifecycle.integration.spec.ts | Any change to lifecycle persistence, the publish guard or the publication timestamp. Starts a disposable PostgreSQL database and runs the whole HTTP stack; no MinIO, because `APP3-B04` performs no object-storage I/O. It carries the two proofs only a live database can give: that a republication of the same version keeps its original `published_at` — an `IS NULL` predicate, not a branch — and the three race families, whose audit assertions are deltas rather than absolutes because one of them publishes during setup. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-A04` | repository tool | checkpoint gate (APP3-A04) | `node tools/check-app3-a04.mjs` | tools/check-app3-a04.mjs | Any change to the Admin Design Template lifecycle screen: the action matrix, the command bodies, the advisory readiness panel, the conflict handling or the curated client boundary. Asserts the **negations** — no readiness endpoint invented (exactly two `queryFn`s exist in the feature), no unprovable row rendered as a pass or used to block publish, no message-string classification, no optimistic status flip, no local version increment, no hard delete, no `ARCHIVED → PUBLISHED`, no restore-and-publish chain, and no lifecycle call from the list or the editor. Also recomputes the frozen OpenAPI digest and generated-client tree hash, because A04 adds zero API operations. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-A04` | repository tool | checkpoint gate tests | `node --test tools/check-app3-a04.test.mjs` | tools/check-app3-a04.test.mjs | Any edit to `check-app3-a04.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the checker refuses it. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-A04-ADMIN` | `@embroidery/admin` | A04 component and boundary tests | `pnpm --filter @embroidery/admin exec jest --testPathPatterns=lifecycle --testPathIgnorePatterns=/node_modules/` | apps/admin/test/components/design-template-lifecycle*.test.tsx · apps/admin/test/model/lifecycle-readiness.test.ts · apps/admin/test/boundary/design-template-lifecycle-source.test.ts | Any change to the lifecycle feature. jsdom, no database and no container. Covers the action matrix, the exact command bodies, success adoption, the advisory readiness states, the 422 refusal and the 409 authoritative re-read. Note that `jest.setup.ts` polyfills `TextEncoder`/`TextDecoder`: jsdom omits both, and without them `prepareDesignDocument` fails canonicalization under test while succeeding in every supported browser. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B04A` | repository tool | checkpoint gate (APP3-B04A) | `node tools/check-app3-b04a.mjs` | tools/check-app3-b04a.mjs | Any change to the Design Template restore (`TR-LC24-06`): the `ARCHIVED`-only source predicate, the cleared `archived_at`, the `expectedCurrentVersion` compare-and-set, the required bounded reason, or the `design_template.restored` audit row. Asserts the **negations** — no publication guard, no `ARCHIVED → PUBLISHED` and no restore alias, no version, document, association, outbox event, delete, scope repair or Session/clone cascade, and no curated-client export before `APP3-A04`. Also recomputes the operation-domain declaration that keeps the eight accepted Admin Template operation ids stable across the controller split, and the 400-line limit on every file of that surface. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B04A` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b04a.test.mjs` | tools/check-app3-b04a.test.mjs | Any edit to `check-app3-b04a.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the checker refuses it. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B04A-API` | `@embroidery/api` | Docker-free B04A contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-template-restore.spec --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-template-restore.spec.ts | Any change to the restore request contract, the admissible source state, the audit vocabulary or the list of things restore must not touch. Reads the committed OpenAPI artifact, so it must run after generation. The pattern ends at `.spec` on purpose: a bare `design-template-restore` also matches the integration suite and would silently start a database. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B04A-INTEGRATION` | `@embroidery/api` | restore live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=design-template-restore.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/design-template-restore.integration.spec.ts | Any change to restore persistence, the archive marker or the transition's preservation guarantees. Starts a disposable PostgreSQL database and runs the whole HTTP stack; no MinIO, because `APP3-B04A` performs no object-storage I/O. It carries the four proofs only a live database can give: that `archived_at` goes back to null while nothing else moves, that version rows and their `published_at` stamps survive byte for byte, that restore-vs-restore leaves exactly one winner and one audit row, and that a restored template — including one public an instant earlier — is invisible to the `APP3-B05` public read. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B05` | repository tool | checkpoint gate (APP3-B05) | `node tools/check-app3-b05.mjs` | tools/check-app3-b05.mjs | Any change to the public Design Template reads: the PUBLISHED-header and published-version predicates, the highest-`published_at` selection, the exact `product → side → area` triple, the read-time scope-eligibility call into Catalog, the single non-disclosing 404, the scope-bound keyset cursor, or the projection. Asserts the **negations** — no third public Template operation and no `APP3-B05A` delivery route, no auth guard, no write, transaction, audit row, outbox event or object-storage reach, no second definition of a Design Document or of Catalog public eligibility, and no migration, dependency or root script. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B05` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b05.test.mjs` | tools/check-app3-b05.test.mjs | Any edit to `check-app3-b05.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the checker refuses it. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B05-API` | `@embroidery/api` | Docker-free B05 contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=public-design-template.spec --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/public-design-template.spec.ts | Any change to the public read contract, the request schemas, the cursor codec or the projection. Reads the committed OpenAPI artifact, so it must run after generation. The pattern ends at `.spec` on purpose: a bare `public-design-template` also matches the integration suite and would silently start a database. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B05-INTEGRATION` | `@embroidery/api` | public Template live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=public-design-template.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/public-design-template.integration.spec.ts | Any change to public-read persistence, the visibility predicates or scope eligibility. Starts a disposable PostgreSQL database and runs the whole HTTP stack; no MinIO, because `APP3-B05` streams no bytes. Carries the four proofs only a live database can give: the full publish/unpublish/republish/archive visibility cycle driven through the real `APP3-B03`/`B03A`/`B04` surfaces; that a hand-written higher **unpublished** version is never leaked even when `current_version` names it; that unpublishing the *Product* hides its Templates while leaving every Template row byte-for-byte unchanged; and a whole-schema digest proving every public read writes nothing. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B05A` | repository tool | checkpoint gate (APP3-B05A) | `node tools/check-app3-b05a.mjs` | tools/check-app3-b05a.mjs | Any change to published Design Template asset delivery: the one contextual route, the six-term authorization, the current-published-version selection, the P01 document-membership proof, the durable association join, the read-time Catalog eligibility call, the derivative predicate, the streaming order or the binary transport. Asserts the **negations** — no generic public asset address, no query or body rendition selector, no ORIGINAL/THUMBNAIL/CATALOG_PREVIEW/PREVIEW_WATERMARKED/MOCKUP, no re-sanitization on read, no presign or storage identity, no ETag/Range/validator/filename, no cacheable response, no write, audit row, outbox event or normalization request, and no migration, dependency, worker, frontend or root script. Also pins the **aliased** newer-version self-join, without which every historical published version stays addressable. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B05A` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b05a.test.mjs` | tools/check-app3-b05a.test.mjs | Any edit to `check-app3-b05a.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working route: the newer-version clause dropped so history stays addressable, the document proof removed so the cumulative association alone authorizes, the eligibility call removed so a withdrawn Product keeps serving, storage opened before the decision, and `image/svg+xml` widened to the raw source. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B05A-API` | `@embroidery/api` | Docker-free B05A contract and authorization tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=public-design-template-asset --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/public-design-template-asset.spec.ts · apps/api/src/modules/design/presentation/public-design-template-asset.contract.spec.ts | Any change to the delivery contract, the path schema, the authorization order or the safe error vocabulary. Docker-free: the repository and the storage port are doubles, so what is proved is the decision — which terms were required, in what order, and that a probe never reaches the provider. The document half runs against **real** P01 structure validation, because a stub answering "does this version place this asset" would test nothing. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B05A-INTEGRATION` | `@embroidery/api` | published Template asset live-stack suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=public-template-asset --runInBand --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/public-template-asset-delivery.integration.spec.ts · apps/api/test/integration/public-template-asset-revocation.integration.spec.ts · apps/api/test/support/published-template-asset-context.ts | Any change to delivery persistence, revocation or streaming. Starts a disposable PostgreSQL **and** MinIO and drives the whole HTTP stack, building the Template through the real B03/B03A/B03B/B04 services so the association under test is the one production writes. Carries the proofs only a live stack can give: the exact bytes and headers for raster and sanitized SVG; unpublish, archive, product withdrawal, side retirement and area retirement each revoking the next request; publishing v2 retiring v1’s address while v1’s `published_at` remains set; association-without-reference and reference-without-association both refused; a missing object and a size contradiction both 503; five invisible states answering byte-identically; and a zero-delta whole-table count across served and refused reads. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B06C` | repository tool | checkpoint gate (APP3-B06C) | `node tools/check-app3-b06c.mjs` | tools/check-app3-b06c.mjs · tools/check-app3-b06c-security.mjs · tools/check-app3-b06c-stream.mjs | Any change to private Design Session asset delivery: the one contextual route, the read guard composed from the accepted `APP3-B06A` primitives, the `DESIGN_SESSION_ASSET` ownership join, the upload lane and inspection verdict, the derivative predicate and `APP3-DB01` quartet, the descriptor-before-storage order or the binary transport. Asserts the **negations** — no generic public asset address and no second Session delivery address, no query or body rendition selector, no `ORIGINAL`/SVG/`THUMBNAIL`/`CATALOG_PREVIEW`/`PREVIEW_WATERMARKED`/`MOCKUP`, no raw path `sessionId` used as an identity, no mutation guard, no `expectedRevision`/CAS/revision advance, no consumption of the PO-07 mutation limit, no issued or rotated cookie, no presign or storage identity, no ETag/Range/validator/filename, no cacheable response, no write, audit row or outbox event, and no migration, dependency, worker, frontend or root script. Also pins the `CONTROLLER_DOMAIN_KEYS` mapping, without which the controller split would silently reissue `publicDesignSessionAsset_create`. Cache and header rules are asserted against the **values sent**, never by banning a word the published description legitimately uses. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B06C` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b06c.test.mjs` | tools/check-app3-b06c.test.mjs | Any edit to `check-app3-b06c.mjs`, its two halves, or the sources they parse. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working route: the route rewritten as a generic asset-by-id, the guard removed, the association predicate dropped so any session reads any asset, the identity taken from the path instead of the credential, `ORIGINAL` made a candidate, the `READY`/`NORMALIZED` predicate dropped, SVG widened into the allowlist, storage opened before the descriptor, the cache made public and immutable, a provider size mismatch streamed anyway, the GET advancing the revision, a second operation added, and Storefront source appearing in the checkpoint. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B06C-API` | `@embroidery/api` | Docker-free B06C contract and authorization tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-session-asset-delivery --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-session-asset-delivery.spec.ts · apps/api/src/modules/design/presentation/design-session-asset-delivery.contract.spec.ts | Any change to the delivery contract, the path schema, the read guard or the safe error vocabulary. Docker-free: the delivery repository and the storage port are doubles, so what is proved is the decision — which terms were required, in what order, and that a probe never reaches the provider. The session half runs against the **real** `AuthorizeDesignSessionService`, verifier and cookie policy, because "a cookie for session A cannot read session B" is the one claim a stubbed authorizer would only assert about itself. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B06C-INTEGRATION` | `@embroidery/api` | Session asset delivery live-stack suite | `pnpm --filter @embroidery/api exec jest --config jest.design-session-asset.config.mjs` | apps/api/test/integration/design-session-asset-delivery.integration.spec.ts · apps/api/test/support/design-session-asset-context.ts | Any change to delivery persistence, session authorization or streaming. Starts a disposable PostgreSQL **and** MinIO and drives the whole HTTP stack with a real `__Host-` session cookie, reusing the accepted `APP3-B06B-C1` harness rather than adding a second one. Carries the proofs only a live stack can give: the exact bytes and headers; a foreign credential and a foreign association both refused with no object read; eleven ineligible states answering identically to an unknown id; a missing object and a size contradiction both 503 with the persisted metadata unrepaired; and a zero-delta whole-table count across served and refused reads. Also proves a `READY` `NORMALIZED` row without the quartet is unrepresentable, which is why that miss is seeded as `PROCESSING`. The same config runs the `APP3-B06B` intake suite; both must pass. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S06` | repository tool | checkpoint gate (APP3-S06) | `node tools/check-app3-s06.mjs` | tools/check-app3-s06.mjs · tools/check-app3-s06.sources.mjs · tools/check-app3-s06-repairs.mjs · tools/check-app3-s06-frontend.mjs | Any change to the Studio image capability or to either cross-layer seam it repaired: the worker inspection lane, the full-decode verification, the Session asset status projection, the cloned-Session media grant, the read limit, the image authority, the polling, the object-URL lifecycle, the SVG renderer or the responsive composition. Asserts the **negations** — no lane matched on a kind alone, no derivative written by the Session lane, no decode skipped where nothing generates one, no status field beyond the `APP3-DB01` quartet, no document grant reaching a customer upload, no Template lineage consulted, no read limit charged after authorization, no `naturalWidth`/`getBBox`/Blob measurement, no polling of the binary route, no object URL in the document or a store, no second SVG root, canvas or stage `<img>`, no second topbar or drawer, no mobile editing surface, and no autosave, layers, history, watermark, crop or opacity pulled forward. Cache, disclosure and replacement rules are asserted against the **values published**, never by banning a word the honest description uses. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S06` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s06.test.mjs` | tools/check-app3-s06.test.mjs | Any edit to `check-app3-s06*.mjs`. Mutation tests: each load-bearing rule is proved to fail when the thing it protects is removed or substituted, so a rule that silently stops asserting anything is caught rather than assumed. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S06-STOREFRONT` | `@embroidery/storefront` | S06 model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-image"` | apps/storefront/test/unit/studio-image-model.test.ts · apps/storefront/test/components/studio-image.test.tsx | Any change to the image affordance, the placement or replacement rules, the candidate authorities, the polling or the responsive composition. The component suite runs entirely on a **fake clock** — polling is half its subject, and a suite that waited two real seconds a poll would take a minute to prove what a virtual clock proves exactly. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S06-WORKER` | `@embroidery/worker` | Session inspection lane, unit and live | `pnpm --filter @embroidery/worker exec jest --testPathPatterns="asset-inspection-lane\|source-verification" && pnpm --filter @embroidery/worker exec jest --runInBand --testPathPatterns="session-inspection-lane[.]integration" --testPathIgnorePatterns=/node_modules/` | apps/worker/src/jobs/asset-inspection/domain/asset-inspection-lane.spec.ts · apps/worker/src/jobs/asset-inspection/application/source-verification.service.spec.ts · apps/worker/src/jobs/asset-inspection/tests/session-inspection-lane.integration.spec.ts | Any change to lane resolution, the full-decode verification or the inspection lifecycle. The live half starts a disposable PostgreSQL **and** MinIO and drives the real use case: a valid Session raster reaches `ACCEPTED` writing no derivative and no object, a truncated PNG that passes every signature check reaches `REJECTED`, both replay, a `CUSTOMER_UPLOAD` carrying the wrong classification is still refused, and the catalog lane still writes both derivatives. Needs a Docker daemon. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S06-API` | `@embroidery/api` | status projection and cloned-media grant | `pnpm --filter @embroidery/api exec jest --testPathPatterns="design-session-asset-status" && pnpm --filter @embroidery/api exec jest --config jest.design-session-asset.config.mjs` | apps/api/src/modules/design/design-session-asset-status.spec.ts · apps/api/test/integration/design-session-asset-status.integration.spec.ts | Any change to the status projection, the Session media grant or the read limit. The unit half proves the read limit on an **injected clock** — 60 admitted, the 61st refused, admitted again once the window moves, a separate network key independent, the mutation counter untouched — with no real waiting anywhere. The live half proves the projection against real rows and the cloned-media grant against a real document, including that a document reference can never reach another customer’s upload. The same config also runs the `APP3-B06B` intake and `APP3-B06C` delivery suites; all three must pass. Needs a Docker daemon. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S04` | repository tool | checkpoint gate (APP3-S04) | `node tools/check-app3-s04.mjs` | tools/check-app3-s04.mjs · tools/check-app3-s04-runtime.mjs · tools/check-app3-s04.sources.mjs · tools/check-app3-s04-status.mjs | Any change to the Studio layer capability: the layer projection, the reorder, the lock or hide mutation, the panel composition, the renderer paint order or the Studio stores. Asserts the **negations** — no second z-order representation anywhere in the feature, no reversed or re-ranked renderer, no index-keyed row, no internal identifier as customer copy, no reorder that touches a transform, a flag, a media field or parentage, no drag-only reorder, no boundary control disabled without a stated reason, no lock implemented as CSS, no hide implemented as a delete or an opacity, no candidate committed without APP3-P01, no autosave or history, no group construction and no multi-selection, no fourth store, no second drawer and no mobile editing surface. The group ruling is asserted as recorded status, because an unauthorized capability and an unfinished one look identical in source. Does not read the completion report. Read-only: no database, no container, no network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S04` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s04.test.mjs` | tools/check-app3-s04.test.mjs | Any edit to `check-app3-s04*.mjs`. Mutation tests: each load-bearing rule is proved to fail when the thing it protects is removed or substituted, so a rule that silently stops asserting anything is caught rather than assumed. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S04-STOREFRONT` | `@embroidery/storefront` | S04 model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-layers"` | apps/storefront/test/unit/studio-layers-model.test.ts · apps/storefront/test/components/studio-layers.test.tsx | Any change to the layer projection, the three document mutations, the panel or its responsive composition. The component suite drives the real `StudioStageScreen`, so a restack is proved against the SVG paint order rather than against the list alone. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S08` | repository tool | checkpoint gate (APP3-S08) | `node tools/check-app3-s08.mjs` | tools/check-app3-s08.mjs · tools/check-app3-s08-runtime.mjs · tools/check-app3-s08.sources.mjs | Any change to Studio undo/redo, the one working-document store, a capability that commits a document, or the APP3-P01 schema. Asserts the **negations** — no engine or renderer history stack, no second current document, no unbounded past, no snapshot or clone on a pointer frame, no debounce timer as an edit boundary, no history in localStorage/sessionStorage/IndexedDB/a cookie/the URL, no autosave, revision move, Asset delete or re-upload from an undo, no selection/zoom/pan/watermark token in a snapshot, no redo surviving a new forward mutation, no raw element/asset/derivative id in a row, no group action, no unapproved time travel in the list, no third keyboard binding, no double-handled text undo, no APP3-S11 mobile sheet and no second drawer at 1024. Does not read the completion report. Read-only: no database, no container, no network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S08` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s08.test.mjs` | tools/check-app3-s08.test.mjs | Any edit to `check-app3-s08*.mjs`. Mutation tests: each load-bearing rule is proved to fail when the thing it protects is removed or substituted, so a rule that silently stops asserting anything is caught rather than assumed. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S08-STOREFRONT` | `@embroidery/storefront` | S08 model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-history"` | apps/storefront/test/unit/studio-history-model.test.ts · apps/storefront/test/components/studio-history.test.tsx · apps/storefront/test/components/studio-history-integration.test.tsx · apps/storefront/test/support/studio-history-harness.tsx | Any change to the history model, the store that holds it, the panel, the shortcuts, or a capability that commits a document. Both component suites drive the real `StudioStageScreen`, so "one gesture is one entry" is proved against a real multi-frame drag and "undo restores the design" against the SVG paint order. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S09` | repository tool | checkpoint gate (APP3-S09) | `node tools/check-app3-s09.mjs` | tools/check-app3-s09.mjs · tools/check-app3-s09-runtime.mjs · tools/check-app3-s09.sources.mjs | Any change to the Studio runtime watermark, the APP3-S07 viewport overlay slot or the APP3-P01 document schema. Asserts the **negations** — no watermark field or element type in APP3-P01, nothing that writes the mark into a document or a store, no anchoring to the transformed layer, no Math.random, no token minted through a recomputable memo or derived from a Session id, cookie, user agent, storage key or asset id, no token persisted or logged, no single contrast treatment, no colour literal, no sampling of customer pixels, no pointer or selection capture, no interactive control inside the mark, no export or download affordance, no PrintScreen/contextmenu/blur screenshot theatre, no second SVG root or canvas, and no watermark built outside the four files this checkpoint owns. Does not read the completion report. Read-only: no database, no container, no network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S09` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s09.test.mjs` | tools/check-app3-s09.test.mjs | Any edit to `check-app3-s09*.mjs`. Mutation tests: each load-bearing rule is proved to fail when the thing it protects is removed or substituted, so a rule that silently stops asserting anything is caught rather than assumed. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S09-STOREFRONT` | `@embroidery/storefront` | S09 model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-watermark"` | apps/storefront/test/unit/studio-watermark-model.test.ts · apps/storefront/test/components/studio-watermark.test.tsx | Any change to the watermark token, pattern, overlay or policy copy. The component suite drives the real `StudioStageScreen` and canonicalizes the working document before and after the overlay mounts, so "not serialized" is proved against APP3-P01 output rather than asserted. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S10` | repository tool | checkpoint gate (APP3-S10) | `node tools/check-app3-s10.mjs` | tools/check-app3-s10.mjs · tools/check-app3-s10-runtime.mjs · tools/check-app3-s10.sources.mjs | Any change to the Studio autosave loop, the save-state surfaces, the conflict decision, the resume handle or the six approved section-14 design rows. Asserts the rules that stay green while being wrong: a cadence of **2500 ms of quiet under a 10000 ms ceiling measured from the first dirty mutation**, with the ceiling proved to participate in the delay rather than merely exist; exactly one request in flight; every `expectedRevision` a revision the client **read**, with `previous + 1` unrepresentable anywhere in the feature; a save answering for the document it was given, so an older response cannot overwrite a newer local edit and the canonical form it returns is adopted through a seam that records no history entry; a `409` and a response-less outcome both reading the latest Session state **once** before anything is decided, with an unmoved revision read as "nothing was written"; exactly the two conflict choices `610:118` authorizes, where loading the latest replaces the branch **and** the past that described it while keeping the local one preserves both and saves once against the revision the server holds. Asserts the **negations** — no `setInterval`, polling, background sync, service worker, `sendBeacon` or save fired from `beforeunload`; no merge, third conflict button or automatic resolution; no unbounded retry; no browser storage outside the one resume-handle module and nothing in it but a validated Session id under a placement-namespaced key — never a document, revision, expiry or secret; no `document.cookie` read and no client-side TTL extension; no durable document or history cache and no persistence dependency; no hand-written API path or raw transport, with the one write called from exactly one file; no APP3-S11 mobile control; a save chip whose every state carries a word and whose time comes only from a real successful save; and an unchanged OpenAPI artifact of 37 paths / 42 operations / 84 schemas, generated client, migration count and root-script count. **World-aware**: every runtime rule applies only once the phase records S10 delivered. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S10` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s10.test.mjs` | tools/check-app3-s10.test.mjs | Any edit to `check-app3-s10*.mjs`. Mutation tests: each load-bearing rule is proved to fail when the thing it protects is removed or substituted — a silently changed cadence, an interval, a second in-flight request, a response revision ignored, a `409` retried blindly, a lost response replayed without reconciliation, a newer local edit overwritten, a conflict auto-merged, a third conflict choice invented, the local branch saved before the customer chose it, a document or secret written to storage, a resume handle stripped of its placement namespace, `document.cookie` read, an expired handle retained, a stale history branch kept after load-latest, an autosave recorded as a history entry, an APP3-S11 mobile control, a manual Axios route replacing the generated client, and a changed API surface. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S10-STOREFRONT` | `@embroidery/storefront` | S10 model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-autosave"` | apps/storefront/test/unit/studio-autosave-model.test.ts · apps/storefront/test/components/studio-autosave.test.tsx · apps/storefront/test/components/studio-autosave-conflict.test.tsx · apps/storefront/test/components/studio-autosave-resume.test.tsx · apps/storefront/test/support/studio-autosave-harness.tsx | Any change to the autosave cadence, state machine, error taxonomy, conflict decision, resume handle or save surfaces. The three component suites drive the real `StudioStageScreen` and `StudioScreen` on a **fake clock**, so the 2500 ms debounce, the 10000 ms ceiling and the [5000, 15000] retry ladder are proved by advancing time rather than by waiting — no real sleep anywhere, and no rate window is ever exercised for real. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-BROWSER-APP3-S10` | repository tool | real-browser journey (APP3-S10) | Playwright against `/san-pham/[slug]/thiet-ke` on the development stack | apps/storefront (route) · tools/smoke-app3-s01-fixtures.mjs · tools/smoke-app3-s01-trustworthy-origin.mjs | The half no unit test can supply: a real anonymous Session on the real route, edited until the loop saves it, at 1440, 1024 and 390. Proves the chip moving `Chưa lưu → Đang lưu… → Đã lưu` against real `PUT /api/public/design-sessions/{id}/document` traffic, an edit made during an in-flight save surviving and being carried by the next one, a genuine two-client `409` reconciled to the approved conflict state with the server left unwritten, an intercepted transport failure producing the offline state with bounded retry rather than a request loop, a full reload resumed from the namespaced non-secret handle, and an invalidated Session producing the expired state with its handle cleared. Also records the shell composition — one topbar, one rail, one drawer, one stage — and that 390 renders no APP3-S11 surface. Requires the development stack and `CMD-SMOKE-APP3-S01-FIXTURES`; **one Session per journey on purpose**, because `IMP-D043` PO-07 caps anonymous Session creation at 5 per hour per IP and the run is designed to fit inside that guard rather than restart the API to clear it. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S11` | repository tool | checkpoint gate (APP3-S11) | `node tools/check-app3-s11.mjs` | tools/check-app3-s11.mjs · tools/check-app3-s11-runtime.mjs · tools/check-app3-s11.sources.mjs | Any change to the Studio touch gestures, the 390 composition, the five mobile sheets or the six approved section-15 design rows. Asserts the rules that stay green while being wrong: an arbitration that is a **total function of the live touch count** rather than an inference from movement, bound in the **capture** phase so a finger landing on an element is still counted; a pinch that resolves to an index in the frozen `APP3-S07` zoom list and never to a scale, measured from the separation the gesture began with; viewport gestures that reach the camera and nothing else — no document seam, no history, no autosave, and no `PointerEvent` in a store; a second finger that closes an open element drag through `APP3-S03`'s own cancellation; every transform-sheet press ruled on by `APP3-P02` with no clamp, snap or repair, writing **scale** rather than a millimetre and reading back a **measured** size; one modal sheet primitive with focus entered, contained both ways and returned, background scroll locked, a scrim that is a button rather than a clickable div, and a conflict that draws no close control at all; the `APP3-S10` conflict **relocated** rather than duplicated, with the desktop region suppressed at 390 and the locked cadence untouched; a composition mounted **by tier rather than hidden by CSS**, with no second toolbar, no create-text control and no group; 44 px bound to the shared token, `touch-action` scoped to the stage surface and never the page, and every glyph carrying a word; a keyboard inset measured from `VisualViewport` rather than assumed, with its listeners removed; and one native SVG, the `APP3-S09` watermark, no export path, no gesture dependency, and an unchanged OpenAPI artifact of 37 paths / 42 operations / 84 schemas, migration count and root-script count. **World-aware**: every runtime rule applies only once the phase records S11 delivered. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S11` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s11.test.mjs` | tools/check-app3-s11.test.mjs | Any edit to `check-app3-s11*.mjs`. Mutation tests: each load-bearing rule is proved to fail when the thing it protects is removed or substituted — an arbitration decided by movement instead of by count, a bubble-phase binding that cannot see a finger on an element, a pinch that computes a scale, a viewport gesture that commits a document or appends history, a second finger that leaves a drag open, a transform press that skips `APP3-P02` or clamps its refusal, a millimetre button that writes width, an echoed rather than measured read-out, a sheet that leaks focus or dismisses a conflict, a duplicated conflict surface, a mobile toolbar hidden rather than absent, a second toolbar, a create-text control, a group control, a sub-44 px target, a globally disabled page scroll, a hard-coded keyboard height, a gesture dependency and a changed API surface. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S11-STOREFRONT` | `@embroidery/storefront` | S11 model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-mobile\|studio-touch"` | apps/storefront/test/unit/studio-touch-model.test.ts · apps/storefront/test/components/studio-mobile-touch.test.tsx · apps/storefront/test/components/studio-mobile-sheets.test.tsx | Any change to the touch arithmetic, the arbitration, the mobile shell or the five sheets. The model suite drives the pinch across every ratio from a tenth to ten times and asserts the answer is always one of the six frozen steps; the component suites drive the **real** `StudioStageScreen` at 390 with synthetic pointer contacts and assert on the persisted document, the viewport store and the history bound rather than on how the chrome looks. Docker-free, no fake clock needed — none of these paths is timed. | `ACTIVE_SCOPED` |
| `CMD-BROWSER-APP3-S11` | repository tool | real-browser journey (APP3-S11) | Playwright against `/san-pham/[slug]/thiet-ke` in a touch-enabled context | apps/storefront (route) · tools/smoke-app3-s01-fixtures.mjs · tools/smoke-app3-s01-trustworthy-origin.mjs | The half no unit test can supply: real touch contacts on a real Session at 390, and the two tiers above it proving nothing mobile leaked. Proves tap-select, a one-finger drag that lands a `P02`-valid document change and exactly one history entry that `APP3-S10` then saves, a pinch and a two-finger pan that move only the camera, the transform sheet committing and refusing, the layer sheet reordering from the `⋮⋮` handle, the text sheet editing with the keyboard raising rather than covering it, the image sheet reaching the existing `APP3-S06` upload path, undo and redo from the toolbar, the `APP3-S10` conflict arriving as `610:514` with no duplicate surface, and no stuck pointer, listener or scroll lock after a cancel. Requires the development stack and `CMD-SMOKE-APP3-S01-FIXTURES`; **one Session per journey on purpose**, because `IMP-D043` PO-07 caps anonymous Session creation at 5 per hour per IP and the run is designed to fit inside that guard rather than restart the API to clear it. | `ACTIVE_SCOPED` |
| `CMD-BENCH-APP3-S11` | repository tool | touch benchmark (APP3-S11) | `node tools/bench-app3-s11-touch.mjs` | tools/bench-app3-s11-touch.mjs · tools/bench-app3-s03-fixtures.mjs | `APP0` assigns benchmark responsibility to S02, S03, S07, **S11** and E01. Measures tap/select, one-finger move, pinch, two-finger pan, sheet open/close and layer reorder on the accepted S/M/L Studio scenes in a Chromium mobile touch context at 390×844, and reports p50/p95 per interaction. Emulation, stated as emulation: a device-pixel-ratio and a synthetic contact are not a finger, and the report says so rather than implying physical-device evidence. Also runs the L=100 desktop transform scene back to back against the immediately preceding accepted commit, so a regression introduced by this checkpoint is separated from `FU-APP3-TRANSFORM-BUDGET-01`, which stays open and un-bisected. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-E01` | repository tool | checkpoint gate (APP3-E01) | `node tools/check-app3-e01.mjs` | tools/check-app3-e01.mjs · tools/check-app3-e01.sources.mjs | Any change to the cross-layer journey harness, the ephemeral network key, the Template fixture lifecycle or an APP3 follow-up disposition. Asserts what a final evidence checkpoint gets wrong while looking right: `APP3-S10` and `APP3-S11` accepted and `APP3-X01` **not started**; the network key taken from the entry the **trusted gateway hop appended** rather than the one a client chose, with the appending gateway itself asserted so the fix cannot rest on an assumption that has changed, and the forged-header regression present in the accepted spec; Session capacity taken from **assigned runner addresses** rather than from a forged header — the harness may not contain the string `X-Forwarded-For` at all — or from a real wait; Template fixtures that restore the state they seed, so two accepted runs need no manual SQL between them; every follow-up this checkpoint owns carrying a recognised disposition, with no closure claimed without the measurement that earned it; and an unchanged artifact set of 37 paths / 42 operations / 84 schemas, 34 migrations and 30 root scripts. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-E01` | repository tool | checkpoint gate tests | `node --test tools/check-app3-e01.test.mjs` | tools/check-app3-e01.test.mjs | Any edit to `check-app3-e01*.mjs`. Mutation tests: each rule is proved to fail when the thing it protects is removed — the network key reverted to the client-controlled left-most entry, an empty forwarded entry allowed to choose the bucket, the appending gateway silently replaced, the forged-header regression deleted, the harness minting an identity with a header or sleeping out a window, a Template seed returned to `on conflict do nothing`, a follow-up closed without its measurement, `APP3-X01` started early, and a changed artifact set. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-JOURNEY-APP3-E01` | repository tool | cross-layer journey (APP3-E01) | `node tools/smoke-app3-e01.mjs` | tools/smoke-app3-e01.mjs · tools/smoke-app3-e01-journey.mjs · tools/smoke-app3-e01-studio.mjs · tools/smoke-app3-e01-security.mjs · tools/smoke-app3-e01-runners.mjs | The complete customer chain on the running topology: Admin publishes a Studio-compatible Product and Template, the Storefront resolves the placement and lists exactly that scope, a Blank and a Clone Session open through the gateway with a real `__Host-` cookie, the Studio renders one native SVG from the canonical document, a customer image travels intake → worker inspection → normalization → private `APP3-B06C` delivery, desktop and 390 touch editing both commit through `APP3-P02`, a genuine two-client stale write produces a real `409` with exactly the two approved choices, and the security negatives hold — no private original, no generic Asset route, no cross-Session upload, no raw secret anywhere. Session capacity comes from **assigned runner container addresses**, one per role, so the `IMP-D043` PO-07 limits are respected rather than evaded; rate-limit *policy* is proved separately on injected time. | `ACTIVE_SCOPED` |
| `CMD-JOURNEY-APP3-E01-C1` | repository tool | revision-seam browser proof (APP3-E01-C1) | `node tools/smoke-app3-e01-c1.mjs [role]` | tools/smoke-app3-e01-c1.mjs · tools/smoke-app3-e01-journey.mjs · tools/smoke-app3-e01-runners.mjs | The one real-browser proof that `FU-APP3-UPLOAD-REVISION-SEAM-01` is repaired: a fresh anonymous Session clones a Template, a real image travels intake → worker inspection → normalization → placement, and the autosave that follows presents **the revision the upload returned** and is accepted `200` rather than refused `409`. Records numbers rather than a verdict — the upload response revision, the revision the save presented, the HTTP status, the response revision, the save chip, and what a full reload restores through the private `APP3-B06C` route — because a journey reporting only "green" is indistinguishable from one measuring the wrong thing. Needs the run identities and the origin allow-list already up (`node tools/smoke-app3-e01.mjs --keep` leaves them); one Session, inside the `IMP-D043` PO-07 limits, and no forged header, waited-out window or restarted API. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-E01-C1-SEAM` | `@embroidery/storefront` | revision-seam focused tests (APP3-E01-C1) | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns=studio-upload-revision-seam` | apps/storefront/test/components/studio-upload-revision-seam.test.tsx | Any change to the Session-revision authority or to either capability that mutates a Session. Cross-capability by construction: the `APP3-S06` and `APP3-S10` suites each proved their own half and both passed while the customer’s save was being refused. Covers the revision an upload returned being the one the next autosave presents with the save accepted, the same in the other direction so an upload after a save is not stale, a revision never computed locally even when the server skips numbers, no advance from a failed upload, adoption marking nothing clean and clearing no history and arming no save, a revision that cannot move backward when an older answer lands last, and a response measured against a Session the customer has left never being adopted. Mocks the generated client, never a raw URL; fake clock; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-CLOSURE` | repository tool | phase closure gate (APP3-X01) | `node tools/check-app3-closure.mjs` | tools/check-app3-closure.mjs · tools/check-app3-closure.sources.mjs · tools/check-app3-closure-artifacts.mjs · docs/implementation/reports/APP3-CLOSURE-MATRIX.md · docs/implementation/phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md §10.1 | Any change to the APP3 closure matrix, the §10.1 closure register, an APP3 follow-up disposition or a frozen APP3 artifact. Holds the closure verdict answerable to the repository rather than to itself: the register carries exactly one current word per key (a duplicate is a failure, not something to resolve by position), every executed checkpoint is derived from the report directory and must be recorded and accepted with two real commits, no ruled-out checkpoint exists and no cancelled correction is counted as delivered, every open follow-up is nonblocking with a concrete owner and an activation condition, accepted performance debt still names its measured numbers, capabilities APP3 did not deliver are recorded as not delivered, the frozen artifacts are recomputed, and APP4 has not started. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-CLOSURE` | repository tool | closure gate tests | `node --test tools/check-app3-closure.test.mjs` | tools/check-app3-closure.test.mjs | Any edit to `check-app3-closure*.mjs`. Twenty-four mutation cases, each breaking exactly one thing the gate protects in a throwaway copy and requiring the matching rule to fail: a second register line for a key, `APP3-E01-C1` un-accepted, the upload-revision finding reopened, a checkpoint dropped from the matrix, a commit replaced by a dash, the closure checkpoint recording itself as accepted rather than delivered, a ruled-out checkpoint on disk, a cancelled correction counted as delivered, an ownerless or blocking follow-up, transform debt falsely closed or stripped of its figures, group creation moved into the delivered column, a production-readiness claim, `PASS` with follow-ups open, artifact drift, and an APP4 report appearing early. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S01` | repository tool | checkpoint gate (APP3-S01) | `node tools/check-app3-s01.mjs` | tools/check-app3-s01.mjs · tools/check-app3-s01.sources.mjs · tools/check-app3-s01-runtime.mjs | Any change to the Storefront Studio bootstrap: the canonical `/san-pham/[slug]/thiet-ke` route, the five approved section-05 design rows, the public placement manifest as the sole Side/Area authority, deterministic IMP-D041 PO-04 selection and its cascade — including that the initial Side is the canonical **first** active row with the ordered list never narrowed by usability first, and that a selected Side with no Area stays selected while closing the Template, preview and Session work downstream of it — exact-triple Template compatibility and keyset continuation, the B05A-contextual preview and its object-URL lifecycle, or the BLANK/CLONE_TEMPLATE bootstrap and resume. **World-aware on the route**: before S01 the Studio route must be absent, after it the same route must exist, so the gate never accepts whatever routes happen to exist. Asserts the **negations** — no competing `/studio`, `/editor`, `/thiet-ke/[id]`, `/design-session/[sessionId]` or `/san-pham/[slug]/studio`; no Admin, autosave or session-asset operation; no raw transport, hard-coded API path or storage address; no fabricated offset, page, total, search or sort; no per-row detail read; no `placeholderData` under the preview key; no clone-to-blank fallback; no localStorage, sessionStorage, cookie write, URL parameter or Zustand store carrying a Session identity; no renderer concern in the bootstrap files S01 owns, and — **world-aware since APP3-S02** — exactly one native-SVG renderer in the feature once S02 has delivered one, none before it, never a canvas, and no Zustand store able to hold a Session identity; and an unchanged OpenAPI artifact, generated client, migration count and root-script count. Also asserts every later Studio design row is **still** `REVIEW_REQUIRED`, so a blanket approval cannot pass. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S01` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s01.test.mjs` | tools/check-app3-s01.test.mjs | Any edit to `check-app3-s01.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working screen: a competing Studio route added, a blanket Studio design approval, the compatibility triple dropped from the query key, `placeholderData` restored to the preview, object-URL revocation removed, a clone failure falling back to a blank session, a Session id written to `localStorage`, and — since APP3-S02 — a second `<svg>` renderer, a renderer inside the bootstrap screen, and a Session identity reaching the interaction store. Also restores the exact refinement human review rejected in `APP3-S01-C1` — an initial Side chosen from a list narrowed by area count, written as a `find`, as a `filter` and as a named helper — and proves the gate refuses each. Rewinds the phase document to prove the pre-S01 bans still bite. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S01-STOREFRONT` | `@embroidery/storefront` | S01 model, component and boundary tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns=studio` | apps/storefront/test/unit/studio-model.test.ts · apps/storefront/test/components/studio-bootstrap.test.tsx · apps/storefront/test/components/studio-session.test.tsx · apps/storefront/test/boundary/design-studio-source.test.ts | Any change to the Studio feature, the shell route authority or the curated api-client exports it consumes. Covers deterministic Side/Area selection and the reducer cascade, the canonical first active Side and the bounded state of a selected Side with no Area (no Template, detail, preview or Session request, no Blank or Clone affordance, no auto-jump to a usable Side), exact-triple compatibility and the late-response defence, keyset continuation, the empty list preserving an explicit Blank, the B05A preview blob create/revoke lifecycle with its 404 and 503 branches, text-only Templates, both bootstrap branches with no clone-to-blank fallback, duplicate-submit blocking, resume by the in-memory id with no secret argument, the 401 expiry state, and the static source boundaries. Mocks the generated client, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S02` | repository tool | checkpoint gate (APP3-S02) | `node tools/check-app3-s02.mjs` | tools/check-app3-s02.mjs · tools/check-app3-s02.sources.mjs · tools/check-app3-s02-runtime.mjs | Any change to the Storefront Studio stage: the renderer adapter, the native SVG scene, the interaction store, the Side-background consumer or the three approved section-06 design rows. Asserts **exactly one** `<svg>` root in the feature and no `<canvas>`; no rendering or interaction engine in the Storefront manifest or its source; that the adapter validates the document through APP3-P01 and takes every matrix, bound, stroke constant and font from APP3-P02 and the controlled registry; that the stage consumes the adapter rather than resolving geometry itself; that the viewBox is the document placement canvas and the element array is never sorted or reversed. Asserts the **negations** — no local matrix, trigonometry or px↔mm arithmetic; no geometry taken from the DOM (`getBoundingClientRect`, `DOMRect`, `DOMMatrix`, `getBBox`); no spike or Admin-feature import; no multi-selection, DOM node, blob, object URL, session identity or later-checkpoint state in the store; no `placeholderData` and no retained no-store blob; no Session asset route, no B05A shortcut from an open stage, no generic media route, no autosave; no transform mutation, drag, wheel, touch, layer, watermark or undo capability; and an unchanged OpenAPI artifact, generated client, migration count and root-script count. **World-aware**: every runtime rule applies only once the phase records S02 delivered, and the design rows must be `REVIEW_REQUIRED` before it. Also asserts every later Studio capability row is **still** `REVIEW_REQUIRED` and the 1024 tablet frame still belongs to APP3-D01-C1, so neither a blanket approval nor a re-attributed reference can pass. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S02` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s02.test.mjs` · `node --test tools/check-app3-s02-evolution.test.mjs` | tools/check-app3-s02.test.mjs · tools/check-app3-s02-evolution.test.mjs | Any edit to `check-app3-s02.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working screen: a second `<svg>` renderer, a matrix composed in the stage instead of asked of the engine, a reversed paint order, a selection outline measured with `getBoundingClientRect`, a rendering engine added to the manifest, an object URL never revoked, `placeholderData` restored over the no-store background, the Template asset route used as a Session media shortcut, a pointer-move handler added early, and a blanket Studio design approval. Rewinds the phase document to prove the pre-S02 rules still bite. The **evolution** half is a sibling suite by responsibility: it asks whether narrowing a ban for `APP3-S07` or `APP3-S03` opened a hole in it — a gesture or a measurement in a file the later checkpoint did not introduce, either of them arriving before that checkpoint opened, or a design row approved ahead of the capability that owns it. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S02-STOREFRONT` | `@embroidery/storefront` | S02 adapter, stage and media tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns=studio-stage` | apps/storefront/test/unit/studio-scene.test.ts · apps/storefront/test/components/studio-stage.test.tsx · apps/storefront/test/components/studio-stage-media.test.tsx · apps/storefront/test/components/studio-stage-scale.test.tsx | Any change to the renderer adapter, the stage components, the interaction store or the Side-background hook. Covers the P01 document boundary and its three bounded refusals, paint order, the engine matrix and bounds for transformed and grouped elements, the controlled font registry, hidden elements, single-element selection by pointer and keyboard with stale-selection reconciliation, the background blob create/revoke lifecycle with its 404 and 503 branches, the deferred image placeholder at real geometry with no asset identity on screen, and the absence of every S03–S11 capability. Mocks the generated client, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-BENCH-APP3-S02-EDITOR` | `@embroidery/storefront` | production S/M/L scene proof (APP3-S02) | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns=studio-stage-scale` | apps/storefront/test/components/studio-stage-scale.test.tsx | Deliberate structural measurement of the shipped stage at 10, 50 and 100 elements (APP3-P01's own `maxElements`). Counts rendered SVG nodes against the document exactly, so wrapper explosion, a duplicated scene, a hidden second copy or a detached subtree left after unmount each show as an arithmetic disagreement. Structure only — `CMD-SPIKE-EDITOR-BENCHMARK` remains the sole performance authority and no budget is redefined here. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S07` | repository tool | checkpoint gate (APP3-S07) | `node tools/check-app3-s07.mjs` | tools/check-app3-s07.mjs · tools/check-app3-s07.sources.mjs · tools/check-app3-s07-runtime.mjs | Any change to the Storefront Studio viewport: zoom, pan, the safe-area toggle, the transformed wrapper or the three approved section-11 design rows. Asserts the WebKit mitigation **structurally**: the zoom is an index into a frozen finite set with no multiplier, no `Math.pow`, no wheel and no arbitrary scale anywhere in the feature; the transform is produced in one model and applied on one wrapper; the adapter, the SVG, the element component and the selection outline may not contain the word `zoom` at all, so no element matrix can be multiplied by it and no scene can be rebuilt for it; and `buildRenderableScene` stays memoised on the document alone. Asserts the **negations** — no viewport state in the selection store and no document, snapshot, DOM node, ref or animation-frame handle in the viewport store; no persistence to storage, cookie or URL; no API call, query or client reachable from the viewport; no viewport term in the background query key; no transform handle, touch gesture, layer, upload, undo or watermark capability; no locally derived second safe-area boundary; and an unchanged OpenAPI artifact, generated client, curated boundary, migration count and root-script count. Also asserts the APP3-S02 gate was **narrowed, not deleted**. **World-aware**: every runtime rule applies only once the phase records S07 delivered, and the design rows must be `REVIEW_REQUIRED` before it. Structural only — it cannot measure a millisecond, so `CMD-BENCH-APP3-S07-VIEWPORT` is required beside it. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S07` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s07.test.mjs` | tools/check-app3-s07.test.mjs | Any edit to `check-app3-s07.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working viewport: a zoom multiplied instead of stepped, a wheel handler restoring the continuous path, a scale reaching an element transform, the scene rebuilt on a zoom change, the viewport written to `localStorage`, a safe-area rectangle recomputed locally, a second `<svg>` added behind the first, a touch gesture arriving before APP3-S11, and a blanket Studio design approval. Rewinds the phase document to prove the pre-S07 rules still bite. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S07-STOREFRONT` | `@embroidery/storefront` | S07 viewport model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns=studio-viewport` | apps/storefront/test/unit/studio-viewport.test.ts · apps/storefront/test/components/studio-stage-viewport.test.tsx | Any change to the viewport model, the viewport store, the transformed wrapper or the controls. Covers the finite zoom set and its exact return to 1 after a round trip, the pan interval that collapses at the fitted step, the drag-to-ratio conversion and its bound, the transform string, and — in a rendered stage — that every element transform is byte-identical across zoom, pan, fit and the safe-area toggle, that the Session document object is unchanged by identity, that selection survives the transform, that the trailing click of a drag is swallowed while the next click is not, that a touch pointer never pans, that the background is fetched exactly once across every viewport action, and that the viewport resets on a new Session identity. Mocks the generated client, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-BENCH-APP3-S07-VIEWPORT` | repository tool | measured WebKit viewport proof (APP3-S07) | `node tools/bench-app3-s07-viewport.mjs` | tools/bench-app3-s07-viewport.mjs · tools/bench-app3-s07-fixtures.mjs | The measured half of the mitigation `ADR-APP0-001` assigned to this checkpoint, and the only authority for whether the risk is closed. Drives **Chromium and WebKit** through the real Storefront route, opening a real Session cloned from a seeded Template at the frozen APP0-R01 scene sizes (10, 50, 150 elements) and clicking the shipped controls — no mounted component and no synthetic scene. Records p50/p95, long tasks and DOM-node counts for one zoom step in, one step out, Fit and a bounded pan, and on the same page reproduces the **historical continuous path** by writing sixty arbitrary scales straight onto the layer, so the stepped result has a control group rather than an adjective. Fails on the frozen 20 ms p95 desktop transform budget; defines no new budget. Requires the development stack, `CMD-SMOKE-APP3-S01-ORIGIN` and `CMD-BENCH-APP3-S07-FIXTURES`. Long-running: one baseline run and one post-implementation run, never a confidence loop. | `ACTIVE_SCOPED` |
| `CMD-BENCH-APP3-S07-FIXTURES` | repository tool | benchmark scenes (APP3-S07) | `node tools/bench-app3-s07-fixtures.mjs seed` · `… revert` | tools/bench-app3-s07-fixtures.mjs | The viewport benchmark only. Seeds three published Design Templates carrying 10, 50 and 150 shape elements — the frozen APP0-R01 S/M/L sizes, so the numbers stay comparable with the ADR's — on the placement `CMD-SMOKE-APP3-S01-FIXTURES` already establishes, imported rather than copied so the two cannot drift onto different Sides. Fixture setup, never a command: no readiness evaluation, no concurrency token, no Audit or Outbox row. Targets the **development** database; never run it against another. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S03` | repository tool | checkpoint gate (APP3-S03) | `node tools/check-app3-s03.mjs` | tools/check-app3-s03.mjs · tools/check-app3-s03.sources.mjs · tools/check-app3-s03-runtime.mjs · tools/check-app3-s03-reuse.mjs | Any change to the Studio transform controls: the working document, the move/resize/rotate candidates, the DOM handle overlay, the physical read-out or the three approved section-07 design rows. Asserts the rules that stay green while being wrong: a move expressed in the **parent** frame through `APP3-P02` `invertMatrix` + `transformVector` rather than by adding a raw document delta to `x`/`y`; a resize persisted as `scaleX`/`scaleY` about the untransformed local-box centre and never as `width`/`height`; a rotation built on the starting angle about that same centre with no reach for bounds; and every candidate **quantized through `APP3-P01` before** `APP3-P02` measures containment and physical size, because a candidate that fits by a ten-thousandth of a pixel passes on the raw number and fails on the number that is stored. Asserts the **negations** — no clamp, snap, scale-down or warn-but-persist; no second store holding a document; no Session identity, DOM node or ref in the working-document store; no browser persistence; no `Math.cos`/`sin`/`tan`, `multiplyMatrices` or `DOMMatrix` anywhere in the feature and no `Math.atan2` outside the transform model; no pointer handler on the drawn element; no touch transform, undo, layer, text-property, upload, watermark or autosave capability; and an unchanged OpenAPI artifact, generated client, curated boundary, migration count and root-script count. Also asserts the S02 and S07 gates were **narrowed, not deleted**. **World-aware**: every runtime rule applies only once the phase records S03 delivered. Structural only — `CMD-BENCH-APP3-S03-TRANSFORMS` is required beside it. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S03` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s03.test.mjs` · `node --test tools/check-app3-s03-reuse.test.mjs` | tools/check-app3-s03.test.mjs · tools/check-app3-s03-reuse.test.mjs | Any edit to `check-app3-s03.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working editor: a resize that writes `width`, a grouped-child move that adds the document delta straight to `x`/`y`, containment measured before quantization, a clamp that quietly fits an invalid candidate, a rotation about the transformed-AABB centre, a second store holding a document, a Session id coming to rest in one, a pointer handler on the drawn element, and a touch gesture arriving before `APP3-S11`. Rewinds the phase document to prove the pre-S03 rules still bite. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S03-STOREFRONT` | `@embroidery/storefront` | S03 transform model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-transform|studio-scene-identity"` | apps/storefront/test/unit/studio-transform.test.ts · apps/storefront/test/unit/studio-scene-identity.test.ts · apps/storefront/test/components/studio-transform.test.tsx · apps/storefront/test/components/studio-transform-render.test.tsx | Any change to the transform candidates, the handle set, the candidate authority, the gesture hook or the overlay. Every geometric assertion is made against **the engine's own answer** rather than a transcribed constant: a move is checked by asking `APP3-P02` where the element ended up, a resize by asking how large it became, a rotation by asking where the pivot went. Covers root and grouped-child moves through a rotated parent, scale-only resize for all eight handles, centre-pivot invariance, clockwise rotation, quantization, the blocking refusals for containment and physical size with no clamp, the millimetre read-out and its independence from zoom, transforms at 400 % matching those at 100 %, handle counter-scaling, locked and hidden elements, touch refusal, and the working document's Session-identity lifecycle. Mocks the generated client; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-S05` | repository tool | checkpoint gate (APP3-S05) | `node tools/check-app3-s05.mjs` | tools/check-app3-s05.mjs · tools/check-app3-s05.sources.mjs · tools/check-app3-s05-runtime.mjs · tools/check-app3-s05-responsive.mjs | Any change to the Studio text capability: the editable field set, the font picker, the controlled-font delivery, the candidate authority, the inspector, or the three approved section-09 design rows. Asserts the rules that stay green while being wrong: a font picker derived from `DESIGN_FONT_REGISTRY` rather than from a literal list that agrees with it today; the exact `APP3-F01` Inter binaries served from the package that owns the registry, with no copy under `public/`, no CDN and no Google Fonts host anywhere in the feature; only the v1 `TextElement` fields editable, with no `letterSpacing`, `lineHeight`, curved text, text-on-path or thread-colour field; every limit and range read from `APP3-P01` rather than restated as a literal; characters counted as **code points**; a candidate validated for structure **before** complexity, then for the controlled variant, then by `APP3-P02`; and the **negations** — no `normalize('NFC')` on the write path, no truncation, slice or clamp that repairs a refused candidate; no `measureText`, `getBBox`, `createRange` or other local text-layout engine; no browser-persisted draft; no second store holding a document and no second `<svg>`; the `APP3-S03-C1` memoized element and previous-scene reuse still in place; no autosave, history, layer, upload, watermark or touch capability; and an unchanged OpenAPI artifact, migration count and root-script count. Since `APP3-S05-C1` it also rules on **where** the inspector goes and **which face** the browser was asked about: the tier authority routing desktop, tablet and mobile to three distinct compositions with no editing surface reachable from the mobile branch; the tablet drawer absolutely positioned, so opening it cannot re-lay-out the stage, carrying `aria-expanded`, `aria-controls` and a focus return to its trigger; the desktop breakpoint mirrored between `studio-responsive.ts` and the stylesheet; and a readiness probe built from the exact `fontId`, `fontStyle` and `fontWeight` with **no fallback family**, whose failure is proved to block the commit rather than merely warn, and whose superseded results are proved to be discarded. **World-aware**: every runtime rule applies only once the phase records S05 delivered, and the correction's own rules only once it records `APP3-S05-C1` delivered. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-S05` | repository tool | checkpoint gate tests | `node --test tools/check-app3-s05.test.mjs` | tools/check-app3-s05.test.mjs | Any edit to `check-app3-s05.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working editor: an arbitrary font added to the picker, a controlled binary copied into `public/`, a P01 text limit restated as a literal, an `NFC` rewrite on the write path, a truncation that repairs an over-long candidate, a glyph measurement used as geometry, a second `<svg>`, a de-memoized element component, an autosave call, a premature `APP3-S11` bottom sheet, and a blanket approval of a Studio row whose checkpoint has not opened. Since `APP3-S05-C1`, also: a family-only readiness probe, a probe missing its style or its weight, a fallback family added to the probe, a failed variant that commits anyway, a stale variant result that is not discarded, a tablet drawer put back into flow, an editing surface rendered at the mobile tier, an inspector mounted past the tier authority, and a stylesheet breakpoint moved away from the one the renderer uses. Rewinds the phase document to prove the pre-S05 rules still bite, and to prove the correction's rules do not run before it ships. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-S05-STOREFRONT` | `@embroidery/storefront` | S05 text model and component tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="studio-text"` | apps/storefront/test/unit/studio-text-model.test.ts · apps/storefront/test/components/studio-text.test.tsx · apps/storefront/test/components/studio-text-responsive.test.tsx | Any change to the editable text fields, the candidate authority, the controlled-font hook, the inspector controller or the inspector itself. The model half calls the authority directly: the registry-derived picker, the 500-code-point and 5000-document ceilings proved on both sides of the boundary and with an astral character, the non-NFC refusal built by normalizing rather than by a literal that a formatter could recompose, the unknown-font and unsupported-variant refusals, and the proof that a text edit changes text and leaves the id, transform, opacity, flags, `fill` and z-order identical. The component half drives the form: the four editability states, a live edit reaching the SVG, an IME composition that never commits mid-flight, a refused draft that cannot cross a selection change, zero API calls, one `<svg>`, and the `APP3-S03-C1` regression showing a one-element text edit re-renders zero unchanged siblings. Since `APP3-S05-C1` the responsive half drives the three compositions at three widths — the 1440 inspector in flow, the 1024 topbar-triggered drawer whose open state leaves the SVG's authored geometry byte-identical and whose close returns focus, and a 390 viewport with no editable field, no drawer and no bottom sheet — and the font half proves that upright and italic are distinct probes, that a successful upright does not make italic ready, that a Session opening on a non-default variant probes that variant, that a failed variant preserves the previous document value while a successful one commits the exact P01 value, and that a superseded result cannot overwrite a newer selection. Mocks the generated client; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-BENCH-APP3-S03-TRANSFORMS` | repository tool | measured transform proof (APP3-S03) | `node tools/bench-app3-s03-transforms.mjs --browser=chromium` · `… --browser=webkit` | tools/bench-app3-s03-transforms.mjs · tools/bench-app3-s03-fixtures.mjs | The measured half of this checkpoint, and the only authority for whether a transform fits in a frame. Drives the real Storefront route with a real Session at all three production scene sizes (10, 50 and **100** — `APP3-P01`'s own `maxElements`), selects a rotated element and drives move, resize and rotate one pointer move per animation frame, recording p50/p95, dropped frames, long tasks, DOM-node and handle counts. Fails on the frozen 20 ms p95 desktop transform budget from `ADR-APP0-001` §6; defines no new budget. **One browser per invocation on purpose**: `IMP-D043` PO-07 caps anonymous Session creation at 5 per hour per IP and three scenes is three Sessions, so the run is designed to fit inside the guard rather than restart the API to clear it. Requires the development stack, `CMD-SMOKE-APP3-S01-ORIGIN` and `CMD-BENCH-APP3-S03-FIXTURES`. Long-running; one run per browser, and one post-fix run only when an optimization is required. | `ACTIVE_SCOPED` |
| `CMD-BENCH-APP3-S03-FIXTURES` | repository tool | benchmark scenes (APP3-S03) | `node tools/bench-app3-s03-fixtures.mjs seed` · `… revert` | tools/bench-app3-s03-fixtures.mjs | The transform benchmark only. Seeds three published Design Templates carrying 10, 50 and 100 stroked rectangles — every seventh one rotated, because those are the elements whose handles must be placed on the oriented box — laid out **inside** the persisted Embroidery Area, which is read rather than assumed: a Session refuses a document that leaves the area, so a scene laid out on the canvas would simply fail to open. Fixture setup, never a command: no readiness evaluation, no concurrency token, no Audit or Outbox row. `revert` archives rather than deletes, because the database refuses to delete a frozen published version. Targets the **development** database; never run it against another. | `ACTIVE_SCOPED` |
| `CMD-SMOKE-APP3-S01-FIXTURES` | repository tool | browser-proof fixtures (APP3-S01) | `node tools/smoke-app3-s01-fixtures.mjs seed` · `… revert` | tools/smoke-app3-s01-fixtures.mjs · tools/smoke-app3-s01-object-copy.cjs | The Studio browser proof only. Publishes one development Product, adds a second Side and a second Area, copies a real `NORMALIZED` object to a `TEMPLATE_SOURCE` artwork key through the API container, and seeds 14 published Templates on one placement — one of them text-only — so the picker, the keyset continuation and the B05A preview run against real bytes. Fixture setup, never a command: no readiness evaluation, no concurrency token, no Audit or Outbox row. `revert` archives the Templates and retires the placement rows rather than deleting them, because the database refuses to delete a frozen published version or a placement row a Session references. Targets the **development** database; never run it against another. | `ACTIVE_SCOPED` |
| `CMD-SMOKE-APP3-S01-C1-FIXTURES` | repository tool | correction browser-proof fixture (APP3-S01-C1) | `node tools/smoke-app3-s01-c1-fixtures.mjs seed` · `… revert` | tools/smoke-app3-s01-c1-fixtures.mjs | The `APP3-S01-C1` browser proof only, layered on top of `CMD-SMOKE-APP3-S01-FIXTURES`. Adds one active Side at `display_order = -1` with **no** Embroidery Area, which is the placement the correction is about and which no development Product otherwise has: the API derives `studioEligible` from *one* completely usable Side, so a leading Area-less Side leaves the Product genuinely eligible while the canonical first row cannot be worked on. Every other Side keeps its Areas and Templates, so the same run also proves the normal path once the customer moves. Fixture setup, never a command: no readiness evaluation, no concurrency token, no Audit or Outbox row. `revert` retires the Side rather than deleting it, because the database refuses to delete a placement row a Session references. Targets the **development** database; never run it against another. | `ACTIVE_SCOPED` |
| `CMD-SMOKE-APP3-S01-ORIGIN` | repository tool | trustworthy origin for the Session half (APP3-S01) | `node tools/smoke-app3-s01-trustworthy-origin.mjs apply` · `… restore` | tools/smoke-app3-s01-trustworthy-origin.mjs | The Studio browser proof only. A browser sends `Sec-Fetch-*` and keeps a `__Host-` cookie only for a **potentially trustworthy** origin, and the development gateway is plain HTTP on a non-loopback name — so every Session mutation is refused 403 there. This adds one temporary Nginx server block for `server_name localhost` on the listener that already exists and recreates the API with two non-secret values overridden for the run (`DESIGN_SESSION_ALLOWED_ORIGINS`, `DESIGN_SESSION_COOKIE_SECURE`). Nothing tracked changes, no `.env` is written, no guard is weakened and no cookie is injected. `restore` puts both containers back to the tracked configuration. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B02A` | repository tool | checkpoint gate (APP3-B02A) | `node tools/check-app3-b02a.mjs` | tools/check-app3-b02a.mjs | Any change to Admin Side-background delivery: the single route and its guard, the Product↔Side membership join, the deliberate **absence** of a publication predicate, the reused editor-safe media policy, the resolve-before-storage order, the size reconciliation, or the published binary contract. Asserts the entry authority (B02 accepted, A01 delivered and carrying the background-preview blocker) and the **negations** — no presign, upload, mutation, ORIGINALS bucket, asset-by-id route, Range/ETag surface, second operation, envelope-wrapped binary or new root script. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B02A` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b02a.test.mjs` | tools/check-app3-b02a.test.mjs | Any edit to `check-app3-b02a.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it — including the mutations that leave a working route: a publication predicate reintroduced, the membership join dropped, the guard removed, and storage opened before the contextual read. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B02A-API` | `@embroidery/api` | Docker-free B02A contract tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=admin-side-background.spec --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/catalog/admin-side-background.spec.ts | Any change to the Admin delivery contract, guards, params, policy re-export or module boundary. The pattern ends at `.spec` on purpose: a bare `admin-side-background` also matches the integration suite and would silently start a database and MinIO. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B02A-INTEGRATION` | `@embroidery/api` | Admin background live-stack suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=admin-side-background.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/admin-side-background.integration.spec.ts | Any change to Admin delivery persistence, authorization or streaming. Starts a disposable PostgreSQL **and** MinIO and runs the whole HTTP stack. Carries the proofs only a live stack can give: an **unpublished** Product streams its exact bytes while the public route refuses the same background at the same moment; a Side of another Product, an unknown Product and an unknown Side are refused indistinguishably; an anonymous caller gets 401; a deleted object and a size contradiction both fail 503; and three reads mutate no row, no audit, no outbox and no object. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-A01` | repository tool | checkpoint gate (APP3-A01) | `node tools/check-app3-a01.mjs` | tools/check-app3-a01.mjs · tools/check-app3-a01-background.mjs | Any change to Admin placement authoring: the protected route, the two generated operations it consumes, the `expectedUpdatedAt` echo, the conflict classification, the retirement-by-omission mapping, the responsive floor, or the design tokens. Asserts the **entry authority** as well as the code — `D01`/`D01-C1`/`B01`/`B01-C1` accepted, and the A01/A02/A03 registry rows genuinely `APPROVED_FOR_IMPLEMENTATION`. Asserts the **negations** in both directions — no second placement route, no raw transport or hard-coded endpoint, no public placement/background operation, no A02/A03/A04/Studio feature, no colour literal, no OpenAPI or generated-client edit, no new root script — and that A04 and Studio rows are *still* unapproved, so a blanket approval cannot pass. Since `APP3-A01-C1` it also rules on the authorized Side background: the curated export, one consuming service, the object-URL create/revoke lifecycle, the SVG image layer in the Side pixel space, the persisted-association gate, and that mobile fetches nothing — mode-aware, so it asserts the pre-C1 world too. Does not read the completion report. Read-only: no database, no container, no network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-A01` | repository tool | checkpoint gate tests | `node --test tools/check-app3-a01.test.mjs tools/check-app3-a01-background.test.mjs` | tools/check-app3-a01.test.mjs · tools/check-app3-a01-background.test.mjs | Any edit to `check-app3-a01.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working screen: a Studio row silently approved, `retry: true` on the replace mutation, a conflict test widened to the bare 409, and a viewport hook that fails to `mobile`. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-A01-ADMIN` | `@embroidery/admin` | A01 component, model and boundary tests | `pnpm --filter @embroidery/admin exec jest --testPathPatterns=placement` | apps/admin/test/components/placement-*.test.tsx · apps/admin/test/model/placement-mapping.test.ts · apps/admin/test/boundary/placement-source.test.ts | Any change to the placement feature, the shared Admin field primitive or the products detail entry affordance. Covers the load states, the Product → Side → Area hierarchy with retired rows visible, selection-driven preview and inspector, the three viewports, the draft → replace-body mapping (ids retained, new rows id-less, retirement by omission), the conflict path, local geometry feedback and the background-asset boundary. Mocks the generated client, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-A02` | repository tool | checkpoint gate (APP3-A02) | `node tools/check-app3-a02.mjs` | tools/check-app3-a02.mjs | Any change to the Admin Design Template list: the protected route, the two generated operations it consumes, the keyset pagination, the filter model, the row projection or the design tokens. Asserts the **entry authority** — `D01`/`D01-C1`/`B03`/`A01` accepted, and the five `FIG-ADMIN-TEMPLATELIST-*` rows genuinely `APPROVED_FOR_IMPLEMENTATION` while A04's lifecycle rows are *still* `REVIEW_REQUIRED`, so a blanket approval cannot pass. Asserts the **absences** that no rendered DOM can show: `adminDesignTemplate_detail` and every lifecycle operation stay off the curated client boundary; no search, sort, offset, page-number or total request parameter is built; no cursor is parsed or rebuilt; no A03/A04/Studio feature exists. Asserts the two **truthfulness** rules — an unreported version is distinguished from an absent one and no version is fabricated, and an absent placement scope is stated rather than implied. Does not read the completion report. Read-only: no database, no container, no network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-A02` | repository tool | checkpoint gate tests | `node --test tools/check-app3-a02.test.mjs` | tools/check-app3-a02.test.mjs | Any edit to `check-app3-a02.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working screen: a detail read added to the curated boundary, a fabricated `v1` in the version cell, a decoded cursor, and an A04 row approved early. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-A02-ADMIN` | `@embroidery/admin` | A02 component, navigation and boundary tests | `pnpm --filter @embroidery/admin exec jest --testPathPatterns=design-template` | apps/admin/test/components/design-template-*.test.tsx · apps/admin/test/boundary/design-template-source.test.ts | Any change to the Design Template list feature or the Admin shell entry that reaches it. Covers the load, empty, filtered-empty and error states, the table and the mobile card adaptation, status and scope rendering, the version cell that reports where the number lives, keyset paging with a filter change resetting the collection, the create dialog including the duplicate-slug conflict, and the withheld-operation boundary. Mocks the generated client, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-A03` | repository tool | checkpoint gate (APP3-A03) | `node tools/check-app3-a03.mjs` | tools/check-app3-a03.mjs | Any change to the Admin Design Template editor: the protected `[templateId]` route, the two generated operations it consumes, the local document model, the stage, the save body or the conflict flow. Asserts the **entry authority** — `D01`/`D01-C1`/`A02`/`B03`/`B03A`/`P01`/`P02` accepted, the six `FIG-ADMIN-TEMPLATEEDITOR-*` rows genuinely `APPROVED_FOR_IMPLEMENTATION` while A04 lifecycle rows and every Studio row are *still* `REVIEW_REQUIRED`, and `FU-APP3-TEMPLATE-SOURCE-ASSET-INTAKE-01` still open. Asserts the **authorities**: no second Design Document schema, validator or schema-version constant exists (`APP3-P01` owns them) and no transform, bounds or px-to-mm conversion is computed outside `@embroidery/design-engine` (`IMP-D045`). Asserts the **save contract** anchored to the request literal — exactly `expectedCurrentVersion` and `document`, `0` for a never-saved template, no fabricated version — and the **conflict semantics**: only the exact published stale code opens the flow, dismissing the dialog never clears the conflict, and no merge, force-save or automatic retry exists. Asserts the **absences** no rendered DOM can show: every lifecycle operation off the curated boundary and unconsumed, no `APP3-B05A`/`B06C` route or storage address reachable, exactly two authorized Side-background consumers (`A01` and `A03`), and Template image intake visible-but-disabled rather than substituted. Both directions of the `A02` activation are pinned: the list links into the editor through one route authority and still performs no detail read. Does not read the completion report. Read-only: no database, no container, no network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-A03` | repository tool | checkpoint gate tests | `node --test tools/check-app3-a03.test.mjs` | tools/check-app3-a03.test.mjs | Any edit to `check-app3-a03.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working editor: a lifecycle operation added to the curated boundary, a status-only conflict classification, a fabricated first version, a merge helper, a second schema-version constant, a locally computed rotation, and a Studio design row approved early. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-A03-ADMIN` | `@embroidery/admin` | A03 component, model and boundary tests | `pnpm --filter @embroidery/admin exec jest --testPathPatterns=design-template-editor` | apps/admin/test/components/design-template-editor*.test.tsx · apps/admin/test/model/design-template-editor-model.test.ts · apps/admin/test/boundary/design-template-editor-source.test.ts | Any change to the Design Template editor feature. Covers the load, not-found, unscoped, unresolved-scope and invalid-document states; the zero-version empty document and its `expectedCurrentVersion = 0`; the exact save body and the absence of a second detail read after a save; the full stale-version conflict flow including keep-local and reload-with-confirmation; read-only presentation of `PUBLISHED` and `ARCHIVED`; the disabled Template-image control and the honest placeholder for an existing image reference; the object-URL revocation lifecycle; and the mobile read-only branch that issues no background request. Mocks the generated client, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B03B` | repository tool | checkpoint gate (APP3-B03B) | `node tools/check-app3-b03b.mjs` | tools/check-app3-b03b.mjs | Any change to the initial Design Template scope assignment: the route, the body, the compare-and-set predicate, the Audit row or the scope authority it reuses. Asserts the **one-time semantics structurally** — the CAS pins DRAFT, counter zero, all three scope columns NULL and zero version rows in a single predicate, and no clear/rescope/PATCH path exists on the route, the port or the adapter, so a rescope is unrepresentable rather than merely unimplemented. Asserts the **absences** no response can show: no version, document, Asset association, normalization event or lifecycle mutation, and no Product-publication predicate (GRD-T01 belongs to APP3-B04). Asserts the surface is exactly 33 paths / 38 operations with the body a strict complete triple carrying no expected-version token, that the persistence guard is translated to 409 rather than leaking a 500, that the scope and its Audit row share one transaction, and that the curated api-client still does **not** export the operation because APP3-A03-C1 owns that. Also pins the reconciled follow-up wording so the record cannot start claiming general rescope. Does not read the completion report. Read-only: no database, no container, no network. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B03B` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b03b.test.mjs` | tools/check-app3-b03b.test.mjs | Any edit to `check-app3-b03b.mjs` or the sources it parses. Each case breaks exactly one ruled property in a throwaway copy of the repository and proves the gate refuses it, including the mutations that leave a working endpoint: a dropped isNull clause that silently enables rescoping, a read-then-update that loses the race, an outbox append, a publication predicate, a curated-client export, and a follow-up reworded to claim full resolution. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B03B-API` | `@embroidery/api` | B03B focused API tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-template-scope-assign` | apps/api/src/modules/design/design-template-scope-assign.spec.ts | Any change to the scope-assignment use case, DTO, controller route or repository method. Docker-free: every collaborator is a double, so what is proved is the decision — what the CAS is asked for, which Audit row is written, and that the version/association/publication methods the repository double offers are never called. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B03B-INTEGRATION` | `@embroidery/api` | B03B PostgreSQL integration | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-template-scope-assign.integration` | apps/api/src/modules/design/tests/integration/design-template-scope-assign.integration.spec.ts | Any change to the CAS predicate, the transaction boundary or the error mapping. Drives the real HTTP stack (same ValidationModule, HttpResponseModule and guards as production, only the three staff guards stubbed) against a disposable PostgreSQL: proves the success, each unresolvable scope, every non-assignable source state, that a refusal leaves all three columns untouched, that the Audit row commits with the scope, and that two concurrent assignments yield exactly one winner and one complete triple. Needs the dev PostgreSQL container; no MinIO. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B07-INTEGRATION` | `@embroidery/api` | Session bootstrap live-stack suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=design-session-bootstrap.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/design-session-bootstrap.integration.spec.ts | Blank and clone bootstrap against disposable PostgreSQL: ACTIVE/revision 0/30-day expiry, HMAC-only persistence, exact Set-Cookie, deep clone independence under Template drift, refusals collapsing to one shape, and atomic single-winner resume rotation. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-B06A` | repository tool | checkpoint gate (APP3-B06A) | `node tools/check-app3-b06a.mjs` | tools/check-app3-b06a.mjs | Any change to the anonymous Session authorization foundation: cookie-name derivation, the peppered-HMAC verifier and its timing-safe comparison, the ACTIVE/expiry checks, the single non-enumerating outcome and the cookie-clearing rule, the Origin/Fetch-Metadata policy, the reused rate-limit primitive, the request-scoped context, or the expected-revision CAS seam. Chains W01C, G08 and G03. Asserts the **negations** — no secret minted, no success cookie, no HTTP operation, no Asset or upload behaviour, no actor binding, no second rate-limit framework, no migration, dependency or root script. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-B06A` | repository tool | checkpoint gate tests | `node --test tools/check-app3-b06a.test.mjs` | tools/check-app3-b06a.test.mjs | Any edit to the B06A checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B06A-API` | `@embroidery/api` | Docker-free Session authorization tests | `pnpm --filter @embroidery/api exec jest --testPathPatterns=design-session-auth --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/design/design-session-auth.spec.ts | Any change to the configuration, verifier, cookie policy, origin policy, rate limiter, network key or authorization service. Docker-free: the repository is a double, so what is proved is the decision rather than the SQL. Also matches the live-stack suite by name — narrow to `design-session-auth.spec` when only the unit half is wanted. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-B06A-INTEGRATION` | `@embroidery/api` | Session authorization live-stack suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns=design-session-auth.integration --testPathIgnorePatterns=/node_modules/` | apps/api/test/integration/design-session-auth.integration.spec.ts | The composed Nest pipeline against disposable PostgreSQL: guard order, real cookies and headers, authorization writing nothing, and the CAS seam under genuine concurrency. Mounts a **test-only** controller — `APP3-B06A` publishes no operation. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-W01C` | repository tool | checkpoint gate (APP3-W01C) | `node tools/check-app3-w01c.mjs` | tools/check-app3-w01c.mjs | Any change to the Session inspection-pending retry: the transient status set, the retry error class, the narrowing to `DESIGN_SESSION_ASSET`, the order of the context resolve against the derivative claim, or the convergence proof. Chains G08, W01A, W01B and G06. Asserts the **negations** — no widening to Product Side or Template, no `UPLOADED` wait, no scheduler, sweep, poll, sleep, second queue or second event, no stale `PROCESSING` claim, and no API, schema, migration, dependency or root-script change. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-W01C` | repository tool | checkpoint gate tests | `node --test tools/check-app3-w01c.test.mjs` | tools/check-app3-w01c.test.mjs | Any edit to the W01C checker or the sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01C-UNIT` | `@embroidery/worker` | Docker-free association-resolution tests | `pnpm --filter @embroidery/worker exec jest --testPathPatterns=association-resolution` | apps/worker/src/jobs/asset-normalization/application/association-resolution.service.spec.ts | Any change to profile derivation, the eligible-status classification or the Session inspection-pending retry. Docker-free: the repository is a double, so what is proved is the decision rather than the SQL. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01C-INTEGRATION` | `@embroidery/worker` | Session inspection-retry live-stack suite | `pnpm --filter @embroidery/worker exec jest --runInBand --testPathPatterns=asset-normalization-session-inspection.*[.]integration --testPathIgnorePatterns=/node_modules/` | apps/worker/src/jobs/asset-normalization/tests/asset-normalization-session-inspection.integration.spec.ts | The convergence proof against live PostgreSQL and MinIO: retry leaves no row or object, the same event yields exactly one `READY NORMALIZED` derivative after `ACCEPTED`, terminates after `REJECTED`, and appends no second producer event. Also reachable through `CMD-TEST-APP3-W01A-INTEGRATION`, whose pattern already covers it. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-G08` | repository tool | checkpoint gate (APP3-G08) | `node tools/check-app3-g08.mjs` | tools/check-app3-g08.mjs, tools/check-app3-g08-architecture.mjs | Any change to `IMP-D048`, the Session upload architecture ruling, the `APP3-B06` replan into `B06A`/`B06B`, the Session-bootstrap owner, the `B06B` route and expected `20 / 24` surface, the media allowlist and 10 MiB limit, the transaction staging, the normalization payload, the association identity contract or the `APP3-W01C` ordering route. Chains P03 (and through it B02, B01, B01N and the whole accepted APP3 gate set). Asserts the **negations** — no presign, no `ObjectStoragePort` change, no second upload-token architecture, no queue/scheduler/polling, no application, package, schema, migration, OpenAPI or client change in G08, and no root script. | `HISTORICAL_SCOPED` |
| `CMD-TEST-APP3-G08` | repository tool | checkpoint gate tests | `node --test tools/check-app3-g08.test.mjs` | tools/check-app3-g08.test.mjs | Any edit to the G08 checker or the documents it parses. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP3-W01B` | repository tool | checkpoint gate (APP3-W01B) | `node tools/check-app3-w01b.mjs` | tools/check-app3-w01b.mjs, tools/check-app3-w01b-boundaries.mjs | Any change to the Template SVG sanitizer, its dependency pins, the element/attribute allowlists, the value or path grammar, the complexity limits, the DOMPurify configuration, the structural comparison, the fixed point or the canonical serializer. Chains W01A and G07 (and through them G06, B01, P02, G05, P01, F01, DB01, G04–G01). Asserts the **negations** — no second handler or event, no transaction opened by the SVG lane, no optimizer after the sanitizer, no `DOMPurify.removed` decision, no raster decoder reachable from markup, no sanitizer outside the worker, no derivative kind, migration or HTTP operation. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-APP3-W01B-BOUNDARIES` | repository tool | checkpoint gate (APP3-W01B negations) | `node tools/check-app3-w01b-boundaries.mjs` | tools/check-app3-w01b-boundaries.mjs | The negation half, runnable alone when only a boundary is in question. Already chained by `CMD-CHECK-APP3-W01B`; run directly when reviewing what the checkpoint did **not** add. | `HISTORICAL_SCOPED` |
| `CMD-CHECK-APP3-W01B-GRAMMAR` | repository tool | checkpoint gate (APP3-W01B grammar) | `node tools/check-app3-w01b-grammar.mjs` | tools/check-app3-w01b-grammar.mjs | The policy half — allowlists, value and path grammar, ceilings, canonical output and corpus coverage — runnable alone. Already chained by `CMD-CHECK-APP3-W01B`; run directly when only the grammar is in question. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01B-GRAMMAR` | repository tool | checkpoint gate tests (grammar) | `node --test tools/check-app3-w01b-grammar.test.mjs` | tools/check-app3-w01b-grammar.test.mjs | Any edit to the grammar checker or the policy sources it parses. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01B` | repository tool | checkpoint gate tests | `node --test tools/check-app3-w01b.test.mjs` | tools/check-app3-w01b.test.mjs | Any edit to either W01B checker or the sources they parse. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01B-UNIT` | `@embroidery/worker` | Template SVG sanitizer unit and corpus tests | `pnpm --filter @embroidery/worker exec jest src/jobs/asset-normalization/domain/svg src/jobs/asset-normalization/application/template-svg` | apps/worker/src/jobs/asset-normalization/domain/svg/**, apps/worker/src/jobs/asset-normalization/application/template-svg*.spec.ts | Any change to the value grammar, the path parser, the source-form gate, the canonical serializer or the security corpus. Docker-free, and runs against the **real** jsdom and DOMPurify rather than a mock of the parser under test. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01B-INTEGRATION` | `@embroidery/worker` | Template SVG live-stack suite | `pnpm --filter @embroidery/worker exec jest --runInBand --testPathPatterns=template-svg-normalization[.]integration --testPathIgnorePatterns=/node_modules/` | apps/worker/src/jobs/asset-normalization/tests/template-svg-normalization.integration.spec.ts | Any change to the Template SVG object protocol, the quartet, idempotency, concurrent convergence or association revalidation. Starts a **disposable** MinIO and a disposable PostgreSQL with every migration; needs a Docker daemon. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP3-W01B-DETERMINISM` | `@embroidery/worker` | cross-process and container determinism proof | `pnpm --filter @embroidery/worker exec jest --runInBand --testPathPatterns=template-svg-determinism --testPathIgnorePatterns=/node_modules/` | apps/worker/test/process/template-svg-determinism.process.spec.ts | Any change to the sanitizer, the serializer or either dependency pin. Requires `apps/worker/dist` and builds the production worker image: the only proof that jsdom 29.1.1 loads under the locked Node 22.14.0 and that Alpine output is byte-identical to the host. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP4-G01` | repository tool | checkpoint gate (APP4-G01) | `node tools/check-app4-g01.mjs` | tools/check-app4-g01.mjs · tools/check-app4-g01-authority.mjs | Any change to `IMP-D049` or `ADR-APP4-001`: the four APP4 policy keys or their locked values, the contact normalization and masking rules, the `404`/`SECURE_LINK_UNAVAILABLE` non-enumerating contract, the `409`/`REISSUE_REQUIRED` refusal, the three route slugs, the opaque-secret and pepper-separation rules, the envelope algorithm/version/key authority, the `NOTIFICATION_INTENT` outbox linkage, the lineage-only encrypted intent reference, the retry/replay/resend vocabulary, or the deterministic replay-key derivation. Also reads the policy seed dataset and `.env.example`. Asserts the **negations** — no provider selected, no third-party crypto dependency, no `packages/notification-delivery` yet, no APP4 runtime module, no migration, no root script, and no production key, token or code literal in any APP4 authority document. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-G01` | repository tool | checkpoint gate tests | `node --test tools/check-app4-g01.test.mjs` | tools/check-app4-g01.test.mjs | Any edit to the G01 checker or the documents it parses. Mutation tests: each case breaks exactly one ruling in a throwaway copy and proves the gate refuses it. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP6-G01` | repository tool | checkpoint gate (APP6-G01) | `node tools/check-app6-g01.mjs` | tools/check-app6-g01.mjs · tools/check-app6-g01-authority.mjs | Any change to `IMP-D051`, `ADR-APP6-001` or the APP6 policy dataset: the Catalog-XOR-COP branch rule, the COP placement-envelope semantics, `DB01_SCHEMA_CONTRACT`, quotation validity, the deposit split, the required agreement type set, the client-side render ruling, or the projection-versus-command transition rule. Reconciles every policy value **twice** — dataset against ADR fact table, in both directions — so a single-sided drift is visible. Asserts the **negations**: the four Catalog placement columns still `NOT NULL` on both design tables and no `customer_owned_product_id` yet (`APP6-DB01` not executed early), no quotation application or HTTP layer, no APP6 dataset reader or publisher (owned by `APP6-B01`), 35 migrations, 30 root scripts, an unchanged 58/63/132 OpenAPI artifact, and zero `APP_06` Figma references. Read-only: no database, no container, no network, no generation. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP6-G01` | repository tool | checkpoint gate tests | `node --test tools/check-app6-g01.test.mjs` | tools/check-app6-g01.test.mjs | Any edit to the APP6-G01 checker or the documents it parses. Sixteen mutation cases: each breaks exactly one ruling in a throwaway copy and proves the gate refuses it — a dataset value drifting from the ADR, an ADR fact drifting from the dataset, a required agreement type dropped, a `system` projection relabelled `admin`, the sync-state prohibition deleted, the no-fabrication and COP-dimension rules softened, the decision unlocked or a ruling dropped, the DB01 contract changed on one side only, the schema change made early, an `APP_06` Figma row or an OpenAPI operation added, secret-bearing material added to the dataset, and a roadmap that never records the outcome. Docker-free. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP4-B01` | repository tool | checkpoint gate (APP4-B01) | `node tools/check-app4-b01.mjs` | tools/check-app4-b01.mjs | Any change to the shared delivery envelope or the notification intake: the package name or its dependency boundary, the envelope version/algorithm/nonce/key rules, `OUTBOX_AGGREGATE_KINDS`, the `NotificationModule` composition, the intent-key derivation, the reference union, or the outbox linkage. Asserts the **negations** — exactly one AES-GCM implementation and it is inside the package, no `apps/*`/database/persistence/Nest import in the package, no provider or crypto dependency in either app manifest, no `openDeliveryEnvelope` in the API, no `claimBatch` production caller, no `NotificationChannelPort`, no ciphertext query, no app-to-app import, no migration, no base64-shaped literal in production source. Reads code with comments stripped, never prose. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B01` | repository tool | checkpoint gate tests | `node --test tools/check-app4-b01.test.mjs` | tools/check-app4-b01.test.mjs | Any edit to the B01 checker or the sources it parses. 29 mutation cases: each breaks exactly one ruling in a throwaway copy and proves the gate refuses it, plus two that keep the gate honest — that it reads code rather than its own doc comments, and that a *declared* `claimBatch` is still allowed. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B01-PACKAGE` | `@embroidery/notification-delivery` | envelope unit suite | `pnpm --filter @embroidery/notification-delivery exec jest` | packages/notification-delivery/test/unit/delivery-envelope.spec.ts | Any change to the envelope contract, key parser or AES-256-GCM codec. Docker-free. Covers key validation and its fail-closed messages, both secret-kind round trips, nonce size and freshness, the outer shape carrying no plaintext, and tamper detection on ciphertext, auth tag, nonce and key. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B01-INTAKE` | `@embroidery/api` | notification intake live-database suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns="notification-intake.integration\|notification-intent-key" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/notification/tests/integration/notification-intake.integration.spec.ts, apps/api/src/modules/notification/domain/notification-intent-key.spec.ts | Any change to intent-key derivation, intent persistence, masking at intake, envelope sealing or the outbox append. The integration half starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; it proves one intent plus one `PENDING` event commit together, that a duplicate collapses to one of each, that the event resolves its intent through `aggregate_id` alone, and that neither the intent row nor the outbox payload contains a plaintext secret or recipient. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP4-B02` | repository tool | checkpoint gate (APP4-B02) | `node tools/check-app4-b02.mjs` | tools/check-app4-b02.mjs | Any change to the customer identity application layer, its module composition or the customer repository adapter. Asserts the **negations** — no controller, route, DTO or OpenAPI decorator anywhere in the module and no customer path in the generated contract; exactly one production caller of `createWithVerifiedContact`, inside the existing transaction boundary; no raw `23505` match and no conflict guard that stops at `kind === 'CONFLICT'`; no `diagnostics`, `sqlState`, `DETAIL`, driver message, logger or error cause; no merge write, business profile, search, credential, anonymization, duplicated normalization or direct `libphonenumber-js`; a closed failure set with every raised code declared; both mutations audited through the existing capability with no contact value in the summary; and no schema or migration. Reads code with comments stripped, never prose. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B02` | repository tool | checkpoint gate tests | `node --test tools/check-app4-b02.test.mjs` | tools/check-app4-b02.test.mjs | Any edit to the B02 checker or the sources it parses. 36 mutation cases: each breaks exactly one ruling in a throwaway copy and proves the gate refuses it, plus three that keep the gate honest — that it reads code rather than its own doc comments, that it passes against a faithful copy at another path, and that a properly declared new failure code is allowed. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B02-IDENTITY` | `@embroidery/api` | verified-identity suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns="resolve-or-create-verified-customer\|verified-identity" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/customer/application/resolve-or-create-verified-customer.spec.ts, apps/api/src/modules/customer/tests/integration/verified-identity-*.integration.spec.ts | Any change to verified-identity resolution, contact attachment, the conflict guard or the identity audit writer. The Docker-free unit half proves the guard translates only the CST-005 code and lets every other persistence failure travel as itself. The integration half starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; it proves first-identity creation with one primary contact and one audit row, idempotent re-resolution that writes nothing, that an unverified or deactivated match never links, attachment without a second primary, refusal to steal a contact or resolve through a merge tombstone, rollback leaving no orphan inside a caller's transaction, and a real two-transaction CST-005 race with one winner, a bounded loss and no PostgreSQL detail or contact value in the error or the audit trail. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP4-B03-CONTRACT` | repository tool | checkpoint gate (APP4-B03) | `node tools/check-app4-b03-contract.mjs` | tools/check-app4-b03-contract.mjs | Any change to the public verification surface, its policy read, its conflict handling or the generated contract. Reads the generated OpenAPI document **and** source. Asserts the **negations** — exactly two POSTs at the canonical issue/resend paths and no B04 attempt or status route; no code, hash, customer or contact-point field anywhere in the published contract; the locked purpose pair; no customer-repository reach and no refusal naming a customer; no direct seal/open/cipher; no attempt row, grant, retry behaviour, schema or migration; no raw `23505` match; no fallback TTL, cooldown or rate constant; no policy publication; a live challenge returned rather than rotated; and a resend that cancels its source rather than expiring it. Reads code with comments stripped, never prose. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B03-CONTRACT` | repository tool | checkpoint gate tests | `node --test tools/check-app4-b03-contract.test.mjs` | tools/check-app4-b03-contract.test.mjs | Any edit to the B03 gate or the sources and contract it parses. 37 mutation cases: each breaks exactly one ruling in a throwaway copy and proves the gate refuses it, plus four that keep the gate honest — that it reads code rather than its own doc comments, that it passes against a faithful copy at another path, and that it tolerates the neighbouring B02 identity service, which legitimately reaches the customer repository. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B03-VERIFICATION` | `@embroidery/api` | verification issue/resend suite | `pnpm --filter @embroidery/api exec jest --runInBand --testPathPatterns="verification-challenge\|verification-unconfigured" --testPathIgnorePatterns=/node_modules/` | apps/api/src/modules/customer/tests/integration/verification-challenge-*.integration.spec.ts, apps/api/src/modules/customer/tests/integration/verification-unconfigured.integration.spec.ts | Any change to challenge issuance, business resend, the issuance rate window or the atomic delivery hand-off. Starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; publishes the policy through its canonical versioned path and overrides only the verification clock and the code minter. Proves hash-only storage, policy-derived expiry and resend timing, one secret-free intent and one `PENDING` delivery event per issuance, the sealed envelope carrying exactly the issued code, an already-open challenge returned rather than rotated, stale expiry before insert, CST-007 under concurrency, rollback leaving no partial rows, `ISSUED → CANCELLED` on resend with a new code/intent/event/envelope, cooldown and rate refusals that write nothing, purpose isolation, fail-closed policy, and that no synthetic code reaches any persisted column. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP4-W01` | repository tool | checkpoint gate (APP4-W01) | `node tools/check-app4-w01.mjs` | tools/check-app4-w01.mjs · tools/check-app4-w01-boundaries.mjs | Any change to the notification delivery worker: its registration, the aggregate-linkage lookup, the envelope opening, the channel port or recording adapter, the delivery persistence, or the `notification.delivery` policy read. Asserts the **negations** — one registration, no second queue or poll loop, no `claimBatch` caller, no AES-GCM outside the shared package, no `originNotificationIntentId` read, no `DEAD_LETTER` reset, no `FAILED → PENDING` intent transition, no fallback retry constant, no policy publication, no Admin identity, no provider SDK, no app-to-app import, no plaintext column or log line, no migration, no HTTP surface, no UI. Reads code with comments stripped, never prose. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-W01` | repository tool | checkpoint gate tests | `node --test tools/check-app4-w01.test.mjs` | tools/check-app4-w01.test.mjs | Any edit to the W01 checker or the sources it parses. 35 mutation cases: each breaks exactly one ruling in a throwaway copy and proves the gate refuses it, plus three that keep the gate honest — that it reads code rather than its own doc comments, that it passes against a faithful copy at another path, and that a properly *declared* new failure class is allowed. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP4-B07-CONTRACT` | repository tool | checkpoint gate (APP4-B07) | `node tools/check-app4-b07-contract.mjs` | tools/check-app4-b07-contract.mjs | Any change to the Admin Customer or secure-grant support surface, its guards, its projections, the customer-scoped grant read or the generated contract. Reads the generated OpenAPI document, the generated client **and** source. Asserts the **negations** — no fourth route, no customer list, search or contact-lookup parameter, no customer mutation, no merge or anonymization read; no raw, normalized, display or source contact value and no Business Profile in the published contract; no token, hash, digest or ciphertext in any B07 file, schema or generated symbol, and none selected by the repository read; no second guard, no role or permission vocabulary and no session parsing in a controller; no direct repository `revoke`, no `supersede`, no Admin issue or reissue; no notification import, table or module; and no schema, migration or APP5–APP7 business content. Reads code with comments stripped, never prose. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B07-CONTRACT` | repository tool | checkpoint gate tests | `node --test tools/check-app4-b07-contract.test.mjs` | tools/check-app4-b07-contract.test.mjs | Any edit to the B07 gate or the sources and contract it parses. 47 mutation cases: each breaks exactly one ruling in a throwaway copy and proves the gate refuses it, plus four that keep the gate honest — that it reads code rather than its own doc comments, that it passes against a faithful copy at another path, that a missing owned file fails, and that the real repository passes unchanged. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B07-SUPPORT` | `@embroidery/api` | Admin support live-database suite | `pnpm --filter @embroidery/api exec jest --config jest.config.mjs --runTestsByPath src/modules/customer/tests/integration/admin-customer-support.integration.spec.ts src/modules/customer/tests/integration/admin-customer-grants.integration.spec.ts src/modules/customer/tests/integration/admin-secure-grant-revoke.integration.spec.ts` | apps/api/src/modules/customer/tests/integration/admin-*.ts | Any change to the three Admin support routes, the query, the revocation use case, the guards they reuse or the grant repository read. Boots the real HTTP application with the **real** `AuthenticatedAdminGuard` against a disposable PostgreSQL database and a real `admin_sessions` row. | `ACTIVE_SCOPED` |
| `CMD-CHECK-APP4-B08-CONTRACT` | repository tool | checkpoint gate (APP4-B08) | `node tools/check-app4-b08-contract.mjs` | tools/check-app4-b08-contract.mjs · tools/check-app4-b08-replay.mjs | Any change to the Admin notification list, the manual transport replay, the replay key or reference contract, the notification intent read model or the outbox terminal-source lookup. Reads the generated OpenAPI document, the generated client **and** source. Asserts the **negations** — no `/retry` route anywhere, no third Admin notification route, no customer-facing notification surface, no recipient/customer/template search; no raw recipient, `params`, provider body, exception or scheduler internal in the published contract; no settle, dead-letter, re-claim, attempt-counter or row update in any B08 file; no payload or ciphertext query for source discovery and no field read out of the envelope; no `openDeliveryEnvelope`, `sealDeliveryEnvelope`, cipher or secret issuer, and no `@embroidery/notification-delivery` import at all; a deterministic SHA-256 replay key over the locked pair with no clock, actor, request or random component and no second idempotency framework; expiry enforced on **both** referenced aggregates; an Admin-only audit actor with no secret in the summary; and no provider SDK, schema, migration or APP5–APP7 content. Reads code with comments stripped, never prose. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B08-CONTRACT` | repository tool | checkpoint gate tests | `node --test tools/check-app4-b08-contract.test.mjs` | tools/check-app4-b08-contract.test.mjs | Any edit to the B08 gate or the sources and contract it parses. 58 mutation cases: each breaks exactly one ruling in a throwaway copy and proves the gate refuses it, plus four that keep the gate honest — it passes against the real repository and against a faithful copy at another path, it fails when an owned file is deleted, and it **reads code rather than prose**. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-B08-ADMIN` | `@embroidery/api` | Admin notification live-database suite | `pnpm --filter @embroidery/api exec jest --config jest.config.mjs --runTestsByPath src/modules/notification/tests/integration/admin-notification-list.integration.spec.ts src/modules/notification/tests/integration/admin-notification-replay.integration.spec.ts src/modules/notification/tests/integration/admin-notification-replay-refusal.integration.spec.ts` | apps/api/src/modules/notification/tests/integration/admin-notification-*.ts | Any change to the two Admin notification routes, the replay transaction, the eligibility resolver or the repositories they read. Boots the real HTTP application with the **real** `AuthenticatedAdminGuard` against a disposable PostgreSQL database, and drives the terminal-delivery fixture through the production intake, attempt-recording and settle path rather than writing a status column. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP4-W01-DELIVERY` | `@embroidery/worker` | notification delivery suite | `pnpm --filter @embroidery/worker exec jest src/jobs/notification-delivery` | apps/worker/src/jobs/notification-delivery/**/*.spec.ts | Any change to the delivery capability. The Docker-free unit half covers the recording adapter (including that it opens no socket), the policy parser and the bounded failure taxonomy. The integration half starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; it claims through the real `WorkerJobQueueRepository` and runs the real `JobExecutionService` with the poll loop held closed, proving success, the already-`SATISFIED` no-op, retry on the same row with the identical plaintext, the three-attempt bound on the `[60, 300]` schedule, dead-lettering, tamper and expiry refusals, fail-closed policy, the linkage-versus-lineage distinction, and that no synthetic secret reaches any persisted column or log line. | `ACTIVE_SCOPED` |

| `CMD-TEST-APP5-B03-STATUS` | `@embroidery/api` | grant-scoped request status suite | `pnpm --filter @embroidery/api exec jest --testPathPatterns="request-status\|custom-request-status"` | apps/api/src/modules/order/application/status/request-status.projection.spec.ts · apps/api/src/modules/order/presentation/custom-request-status.contract.spec.ts · apps/api/src/modules/order/tests/integration/request-status.integration.spec.ts | Any change to the customer status read: the grant-scoped query, the subject/reason projection, the read-side repository, the catalog subject port, the published contract, or the `AuthorizeSecureLink` seam it consumes. Three halves. The **unit** half is Docker-free and decides subject discrimination and public-reason selection. The **contract** half builds the real OpenAPI document in process — also Docker-free — and asserts one operation under `publicCustomRequest`, the credential in the body with no path/query/cookie parameter, no public request list anywhere on the surface, a body of exactly `token`, a truthful `oneOf` subject with both members published, no storage/credential/session/moderation property in any owned schema, and no token example. The **integration** half starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; it overrides nothing, mints real tokens and stores only their peppered digests, and proves grant-scoped resolution, same-customer two-request isolation, that neither the code nor the request id can address the endpoint, one indistinguishable refusal across unknown/expired/revoked/wrong-credential, both subject branches, truthful quantities, the reason rule across NEW/UNDER_REVIEW/NEEDS_CLARIFICATION/REJECTED/CANCELLED, that a superseded status drops its predecessor's message, the shared secure-link abuse budget charged before the credential, fail-closed policy, and that reading neither consumes the grant nor writes a transition row. | `ACTIVE_SCOPED` |

| `CMD-TEST-APP5-B04-ADMIN` | `@embroidery/api` | Admin request queue & detail suite | `pnpm --filter @embroidery/api exec jest --testPathPatterns="admin-request\|admin-custom-request"` | apps/api/src/modules/order/application/admin/admin-request.projection.spec.ts · apps/api/src/modules/order/presentation/schemas/admin-custom-request.request.spec.ts · apps/api/src/modules/order/presentation/admin-custom-request.contract.spec.ts · apps/api/src/modules/order/tests/integration/admin-request-queue.integration.spec.ts · apps/api/src/modules/order/tests/integration/admin-request-detail.integration.spec.ts | Any change to the Admin request read side: the queue or detail query, the projection, the Admin read repository, the queue filter schema, the published response contract, or the Catalog / Customer / Asset ports it consumes. Three halves. The **unit** half is Docker-free and decides subject discrimination, the default triage status set, the internal-versus-customer-visible reason split, and filter parsing (whole-code equality, paired contact filter, inverted range, unknown parameter). The **contract** half builds the real OpenAPI document in process — also Docker-free — and asserts exactly two `GET` operations under `adminCustomRequest`, no mutation route anywhere beneath `/api/admin/custom-requests`, no `adminId` parameter, a truthful `oneOf` subject discriminator, two distinct reason fields on both the root and each history entry, attachment metadata with no locator, no credential or object-storage vocabulary in any owned schema, and — the one B04→B03 cross-check — that no Admin-only field reached the customer status component. The **integration** half starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; it boots the real HTTP application with the **real** `AuthenticatedAdminGuard` and overrides nothing, proving refusal without a session, the triage default versus an explicitly requested canonical status, every filter, keyset paging across three pages of rows sharing one `created_at` with no duplicate or skip, both subject branches, truthful quantities and attachment metadata, ordered transition history and moderation notes, the absent creation transition, that two reads write nothing, and that no grant digest, storage key, correlation id or raw contact appears in either response. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP5-B07-VARIANTS` | `@embroidery/api` | public catalog variant selection suite | `pnpm --filter @embroidery/api exec jest --testPathPatterns="public-product-variant"` | apps/api/src/modules/catalog/presentation/public-product-variant.contract.spec.ts · apps/api/src/modules/catalog/tests/integration/public-product-variant.integration.spec.ts | Any change to the public variant read: the controller, the query and its projection, the Drizzle repository, the published response classes, or the public-visibility predicate they share with the catalogue and the placement manifest. Two halves. The **contract** half is Docker-free and asserts the absences that have no runtime signal — no guard and no identity import, exactly one handler and it is a `GET`, no mutation verb, no body and no query parameter of any kind, an operation id equal to what the naming policy derives (so no `CONTROLLER_DOMAIN_KEYS` entry is owed), a strict slug, a published payload of exactly five properties, none of them SKU, price, stock, inventory, lifecycle or authoring, a repository that never names `skus` so the price omission is structural rather than a redaction, and no notion of a default variant anywhere. The **integration** half starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; it proves the returned ids really exist in `product_variants` (so `fk_custom_requests__product_variant_id` accepts them), both nullable attribute columns published exactly as stored, no commerce value leaking even when the variant has a priced SKU, ordering stable across reads with the id breaking a `display_order` tie, one product’s variants never appearing under another’s slug, a delisted variant excluded while its row still exists, an empty list rather than a 404 for a published product with nothing selectable, one identical `PUBLIC_PRODUCT_NOT_FOUND` for unknown, draft, archived and non-public-category alike, unreachability the moment a product is unpublished, and no audit, outbox or domain row written by either a read or a refusal. | `ACTIVE_SCOPED` |

| `CMD-TEST-APP5-S01-STOREFRONT` | `@embroidery/storefront` | S01 model, component and route tests | `pnpm --filter @embroidery/storefront exec jest --testPathPatterns="custom-request"` | apps/storefront/test/unit/custom-request-model.test.ts · apps/storefront/test/components/custom-request-catalog.test.tsx · apps/storefront/test/components/custom-request-cop.test.tsx · apps/storefront/test/components/custom-request-route.test.tsx · apps/storefront/test/support/custom-request-fixture.ts | Any change to the custom-request feature, the `/yeu-cau` route segments, or the curated api-client and `contact-verification` / `design-studio` exports it consumes. Four halves. The **model** half calls the decision layer directly — subject XOR made structural by the reducer, a quantity line with nowhere to put a variant id, `bindable` copied verbatim rather than derived from `ACCEPTED`, and a customer-owned payload that cannot contain a catalog field. The **catalog** half drives the real screen through the four `§17` outcomes that must stay apart (an empty list, an unavailable product, a transient failure and a stale Design Session), proves no variant is preselected and none is invented from a null label pair, and ends by submitting the **second** variant so "the chosen one" cannot be confused with "the first one". The **customer-owned** half proves verification gates both later steps, that the retained `verifiedChallengeId` scopes the upload and the submission after `APP4`'s reducer has dropped it, that `Idempotency-Key` reaches the generated operation's per-call config and is *reused* by a retry, that an `ACCEPTED`-but-not-bindable id is unusable, and the four submit-once outcomes — one mutation per double click, a replay treated as success, an uncertain outcome offering the identical body again, and an idempotency conflict that starts nothing on its own. The **route** half proves the App Router boundary without a production build. Mocks the generated operations, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP5-B06-ASSET-DELIVERY` | `@embroidery/api` | Admin request-asset delivery suite | `pnpm --filter @embroidery/api exec jest --testPathPatterns="admin-custom-request-asset\|admin-request-asset"` | apps/api/src/modules/order/presentation/admin-custom-request-asset.contract.spec.ts · apps/api/src/modules/order/tests/integration/admin-request-asset-delivery.integration.spec.ts · apps/api/src/modules/order/tests/integration/admin-request-asset-handoff.integration.spec.ts · apps/api/src/modules/order/tests/integration/admin-request-asset-context.ts | Any change to the Admin request-asset delivery route, the delivery policy or error vocabulary, the request↔asset association read, the source descriptor it resolves through `ASSET_REPOSITORY`, or the shared client-disconnect seam. Three halves. The **contract** half builds the real OpenAPI document in process — Docker-free — and asserts exactly one `GET` at the contextual address under `adminCustomRequestAsset`, that it is the only *new* Admin binary (`APP3-A01`'s placement background predates it and `APP2-B01`'s `/api/admin/assets/{assetId}` is still JSON), no body, no query parameter, no `adminId`, a binary success body limited to JPEG/PNG/WebP with `image/svg+xml` absent, the collapsed `404` and bounded `503`, no storage or credential vocabulary anywhere in the operation, and B04/B05's four accepted ids untouched. The **integration** half starts a **disposable** PostgreSQL with every migration and needs a Docker daemon; it boots the real HTTP application with the **real** `AuthenticatedAdminGuard`, overrides only `OBJECT_STORAGE` with a **call-counting** fake, and proves the exact stored bytes for a bound `COP_IMAGE` JPEG and a `REFERENCE` PNG/WebP with `private, no-store` / `nosniff` / reconciled `Content-Length`, then that every association and eligibility miss — wrong request, another request's asset, unknown ids, unbound, `ATTACHMENT`, `INSPECTING`, `REJECTED`, tombstoned, wrong kind or classification, undeliverable media type — is the *same* `404` with **zero** object-storage calls, that a missing object and a provider/persisted size contradiction are bounded `503`s with no partial body, that an unauthenticated caller is refused before storage, that a mid-stream disconnect destroys the upstream body **before it ended**, and that a successful read moves no request, asset, transition, note or outbox row. The **handoff** half boots B04 beside it and proves a `requestId` + `assetId` taken from the detail response streams the bytes with nothing else — the whole of what `APP5-A02` will need. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP5-A01-ADMIN` | `@embroidery/admin` | A01 model, component and navigation tests | `pnpm --filter @embroidery/admin exec jest --testPathPatterns="custom-request-queue\|requests-navigation"` | apps/admin/test/model/custom-request-queue-model.test.ts · apps/admin/test/components/custom-request-queue-render.test.tsx · apps/admin/test/components/custom-request-queue-filters.test.tsx · apps/admin/test/components/custom-request-queue-pagination.test.tsx · apps/admin/test/components/requests-navigation.test.tsx · apps/admin/test/support/custom-request-fixture.ts | Any change to the Admin custom-request queue feature, the `/requests` segment, the shell entry that reaches it, or the curated `adminCustomRequestList` export it consumes. Four halves. The **model** half calls the pure layer directly — the default status filter serializing to *nothing* so `APP5-B04` stays the authority on the triage set, an explicit status crossing as the contract's repeatable array, a hand-edited URL value normalizing to the default, an APP6 state named truthfully while an undefined one degrades without echoing its token, and a row projection that carries no `customerId` and keeps `requestId` only as an address. The **render** half drives the real screen through the five approved states and asserts what must never appear: an empty queue after a failure, a server message or SQL fragment in the failure copy, a raw identifier as operator-facing text, or any moderation, quotation or payment control. The **filters** half replays `router.replace` against a mutable search string so the URL, the request parameters, the filtered-empty state distinct from the true-empty state, the reset and the cursor-chain reset are all observed rather than asserted on a query key. The **pagination** half proves the opaque cursor round-trip, that a second click while a page is in flight cannot issue a duplicate request, that `hasNext=false` removes the control entirely, and that a rejected cursor offers a first-page reload rather than a doomed retry. Mocks the generated operation, never a raw URL; no database, container or network. | `ACTIVE_SCOPED` |

| `CMD-TEST-APP6-E01` | `@embroidery/api` | APP6 focused cross-layer acceptance | `pnpm --filter @embroidery/api exec jest --config jest.app6-e01.config.mjs` | apps/api/jest.app6-e01.config.mjs · apps/api/test/acceptance/app6-e01/**/*.acceptance.spec.ts · apps/api/test/acceptance/app6-e01/app6-e01-context.ts · app6-e01-world.ts · app6-e01-journey.ts | The `APP6-E01` acceptance package: six serial cases proving the delivered APP6 slices compose into one commission. Boots **one** real HTTP application carrying every APP6 module at once — quotation drafting/read/send, the customer quotation read and its decisions, the moderation transition that commands `DIGITIZING`, the Admin submitted-design read, design-version authoring/detail/send, and the customer design review and its decisions — against a **disposable** PostgreSQL with every migration; needs a reachable PostgreSQL and a Docker daemon. Overrides nothing: real guards, real peppered digests, and the three delivered bootstrap publishers (`PublishApp4PolicyUseCase`, `PublishApp6PolicyUseCase`, `PublishApp6AgreementsUseCase`) reading the committed seed datasets, so the validity window, deposit split, required agreement types and agreement text are production's. Seeds only prerequisite state up to `UNDER_REVIEW`; `QUOTED`, `QUOTE_ACCEPTED`, `DESIGN_REVIEW` and `APPROVED` are never written by the harness. Serial by config (`maxWorkers: 1`) and scoped by `testMatch` — it can never become a repository-wide aggregate. Run when any APP6 backend slice, its lifecycle projection, the secure-link/step-up seam it consumes, or the published APP6 customer contract changes. | `ACTIVE_SCOPED` |
| `CMD-TEST-APP6-E01-WATERMARK` | `@embroidery/storefront` | APP6-E01 watermark authority parity | `pnpm --filter @embroidery/storefront exec jest test/model/app6-e01-watermark-authority.test.ts` | apps/storefront/test/model/app6-e01-watermark-authority.test.ts · src/features/design-studio/model/studio-watermark.ts · src/features/secure-design-review/model/review-watermark.ts | The `APP6-E01` §9 cross-layer proof that the Studio (`APP3-S09`) and the secure design review (`APP6-S02`) resolve to **one** runtime watermark authority — the Product Owner's `APP6_WATERMARK_RUNTIME_AUTHORITY = DELIVERED_APP3_S09` ruling. Compares exported behaviour, not source text: the angle, the row/column grid, the generated tiles mark for mark, the token alphabet/length and the no-randomness fallback, plus that a tile carries no field a `DesignDocument` could serialize. Docker-free, no network, no Figma access, and deliberately **not** gated against `APP6-D01`'s drawn measurements (`FU-APP6-S02-WATERMARK-DRAWN-VS-DELIVERED-01`, owned by `APP6-X01`). Run when either watermark model changes. | `ACTIVE_SCOPED` |

## 4. Adding a command

A future checkpoint:

- creates focused tests/checkers when needed, and **runs them directly**;
- adds a semantic row here when future reuse is plausible;
- **does not edit root `package.json`** unless it introduces a genuinely global
  orchestration or infrastructure command;
- states the owner and the impact reason for every validation it runs.

Command IDs are stable and unique. Never renumber one; supersede it with a new
id and mark the old row's status instead.

## 5. Related authority

- [`VALIDATION_GOVERNANCE.md`](./VALIDATION_GOVERNANCE.md) — which validations
  run, when and why; the three global controls; the full-regression triggers.
- [`07-TESTING-AND-ACCEPTANCE-GATES.md`](./07-TESTING-AND-ACCEPTANCE-GATES.md) —
  required evidence by checkpoint and phase.
- [`reports/GOV-Q01-C1-COMPLETION-REPORT.md`](./reports/GOV-Q01-C1-COMPLETION-REPORT.md)
  — the correction that produced this index.
