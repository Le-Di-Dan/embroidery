# Master Application Roadmap

## 1. Strategy

The implementation stage is ordered by dependency and business value. Admin authoring/operations lead the Storefront by one capability; both are completed inside the same business phase. Design is audited per phase and produced as a whole package only when needed.

## 2. Ordered phases

| Phase | Name | Primary outcome | Design expectation |
|---|---|---|---|
| APP0 | Application Delivery Foundation | Reviewable delivery system, Swagger/OpenAPI, generated client, SCSS foundation, testing and technical spikes. | `NONE` except technical UI/style verification. |
| APP1 | Staff Access and Application Shells | Secure Admin login, permission-aware shell, Storefront shell. | Audit; likely `SUPPLEMENT` for Admin shell. |
| APP2 | Assets and Catalog Publication | Admin publishes products/assets; Storefront lists and displays published products. | Audit existing public UI; Admin likely `NEW/SUPPLEMENT`. |
| APP3 | Design Templates and 2D Design Studio | Admin publishes compatible templates; customer creates watermark-protected 2D customization. | Audit existing studio design; package gaps as one phase. |
| APP4 | Customer Identity, Verification, Secure Access and Notification Core | Customer/contact foundation and secure links support later review/payment journeys. | Limited customer-facing package if missing; Admin operations may need design. |
| APP5 | Custom Requests and Customer-Owned Products | Customer submits embroidery request; Admin triages and manages it. | Whole request journey package when gaps exist. |
| APP6 | Design Review, Approval and Quotation | Versioned design review, secure approval and immutable quotation workflow. | Whole Admin/customer review and quotation package. |
| APP7 | Deposit Payment and Order Creation | Verified deposit converts approved quote into an order safely. | Payment/status package only after provider UX is known. |
| APP8 | Inventory Reservation and Production Operations | Order reserves inventory and moves through production with concurrency safety. | Mostly Admin; design package if production board absent. |
| APP9 | Remaining Payment, Fulfillment and Completion | Remaining obligation, fulfillment freeze, completion/cancellation/refund according to locked policy. | Customer order/payment and Admin fulfillment package. |
| APP10 | Customer Operations and Communication | Profiles, merge, notification operations, simple Zalo/Messenger handoff. | Admin/customer operations package as needed. |
| APP11 | Gallery, Content, SEO and Store Presentation | Public content/discovery and operational content management. | Reuse existing public designs; supplement CMS/Admin gaps. |
| APP12 | Hardening, UAT and Production Readiness | Security, resilience, performance, observability, runbooks and go-live evidence. | No broad redesign; only defect corrections. |

## 3. Dependency chain

```text
APP0
→ APP1
→ APP2
→ APP3
→ APP4
→ APP5
→ APP6
→ APP7
→ APP8
→ APP9
→ APP10
→ APP11
→ APP12
```

Some discovery/design work for the next phase may run while engineering implements the current phase, but implementation dependencies remain ordered unless an explicit re-plan proves independence.

## 4. First implementation target

After APP0 and APP1 foundations, the first full business vertical slice is:

```text
Admin login
→ upload/process asset
→ create product draft
→ publish product
→ Storefront server-renders product list/detail
→ unpublish removes public visibility
```

This slice proves authentication, authorization, application services, persistence, object storage, worker processing, Swagger/client, Admin, Storefront, SSR/SEO, SCSS, gateway, logging and E2E integration.

## 5. Phase planning depth

Detailed candidate checkpoints are in `phases/APP*.md`. They are a roadmap, not authorization to execute more than one checkpoint at a time.

Exact endpoints may be corrected during the phase contract audit when repository truth requires it, but corrections must preserve:

- Phase outcome.
- Five-endpoint maximum.
- Human review boundaries.
- Existing lifecycle/database semantics.
- Vertical delivery order.

## 6. Current phase status

This section is the canonical phase-status record for the application stage. Status values come from `11-TRACEABILITY-AND-STATUS-MATRIX.md` §3.

| Phase | Status | Evidence |
|---|---|---|
| APP0 | `COMPLETE` | Closed at `APP0-X01`; all 18 preceding checkpoints complete. Report: [`reports/APP0-X01-COMPLETION-REPORT.md`](./reports/APP0-X01-COMPLETION-REPORT.md); plan: [`phases/APP0-APPLICATION-DELIVERY-FOUNDATION.md`](./phases/APP0-APPLICATION-DELIVERY-FOUNDATION.md). |
| APP1 | `COMPLETE — PASS_WITH_FOLLOW_UPS` | **Closed at `APP1-X01`** (verdict `PASS_WITH_FOLLOW_UPS`; zero blocking follow-ups): report [`APP1-COMPLETION-REPORT.md`](./reports/APP1-COMPLETION-REPORT.md), evidence [`APP1-CLOSURE-EVIDENCE.md`](./reports/APP1-CLOSURE-EVIDENCE.md). Baselines frozen: OpenAPI `ae015dd6…`, API-client `89c1aace…`, DB 78 tables / 31 migrations / `4ca56a59…` (`NO_APP1_SCHEMA_OR_MIGRATION_CHANGE`); Figma D01 (13 rows) + D02 (7 rows) `APPROVED_FOR_IMPLEMENTATION`. Milestone **R0 achieved** (APP0 + APP1). Pre-implementation audit and `APP1-DEC-AUTH` decision complete. Plan: [`phases/APP1-STAFF-ACCESS-AND-SHELLS.md`](./phases/APP1-STAFF-ACCESS-AND-SHELLS.md); audit: [`audits/APP1_PRE_IMPLEMENTATION_AUDIT.md`](./audits/APP1_PRE_IMPLEMENTATION_AUDIT.md); reports: [`APP1-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md`](./reports/APP1-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md), [`APP1-DEC-AUTH-COMPLETION-REPORT.md`](./reports/APP1-DEC-AUTH-COMPLETION-REPORT.md). Staff auth locked by IMP-D027 / [`ADR-APP1-001`](../adr/backend/ADR-APP1-001-STAFF-AUTHENTICATION-AND-SESSIONS.md) (no migration). **`APP1-D01` = `COMPLETE — ADMIN DESIGN APPROVED`** (Product Owner approved the Admin login + shell designs; recorded at `APP1-A01` as `FIG-APPROVAL-APP1-D01-ADMIN-001`, 13 Admin registry rows promoted to `APPROVED_FOR_IMPLEMENTATION`; [`APP1-D01-COMPLETION-REPORT.md`](./reports/APP1-D01-COMPLETION-REPORT.md); registry [`FIGMA_DESIGN_INDEX.md`](../design/FIGMA_DESIGN_INDEX.md)). **`APP1-B01` = `COMPLETE — CORRECTED`** (staff session auth; two endpoints; `NO_MIGRATION_REQUIRED` — [`APP1-B01-COMPLETION-REPORT.md`](./reports/APP1-B01-COMPLETION-REPORT.md); validation corrected to the canonical Zod pipeline by `APP1-B01-C1` — [`APP1-B01-C1-CORRECTION-REPORT.md`](./reports/APP1-B01-C1-CORRECTION-REPORT.md)). **`APP1-B02` = `COMPLETE — CORRECTED`** (current staff `GET /api/staff/me`; one endpoint; `NO_MIGRATION_REQUIRED` — [`APP1-B02-COMPLETION-REPORT.md`](./reports/APP1-B02-COMPLETION-REPORT.md); success-data requirement corrected by `APP1-B02-C1` — [`APP1-B02-C1-CORRECTION-REPORT.md`](./reports/APP1-B02-C1-CORRECTION-REPORT.md)). **`APP1-A01` = `COMPLETE — CORRECTED, DELIVERED_FOR_PRODUCT_OWNER_REVIEW`** (Admin `/login` screen; approval recorded + 13 rows promoted; `staffSessionCreate`; no schema change — [`APP1-A01-COMPLETION-REPORT.md`](./reports/APP1-A01-COMPLETION-REPORT.md)). **`APP1-A01-C1` = `COMPLETE`** (login readiness correction — password-toggle hydration via the dev gateway, bidirectional route protection, automatic Compose admin bootstrap; no migration; live-tested through `admin.embroidery.local` — [`APP1-A01-C1-CORRECTION-REPORT.md`](./reports/APP1-A01-C1-CORRECTION-REPORT.md)). **`APP1-A01-C2` = `COMPLETE`** (real isolated-Compose evidence for the bootstrap environment policy — dev-fail readiness-block, create/reuse, prod-skip, unknown-env fail-closed, zero residue; a genuine result-line observability defect found and fixed — [`APP1-A01-C2-CORRECTION-REPORT.md`](./reports/APP1-A01-C2-CORRECTION-REPORT.md)). The Product Owner completed the A01 live test and accepted it, so **`APP1-A01` = `COMPLETE — CORRECTED — PRODUCT_OWNER_ACCEPTED`**. The Product Owner live-tested and accepted A02, so **`APP1-A02` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (authenticated Admin shell — protected-layout shell, single server-hydrated current-staff query, logout, client session-expiry modal, mobile drawer; no migration — [`APP1-A02-COMPLETION-REPORT.md`](./reports/APP1-A02-COMPLETION-REPORT.md)). The Product Owner reviewed and PASSED the Storefront shell & not-found design SUPPLEMENT, so **`APP1-D02` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (seven §4.2 rows `APPROVED_FOR_IMPLEMENTATION` under `FIG-APPROVAL-APP1-D02-STOREFRONT-001` — [`APP1-D02-COMPLETION-REPORT.md`](./reports/APP1-D02-COMPLETION-REPORT.md)). The Product Owner live-tested and accepted S01A, so **`APP1-S01A` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (shared Storefront shell + responsive navigation; server-first layout, responsive Full/Compact header, focus-trapped mobile drawer, non-interactive nav + honest search, `<main>` slot; SCSS-only — [`APP1-S01A-COMPLETION-REPORT.md`](./reports/APP1-S01A-COMPLETION-REPORT.md)). The Product Owner live-tested and accepted S01B, so **`APP1-S01B` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (Storefront not-found boundary — canonical `app/not-found.tsx` inside the accepted shell, unmatched URL → HTTP 404, safe Vietnamese copy with no path echo, `/` primary recovery, honestly-unavailable secondary; Server Component; SCSS-only; no API/schema change — [`APP1-S01B-COMPLETION-REPORT.md`](./reports/APP1-S01B-COMPLETION-REPORT.md)); with S01A accepted, the parent **`APP1-S01` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`**. **`APP1-E01` = `COMPLETE — CORRECTED — REVIEW_ACCEPTED`** (staff access and shared-shell cross-layer acceptance; 17 tests; corrected by `APP1-E01-C1` — [`APP1-E01-COMPLETION-REPORT.md`](./reports/APP1-E01-COMPLETION-REPORT.md)). **`APP1-E01-C1` = `COMPLETE`** (the two missing cross-layer failure journeys — invalid/stale-cookie route matrix reaching authoritative 401, and initial protected-page API unavailability surfacing the safe framework 5xx boundary; test/harness only — [`APP1-E01-C1-CORRECTION-REPORT.md`](./reports/APP1-E01-C1-CORRECTION-REPORT.md)). **`APP1-X01` = `COMPLETE`** — phase closure, R0 evaluation, APP2 handoff. |
| APP2 | `STARTED_WITH_FOUNDATION_ONLY` (engineering) — `AUDITED — PASS_WITH_REQUIRED_DECISIONS` | Pre-implementation audit complete (`APP2-PRE-AUDIT`): audit [`audits/APP2_PRE_IMPLEMENTATION_AUDIT.md`](./audits/APP2_PRE_IMPLEMENTATION_AUDIT.md), report [`reports/APP2-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md`](./reports/APP2-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md). Verdict `NO_APP2_MIGRATION`; asset/catalog modules are persistence `SCAFFOLD` only. Two required decisions before code — `IMP-O002` object storage (`ADR_REQUIRED` → `APP2-DEC-STORAGE`) and `IMP-O003` job runtime (`ADR_REQUIRED`, DB claim path pre-designed by `ADR-DB5-003` → `APP2-DEC-JOBS`) — plus one design package (`APP2-D01`, Admin `NEW` + Storefront list/detail `SUPPLEMENT`). Corrected checkpoint map in [`phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md`](./phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md) §6.1. **`APP2-DEC-STORAGE` = `COMPLETE — CORRECTED, DELIVERED_FOR_REVIEW`** — object-storage/asset-intake architecture locked by IMP-D028 / [`ADR-APP2-001`](../adr/backend/ADR-APP2-001-OBJECT-STORAGE-AND-ASSET-INTAKE.md) (S3-compatible/MinIO dev, AWS SDK v3, API-proxied streaming upload, two private buckets, server SHA-256, SVG rejected, proxied publication-gated delivery, `NO_APP2_MIGRATION`), verdict `PASS_WITH_PRODUCT_PARAMETERS`; adds a narrow `APP2-I01` object-storage foundation checkpoint before `APP2-B01` (map now **18 checkpoints**). Report: [`reports/APP2-DEC-STORAGE-COMPLETION-REPORT.md`](./reports/APP2-DEC-STORAGE-COMPLETION-REPORT.md). **Corrected by `APP2-DEC-STORAGE-C1`** (post-object `UPLOADED` insert / storage-first two-transaction lifecycle; T1 `busboy`+`lib-storage` streaming multipart transport; route-scoped nginx `proxy_request_buffering off`; server-only SHA-256; `OBJECT_STORAGE_PUBLIC_BASE_URL` removed) — [`reports/APP2-DEC-STORAGE-C1-CORRECTION-REPORT.md`](./reports/APP2-DEC-STORAGE-C1-CORRECTION-REPORT.md). **Corrected by `APP2-DEC-STORAGE-C2`** (upload idempotency model I1 pre-stream durable allocation + pre-stream fingerprint, CW-01…CW-07; `@aws-sdk/s3-request-presigner` removed from the APP2 set; `lib-storage` memory-bound policy + C1 heap-claim corrected; repository implementation gap not schema gap, `NO_APP2_MIGRATION` re-confirmed) — [`reports/APP2-DEC-STORAGE-C2-CORRECTION-REPORT.md`](./reports/APP2-DEC-STORAGE-C2-CORRECTION-REPORT.md). **`APP2-DEC-STORAGE` accepted** as `ACCEPTED_WITH_BLOCKED_IMPLEMENTATION_GAPS` (no C3); `STORAGE-BLK-01..03` routed to the `APP2-B01` entry gate. **`APP2-DEC-JOBS` = `COMPLETE — CORRECTED, DELIVERED_FOR_REVIEW`** — asynchronous job runtime locked by IMP-D029 / [`ADR-APP2-002`](../adr/backend/ADR-APP2-002-ASYNCHRONOUS-JOB-RUNTIME.md) (PostgreSQL-backed claim queue on the existing persistence — `outbox_events` + `background_job_attempts` + `FOR UPDATE SKIP LOCKED` + visibility-timeout lease on `next_attempt_at`; J2 Redis/BullMQ + J3 RabbitMQ rejected; **no new runtime dependency, `NO_APP2_MIGRATION`**), verdict `PASS_WITH_CONFIG_PARAMETERS`; adds a narrow `APP2-I02` worker-runtime foundation (owns `FU-A07`) before `APP2-W01` (map now **19 checkpoints**). Report: [`reports/APP2-DEC-JOBS-COMPLETION-REPORT.md`](./reports/APP2-DEC-JOBS-COMPLETION-REPORT.md). **Corrected by `APP2-DEC-JOBS-C1`** ([`reports/APP2-DEC-JOBS-C1-CORRECTION-REPORT.md`](./reports/APP2-DEC-JOBS-C1-CORRECTION-REPORT.md)): **`PENDING` is the only automatic claim/retry state** (matches partial index IDX-088), retryable failure returns to `PENDING`+backoff (never `FAILED`; `FAILED` reserved/never emitted), ownership-guarded atomic completion transactions, expired-lease attempt evidence, no-heartbeat timeout invariant; spikes 23/23 + 25/25. Governance: a checkpoint may receive **at most one** correction — this is it. `APP2-DEC-JOBS` review **accepted**, which unblocked `APP2-D01` and `APP2-I02`. **`APP2-D01` = `DELIVERED_FOR_PRODUCT_OWNER_REVIEW`** — the Assets and Catalog Publication design package on the new Figma page **APP_02** (`419:3`), section `423:3`: 27 screen/state frames + 2 annotation frames + 1 DS supplement (`FIG-DS-INPUT-APP2`, closing `GAP-D01` for APP2 only; `GAP-D02` stays a local `ink/900 @45%` composition). Admin asset/product/catalog/publication `NEW`, Storefront list/detail `SUPPLEMENT` cloned from the approved APP1-D02 shells (APP1 frames unmodified, **no APP1 row superseded**). 30 new `REVIEW_REQUIRED` rows → registry now **69 IDs** ([`FIGMA_DESIGN_INDEX.md`](../design/FIGMA_DESIGN_INDEX.md) §4.3/§6.1b); handoff in [`phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md`](./phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md) §6.2; report [`reports/APP2-D01-COMPLETION-REPORT.md`](./reports/APP2-D01-COMPLETION-REPORT.md). **Product Owner review returned `PARTIAL_PRODUCT_OWNER_APPROVAL`, corrected by `APP2-D01-C1`** ([`reports/APP2-D01-C1-STOREFRONT-SOURCE-CORRECTION-COMPLETION-REPORT.md`](./reports/APP2-D01-C1-STOREFRONT-SOURCE-CORRECTION-COMPLETION-REPORT.md)): **Admin = `COMPLETE — PRODUCT_OWNER_APPROVED — FROZEN`**; **Storefront Product List = `REJECTED_NODE_SET_REMOVED` / `SOURCE_AUTHORITY_CORRECTED_TO_UI02`** — the four uniform card-grid frames (`444:204`, `445:204`, `445:210`, `446:223`) were deleted from Figma and their registry rows removed because the equal-height ecommerce grid conflicted with the established Pinterest-inspired image-led masonry of **UI02 – Discover Feed**, which is now the mandatory `APP2-S01` authority (`208:538`; desktop `208:2002`, tablet `224:871`, mobile `226:1038`; masonry 5/3/2); **Storefront Product Detail = `NOT_APPROVED` / `WITHHELD_PENDING_UI03_RECONCILIATION`** — frames unmodified but not implementation authority, requiring a separate UI03 reconciliation checkpoint. Registry stays **69 IDs** (−4 rejected, +4 UI02 authority rows); 31/31 frozen nodes verified unchanged. `APP2-A01..A04` = `BLOCKED_BY_D01_APPROVAL_AND_BACKEND`; `APP2-S01` = `BLOCKED_BY_BACKEND` (authority now UI02); `APP2-S02` = `BLOCKED_BY_UI03_RECONCILIATION_AND_BACKEND`; **`APP2-I01` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the S3-compatible object-storage foundation `@embroidery/object-storage` (six-operation port by `ORIGINALS`/`DERIVATIVES` alias with no presign and no list-all; streaming `lib-storage` upload 5 MiB × 2 with `leavePartsOnError: false`; PII-free UUIDv7 keys with SVG rejected; strict config with paired credentials and secret redaction; closed provider-error taxonomy), pinned `minio/minio:RELEASE.2025-04-08T15-41-24Z` development Compose service with in-package bucket bootstrap, and an **inactive** route-scoped nginx upload seam (`proxy_request_buffering off`, body-size/timeout hooks deliberately unset pending the open max-image-bytes parameter). Only `@aws-sdk/client-s3` + `@aws-sdk/lib-storage` `3.1095.0`; `NO_APP2_MIGRATION`, OpenAPI/API-client/DB/Figma baselines unchanged. 150 unit tests + 20/20 disposable-MinIO contract cases; contract case 16 found and fixed a real dangling-multipart defect (`Upload.abort()` lets `done()` reject before `AbortMultipartUpload` lands). Preflight recorded `PARTIAL` — the tree was not clean on entry. Report: [`reports/APP2-I01-COMPLETION-REPORT.md`](./reports/APP2-I01-COMPLETION-REPORT.md). **`APP2-I02` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the PostgreSQL worker-runtime foundation implementing IMP-D029 / [`ADR-APP2-002`](../adr/backend/ADR-APP2-002-ASYNCHRONOUS-JOB-RUNTIME.md) as corrected by `APP2-DEC-JOBS-C1`, closing `FU-A07`. `WorkerJobQueueRepository` (`packages/persistence`) provides the narrow typed seam — `claimRegisteredBatch`, `completeSucceededAttempt`, `completeRetryableAttempt`, `completeTerminalAttempt`, `probeWorkerDatabase` — with a single-statement database-time claim (`FOR UPDATE SKIP LOCKED`, conflict-safe `WORKER_LEASE_EXPIRED` evidence via a data-modifying CTE, lease on `next_attempt_at`, status stays `PENDING`) and ownership-guarded atomic completions returning typed `STALE_JOB_LEASE`; `FAILED` is never emitted. `apps/worker` gains the handler registry (one handler per event type, duplicate registration fails startup, an empty registry claims nothing), the closed error taxonomy, the exact capped backoff `min(base × 2^(n−1), max)`, worker identity + `AsyncLocalStorage` correlation with an allow-list log projection, a bounded poll loop with per-attempt `AbortController` timeout, graceful shutdown and readiness; the no-op keep-alive service is removed. Runtime policy is read from the canonical `policy_configurations` path under key `worker.runtime` — **no environment duplication, no production default, and no development bootstrap** (none exists to reuse), so an unconfigured worker reports `WORKER_POLICY_MISSING`, stays up and claims nothing. **No new dependency, `NO_APP2_MIGRATION`**, OpenAPI/API-client/DB/Figma baselines unchanged, production registry empty (no Asset handler). 115 worker + 107 persistence unit tests, all 27 required disposable-PostgreSQL cases (29 total) and a 6-assertion real-process smoke pass; two real defects were found by tests and fixed before Commit A. Report: [`reports/APP2-I02-COMPLETION-REPORT.md`](./reports/APP2-I02-COMPLETION-REPORT.md). **Review returned `CORRECTION_REQUIRED`; corrected by `APP2-I02-C1`** → **`APP2-I02` = `COMPLETE — CORRECTED — DELIVERED_FOR_REVIEW`**. Two blocking defects fixed. (1) A timed-out handler that ignored its `AbortSignal` was abandoned while the runtime recorded a retryable timeout and **released the lease**, so the still-running handler could overlap a retry of the same job with the same `effectKey` — Node cannot cancel a running promise, and the I02 report wrongly recorded this as a limitation rather than a defect. The handler promise is now never detached: at `handlerTimeoutMs` the runtime aborts and then waits on it until a hard stop of `min(timeoutInstant + leaseSafetyMarginMs, leaseExpiresAt − fatalExitSafetyMs)` (new internal constant `fatalExitSafetyMs = 250`, not an environment variable, enforced at policy startup by requiring `leaseSafetyMarginMs` to exceed it). A **cooperative** handler settles inside the deadline and completes through the existing atomic transaction as `JOB_HANDLER_TIMEOUT` — a late success can never become `SUCCEEDED`. An **uncooperative** handler writes no completion at all: the row keeps `PENDING`/`claimed_by`/`attempt_count`/`next_attempt_at` exactly as claimed, the runtime enters `FATAL_HANDLER_UNRESPONSIVE`, stops claiming, reports unready, emits one allow-list fatal log, closes context and pool once and exits `1` before the lease expires; a later worker reclaims after expiry and writes the single `WORKER_LEASE_EXPIRED` attempt. The process boundary is the only cancellation a JavaScript handler cannot ignore. (2) The acceptance smoke used `process.emit('SIGTERM')`, which proves a listener is attached and nothing more; a **real Linux SIGTERM** is now delivered by `docker kill --signal=TERM` to the shipped `runner` image (**exit 0 in 362 ms**), with `process.emit` demoted to a fast unit check. That smoke also exposed a pre-existing defect: the production image had never been executed (the dev stack uses the `dev` target) and could not start, because the `deps`/`prod-deps` stages never copied the `packages/database` and `packages/persistence` manifests, leaving their `node_modules` uninstalled — fixed in `worker.Dockerfile`. No dependency, no migration, no schema/status/outcome change, no persistence change (`leaseExpiresAt` was already projected), all frozen baselines unchanged. 130 worker + 107 persistence tests, 29 queue-integration cases, 3 timeout cases, 5 real-signal cases, zero Docker/database residue. Report: [`reports/APP2-I02-C1-CORRECTION-REPORT.md`](./reports/APP2-I02-C1-CORRECTION-REPORT.md). Governance: at most one correction per checkpoint — this is it; `APP2-I02-C2` must not be created. **Final review ruled C1 `IMPLEMENTATION_SUBSTANTIALLY_CORRECT BUT ACCEPTANCE_NOT_PROVEN`, resolved by the mandatory final directive `APP2-I02-FD1` → `APP2-I02` = `COMPLETE — CORRECTED — REVIEW_ACCEPTED`, `APP2-I02-C1` = `SUPERSEDED_BY_FINAL_PROCESS_PROOF`, `APP2-I02-FD1` = `COMPLETE`.** C1 had proven the uncooperative path with an injected process-exit seam inside Jest, so worker A's never-settling promise stayed alive in the test process and worker B started after an exit *record* rather than after worker A had actually terminated — ordering between two objects in one process cannot rule out overlap. FD1 replaces that with a **real Linux process boundary**: worker A runs the genuine Nest runtime as PID 1 in a container using the production exit seam, its handler ignores `AbortSignal` and never settles, and the real fatal path ends the real container; worker B is a second container started only after `docker inspect` reports worker A stopped **and** `clock_timestamp()` has passed the lease deadline. Proven externally over **three consecutive runs**: `ExitCode 1` / `Running false` ~17 s before the lease deadline; the row stays `PENDING`, owned, `attempt_count = 1`, `next_attempt_at` unchanged, `last_error` NULL; **zero** attempt rows before reclaim; worker B's handler starts ~18 s after worker A's `FinishedAt`; attempts become exactly `1/FAILED_RETRYABLE/WORKER_LEASE_EXPIRED` then `2/SUCCEEDED`; final `DISPATCHED`; worker B exits 0. All cross-worker ordering uses the database clock and Docker's `FinishedAt`, never a host clock or an in-process observer. **The production runtime needed no change** — the C1 state machine passed on the first complete run, and the only defects found were two of my own in the test (reading stdout hid Nest's stderr fatal line; one assertion targeted a warning that is unreachable once the fatal closer aborts the shutdown signal first). Test-only `process-test` Docker target; the production `runner` image copies nothing from it and normal Compose never references it. No dependency, no migration, no schema change. Report: [`reports/APP2-I02-FD1-FINAL-VERIFICATION-REPORT.md`](./reports/APP2-I02-FD1-FINAL-VERIFICATION-REPORT.md). **`APP2-B01-G01` = `COMPLETE — ENTRY_GATE_CLOSED`** — the asset-intake entry gate, executed under `APP2-B01` ownership and explicitly **not** a third storage correction (`APP2-DEC-STORAGE-C3` must not be created). It closes `STORAGE-BLK-01/02/03` (`RESOLVED_BY_APP2-B01-G01`) and `UPLOAD-POLICY-BLK-01` (`RESOLVED_BY_PRODUCT_OWNER`). Binding Product Owner policy: **25 MiB / 26 214 400 bytes** API-authoritative maximum raster product image; accepted media exactly `image/png`, `image/jpeg`, `image/webp`; **SVG rejected**; non-image embroidery source files excluded from APP2; originals retained while the asset exists (unpublish never deletes an original, no age-based deletion); a coarse **route-scoped 27 MiB / 28 311 552 bytes** gateway multipart ceiling that is not the file-size validator; **5-minute** API hard upload duration with strictly longer gateway timeouts; **15-minute** allocation TTL. Contract locked in [`ADR-APP2-001`](../adr/backend/ADR-APP2-001-OBJECT-STORAGE-AND-ASSET-INTAKE.md) §4.2f — a **two-stage identity** (immutable pre-stream request fingerprint in the `fingerprint` column + post-stream content fingerprint in the result, so a completed replay is content-complete only when it **resends and re-verifies the whole body** with zero object writes); a **versioned discriminated** `ASSET_UPLOAD_ALLOCATION`/`ASSET_UPLOAD_COMPLETED` result union with strict decoding and a state/result invariant (malformed → `IDEMPOTENCY_RESULT_INVALID`, safe 5xx, no object write, no transition); a **`claimToken`** rotated only on expired reclaim — preserving `assetId`/`objectKey` — and verified inside both Tx A and Tx B (stale → `STALE_UPLOAD_CLAIM`, nothing mutated), superseding the C2 delete-and-re-claim row that would have minted a second asset identity; and a strictly ordered reclaim that never deletes an existing durable asset and, when none exists, finishes cleanup and re-verifies the token **before** replacement streaming (cleanup failure accepts no object). `APP2-B01` is scoped to **exactly three operations** (Admin asset upload / detail / list), dropping the historical retry endpoint rather than padding a five-endpoint budget. Recorded reconciliations rather than silent fixes: the gate input's `PRODUCT_IMAGE` asset kind does not exist in the repository, so the locked `ASSET_KINDS` value **`CATALOG_MEDIA`** governs; and the http-level `client_max_body_size 20m` stays with the upload location overriding it upward to `27m` route-scoped. `NO_APP2_MIGRATION` re-confirmed — **9/9** disposable-PostgreSQL checks against the real 31-migration / 78-table schema, zero residue; all remaining shortfalls are **repository implementation gaps** (ADR §4.2f-7) owned by `APP2-B01`. Report: [`reports/APP2-B01-G01-COMPLETION-REPORT.md`](./reports/APP2-B01-G01-COMPLETION-REPORT.md). **`APP2-B01` = `READY — NOT STARTED`**; `APP2-W01` blocked by `B01`. **`APP2-B01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the three Admin asset operations (`adminAsset_upload` / `adminAsset_detail` / `adminAsset_list`): single-pass `busboy` streaming into the private `ORIGINALS` bucket with a bounded signature prefix settled before any byte reaches storage, an incremental authoritative 25 MiB counter and a server SHA-256 on the same bytes (no whole-file buffer anywhere); the pre-stream claim commits the allocation before a single file byte is read; a completed replay re-reads and re-verifies the whole resent body writing **zero** objects; an expired allocation is reclaimed by rotating the claim token in place and resuming from durable asset truth, never deleting an existing asset and always completing cleanup before a replacement stream; Tx A inserts-or-recovers as `UPLOADED` with no event, Tx B does the guarded `UPLOADED → INSPECTING` transition, appends **exactly one** `asset.inspection.requested` event and completes the record with its id, both verifying the claim token inside their own transaction; exactly-once not claimed. New `IdempotencyAllocationStore` (claim-with-allocation / locked read / expired renewal / guarded completion, expiry by database time) plus `AssetRepository` insert-or-recover, guarded transition and scoped keyset list. **No schema change, no migration**; only `busboy` `1.6.0` + `@types/busboy` `1.5.4` added. Nginx upload seam **activated** on one exact-match location (`27m`/`360s`/buffering off, values from the environment; global limits unchanged, MinIO unexposed). Three real streaming defects found by the live suite and fixed (errored sink destroyed without a listener crashed the process; `pipe` does not forward source errors so a disconnecting client emitted an unhandled `error`; a stalled client hung past its deadline because the reader only checked the abort signal on a chunk), plus a structural split of `AssetModule` into persistence and `AssetIntakeModule` after an existing DB7 suite exposed the coupling. 194 unit tests, **25 live PostgreSQL+MinIO cases covering all 30 required scenarios**, 27 API cases, 20 gateway cases including a real `nginx:1.27.3-alpine` render + `nginx -t`; `pnpm quality` `EXIT=0` (90/90 API suites, 1112 tests); OpenAPI `ae015dd6…`→`e19c2f76…`, client `89c1aace…`→`55de1cc1…` (+197/−0), DB 78/833/31 and Figma 69 unchanged — [`reports/APP2-B01-COMPLETION-REPORT.md`](./reports/APP2-B01-COMPLETION-REPORT.md). **`APP2-W01` = `READY — NOT STARTED`**. No upload, processing, publication or public product functionality yet. **`APP2-W01` entry gate returned `BLOCKED_BY_PROVEN_SCHEMA_OR_DOMAIN_GAP` (no commit, no file changed): `asset_derivatives.kind` had no non-watermarked catalog display kind — `PREVIEW_WATERMARKED` is bound to INV-22/BR-012 and the `ADR-APP2-001` §4.7 public preview contract, and `NORMALIZED` would redefine the artwork concept DB2 CON-042/CON-061 merged — so the map gains two prerequisites → **21 checkpoints**, order `APP2-DB01 → APP2-I03 → APP2-W01`.** **`APP2-DB01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — IMP-D030: adds the derivative kind **`CATALOG_PREVIEW`** (non-watermarked catalog display derivative of a `CATALOG_MEDIA` asset) and INV-22's first physical half `ck_asset_derivatives__watermark_by_kind` (**CST-126**: `PREVIEW_WATERMARKED → true`, `CATALOG_PREVIEW → false`; the other three kinds unconstrained) in forward-only migration **`0032`** with two preflight guards; aligns DB3 **LC-06 into two lanes** — the catalog-media lane prepares both rows at `PENDING`, transitions to `PROCESSING` through a guarded update (never inserted as `PROCESSING`), generates while the asset is `INSPECTING`, and writes both-`READY` + one inspection + `ACCEPTED` in one transaction, while the design/artwork lane keeps its after-`ACCEPTED` SE-013 timing; and locks the W01 job kind to **`ASSET_PROCESSING`** (the I02 seam already carries a handler-provided kind, so no runtime change). Migrations 31 → 32, CHECK 189 → 190, fingerprint `4ca56a59…` → `82864268…`; **78 tables / 833 columns unchanged**, no table/column added, uniqueness and READY/storage-key behaviour untouched, no historical row rewritten; OpenAPI `e19c2f76…`, client `55de1cc1…` and Figma 69 unchanged. 192 database + 112 persistence tests including a real upgrade run from the 0000–0031 baseline that preserves every seeded row and reproduces the fresh-install fingerprint — [`reports/APP2-DB01-COMPLETION-REPORT.md`](./reports/APP2-DB01-COMPLETION-REPORT.md). **`APP2-I03` = `READY — NOT STARTED`** (wire idempotent private-bucket bootstrap into the API and worker composition roots — `ensurePrivateBuckets` has no production caller); **`APP2-W01` = `BLOCKED_BY_APP2-I03`**; `APP2-A01` = `BLOCKED_BY_APP2-W01`. **`APP2-I03` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the private-bucket bootstrap is finally wired into both composition roots: `ensurePrivateBuckets` had existed since I01 but **no production caller** did, so a real deployment met its first upload with no bucket. The API resolves and awaits the bootstrap **before `app.listen`** (extracted into `start-api.ts` so the ordering is asserted, not described); on failure it never listens, closes the context exactly once and exits non-zero, with no retry loop. The worker gates its poll loop behind an abstract **`WORKER_STARTUP_GATE`** that the generic runtime injects but does **not** provide — the composition root binds it to the same bootstrap — awaited before `started`, so a closed gate issues no claim query at all; a closed gate is a *result*, not a throw, so the process stays up, reports `STARTUP_GATE_CLOSED` and `main.ts` closes the context once and exits non-zero. Bootstrap is memoized per process (concurrent callers share one attempt, never two `CreateBucket` calls), sticky on success, cleared on failure; four startup classes are mapped from the existing `ObjectStorageError` taxonomy, never from a message, and access-denied is never downgraded to unavailable. Logs carry only `component`/`operation`/`ready`/class. Readiness: the API adds **no** public field (listen *is* the storage gate, and a schema change would move the frozen OpenAPI artifact); worker readiness now requires policy + database + every gate + started. **No package correction was needed** — the existing `BucketAlreadyOwnedByYou` branch already makes a lost race a success. Proven live: a cross-process smoke starts the **built** API and worker back-to-back against an empty MinIO → both ready, both verified, **exactly two** private buckets, anonymous access refused, restart idempotent, zero residue (11/11); a worker-only smoke against a store with no buckets at all (9/9); plus 35 deterministic API cases and 11 new worker gate cases. All I02 regressions re-run green, including the **real Linux SIGTERM (exit 0 in 388 ms)** and the 16-case fatal-process isolation proof. Compose gives the worker the same storage configuration and orders both after a healthy MinIO (still no host port, still no Nginx route); the E2E edge gains an ephemeral tmpfs MinIO because the API no longer listens without a store. **Found and fixed a real production defect:** the shipped worker image never shipped `@embroidery/object-storage`, so it could not start — the same class of image defect I02-C1 and DB01 found before it. No migration, no schema change, no new external dependency (one workspace link), OpenAPI/client/Figma unchanged; `pnpm quality` `EXIT=0` (API 1136, worker 141, persistence 112, database 192, object-storage 150+20) — [`reports/APP2-I03-COMPLETION-REPORT.md`](./reports/APP2-I03-COMPLETION-REPORT.md). **`APP2-W01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the catalog asset inspection worker: one production handler for `asset.inspection.requested` filed under the domain kind **`ASSET_PROCESSING`** (not the transport `OUTBOX_DISPATCH`), effect key `asset-inspection:v1:{assetId}`, registered in `onModuleInit` so it is in the registry before the poll loop issues its first claim. `sharp@0.35.3` exact (Apache-2.0, libvips 8.18.3) added to `apps/worker` only — no `@types/sharp`, no second image library, nothing added to API/Admin/Storefront/object-storage. `ASSET_PROCESSING_POLICY_V1` is a frozen worker-owned object (PNG/JPEG/static WebP; `failOn: warning`, `unlimited: false`, `sequentialRead: true`, `animated: false`; 12,000 × 12,000, 40 MP, 4 channels, 1 page), and the two outputs are WebP `q82`/`alphaQuality 100`/`effort 4`/`smartSubsample` inside 480² and 1920² with `fit: inside` + `withoutEnlargement` — auto-oriented, sRGB, alpha preserved, every metadata block stripped, both `THUMBNAIL` and `CATALOG_PREVIEW` unwatermarked per DB01/CST-126. Durable preparation commits first (both rows inserted `PENDING`, then guarded `PENDING → PROCESSING`, never inserted as `PROCESSING`), external work happens outside every transaction, and one terminal transaction writes both `READY` rows + exactly one `ACCEPTED` inspection + the guarded `INSPECTING → ACCEPTED` together; an unpublishable image is a **business rejection** (both rows `FAILED`, one closed code, original retained), never a dead letter, and on the final attempt a retryable failure becomes `PROCESSING_RETRY_EXHAUSTED` so nothing sits in `INSPECTING` forever. Terminal replay writes zero objects and zero rows; a contradiction fails closed with `JOB_INVARIANT_VIOLATION` and is never repaired. **Nothing is buffered**: four bounded reads per attempt (integrity, metadata, one per output), each piped into the encoder and into managed multipart — no full-file `Buffer`, no `toBuffer`, no temp file. The V1 inspection detail is an allow-list in the existing nullable `text` column, bounded at 8 KiB, so no key/filename/EXIF/stack can reach it. **Five real defects were found by these tests and fixed before Commit A** — an upload that failed to open stalled the pipeline on backpressure; a decode failure was reported as the storage failure it cascaded into; contradictions raised outside the processing block escaped unclassified and were therefore *retried*; a cleanup failure bypassed the final-attempt rule; and `DrizzleRepository.run`'s error mapper flattened every contradiction into a generic `PersistenceError`, turning a fail-closed stop into a retry (only visible against a real database — the unit doubles are not wrapped by the mapper at all). 24 worker suites / **283** unit tests, **4 live PostgreSQL+MinIO suites / 52 tests covering all 40 required scenarios** with recovery states produced through real failure seams, and a **shipped-image Linux smoke** (12 tests: gate opens, Sharp musl binary loads, exactly 1 handler, claimed under `ASSET_PROCESSING`, asset `ACCEPTED`, both derivatives `READY`, both private objects, one inspection, outbox `DISPATCHED` with one `SUCCEEDED` attempt, no key/checksum/secret/`requestId` in the logs, real SIGTERM → **exit 0 in 309 ms**, zero residue). All I02/I03 regressions green (signal smoke exit 0 in 320 ms; 16-case fatal-process proof). **No migration, no schema change, no HTTP operation, no forbidden-area change**; OpenAPI `e19c2f76…`, client `55de1cc1…`, DB 78/833/190/`82864268…` and Figma 69 all unchanged; `pnpm quality` `EXIT=0` — [`reports/APP2-W01-COMPLETION-REPORT.md`](./reports/APP2-W01-COMPLETION-REPORT.md). **`APP2-A01` = `READY — NOT STARTED`**; `APP2-B02` = `BLOCKED_BY_APP2-A01`. **`APP2-A01` entry gate returned `BLOCKED_BY_MISSING_ASSET_LIST_CONTINUATION_DESIGN` (no commit, no file changed):** `adminAsset_list` returns `hasNext`+`nextCursor` (`limit` ≤ 100, no delete API), but none of the seven approved frames, the handoff-notes node `450:404`, the registry, the D01 reports or the phase plan defined a continuation interaction — proven from node metadata, since the Uploading/Processing/Rejected frames clip their grid at the 1024 px frame bottom; page-1-only would silently discard pages and inventing a control would be self-approved design. The same audit reproduced a second gap (every approved card/row displayed a **source filename** B01 neither persists nor exposes) and **cleared** two candidate blockers (thumbnails are honest placeholder glyphs, so no media-delivery contract is required; the side nav locks a dedicated Assets destination, so `/assets` is not invented). **`APP2-D02` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the narrow design-reconciliation checkpoint curing both gaps, **design and documentation only**: IMP-D031 locks an explicit user-triggered **`Tải thêm tài sản`** control shown only when `hasNext = true` (append, preserve items and scroll; loading = disabled + `Đang tải thêm…` + `aria-busy` + one polite announcement; failure keeps items and offers `Thử lại` on the **same** cursor; no control at `hasNext = false`; **no total-count copy** — the API exposes no total; infinite scroll, viewport-triggered loading, offset/page-number paging and silent truncation all rejected) and a **server-backed identity** (`image/png → Ảnh PNG`, `image/jpeg → Ảnh JPEG`, `image/webp → Ảnh WebP`, unknown → `Tài sản hình ảnh`; secondary line `{size} · {createdAt}` in `vi-VN`, `dd/MM/yyyy, HH:mm`) with **no backend field added and no filename fabricated** — `File.name` stays transient local upload state. Applied in place to the eight existing Admin Assets nodes (desktop Default grew 1024 → 1092 so the control is visible, not clipped; mobile rows 80 → 95 for the second line); the seven `FIG-ADMIN-ASSETS-*` rows are promoted to `APPROVED_FOR_IMPLEMENTATION` under **`FIG-APPROVAL-APP2-D01-ADMIN-001`** ([`../design/approvals/APP2-D01-ADMIN-ASSETS-DESIGN-APPROVAL.md`](../design/approvals/APP2-D01-ADMIN-ASSETS-DESIGN-APPROVAL.md)), closing the divergence between the frozen ruling and the rows, and one annotation node `FIG-ADMIN-ASSETS-CONTINUATION-IDENTITY` (`484:272`) is registered — **registry 69 → 70**; design authority is now `PRODUCT_OWNER_APPROVED — FROZEN — D02_RECONCILED`; map now **22 checkpoints**; OpenAPI `e19c2f76…`, client `55de1cc1…` and DB 78/833/190/`82864268…` unchanged — [`reports/APP2-D02-COMPLETION-REPORT.md`](./reports/APP2-D02-COMPLETION-REPORT.md). **`APP2-A01` = `READY — NOT STARTED`** (unblocked); `APP2-B02` = `BLOCKED_BY_APP2-A01`. **`APP2-A01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-28) — the Admin asset library at `/assets` inside the accepted APP1 shell, implementing IMP-D031 exactly (explicit `Tải thêm tài sản` only when `hasNext` and a usable cursor, append + same-cursor retry + one polite announcement, no total-count copy; identity from `mediaType` with a `{size} · {createdAt}` `vi-VN` line and **no filename anywhere**). Consumes only the three accepted B01 operations: generated multipart with ordered parts + `Idempotency-Key` + `AbortSignal` + real transferred-byte progress (never 100% before the HTTP result), honest ambiguous cancellation that reuses the same key, and detail-only processing reconciliation on one `ASSET_PROCESSING_POLL_INTERVAL_MS = 3000` constant that stops at any terminal or uninterpretable status. Thumbnails stay honest placeholders — APP2 exposes no media-delivery operation. Shell navigation gains its first real destination (links + pathname-derived current). 77 new tests (220 Admin total), `pnpm quality` `EXIT=0`; OpenAPI `e19c2f76…`, client `55de1cc1…`, DB 78/833/190/`82864268…` and Figma registry 70 all unchanged; **no new dependency** — [`reports/APP2-A01-COMPLETION-REPORT.md`](./reports/APP2-A01-COMPLETION-REPORT.md). Reviewer decision open: the approved Rejected-state action `Xoá khỏi danh sách` has **no backend capability** and is deliberately not implemented. **`APP2-B02` = `READY — NOT STARTED`**; `APP2-A02` = `BLOCKED_BY_APP2-B02`. **`APP2-B02` entry gate (2026-07-28):** the B02 pre-code audit blocked as **`BLOCKED_BY_PRODUCT_DRAFT_FIELD_DECISION`** (no commit, no tracked file changed) — `products` carries 10 NOT NULL columns with no default, and after DB7's four locked rules five mandatory values had none: `category_id`, `slug`, `base_price_amount`, `display_order`, `product_media.role`; `category_id` was unsatisfiable because zero categories existed, no category operation exists and B02 may not add a sixth. **`APP2-B02-G01` = `COMPLETE — ENTRY_GATE_CLOSED`** locks **IMP-D032** (fixed `thu-bong`/`khan`/`quan-ao`/`khac` taxonomy addressed by `categorySlug`, server-owned immutable product slug, `DRAFT` price sentinel `0` VND, draft `display_order = 0`, first media `THUMBNAIL` + rest `GALLERY`, `DETAIL` excluded) and provisions the taxonomy in **data-only migration `0033`**: 32 → 33 migrations with **78 tables / 833 columns / 190 CHECKs and fingerprint `82864268…` unchanged**, OpenAPI `e19c2f76…`, client `55de1cc1…`, Figma 70 and dependencies unchanged — [`reports/APP2-B02-G01-COMPLETION-REPORT.md`](./reports/APP2-B02-G01-COMPLETION-REPORT.md). **`APP2-B02` = `READY — NOT STARTED`** with its five-operation scope intact; `FU-APP2-CATEGORY-MANAGEMENT-01` = `DEFERRED_BEYOND_CATALOG_ALPHA` and `FU-APP2-THUMBNAIL-01` = `ROUTED_TO_APP2-T01`, both nonblocking. **`APP2-B02` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-31) — the Catalog Draft backend: exactly five Admin operations (`adminProduct_list/detail/create/update/archive`), no sixth and **no category endpoint**. Implements IMP-D032 as locked: `categorySlug` on the wire (closed enum, resolved in-transaction, **category UUID never exposed**), server-owned **immutable** slug (a rename never moves the public address), `DRAFT` price sentinel `0` VND rendered as whole đồng (`numeric(14,2)` returns `0.00`; VND has no minor unit), `display_order 0` with no `max+1`, and complete all-or-nothing ordered media replacement where the first Asset is the single `THUMBNAIL`, the rest `GALLERY` and `DETAIL` is never written. Concurrency uses `updated_at` **truncated to milliseconds** — the precision of the ISO token a client can echo; the raw microsecond comparison failed every guarded write. Archive is a guarded `DRAFT → ARCHIVED` that deletes nothing (`PUBLISHED` is a safe 409 because unpublication is B03's). **No ownership column exists** on `products`, so authentication is the scope and no audit actor is storable — recorded, not assumed. 88 new tests (53 unit, 21 live-PostgreSQL, 14 API, 6 gateway); `pnpm quality` `EXIT=0`; OpenAPI `e19c2f76…` → `b789cc99…` (7 → 10 paths, 8 → 13 operations) and client `55de1cc1…` → `66d1c991…`, additions only; **database baseline, Figma (70) and dependencies unchanged; no migration** — [`reports/APP2-B02-COMPLETION-REPORT.md`](./reports/APP2-B02-COMPLETION-REPORT.md). **`APP2-A02` and `APP2-A03` = `READY — NOT STARTED`**; `APP2-B03` = `BLOCKED_BY_APP2-B02`; `APP2-T01` stays `ROUTED — NOT PLANNED_FOR_EXECUTION`. **`APP2-B02-C1` (2026-07-31) — security & concurrency correction, `APP2-B02` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`.** Three reviewer defects, each reproduced before being fixed: mutations carried `StaffOriginGuard` but **not** `StaffJsonBodyGuard` (never exported from `IdentityModule`), so a `text/plain` post answered **400 instead of 415** — all three now carry the canonical `AuthenticatedAdminGuard + StaffOriginGuard + StaffJsonBodyGuard`; `updated_at` truncated to milliseconds was comparable but **not monotonic**, so two writes in one millisecond published the same token and a stale value still matched — the database now computes `GREATEST(date_trunc(ms, clock_timestamp()), date_trunc(ms, updated_at) + 1ms)` and returns it, with **no application time anywhere on the write path**; and the **invented 12-item media limit** is removed (it existed only to bound a per-item loop, had no product authority, and never reached OpenAPI). The fourth invariant is now proved: validation is one transaction-scoped `FOR SHARE` batch read, so a selected Asset **cannot leave `ACCEPTED` between validation and the media write** — two real transactions, rival transition fails with SQLSTATE `55P03` while the lock is held and succeeds after commit. Description semantics locked (absent = unchanged, `null`/blank = clear). 19 new tests (132 catalog tests total); `pnpm quality` `EXIT=0`; OpenAPI `b789cc99…` → `c4d1fef8…` and client `66d1c991…` → `3e3e267d…` — **paths and operation IDs unchanged**, the only diff being the description wording and the 403/415 the guards produce; DB baseline, Figma (70) and dependencies unchanged — [`reports/APP2-B02-C1-CORRECTION-REPORT.md`](./reports/APP2-B02-C1-CORRECTION-REPORT.md). **`APP2-B02-C2` = `MUST_NOT_BE_CREATED`**; `APP2-A02` and `APP2-A03` = `BLOCKED_BY_PRODUCT_OWNER_DESIGN_APPROVAL`. **`APP2-A02` blocked, then cured by `APP2-D03` (2026-07-31).** The A02 gate stopped before any code — no commit, no tracked change — as `BLOCKED_BY_CATALOG_LIST_DESIGN_CONTRADICTION`: the canonical A02 scope locks **filters**, but none of the three frozen Catalog nodes had a status or category filter, and their `Tạo sản phẩm` / `Chỉnh sửa` / `Xuất bản` / `Gỡ xuất bản` controls belong to `APP2-A03`/`APP2-A04`, whose routes do not exist yet. `APP2-D03` reconciles the three nodes in place (IMP-D033): `Trạng thái` and `Danh mục` filters added (desktop side-by-side, mobile stacked full-width), `Tải thêm sản phẩm` continuation reusing the `APP2-D02` cursor pattern, every create/edit/publication control removed so A02 is a truthful **read-only** list, and the empty state's dead CTA replaced. Price and search stay out of A02; A03 restores create/edit and A04 adds publish/unpublish as supplements. The A02/A03 label inversion in phase-plan §6.2 and annotation `450:404` is corrected. Three Catalog rows promoted under `FIG-APPROVAL-APP2-D03-CATALOG-LIST-001`, registry 70 → 71; OpenAPI, generated client and database unchanged — [`reports/APP2-D03-COMPLETION-REPORT.md`](./reports/APP2-D03-COMPLETION-REPORT.md). **`APP2-D03` = `COMPLETE — DELIVERED_FOR_REVIEW`**, so **`APP2-A02` = `READY — NOT STARTED`**. **`APP2-A02` delivers the read-only Admin Product List** at `/products`: `Trạng thái`/`Danh mục` filters with URL-owned state, `Tải thêm sản phẩm` cursor continuation with same-cursor retry, a semantic desktop table and a mobile card collection, and four truthful list states (loading, unavailable, unfiltered-empty, filtered-empty). `adminProduct_list` only — create/detail/update/archive are deliberately not re-exported from `@embroidery/api-client`, so no mutation is reachable before the checkpoint that owns it; no price, no search, no action control, honest media placeholder pending `APP2-T01`. 50 new component tests; production Admin reviewed behind the real gateway at 1440 and 390. OpenAPI, generated client, database and Figma registry unchanged; no dependency added — [`reports/APP2-A02-COMPLETION-REPORT.md`](./reports/APP2-A02-COMPLETION-REPORT.md). **`APP2-A02` = `COMPLETE — DELIVERED_FOR_REVIEW`**, so **`APP2-A03` = `READY — NOT STARTED`**. **`APP2-A03` then blocked on its mandatory pre-code audit** (`BLOCKED_BY_PRODUCT_FORM_DESIGN_CONTRACT_CONTRADICTION`, no source/commit/Figma edit): all five approved Product Draft nodes were one `Sản phẩm mới` create screen with a single atomic `Lưu bản nháp`, while `adminProduct_create` is `.strict()` and accepts only `categorySlug`/`name`/`description` — so media needed a second PATCH, `Phiên bản & SKU` matched no B02 field at all, there was **no edit/detail design**, and `basePriceAmount` had no approved surface. **`APP2-A03-G01`** — a narrow entry gate under A03 ownership, **not `APP2-D04`**, which must not be created — reconciles the five existing nodes in place into a two-mode capability: minimal create at `/products/new` (three POST fields, `Tạo bản nháp`, redirect to detail) and edit/detail at `/products/{productId}` (`Lưu thay đổi`, `Giá cơ bản`, read-only slug/status, ordered media via PATCH). Variants/SKU removed and deferred (`FU-APP2-PRODUCT-VARIANTS-SKU-01`); publication-readiness rail and `Tới bước xuất bản` removed (A04/B03); fabricated filenames replaced by B01/D02 server identity; keyboard media ordering added. IMP-D034 locked, five rows promoted under `FIG-APPROVAL-APP2-A03-G01-PRODUCT-FORM-001`, registry 71 → 72; OpenAPI, generated client, database and application source unchanged — [`reports/APP2-A03-G01-COMPLETION-REPORT.md`](./reports/APP2-A03-G01-COMPLETION-REPORT.md). **`APP2-A03-G01` = `COMPLETE — CORRECTED (C1) — REVIEW_ACCEPTED`** (the G01 promotion was spliced onto the document title instead of the canonical rows, leaving them `REVIEW_REQUIRED` behind a gate blind to rows outside a table; `APP2-A03-G01-C1` repaired the registry and added `document-title` / `row-placement` / `a03-approval` invariants — [`reports/APP2-A03-G01-C1-CORRECTION-REPORT.md`](./reports/APP2-A03-G01-C1-CORRECTION-REPORT.md)). **`APP2-A03` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** — Admin Product form/detail delivered against that authority: minimal POST create at `/products/new` redirecting to the authoritative `productId`, PATCH-capable edit/detail at `/products/{productId}` sending `expectedUpdatedAt` plus only changed fields (description absent-vs-`null`, whole-VND string price with the `"0"` sentinel, read-only slug/status), ordered `mediaAssetIds` with keyboard reordering and a cursor-paginated `ACCEPTED`-only asset picker, `PRODUCT_VERSION_CONFLICT` reload dialog with no force-save, and unsaved-change protection; A02 regains exactly `Tạo sản phẩm` and `Chỉnh sửa` while `adminProductArchive` stays off the client boundary. 403 admin tests green twice, production build reviewed live at 1440/390; OpenAPI, generated client, database and Figma unchanged — [`reports/APP2-A03-COMPLETION-REPORT.md`](./reports/APP2-A03-COMPLETION-REPORT.md). **Corrected by `APP2-A03-C1`** (plaintext development credential removed from evidence history and the credential rotated + reconciled to one authenticating credential; new canonical `pnpm check:secrets` gate; only the exact `PRODUCT_VERSION_CONFLICT` code opens the stale-version dialog, so a lifecycle or media-eligibility `409` no longer invites a reload that discards unsaved edits) — [`reports/APP2-A03-C1-CORRECTION-REPORT.md`](./reports/APP2-A03-C1-CORRECTION-REPORT.md). **`APP2-B03` blocked at its representability gate and was cured by the entry gate `APP2-B03-G01`** — accepted LC-04 authority had no exit from `PUBLISHED` except archive, so unpublish had no canonical transition; IMP-D035 adds **`TR-LC04-05` `PUBLISHED → DRAFT` (Unpublish Product)**, removing public visibility without archiving or deleting and returning the product to the editable draft state, with archive kept distinct as `PUBLISHED → ARCHIVED` (LC-04 4 TR → 5 TR; new `pnpm check:lifecycle` gate; no schema, source, OpenAPI, client or Figma change). **`APP2-B03-G01` = `COMPLETE — DELIVERED_FOR_REVIEW`**, **`APP2-B03` = `READY — NOT STARTED`** — [`reports/APP2-B03-G01-COMPLETION-REPORT.md`](./reports/APP2-B03-G01-COMPLETION-REPORT.md). **`APP2-B03` = `COMPLETE — DELIVERED_FOR_REVIEW`** — three Admin publication operations (`adminProduct_publicationReadiness`, `adminProduct_publish` `TR-LC04-01`, `adminProduct_unpublish` `TR-LC04-05`) with a closed seven-code readiness set, a single locked transaction that re-evaluates readiness rather than trusting the readiness GET, the B02-C1 monotonic `updated_at` token, and atomic `product.published`/`product.unpublished` audit + outbox evidence. No migration; OpenAPI 10 → 13 paths / 13 → 16 operations, generated-client additions only; database (33/78/833/190) and Figma (72/72) unchanged. Report: [`reports/APP2-B03-COMPLETION-REPORT.md`](./reports/APP2-B03-COMPLETION-REPORT.md). **`APP2-A04` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the Admin publication interaction at `/products/[productId]/publication`, consuming exactly those three operations through three new hand-written client exports (archive still withheld); a lifecycle action is offered only from a coherent detail+readiness snapshot and sends `{ expectedUpdatedAt }` from it, nothing is optimistic, all seven requirement codes render in server order with an unknown code never counted as satisfied, refusals are classified by exact domain code (only `PRODUCT_VERSION_CONFLICT` opens the reload dialog), unpublish is independent of readiness behind a `role="alertdialog"` confirmation, and the bare server slug is shown as read-only metadata because `450:404` marks `/san-pham/<slug>` `CHƯA CHỐT`; no API, OpenAPI, generated-client, database or Figma change ([`reports/APP2-A04-COMPLETION-REPORT.md`](./reports/APP2-A04-COMPLETION-REPORT.md)). **Corrected by `APP2-A04-C1` = `COMPLETE — DELIVERED_FOR_REVIEW`** — closes the missing production-runtime evidence: an ephemeral `runner`-stage image serves the real gateway as the `admin` upstream (zero bind mounts, `NODE_ENV=production`, `node apps/admin/server.js`), isolated from the active dev `.next` by `.dockerignore` and swapped through a temp-dir Compose override so no tracked Nginx/Compose file changes; direct route load, hard refresh, asset delivery, console cleanliness, a real `PRODUCT_VERSION_CONFLICT` and every desktop scenario pass, dev stack and mutable Product state restored exactly. **Retracts A04's `.next`-collision root cause** — `apps/admin/.next` is not bind-mounted and a host build provably does not disturb the dev container. The smoke's first run found a real defect A04 had recorded as passing — `a.product-publication__back` was a **16 px** touch target at 390 px — which the reviewer ruled in scope; it is repaired with the existing `$size-touch-target-min` token (label, `<Link>` semantics and route unchanged), and the mobile assertion now sweeps blocked DRAFT, ready DRAFT, PUBLISHED and the open dialog with a frozen single-entry exclusion list so it cannot be narrowed past a real failure. Final smoke **11/11 harness, 20/20 browser** ([`reports/APP2-A04-C1-CORRECTION-REPORT.md`](./reports/APP2-A04-C1-CORRECTION-REPORT.md)). **`APP2-A04-C2` = `MUST_NOT_BE_CREATED`**. **`APP2-B04` then blocked at its own entry gate — `BLOCKED_BY_PUBLIC_MEDIA_DELIVERY_CONTRACT_GAP`**: `Q-01`/`Q-02` require public product images, but no route in the repository served image bytes (16 operations, none public or binary; `apps/api` never called `getObjectStream`; `ADR-APP2-001` §4.7 designs the delivery it never built). `PUBLIC_CATALOG_SCHEMA_GATE` passed — only the transport was missing — so the Product Owner activated **`APP2-T01` as a mandatory predecessor of `B04`** (IMP-D036). **`APP2-T01` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW` (production-runtime evidence supplied by `APP2-T01-C1`, which also reproduced and fixed a production-only module-resolution defect that made the API production image unstartable — [`reports/APP2-T01-C1-CORRECTION-REPORT.md`](./reports/APP2-T01-C1-CORRECTION-REPORT.md); `APP2-T01-C2` = `MUST_NOT_BE_CREATED`)** — one anonymous binary operation `publicProductMedia_get`, publication re-proved per request in one bounded query, `getObjectStream` streaming with client-disconnect teardown, `image/webp`+`nosniff`+`inline`+`no-store`, and the shared `buildPublicProductMediaPath` helper for B04/S01/S02; OpenAPI 13→14 paths / 16→17 operations, no migration, no Figma, no Admin/Storefront/worker/gateway change ([`reports/APP2-T01-COMPLETION-REPORT.md`](./reports/APP2-T01-COMPLETION-REPORT.md)). **`APP2-B04` = `COMPLETE — DELIVERED_FOR_REVIEW` — two anonymous public operations `publicProduct_list` and `publicProduct_detail`, OpenAPI 14→16 paths / 17→19 operations, generated-client additions only, `NO_APP2_B04_MIGRATION`; `no-store` until a cache-invalidation consumer exists, and one recorded ADR-DB5-001 pagination conflict ([`reports/APP2-B04-COMPLETION-REPORT.md`](./reports/APP2-B04-COMPLETION-REPORT.md)) — **corrected by `APP2-B04-C1` = `COMPLETE — DELIVERED_FOR_REVIEW`**, so **`APP2-B04` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** and **`APP2-B04-C2` = `MUST_NOT_BE_CREATED`**: the Product Owner ruled **keyset** (IMP-D037), `ADR-DB5-001` gained **R10** and the DB5 query-shape/pagination/access-path/completeness/EXPLAIN/handoff documents were reconciled, with the prior offset classification preserved as dated history. The access path was measured on a disposable PostgreSQL 16.14 rather than asserted (`pnpm explain:q01`, [`../database/DB5_Q01_ACCESS_PATH_EVIDENCE.md`](../database/DB5_Q01_ACCESS_PATH_EVIDENCE.md)): both delivered forms sequential-scan and sort at MVP cardinality and are accepted, continuation pages eliminate rows at the scan and the thumbnail subquery is bounded by the page — and **IDX-065 orders the listing only from a `category_id`-constant form the public contract does not issue**, which is recorded precisely and routed to DB10 instead of being fixed inside frozen B04 source. New gate `pnpm check:pagination-authority`; no index, migration, application, OpenAPI, generated-client, Figma or dependency change ([`reports/APP2-B04-C1-CORRECTION-REPORT.md`](./reports/APP2-B04-C1-CORRECTION-REPORT.md))**, **`APP2-S01` = `READY — NOT STARTED`** (design authority **UI02 Discover Feed**, `REUSE_AND_SUPPLEMENT_ONLY`, 5/3/2-column masonry with linear DOM reading order) and **`APP2-S02` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** — Product Detail delivered at `/san-pham/[slug]` (server-first, `publicProductDetail` only, ordered catalog-preview gallery with an accessible lightbox, one 640px description section, browser-local share, S01 card upgraded to one semantic link; 99/99 production browser scenarios twice; corrected by `APP2-S02-C1`, which set the mobile content band to 24px/342px with a feature-local inset (the APP1 shell is untouched) and locked the streamed not-found transport as `SAFE_STREAMED_NOT_FOUND` — measured 200 with `noindex`, no Product canonical/metadata/data and five indistinguishable causes (IMP-D040); open non-blocking follow-ups `FU-APP2-DETAIL-NOT-FOUND-STATUS-01` (`ROUTED — FRAMEWORK_TRACKING — NONBLOCKING_AFTER_C1`), `FU-APP2-STOREFRONT-CONTENT-BAND-01` and `FU-APP1-SHELL-BRAND-TOUCH-TARGET-01`, [`APP2-S02-COMPLETION-REPORT.md`](./reports/APP2-S02-COMPLETION-REPORT.md), correction [`APP2-S02-C1-CORRECTION-REPORT.md`](./reports/APP2-S02-C1-CORRECTION-REPORT.md); `APP2-S02-C2` = `MUST_NOT_BE_CREATED`); it had been `READY — NOT STARTED` since `APP2-S02-G01`** — it was `BLOCKED_BY_UI03_RECONCILIATION` because `APP2-D01-STOREFRONT-PRODUCT-DETAIL` was `NOT_APPROVED — WITHHELD_PENDING_UI03_RECONCILIATION` with reuse policy `SEPARATE_RECONCILIATION_REQUIRED` against UI03 `261:1290` / `262:1291` / `273:1409` / `279:1504`, and backend completion does not lift a design blocker; **`APP2-S02-G01` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** cured it by reconciling that draft against the delivered `publicProductDetail` contract — all twelve UI03 roots audited live and preserved as `HISTORICAL_DRAFT_SOURCE`, a new sibling section `529:2224` approved under `FIG-APPROVAL-APP2-S02-G01-PRODUCT-DETAIL-001`, the detail route locked to **`/san-pham/[slug]`** and the scope locked to gallery + lightbox, name/category identity, exactly one description section, browser-local share and Discover continuation, with **no price or stock display** and materials/process/related/save/commission deferred because the contract does not represent them (IMP-D039, new gate `pnpm check:storefront-product-detail-authority`, [`APP2-S02-G01-COMPLETION-REPORT.md`](./reports/APP2-S02-G01-COMPLETION-REPORT.md)); **`APP2-E01` = `BLOCKED_BY_APP2-S01_AND_APP2-S02`**; `FU-APP2-ADMIN-MEDIA-PLACEHOLDER-01` = `DEFERRED — NONBLOCKING_FOR_APP2-B04`; `FU-APP2-PRODUCT-ARCHIVE-LIFECYCLE-01` = `ROUTED — NONBLOCKING_FOR_A04`, `FU-APP2-PRODUCT-ARCHIVE-UI-01` = `DEFERRED_PENDING_PRODUCT_OWNER_SURFACE_DECISION`. **`APP2-S01-G01` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the first `APP2-S01` attempt blocked at its route gate with `BLOCKED_BY_STOREFRONT_DISCOVER_ROUTE_DECISION` **having changed no file** (the Storefront had exactly one browser route, `/`, Homepage-owned; the shell's `discover` item was deliberately `route: null`; the approved 404 tagged *Khám phá tác phẩm* `Sắp ra mắt`; and no accepted document named a Discover path — a Figma label is not a URL contract). The Product Owner ruling **IMP-D038** locks the Discover route **`/kham-pha`** (no alias, no redirect; `/`, `/discover`, `/catalog`, `/products`, `/san-pham` rejected), category state `?category=<slug>` over `thu-bong`/`khan`/`quan-ao`/`khac`, the narrow S01 shell and not-found activation, and **explicitly authorizes staged non-interactive Product cards** so the unresolved Product Detail route no longer blocks the feed; `/san-pham/<slug>` stays `CHƯA CHỐT`/`NOT_CANONICAL` and `APP2-S02` stays `BLOCKED_BY_UI03_RECONCILIATION`. New gate `pnpm check:storefront-route-authority`; no application, OpenAPI, generated-client, database, Figma, infrastructure or dependency change ([`reports/APP2-S01-G01-COMPLETION-REPORT.md`](./reports/APP2-S01-G01-COMPLETION-REPORT.md)); `APP2-S01-C1` = `NOT_APPLICABLE`. **`APP2-S01` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the anonymous Discover feed at **`/kham-pha`**, server-first (`force-dynamic`; the first keyset page is in the HTML, only continuation is hydrated), consuming `publicProductList` **only**, with UI02's masonry preserved as CSS multi-column so **DOM reading order stays linear** while the browser paints 5 / 3 / 2 columns — audited live against `208:2002` / `224:871` / `226:1038` and then **measured** in a production browser at 1440 / 1024 / 390 with identical DOM order at each. Cards are staged non-interactive articles per IMP-D038; price and the out-of-stock flag are dropped at the projection boundary; unsupported UI02 modules (refine/layout controls, style-and-theme chips, trending, related, recently-completed, collection prompt, soft commission) are omitted rather than faked. Category state is `?category=<slug>` over the four contract slugs, with an unknown or repeated value taking the approved not-found boundary. Continuation is opaque-cursor keyset gated on `hasNext` **and** a usable cursor, deduped by slug, with no auto-retry and an exact-cursor `Thử lại`. Narrow shell activation only: nav `discover` → `/kham-pha` with `aria-current`, and the approved 404's *Khám phá tác phẩm* recovery becomes a real link; the other four areas stay unrouted. Isolated production evidence (`pnpm smoke:app2-s01-discover:production`, twice): canonical Storefront `runner` image (`node apps/storefront/server.js`, `NODE_ENV=production`, **zero** bind mounts) behind the real gateway with a production API and disposable TLS PostgreSQL, 24 synthetic published products against **real** THUMBNAIL bytes, 24/24 orchestration + 43/43 browser scenarios, zero console errors, and DRAFT removal proved (product gone, media 404, no route cache). Storefront tests 17 suites / 118; focused S01 6 suites / 65 twice. **No backend, Admin, worker, schema, OpenAPI, generated-client, object-storage, Figma, tracked-infrastructure or dependency change** ([`reports/APP2-S01-COMPLETION-REPORT.md`](./reports/APP2-S01-COMPLETION-REPORT.md)); `APP2-E01` = `BLOCKED_BY_APP2-S02`. **`APP2-E01` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the publication cross-layer journey, proving the accepted APP2 checkpoints compose into one real production journey: Admin login over HTTPS → real streaming multipart upload of a run-generated PNG → worker inspection to `ACCEPTED` with one READY unwatermarked `THUMBNAIL` and one `CATALOG_PREVIEW` → DRAFT Product with a server-owned slug → Asset attached as ordered media plus a `numeric(14,2)` price → readiness → publish → Discover and Product Detail visibility against **real** derivative bytes → unpublish with both media paths revoked to a real HTTP 404 and the detail route on the accepted `SAFE_STREAMED_NOT_FOUND` surface → republish reusing the same Asset, media, derivatives and slug → final unpublish. Run in a **disposable** production topology: four canonical `runner` images (API, Admin, worker, Storefront) with `NODE_ENV=production` and **zero** development mounts, a disposable TLS PostgreSQL restored schema-only plus the three migration-owned prerequisite tables, a disposable MinIO, and the real gateway carrying **one additive, ephemeral, temp-directory TLS listener** — required because the API refuses a staff session without a `Secure` cookie and the tracked gateway is HTTP-only; the `__Host-adm_session` `Secure`/`HttpOnly`/`SameSite=Strict` cookie is **asserted, never injected**. Evidence is **baseline-and-delta, never deletion** — the append-only immutability triggers refused a destructive first design and drove the model; Audit and Outbox each balance at 2 published / 2 unpublished added, publication events stay `PENDING` (no APP2 consumer), and the worker claimed only `ASSET_PROCESSING` work. `pnpm smoke:app2-e01-publication:production` **105/105 twice**, with every development service restored and `residualImages: 0` / `residualContainers: 0`; 32/32 Docker-free harness regressions; `pnpm quality` 344/344 and `pnpm quality:e2e` 15/15. No repository credential was read, none rotated, none logged or committed; no `.env` write. **No application, OpenAPI, generated-client, database, Figma, tracked-infrastructure or dependency change** ([`reports/APP2-E01-COMPLETION-REPORT.md`](./reports/APP2-E01-COMPLETION-REPORT.md)). Masonry column counts are an upper-bound assertion measured at 1 (one published Product in the disposable copy); population behaviour stays owned by `APP2-S01`. `APP2-X01` **not started**. **Corrected by `APP2-E01-C1`** — the reviewer returned E01 as `CORRECTION_REQUIRED` because the disposable database took its structure from a `pg_dump --schema-only` of the developer's database instead of the canonical migrations; a snapshot inherits local drift, can never fail when the migration runner is broken, and depends on developer state. The disposable TLS database now starts **empty** and is migrated by the tracked `db-migrate` one-shot (`pnpm --filter @embroidery/database db:migrate`, `--no-deps`), then verified before any application starts: `db:status` exit 0 (**33 applied / 33 in repository**), the frozen migration-checksum manifest, the **seven committed DB6 checkers** ending in `db-fingerprint-gate.mjs`, and the physical baseline **78 tables / 833 columns / 190 CHECKs** on PostgreSQL 16.14 — checkers reused, never reimplemented. Prerequisites are independent: categories from **migration 0033** (`khac`/`khan`/`quan-ao`/`thu-bong`), staff through the API bootstrap CLI, and the worker runtime policy published as one immutable version (APP2-I02 gives the runtime no production default). Every mutable table starts at **0** — reached, never cleared. No `pg_dump`/`pg_restore`/`--schema-only`/`--data-only` anywhere in the harness, and no development host is addressed. Journey **121/121 twice** after the final change, retries 0, with full development restoration and zero residue; **42/42** Docker-free regressions; `pnpm quality` **354/354**, `pnpm quality:e2e` **15/15**; per-package api 115/1468, worker 24/283, admin 46/519, storefront 25/231, persistence 9/112, object-storage 5/150. Masonry wording restated honestly: E01 proves the Product renders inside the accepted masonry within the viewport column contract, and the populated 5/3/2 proof stays **`APP2-S01`** evidence. **No application, OpenAPI, generated-client, schema/migration, Figma, dependency, lockfile or tracked-infrastructure change** ([`reports/APP2-E01-C1-CORRECTION-REPORT.md`](./reports/APP2-E01-C1-CORRECTION-REPORT.md)); `APP2-E01` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`, `APP2-E01-C2` = `MUST_NOT_BE_CREATED`, `APP2-X01` = `READY — NOT STARTED`. **`APP2-X01` = `COMPLETE — DELIVERED_FOR_REVIEW`; `APP2` = `COMPLETE — PASS_WITH_FOLLOW_UPS — DELIVERED_FOR_REVIEW`** — the phase is closed against one canonical reconciliation, [`reports/APP2-CLOSURE-MATRIX.md`](./reports/APP2-CLOSURE-MATRIX.md): **30 canonical checkpoints**, every one final with both commits and an existing report, across **94 commits** from the entry audit `8643431b…` to the `APP2-E01-C1` evidence `377321fd…`; **0 blocking checkpoints, 0 blocking follow-ups, 0 unauthorized C2**. Every correction is exactly one implementation commit then one evidence commit; the two disclosed departures are **`APP2-DEC-STORAGE`, which carries C1 *and* C2** (both reviewer-directed and accepted before the one-correction rule tightened) and **`APP2-I02-FD1`**, a final verification rather than a second correction (`APP2-I02-C1` is `SUPERSEDED_BY_FINAL_PROCESS_PROOF`). Four non-checkpoint commits in range — `5dcde4b6`, `7e3e4987`, `2d921513` (A03-C1 §L.1 replay) and `b7df24f2` (documentation-only security governance) — are each disclosed by an accepted report. **10 routed follow-ups, all nonblocking and owned** (9 APP2 + 1 inherited `FU-APP1-SHELL-BRAND-TOUCH-TARGET-01`); the archive-lifecycle gap and the **`PENDING` publication Outbox** are preserved as boundaries, not relabelled as done. Frozen artifacts recomputed at closure and unchanged: OpenAPI `c2c3b874…` **16 paths / 19 operations / 34 schemas**, generated client `7524fc91…`, **33 migrations / 78 tables / 833 columns / 190 CHECKs**, fingerprint `82864268…`, Figma **86/86/11**. Public surface unchanged — `/kham-pha`, `/san-pham/[slug]`, `publicProduct_list`/`publicProduct_detail`/`publicProductMedia_get`, `SAFE_STREAMED_NOT_FOUND` current. New gate `pnpm check:app2-closure` in `pnpm quality`. **No application, OpenAPI, generated-client, schema/migration, Figma, dependency, lockfile or tracked-infrastructure change** ([`reports/APP2-X01-COMPLETION-REPORT.md`](./reports/APP2-X01-COMPLETION-REPORT.md)). Milestone **R1 Catalog Alpha achieved**; `NEXT_CANONICAL_PHASE` = **APP3 — Design Templates and 2D Design Studio** = `READY — NOT STARTED`. |
| APP3 | `COMPLETE — PASS_WITH_FOLLOW_UPS` | Pre-implementation audit complete (`APP3-PRE-IMPLEMENTATION-AUDIT`): audit [`audits/APP3_PRE_IMPLEMENTATION_AUDIT.md`](./audits/APP3_PRE_IMPLEMENTATION_AUDIT.md), report [`reports/APP3-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md`](./reports/APP3-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md). Verdict `PASS_WITH_REQUIRED_GATES`; **human review 2026-08-03 returned `ACCEPTED_WITH_REQUIRED_CORRECTIONS`, reconciled by `APP3-PRE-AUDIT-C1` → `COMPLETE — REVIEW_ACCEPTED_AFTER_CORRECTION`**; migration verdict **`NO_APP3_MIGRATION_UNLESS_G01_OR_G02_OR_G04_PROVES_ONE`** (`G01` added — closed `product_media.role` and `asset_derivatives.kind` CHECK sets, a `secure_access_grants` table that cannot express an anonymous-session grant, and no side/area immutability column all sit in the path of what it may rule), with `APP3-DB01` terminating as `COMPLETE` **or** `NOT_REQUIRED — GATE_RESOLVED` and dependents waiting on `DB-DISPOSITION-RESOLVED`. The Design Template and Design Session repositories already exist with their DB7 guards and the DB9 CC-01 autosave proof, but `DesignModule` is not composed, `design-document`/`design-engine` are empty, and **nothing can author or read `product_sides`/`embroidery_areas`** — which `design_sessions` requires `NOT NULL`. Four authority gates precede code (`APP3-G01` placement/side-media/route, `APP3-G02` template lifecycle + compatibility, `APP3-G03` session TTL `O-008`/anonymous transport/quotas, `APP3-G04` editor-safe derivative/intake lanes/SVG); Figma carries zero Studio or Admin-Template rows so one phase-level design package `APP3-D01` precedes every frontend checkpoint. `design-document` is mandatory before every document-accepting API while `design-engine` blocks only the three writes carrying the named out-of-bounds invariant. Corrected 37-row map in [`phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md`](./phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md) §6.1. Commit A was **not** docs-only (two `tools/` files); disposition `APPROVED_NARROW_GOVERNANCE_DEVIATION`. **`APP3-G01` = `READY — NOT STARTED`**; all other APP3 checkpoints `NOT STARTED`. `FU-APP3-CLOSURE-FIGMA-BASELINE-01` = **`COMPLETE — CLOSED_BY_APP2-X01-C1`** — the unrelated BRD0 commit `2a5d3bf` grew the Figma registry 86/86/11 → 96/96/13, drifting an APP2 frozen artifact and failing `check:app2-closure`, two of its tests and `pnpm quality`. `APP2-X01-C1` replaced the global-total freeze with verification of the **86-record APP2-owned subset** transcribed from the closure commit `8b5f3b0` ([`reports/APP2-X01-C1-COMPLETION-REPORT.md`](./reports/APP2-X01-C1-COMPLETION-REPORT.md), baseline [`reports/APP2-CLOSURE-FIGMA-BASELINE.json`](./reports/APP2-CLOSURE-FIGMA-BASELINE.json)); unrelated rows may be appended while removal, mutation, duplication, section moves and same-count substitution of an owned record still fail. `pnpm quality` is green. **`APP3-G01` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-03) — the first APP3 execution checkpoint locked seven Product Owner placement rulings as **IMP-D041**: placement is Admin-only **Product** authority authored at `APP3-A01`; the public read is a read-only manifest carrying `studioEligible`; the canonical side-background association stays **`product_sides.background_asset_id`** (never `product_media`) with delivery at `APP3-B02` and the derivative kind still owned by `APP3-G04`; the customer selects side/area only where more than one is active; the canonical Studio route is **`/san-pham/[slug]/thiet-ke`**; **global publication does not require placement** while Studio eligibility is separately derived; and referenced placement is **immutable with no hard delete**, giving **`G01_DB_DISPOSITION = REQUIRES_APP3_DB01`**. New gate `pnpm check:app3-g01`; no application source, schema, migration, OpenAPI, generated-client or Figma change ([`reports/APP3-G01-COMPLETION-REPORT.md`](./reports/APP3-G01-COMPLETION-REPORT.md)). **`APP3-G02` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-03) — the second APP3 execution checkpoint locked eight Product Owner rulings as **IMP-D042** and formalised the Design Template lifecycle as **LC-24**, the next free canonical lifecycle: six stable transitions (`TR-LC24-01` create, `-02` publish, `-03` unpublish, `-04` archive draft, `-05` archive published, `-06` restore → `DRAFT`), **no** direct `ARCHIVED → PUBLISHED` and **no** hard delete in APP3. Versions are **immutable from creation**, every `DRAFT` save writes a new monotonic version, `published_at` is **set once and never cleared**, unpublish touches the header only, and public read selects the highest published version. Clone yields an independent document with lineage only. APP3 publishes **area-scoped Templates only** with exact product→side→area compatibility, so **`G02_DB_CONTRIBUTION = NONE`** — `APP3-DB01` still runs solely because `APP3-G01` requires it. **`FU-APP2-PRODUCT-ARCHIVE-LIFECYCLE-01` = `COMPLETE — CLOSED_BY_APP3-G02`**: LC-04 gains `TR-LC04-06` `DRAFT → ARCHIVED` (now 6 transitions), authorising the archive-from-draft `APP2-B02` already shipped, with archive still distinct from unpublish. New gate `pnpm check:app3-g02`; no application source, schema, migration, OpenAPI, generated-client or Figma change ([`reports/APP3-G02-COMPLETION-REPORT.md`](./reports/APP3-G02-COMPLETION-REPORT.md)). **`APP3-G03` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — the third APP3 execution checkpoint locked ten Product Owner rulings as **IMP-D043**, the anonymous Design Session ownership, transport and retention authority. Ownership requires **both** the public `design_sessions.id` **and** a high-entropy secret: 32 server CSPRNG bytes, unpadded base64url, persisted only as **`HMAC-SHA-256(runtime pepper, secret)`** and compared in constant time — **neither half authorizes alone**, and the raw secret is never persisted, logged, placed in a URL/query/fragment, returned in JSON or stored in browser storage. It travels **only** in the per-session cookie **`__Host-nettheu_ds_<session-id>`** (`Secure`, `HttpOnly`, `SameSite=Lax`, `Path=/`, no `Domain`, `Max-Age` never beyond `expires_at`); bootstrap returns the non-secret id, and the Studio route stays `/san-pham/[slug]/thiet-ke`. A successful authenticated resume rotates the secret **atomically under compare-and-swap** with exactly one winner, immediate old-secret invalidation, **no** previous-secret grace and **no TTL extension**; ordinary autosave never rotates. Every state-changing request needs an exact allowed `Origin`, an allowed `Sec-Fetch-Site`, the host-only cookie and the expected revision; credentialed cross-origin CORS is disabled and unauthorized, missing, expired and wrong-chain requests share **one** safe non-enumerating error shape. **`SESSION_TTL = 30 days`, absolute from `created_at`** — never slid by reads, resume, rotation or autosave — expiring `ACTIVE → EXPIRED` (`TR-LC07-04`) on an hourly sweep and hard-deleting (`TR-LC07-05`) after a **24-hour** purge grace, scoped to the exclusively-owned session family; `SUBMITTED` retention and `TR-LC07-03` stay **APP5-owned**. Locked quotas: creation 5/hour (burst 2/minute), read 60/minute, authorization failures 10/15 minutes, mutations 30/minute, **1** in-flight mutation — keyed by an **ephemeral network HMAC under a rotating salt**, with no durable browser identity, no browser session quota and no raw IP persisted. Autosave carries the expected `autosave_revision`; a stale revision returns **`STALE_WRITE` without mutating** and the client refetches before retrying, so no idempotency table is needed. This **closes `O-008`** and the `design_sessions` half of **`DP-RET-01`**, and **supersedes** the earlier sliding `last_activity_at + TTL` direction in `DB4_DELETE_ARCHIVE_RETENTION_MAPPING.md` and `DB10_DATA_DURABILITY_MATRIX.md`. LC-07 keeps its four states and five transition ids; `ABANDONED` is not reintroduced. **`G03_DB_CONTRIBUTION = NONE`** — `session_secret_hash` (NOT NULL, unique), `expires_at`, `created_at`, `status` (LC-07 CHECK) and `autosave_revision` (`>= 0`) were **measured** to already exist, with no customer, email, phone, IP or fingerprint column anywhere on the table. New gate `node tools/check-app3-g03.mjs`; no application source, schema, migration, OpenAPI operation, generated client or Figma change ([`reports/APP3-G03-COMPLETION-REPORT.md`](./reports/APP3-G03-COMPLETION-REPORT.md)). **`APP3-G04` = `COMPLETE — REVIEW_DELIVERED`** — editor-safe media, asset eligibility and complexity authority locked as **IMP-D044** (twelve rulings, §6.7), delivered under a **manual intervention directive**: the first attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED` because the ruling as issued asserted that `asset_derivatives` already carried intrinsic dimensions, and the measured schema (Drizzle source and migrated database) carries none. The Product Owner selected canonical derivative metadata **columns** and explicitly rejected treating `asset_inspections.detail` as runtime authority. `NORMALIZED` is reused as the sole editor-safe kind with **no** new enum value; three processing profiles (`SIDE_BACKGROUND`, `TEMPLATE_ASSET`, `SESSION_UPLOAD`) are application policy, never a database enum; SVG is Admin-only and mandatorily sanitized server-side, rejected for side backgrounds and for anonymous Session uploads; three contextual delivery classes exist and no generic public Asset endpoint does; upload and document complexity limits are exact; documents carry a server-owned `fontId`; the watermark stays runtime-only. **`G04_DB_CONTRIBUTION = REQUIRES_APP3_DB01_ASSET_DERIVATIVE_METADATA`** — `width_px`, `height_px`, `media_type`, `byte_size` on `asset_derivatives`, nullable with an all-or-none invariant, so **`APP3-DB01` = `REQUIRED — READY_FOR_EXECUTION`** with two contribution groups (G01 placement retirement, G04 derivative metadata). `FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01` stays **OPEN — AUTHORITY_LOCKED_BY_APP3-G04**, final owner `APP3-B02`; `FU-APP3-G02-DEPENDENCY-TABLE-BOUND-01` = **`COMPLETE — CLOSED_BY_APP3-G04`**. New gate `node tools/check-app3-g04.mjs`, which passes in both schema modes; no application source, schema, migration, OpenAPI operation, generated client or Figma change ([`reports/APP3-G04-COMPLETION-REPORT.md`](./reports/APP3-G04-COMPLETION-REPORT.md)). **`APP3-G04` = `COMPLETE — REVIEW_ACCEPTED`.** **`APP3-DB01` = `COMPLETE — REVIEW_DELIVERED`** — the one APP3 database checkpoint, delivering both contribution groups in a single forward-only migration `0034_add_app3_placement_and_derivative_authority`. **Group A (IMP-D041):** `product_sides` and `embroidery_areas` gain `code` (stable per-parent machine identity, format `^[a-z0-9][a-z0-9_-]{0,63}$`, backfilled deterministically as `legacy-<uuid>` from the row's own id and **proved** free of null/duplicate/malformed values before `SET NOT NULL`), `retired_at` and `superseded_by_id`; a replacement requires retirement, may not be the row itself, must share the parent and may not form a direct two-row cycle; and once a Template header, a **non-terminal** Design Session or an approval snapshot references a row, its identity and geometry freeze and it cannot be hard-deleted, while display copy, ordering and the retirement pointers stay editable. Enforcement is database triggers in the DB6-S24 class, not application code. **Group B (IMP-D044):** `asset_derivatives` gains `width_px`, `height_px`, `media_type` and `byte_size`, nullable with an all-or-none invariant, positive/non-blank checks, and a READY-`NORMALIZED` requirement — historical READY `THUMBNAIL`/`CATALOG_PREVIEW` rows stay valid and unmeasured, and the migration performs **no** backfill and contacts no object storage. Physical columns 833 → 843. New gate `node tools/check-app3-db01.mjs`; `node tools/check-app3-g04.mjs` now reports `PASS — DERIVATIVE_METADATA_IMPLEMENTED`. No API, worker, package, Figma or UI change ([`reports/APP3-DB01-COMPLETION-REPORT.md`](./reports/APP3-DB01-COMPLETION-REPORT.md)). **`APP3-F01` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — controlled font acquisition, delivered under a **manual intervention directive** after the first `APP3-P01` attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED`. `IMP-D044` PO-10 requires the Design Document font registry to be concrete and file-backed, and the repository controlled **zero** font binaries, no licence artifact, no integrity baseline and no glyph-coverage evidence — `packages/styles/src/settings/_typography.scss` names General Sans and Inter as browser-resolved CSS fallbacks, which is UI styling authority, not a controlled asset, and the only WOFF2 files on disk belonged to a Next dev-tool inside `node_modules`, subsetted `latin`/`latin-ext`. The Product Owner selected **Inter v4.1** from the official upstream `rsms/inter`, resolved commit `e3a3d4c57d5ecc01453a575621882a384c1995a3`, under **SIL Open Font License 1.1**. Two **unmodified** variable WOFF2 files — `InterVariable.woff2` (`normal`, 352 240 B, `sha256:693b77d4…`) and `InterVariable-Italic.woff2` (`italic`, 387 976 B, `sha256:e564f652…`), both carrying `wght 100..900` — are vendored with the exact upstream `LICENSE.txt` at `packages/design-document/assets/fonts/inter/4.1/`, beside a provenance manifest and a Vietnamese-coverage manifest. Coverage was **measured from the real `cmap` tables** of the committed bytes using pinned `fonttools 4.55.3` in a throwaway environment outside every tracked path: **156 / 156 required code points covered, zero missing, in both files** — the Vietnamese letters, the six vowel bases, `Đ`/`đ`, the eight combining marks canonical decomposition needs, and every assigned code point in `U+1EA0..U+1EF9`. Inter ships italic as a separate file rather than an `ital` axis, so italic identity rests on the `OS/2` `fsSelection` and `head` `macStyle` bits, measured set on the italic file and clear on the upright one. New gate `node tools/check-app3-f01-font-assets.mjs`, which hashes the committed bytes against the manifests **and** re-derives the mandated repertoire itself, so a coverage manifest cannot pass by quietly shrinking what it claims to require. The `fontId = inter` registry entry, its files, licence path, styles, weight range and `REJECT_IF_CONTROLLED_FONT_UNAVAILABLE` fallback policy are locked in §6.9.4 and **reserved for `APP3-P01`**; F01 writes no TypeScript. **`APP3-P01` = `BLOCKED_BY_APP3-F01_REVIEW_ACCEPTANCE`**, its first attempt recorded forward as `FAILED — MANUAL_INTERVENTION_REQUIRED`, cause `NO_CONTROLLED_FONT_ASSET_OR_LICENSE_EVIDENCE`, resolution `APP3-F01`. No package implementation, registry code, schema, migration, OpenAPI, generated client, worker, Figma or UI change ([`reports/APP3-F01-COMPLETION-REPORT.md`](./reports/APP3-F01-COMPLETION-REPORT.md)). **`APP3-P01` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — the production Design Document foundation, **resumed** after the manual intervention that `APP3-F01` resolved; `APP3-F01` = **`COMPLETE — REVIEW_ACCEPTED`**. `packages/design-document` stops being an empty stub and now owns the engine-neutral schema (version **1**; root `schemaVersion`/`placement`/`elements`, unknown root or element fields rejected), five element kinds (`text`, `image`, `shape`, `freehand`, `group` — **no** `svg`, which arrives as a sanitized Asset), structural validation with **no coercion** of any kind, complexity validation, contextual asset/font validation, quantization, RFC 8785 canonicalization, document migration and the controlled runtime font registry. Array order is z-order and elements are addressed by stable opaque id, never by index. **Quantization** uses the one precision in accepted authority — `10_000`, four decimal places, `-0 → 0` — traced to `ADR-APP0-001 §4` plus `APP0-R01` evidence and recorded in an exported `…_AUTHORITY` constant rather than a bare literal; integers, strings, ids and array order are never quantized. The pipeline is `version → structure → complexity → quantization → **revalidation** → canonical bytes → size`: the second pass is load-bearing, because a width of `0.00004` is a legal positive number that quantizes to `0`, which the schema forbids. **Canonicalization** is RFC 8785 implemented in-package per ADR-DB1-012 (key order by UTF-16 code unit, never locale collation; ECMAScript number serialization; JSON escaping including lone surrogates; arrays never reordered), with **NFC rejected-if-absent at the input boundary** as ADR-DB1-012 §7 requires. **SHA-256 is server-only**, behind the `@embroidery/design-document/server` subpath — a build-time boundary rather than a runtime branch, because APP0-R01 measured that `crypto.subtle` is undefined outside a secure context; the gate walks the whole module graph reachable from the browser-safe root and fails on any `node:` import. The nine IMP-D044 limits are exported constants asserted at boundary **and** boundary + 1: canonical size is measured on **UTF-8 bytes** after quantization, text length counts **code points**, hidden and locked elements count, and one Asset placed twenty times counts **once** toward the asset and decoded-pixel budgets but twenty times toward image elements. Contextual validation requires a `READY` `NORMALIZED` derivative whose `width_px`/`height_px`/`media_type`/`byte_size` (the `APP3-DB01` quartet) are present, usable and equal to the document's intrinsic dimensions — source-asset metadata is explicitly not a substitute — and it reads no database and no object storage. The **font registry** is version 1 with one entry (`fontId = inter`) exactly as §6.9.4 locked it, including both binary SHA-256 values and `REJECT_IF_CONTROLLED_FONT_UNAVAILABLE`; the metadata is transcribed rather than read at runtime, and a package test re-reads the committed manifests **and re-hashes the committed binaries** so the transcription cannot drift. General Sans is not registered and no font byte, path or URL is ever serialized into a document. New gate `node tools/check-app3-p01.mjs` (17 checks, chains F01 and DB01) with 39 checker tests; **151** package unit tests across 9 suites; the package now builds to `dist` with `test`/`build`/`typecheck`/`lint`. `APP3-P02` stays `READY — NOT STARTED` and owns all geometry, bounds and px↔mm — the gate and an architecture suite both assert none of it entered P01. No API, OpenAPI, generated client, worker, schema, migration, Figma or UI change ([`reports/APP3-P01-COMPLETION-REPORT.md`](./reports/APP3-P01-COMPLETION-REPORT.md)). **`APP3-P01-C1` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — human review returned **`APP3-P01 = COMPLETE — CORRECTION_REQUIRED`** on one contradiction inside the delivered evidence: `IMP-D044`/`APP3-G04` key repeated-reference counting on the **Asset** — once toward the 20-asset limit **and** once toward the 33,554,432 decoded-pixel total — while the P01 implementation summed decoded pixels over unique **`derivativeId`** values. The two are equivalent only while each Asset is reached through one derivative, and diverge the moment one `assetId` appears with two `derivativeId` values, at which point derivative-keying overcharges the budget for a document the Asset rule says costs one decode. Contextual validation now accumulates `Map<assetId, {derivativeId, decodedPixels}>`: the first sighting of an Asset records its derivative and charges its pixels once, a repeat through the **same** derivative charges nothing, and a repeat through a **different** derivative emits `DERIVATIVE_METADATA_MISMATCH` on the **later** element's `$.elements[n].derivativeId` path. The ambiguity is **rejected, never resolved** — counting the first, last, smallest or largest derivative makes the budget depend on element order or an arbitrary choice, summing them charges one Asset twice, and normalizing every reference to one derivative silently rewrites what the customer placed; each breaks one of the two G04 rules, and the ambiguity is equally unanswerable for canonical media identity, intrinsic-dimension validation and deterministic rendering. The restriction is one derivative **per Asset within one document**; an Asset may still have many derivatives globally. Complexity validation already keyed its 20-asset budget on `assetId` and is unchanged, as is the 20-image-element count. No new decision id was created: this **restores** `IMP-D044` rather than amending it. Package tests **151 → 162** (the regression reproduces the exact delivered ambiguity — one `assetId`, two eligible READY NORMALIZED derivatives, different ids — and asserts rejection); P01 checker tests **39 → 45**, with the gate now asserting Asset-keyed accumulation, absence of derivative-keyed accumulation, the conflict check, G04's surviving wording and the presence of the regression itself. `node tools/check-app3-g04.mjs` and `node tools/check-app3-f01-font-assets.mjs` both pass unchanged. **`APP3-P01` = `COMPLETE — CORRECTION_DELIVERED_FOR_REVIEW`**. No schema, canonicalization, hashing, quantization, font, migration, geometry, API, worker, database, Figma or UI change ([`reports/APP3-P01-C1-COMPLETION-REPORT.md`](./reports/APP3-P01-C1-COMPLETION-REPORT.md)). **`APP3-G05` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — geometry coordinate, transform and bounds authority locked as **`IMP-D045`** (twelve rulings, §6.11), delivered under a **manual intervention directive** after the first `APP3-P02` attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED`. **`APP3-P01` = `COMPLETE — REVIEW_ACCEPTED`** after its `C1` correction. The stop was required because **no accepted document defined** rotation origin, rotation direction, scale origin, group coordinate semantics, parent/child composition order or a production bounds strategy, and the only executable source — the APP0-R01 spike — **contradicts itself**: the SVG adapter pivots on the box centre and nests children inside the group transform, Konva pivots top-left and zeroes the group's translation and rotation so only scale and opacity inherit, and Fabric pivots top-left and constructs an empty group so nothing inherits. The identical cross-engine canonical hash proved **serialization** agreement only — it is taken over the document, which stores `x`, `y`, `rotationDeg` whatever a renderer does with them — and APP0-R01 itself recorded that "the document box centre is not a reliable hit point. Hit testing must be asked of the engine, never derived from document geometry." The three adapters are now classified as **comparative research evidence, not production authority**; `IMP-D026` keeps native SVG as the renderer architecture and the renderer **implements** IMP-D045 rather than defining it. The locked model: top-left origin, x right, **y down**, degrees, positive `rotationDeg` **clockwise**; persisted `x`/`y` are the untransformed local-box top-left **in the parent frame** and `width`/`height` its positive unscaled dimensions; rotation and scale share the **untransformed local-box centre** `(width/2, height/2)` with **scale applied before rotation**; column-vector `p' = M x p` with `Mlocal = T(x + width/2, y + height/2) x R_clockwise(rotationDeg) x S(scaleX, scaleY) x T(-width/2, -height/2)` and `a = cos, b = sin, c = -sin, d = cos` under y-down; a group defines a **real local frame** whose children carry group-local coordinates composed **parent-outermost**, with the group's persisted box as local frame and pivot but **never** as bounds and **never** recomputed from descendants; bounds are `CONSERVATIVE_TRANSFORMED_AABB` (four local corners through the effective matrix, ellipse deliberately using its declared box, line/freehand expanded by `strokeWidthPx / 2`, group and document as unions, hidden and locked elements still counting, z-order irrelevant); containment is **blocking** and boundary-inclusive over the complete transformed AABB with **no** clamping, snapping or warn-but-persist; **`product_sides.px_per_mm`** is the sole px↔mm authority with no DPI assumption, no Area-derived ratio and disagreeing axes reported rather than averaged; and retired placement is `NOT_SELECTABLE` for `NEW_EDITING` while remaining valid for `HISTORICAL_RENDER`. **PO-12** binds all of it to `schemaVersion = 1`: because a v1 document records **no pivot marker**, changing any of these would silently relocate every stored design while its SHA-256 stayed valid — the hash is over the bytes, not the interpretation, and `ADR-DB1-012` binds approvals to that hash — so such a change is a **schema-semantic** event requiring a new decision, a schema-version increase and an explicit migration, never an internal refactor. **`G05_DB_CONTRIBUTION = NONE`**. New gate `node tools/check-app3-g05.mjs` (+ `-transform.mjs`) with 50 tests, asserting every fact as an **exact string** and asserting the forbidden alternatives too. **`APP3-P02` = `BLOCKED_BY_APP3-G05_REVIEW_ACCEPTANCE`**, its first attempt recorded forward as `FAILED — MANUAL_INTERVENTION_REQUIRED`, cause `GEOMETRY_SEMANTICS_NOT_AUTHORIZED_AND_SPIKE_DIVERGENT`, resolution `APP3-G05`. No geometry implementation, package source, renderer, API, worker, schema, migration, Figma or UI change ([`reports/APP3-G05-COMPLETION-REPORT.md`](./reports/APP3-G05-COMPLETION-REPORT.md)). **`APP3-G05-C1` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — human review returned **`APP3-G05 = COMPLETE — CORRECTION_REQUIRED`**, accepting the coordinate, pivot, matrix, group, containment, physical-scale and integrity rulings and closing the one bounds-authority gap the delivered report had itself disclosed. **The stroke omission:** `rectangle` and `ellipse` carry `strokeWidthPx` in P01, but PO-08 as delivered expanded only `line` and `freehand` — so a stroked rectangle could paint outside the accepted bounds and still pass containment, making the strategy named `CONSERVATIVE_TRANSFORMED_AABB` optimistic for two of its four stroked kinds. **The unstated line path:** P01's `line` is a `ShapeKind` defined solely by its transform box and stores no endpoints, so P02 could not compute deterministic bounds and no two renderers were obliged to draw the same segment. `IMP-D045` PO-08 is corrected **in place** before review acceptance — no new decision id, no other ruling touched, and **no v1 schema change** (every corrected rule reads existing fields). The corrected model: the stroke is **drawable geometry** for every kind storing `strokeWidthPx` (`rectangle`, `ellipse`, `line`, `freehand`), **centred on the local path**, expanding the local envelope by `strokeWidthPx / 2` on all four sides and participating in bounds, containment, physical-size validation, group bounds and document bounds; a zero stroke expands nothing; stroke is never renderer decoration, never exempted by the presence of a fill and never subtracted from `width`/`height`; `text` and `image` stay unexpanded. Local geometry is now exact per kind — **rectangle** = the declared-box boundary with fixed `lineJoin = miter`, `miterLimit = 4`; **ellipse** = inscribed in the declared box, envelope = declared box + half stroke, still the conservative transformed AABB and never the tight rotated ellipse/stroke AABB; **line** = the straight segment `(0,0)` → `(width,height)` with round cap and join, never a horizontal or vertical centreline, an arbitrary renderer path, a renderer-chosen diagonal or a path with unstored endpoints; **freehand** = an ordered polyline of straight segments over element-local points with round cap and join, no smoothing/interpolation/Bezier/simplification, a single point being a round dot of radius `strokeWidthPx / 2`. **Transform order is locked**: expand the local envelope, transform all four of **its** corners through the effective matrix, then take the document-space AABB — adding an unscaled half-stroke after the transform is forbidden because it ignores scale and rotation; the resulting overestimation for rotated or non-uniformly scaled line/freehand is accepted for v1 containment safety. Containment and physical-size validation are now explicitly **stroke-aware**: a shape whose fill fits while part of its ruled stroke envelope overhangs fails. The fixed cap/join/miter values are **version-level constants, not v1 document fields** — a customer never chooses them, adding them would have been the schema change this correction exists to avoid, and changing one changes painted bounds and is therefore schema-semantic under PO-12. Gate tests **50 → 75**, covering every dropped-stroke, redefined-line, smoothed-freehand, post-transform-expansion and cap/join-as-field variant. **`APP3-G05` = `COMPLETE — CORRECTION_DELIVERED_FOR_REVIEW`**; **`APP3-P02` = `BLOCKED_BY_APP3-G05_CORRECTION_REVIEW`**. No geometry implementation, package source, schema, renderer, API, worker, database, Figma or UI change ([`reports/APP3-G05-C1-COMPLETION-REPORT.md`](./reports/APP3-G05-C1-COMPLETION-REPORT.md)). **`APP3-P02` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — the production geometry foundation, **resumed** after the manual intervention that `APP3-G05` and `APP3-G05-C1` resolved; both are **`COMPLETE — REVIEW_ACCEPTED`**, as are `APP3-P01`/`APP3-P01-C1`. `packages/design-engine` stops being an empty stub and now implements `IMP-D045` exactly: matrices with `a = cos, b = sin, c = -sin, d = cos` (clockwise under y-down), the local matrix `T(x + width/2, y + height/2) x R(rotationDeg) x S(scaleX, scaleY) x T(-width/2, -height/2)` with **scale before rotation**, effective transforms composed **parent-outermost** over a root-first chain, stroke-aware local envelopes (`rectangle` and `ellipse` expanded by `strokeWidthPx / 2` under miter/4, the v1 `line` path locked to `(0,0)`→`(width,height)` with round cap and join, `freehand` an element-local straight polyline with round cap and join and a single point as a round dot), `CONSERVATIVE_TRANSFORMED_AABB` bounds that transform **the envelope's** four corners rather than the declared box's, group bounds as the union of descendant drawable AABBs and never the group's persisted box, blocking boundary-inclusive containment, physical-size validation through **`product_sides.px_per_mm`** with disagreeing axes reported rather than averaged, and `NEW_EDITING` versus `HISTORICAL_RENDER` placement modes. The package **never mutates a document**, never rebases or flattens a group and never moves an element to make it fit; called directly with input P01 never validated it fails safely with a typed finding rather than a throw or an unbounded walk (parent chains are depth-bounded, so a cycle cannot recurse). Quantization **reuses** P01's `quantizeNumber` — no second precision, and no epsilon anywhere near a placement or containment comparison, which are exact on the grid; the one machine-level threshold (`1e-12`) guards matrix inversion only. Only the **public root** of `@embroidery/design-document` is imported: no deep import, no Node built-in, no renderer, no database, no spike, and no module-level mutable state. **118 package tests** across 6 suites (including the `APP3-G05-C1` regressions: a stroked rectangle whose fill fits and stroke does not now fails containment, and a 3× scaled 8px stroke reaches −112 rather than the −104 a post-transform expansion would have given); new gate `node tools/check-app3-p02.mjs` with **34** checker tests, asserting the *semantics* rather than the presence of a function. `APP3-B07` and `APP3-B08` become `READY_BY_P01_AND_P02`, `APP3-B04` is now blocked only by `APP3-B06`, and `APP3-S11` has its geometry foundation. No renderer, API, OpenAPI, generated client, worker, schema, migration, Figma or UI change, and no `packages/design-document` source change ([`reports/APP3-P02-COMPLETION-REPORT.md`](./reports/APP3-P02-COMPLETION-REPORT.md)). **`APP3-P02-C1` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04), moving **`APP3-P02` = `COMPLETE — CORRECTION_DELIVERED_FOR_REVIEW`** — human review accepted the matrix, transform, envelope, stroke, placement, px↔mm, containment and physical-size implementation and rejected one direct-call safety defect: the delivered graph kept the **first** group that claimed a child. The structural contract gives an element at most one direct group parent, so a document with two claims has **no** authoritative effective transform to choose from; selecting one silently accepted invalid input, made geometry depend on group and document order, could produce an incorrect AABB or evaluate containment against the wrong transform, and hid an upstream validation bypass. Parent claims are now **staged** while indexing and become a parent map only once every one of them is unambiguous, so no partial map is ever exposed; a repeated child inside one group, one child claimed by two groups, a group listing itself and a claimed child that is not in the document are all invalid, and **no** first, last, id-order or z-order rule selects a winner. An invalid graph yields `INVALID_PARENT_CHAIN` (`UNKNOWN_ELEMENT` for an unknown child) naming the contested child, in an order the document decides rather than the group order; effective-transform resolution, element/group/document bounds, both containment validators and both physical-size validators fail safely, and none returns a finding beside an authoritative-looking AABB — `getDocumentBounds` returns `Bounds2D **Closed at `APP3-X01`** (verdict `PASS_WITH_FOLLOW_UPS`; zero blocking follow-ups): matrix [`reports/APP3-CLOSURE-MATRIX.md`](./reports/APP3-CLOSURE-MATRIX.md), report [`reports/APP3-X01-COMPLETION-REPORT.md`](./reports/APP3-X01-COMPLETION-REPORT.md), gate `node tools/check-app3-closure.mjs`. Sixty-eight checkpoints executed and accepted, seventeen of them corrections; `APP3-B06` and `APP3-W01` were replanned before execution and `APP3-B06C-C1` was cancelled. The blocking `FU-APP3-UPLOAD-REVISION-SEAM-01` is `COMPLETE — CLOSED_BY_APP3-E01-C1`, repaired by `APP3-E01-C1` with one Session-revision authority shared by the upload and autosave capabilities. **Eleven follow-ups remain open, every one nonblocking and owned** — five routed to `APP4`, three to `APP12`, one to a `LATER_APP3` Studio capability, one to a Product Owner ruling and one to an authorized regression activity — plus two limitations frozen rather than reassigned, because no delivered or planned phase owns them: production Template artwork intake, and the `APP3-A02`/`APP3-D01` Template-list drift. Frozen artifacts recomputed at closure: OpenAPI `f39e9e8f…` **37 paths / 42 operations / 84 schemas**, generated client `c2fb229f…`, **34 migrations / 78 tables / 843 columns**, fingerprint `7abf3708…`, 30 root scripts; the Figma registry (165 rows) was reused unchanged, not reopened. **No application, OpenAPI, generated-client, schema/migration, Figma, dependency or lockfile change at closure.** Group creation is not delivered and is not claimed, and production readiness stays APP12 (IMP-D015). `NEXT_CANONICAL_PHASE` = **APP4 — Customer Identity, Verification, Secure Access and Notification Core** = `READY_FOR_PRE_IMPLEMENTATION_AUDIT`. | GeometryFinding | undefined` so an unmeasurable document cannot read as an empty one. Nothing is normalized: no claim is dropped, no `childIds` rewritten, no group flattened. `IMP-D045` is **unchanged** and every valid-graph vector still holds; **137 package tests** (was 118) and the P02 gate grew the ambiguity checks and 4 regressions (38 checker tests) ([`reports/APP3-P02-C1-COMPLETION-REPORT.md`](./reports/APP3-P02-C1-COMPLETION-REPORT.md)). **`APP3-B01` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — the first APP3 backend slice and the first APP3 HTTP surface: exactly three operations, `adminProductPlacement_get` and `adminProductPlacement_replace` behind an Admin session (plus the staff origin and JSON guards on the write) and the anonymous `publicProductPlacement_get`, delivered by a Product/**Catalog** module (`CatalogPlacementModule`) because IMP-D041 PO-01 puts placement authoring in the Product aggregate and never in Design — no Design module is imported and none exists. **Admin read** returns the whole authoring model including **retired** rows, since those are what an existing Template or Session references; **replace** is one transaction opened by a guarded touch of `products.updated_at`, which simultaneously proves the caller's token is current, takes the row lock that serialises a second replace and advances the token so a stale write is refused — a row carrying an `id` is retained, one without is created, and one **omitted is retired, never deleted** (IMP-D041 PO-07), optionally with `supersedesId` naming the same-parent row it replaces. Everything validates before a single write, so one bad side leaves the previous placement untouched; the `APP3-DB01` triggers stay the authority on a **referenced** row (identity and geometry refused, display copy and ordering allowed, hard delete rejected by the database), and a guard refusal is translated into a stable error rather than worked around by deleting and re-inserting. Scale consistency and area-within-canvas are delegated to **`@embroidery/design-engine`** (IMP-D045) rather than re-derived — the engine reports both axes instead of averaging them and treats a boundary-flush area as contained. The **public manifest** is keyed by Product slug, reuses the existing published-plus-public-category predicate, excludes retired rows in SQL, and carries **no** background asset id, `retiredAt`, `supersededById`, storage key, original or derivative URL, inspection detail or mutation field — a background is addressed by the reference components `APP3-B02` will be keyed by (Product slug + Side code) and by **no URL**, because that route does not exist. **`studioEligible`** is derived, never stored, and true only when one side has both an active Area and a background whose asset is in the `CATALOG_MEDIA` lane, un-tombstoned, with a `READY` `NORMALIZED` unwatermarked derivative carrying the whole `width_px`/`height_px`/`media_type`/`byte_size` quartet (IMP-D044 PO-01/PO-03/PO-07); areas on one side and a usable background on another give `false`, and no dimension is ever read from object storage. Publication and placement stay independent (PO-06): a published Product with no placement returns an empty manifest rather than an error. OpenAPI **16 → 18 paths, 19 → 22 operations, 34 → 42 schemas**; client regenerated. **98 tests** (59 unit/contract, 39 live-database) plus the new gate `node tools/check-app3-b01.mjs` with 27 checker tests. No schema, migration, worker, renderer, UI or Figma change; `FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01` stays **OPEN — FINAL_OWNER_APP3-B02** ([`reports/APP3-B01-COMPLETION-REPORT.md`](./reports/APP3-B01-COMPLETION-REPORT.md)). **`APP3-B01-C1` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04), moving **`APP3-B01` = `COMPLETE — CORRECTION_DELIVERED_FOR_REVIEW`** — human review accepted the operations, DB01 protection, public projection, eligibility predicate, geometry delegation and live-database coverage, and rejected one HTTP-contract gap: the compare-and-set was enforced but not **published**. `ReplaceProductPlacementBody` rendered as an **empty object**, so a client reading the document could not see that `expectedUpdatedAt` exists or is required, and the generated client typed the body as an empty interface — a server-side CAS whose token the client cannot obtain and submit is not a concurrency contract. The cause is platform-wide rather than placement-specific: `createZodDto` carries the Zod schema for the validation pipe and **no** OpenAPI metadata, so every schema-backed body in the repository publishes empty (`UpdateProductBody`, `ArchiveProductBody` and the publication bodies included); runtime validation was never affected. The correction is deliberately scoped to the placement body, because repairing `createZodDto` would change five APP2 schemas belonging to accepted checkpoints. The contract is now explicit and matches the Catalog convention exactly — the Admin read returns **`updatedAt`** (required), the replace requires **`expectedUpdatedAt`** in `z.string().datetime({ offset: true })` format, a successful replace returns the whole model again with the **fresh** token, a stale one is `PLACEMENT_VERSION_CONFLICT` (409) with **no** side or area written, and the public manifest exposes neither field. A second divergence was corrected with it: the placement token used a bare `datetime()` and so refused an offset-bearing token (`+07:00`) that the same client could send to every other Admin Product write. **Zero new paths, zero new operations**; schemas 42 → 44 (the two bounded request component classes that make the documented body usable). 10 new live-database concurrency proofs — the exact `products.updated_at` value in canonical wire format, the token advanced by a display-only change and by retiring the whole placement, no side/area write when the CAS fails, and two writers on one token producing exactly one winner whose returned token is the stored one while the loser retries successfully with it and still fails with the original. The generated-client evidence is now like-for-like: the accepted-B01 and corrected states are both hashed with the repository's own deterministic tree method rather than comparing a 40-character Git object id against a 64-character content hash ([`reports/APP3-B01-C1-COMPLETION-REPORT.md`](./reports/APP3-B01-C1-COMPLETION-REPORT.md)). **`APP3-W01` = `FAILED — MANUAL INTERVENTION REQUIRED`** (2026-08-04), primary cause **`JOB_CONTRACT_INSUFFICIENT`**, secondary cause **`SVG_SANITIZER_NOT_SELECTED`** — the stop was correct and wrote nothing: `asset.inspection.requested` is appended **inside the upload transaction** as an Asset moves `UPLOADED → INSPECTING`, its payload is deliberately only `{ schemaVersion, assetId }` with exact validation, and at that instant no APP3 association can exist (`product_sides.background_asset_id` requires an already `ACCEPTED` Asset — which that very job produces — while `design_template_assets` and `design_session_assets` have no writer because `DesignModule` is not composed into `AppModule`), so the worker could not derive the IMP-D044 profile; and no SVG sanitizer, ADR or deterministic sanitizer contract exists anywhere in the repository. **`APP3-G06` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) supplies exactly that authority as **`IMP-D046`**, twelve locked rulings, implementing nothing. One new event type — **`asset.normalization.requested`** — reuses the existing transactional Outbox, worker dispatcher, claim/heartbeat/retry mechanism, attempt evidence, dead-letter behaviour and the existing `ASSET_PROCESSING` job kind; it is **not** a second queue, scheduler, sweep, claim table or retry framework, and because `outbox_events.event_type` is open text with no CHECK and the worker resolves handlers from a registry keyed on event type, it costs **no migration and no schema change**. `asset.inspection.requested` is untouched and keeps owning original inspection plus the APP2 `THUMBNAIL`/`CATALOG_PREVIEW` outputs. The payload (schema version 1) carries `assetId`, `normalizationPolicyVersion = 1` and an `associationRef` discriminated union — `PRODUCT_SIDE_BACKGROUND`/`productSideId`, `DESIGN_TEMPLATE_ASSET`/`designTemplateAssetId`, `DESIGN_SESSION_ASSET`/`designSessionAssetId`, all stable opaque primary keys proven present — and **never** a profile, ownership claim, storage key, URL, session secret, customer id or raw SVG: the event says which committed association triggered work, not that it is still valid. The **worker derives the profile** by re-reading the association and Asset at claim time under an exact mapping, proving existence, the pointer to `assetId`, owner/context, active lifecycle and Asset classification/kind/state — a discriminator is a lookup key, never authorization, and the worker never scans an Asset's associations to pick one. The producer appends **in the same transaction as the association write** (association → outbox → commit), so a rolled-back association emits nothing; no event on a read, a no-op save, or a removal without a replacement. Producer ownership follows the association: a new bounded **`APP3-B01N`** for `product_sides.background_asset_id` adding **zero** HTTP paths/operations/schemas and **not** reopening B01, `APP3-B03` for `design_template_assets`, `APP3-B06` for `design_session_assets` — and no endpoint exists merely to enqueue normalization. The failed checkpoint is **replanned, never resumed**: `APP3-W01 = REPLANNED — REPLACED_BY_APP3-W01A_AND_APP3-W01B`, where **W01A is raster-only** (JPEG/PNG/WebP across all three profiles, testable by inserting the ruled event directly into the disposable harness before any producer exists) and **W01B covers only Template SVG**. **`APP3-G07`** is created to select and lock the sanitizer implementation, element/attribute sets, namespace, URL, CSS and font policy, `viewBox` authority, node and path-data counting, deterministic serialization, output MIME, licence policy and a security regression corpus; until G07 and W01B are accepted Template **SVG stays authorized by IMP-D044 and operationally unavailable**, rejected safely — no silent Sharp rasterization, no regex sanitization, no browser DOM as an implicit sanitizer, and **no package chosen here**. Idempotency is `assetId` + policy version + kind `NORMALIZED`, so duplicate delivery, different eligible associations and concurrent claims converge on one authoritative row with **no** profile or association id persisted on `asset_derivatives`; a stale association completes as the bounded non-retryable `NORMALIZATION_CONTEXT_NO_LONGER_ELIGIBLE`, never an infrastructure retry. Deployment order is locked **W01A → B01N → G07 → W01B**, never combined. **`APP3-B02` = `BLOCKED_BY_APP3-W01A_AND_APP3-B01N`** — `APP3-B06` is corrected out of the SIDE_BACKGROUND trigger role and owns Session Asset association only — and `FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01` stays **OPEN**, final owner `APP3-B02`, now blocked by `APP3-W01A` + `APP3-B01N`. `FU-PLATFORM-ZOD-DTO-OPENAPI-METADATA-01` stays **OPEN — BLOCKS_NEXT_SCHEMA_BACKED_HTTP_CHECKPOINT`; G06, W01A, B01N, G07 and W01B add no HTTP request-body schema and are not blocked by it. New gate `node tools/check-app3-g06.mjs`. No API, worker, package, schema, migration, OpenAPI, generated-client, dependency, infrastructure or Figma change ([`reports/APP3-G06-COMPLETION-REPORT.md`](./reports/APP3-G06-COMPLETION-REPORT.md)). **`APP3-W01A` = `COMPLETE — REVIEW_DELIVERED`** — the raster consumer of `IMP-D046`. One handler on the existing `JobHandlerRegistry` and the existing `ASSET_PROCESSING` job kind consumes `asset.normalization.requested` schema version 1 / normalization policy version 1; no new queue, scheduler, sweep, claim table or retry framework, no migration, no HTTP operation, no producer, no SVG sanitizer, no new dependency and no root script. The payload names the association and never the profile: the worker loads that association **by id**, proves it points at the requested Asset, is active, and that the Asset sits in the lane the profile requires (`SIDE_BACKGROUND` → `CATALOG_MEDIA`, `TEMPLATE_ASSET` → `TEMPLATE_SOURCE`, `SESSION_UPLOAD` → `CUSTOMER_UPLOAD`), and a stale or mismatched context completes as the bounded non-retryable `NORMALIZATION_CONTEXT_NO_LONGER_ELIGIBLE`. Sources are JPEG/PNG/WebP only under 10 MiB, 4096 px per side and 16,777,216 decoded pixels; Template SVG returns `TEMPLATE_SVG_NORMALIZATION_NOT_AVAILABLE`, so it stays authorized by IMP-D044 and operationally unavailable until `APP3-G07` and `APP3-W01B`. The output is one `NORMALIZED` WebP whose IMP-D044 quartet is **measured** from the encoder report and the counted output stream — never copied from the source Asset — and `READY` plus all four fields land in a single statement guarded on this attempt’s `PROCESSING` claim, with no object-storage call inside a database transaction and `assets.status` untouched. Idempotency is Asset + policy version + kind `NORMALIZED`, so duplicate delivery, different eligible associations and concurrent claims converge on one authoritative row, and an existing `READY` result answers a request only **after** its association is revalidated. New gate `node tools/check-app3-w01a.mjs` (chains `APP3-G06`); `tools/check-app3-g06.mjs` was made mode-aware so the no-implementation rule accepts exactly two consistent worlds — before W01A no application mentions the event, after it the worker owns it and `apps/api/**` still does not — a disclosed narrow-scope deviation, the same pattern applied at `APP3-B01` to G01/DB01. ([`reports/APP3-W01A-COMPLETION-REPORT.md`](./reports/APP3-W01A-COMPLETION-REPORT.md)). **`APP3-B01N` = `COMPLETE — REVIEW_DELIVERED`** — the producer half of `IMP-D046`. The accepted placement replace transaction now appends `asset.normalization.requested` when a Product Side gains or repoints its `background_asset_id`, through the existing `OutboxEventStore` and inside the same compare-and-set transaction, **after** the Side rows are written so the payload names a committed id: zero HTTP paths, operations and schemas (the OpenAPI artifact and the generated client are byte-identical), zero migrations, no new queue, scheduler or endpoint, and no root script. The event vocabulary was extracted to **`@embroidery/domain-types`** — the event type, both versions, the association discriminator, the payload type and a pure builder — and both applications import it, so the producer and the consumer cannot drift; that package was given the standard IMP-D018 runtime build (`dist` main/types/exports) because a source-resolved package would repeat the `APP2-T01` defect and fail at load in the container, and both apps declare it under **dependencies** (`APPROVED_NARROW_SCOPE_DEVIATION = DOMAIN_TYPES_RUNTIME_BUILD_AND_DUAL_APP_WORKSPACE_LINK`). The intents are derived from the placement **plan**, so a rename, a reorder, an Area edit, a geometry edit, a no-op save, a retirement, a read, a stale CAS and any rollback all append nothing, two changed Sides append two deterministic events, two Sides sharing one Asset append two events that W01A converges onto one derivative, and the Asset a Side moved away from is never scheduled and never has its derivative deleted. New gate `node tools/check-app3-b01n.mjs` (chains W01A). `APP3-B02` becomes `READY_BY_APP3-W01A_AND_APP3-B01N` subject to the platform Zod/OpenAPI follow-up, and `FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01` stays **OPEN**, final owner `APP3-B02`, with its processing and trigger foundation now ready ([`reports/APP3-B01N-COMPLETION-REPORT.md`](./reports/APP3-B01N-COMPLETION-REPORT.md)). **`APP3-G07` = `COMPLETE — REVIEW_DELIVERED`** — the sanitizer authority `IMP-D046` PO-06/PO-09 deferred, locked as **`IMP-D047`** in fifteen rulings. The sanitizer is **DOMPurify on server-side Node with jsdom**: `dompurify@3.4.13` (`MPL-2.0 OR Apache-2.0`, zero runtime dependencies) and `jsdom@29.1.1` (`MIT`), both to be pinned **exactly**. The newest jsdom is deliberately refused — `jsdom@30.0.1` requires Node `^22.22.2` and every image is `node:22.14.0-alpine`, so pinning it would have failed only inside the container, and raising the base image is an infrastructure decision this gate has no standing to make. DOMPurify’s **defaults are not the policy**: it runs in the SVG namespace with HTML and MathML disabled and XML parsing retained, and `DOMPurify.removed` is diagnostic only, never the security decision. APP3 adds nine allowed elements and a closed attribute set, total refusal of URLs, CSS, references and fonts, a closed value, transform and path grammar with **real** path parsing (no permissive regex) through a **package-owned** parser and therefore no new dependency, `viewBox`-derived `width_px`/`height_px` with no rounding or root fallback, four complexity limits whose boundary passes and boundary+1 rejects, whole-file rejection as `UNSAFE_OR_UNSUPPORTED_TEMPLATE_SVG` rather than silent removal, and a thirteen-step pipeline whose **second serialization must equal the first**. SVGO is an optimizer, not a sanitizer, and never runs after sanitization — DOMPurify’s own documentation warns that modifying markup afterwards can void it; no regex sanitizer, no headless browser, no `isomorphic-dompurify`, and Sharp never rasterizes a Template SVG. `TEMPLATE_SVG_SANITIZATION_POLICY_VERSION = 1` is worker policy, not a database column. Sanitization does not authorize delivery: a sanitized Template SVG stays private, unwatermarked, `NORMALIZED` and Template-owned. New gate `node tools/check-app3-g07.mjs` (chains B01N). **`APP3-G07` installs nothing** — no dependency, lockfile importer, source file, schema, migration, OpenAPI, generated client, infrastructure or Figma change — and Template SVG stays **authorized but operationally unavailable** until `APP3-W01B` ([`reports/APP3-G07-COMPLETION-REPORT.md`](./reports/APP3-G07-COMPLETION-REPORT.md)). **`APP3-B02`** = **`COMPLETE — REVIEW_ACCEPTED`**, carrying the approved bounded tooling-size deviation `B02_PREDECESSOR_GATE_AND_TEST_FILES`; `FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01` = **`COMPLETE — CLOSED_BY_APP3-B02`** ([`reports/APP3-B02-COMPLETION-REPORT.md`](./reports/APP3-B02-COMPLETION-REPORT.md)). **`APP3-P03`** closes `FU-PLATFORM-ZOD-DTO-OPENAPI-METADATA-01`: every schema-backed request body had published as an empty object because `createZodDto` gives Swagger no property metadata, so runtime validation was correct while the *contract* was not and the generated client typed each body as an open bag. The repair is platform-wide and uses Zod 4.4.3's own `z.toJSONSchema` exporter — no new dependency — configured with `io: 'input'`, `unrepresentable: 'throw'` and `cycles: 'throw'` so an inexpressible construct **fails the build** rather than publishing `{}`; every `createZodDto` consumer is registered by class name, the document augmentation refuses to emit a document in which any JSON body or anything it references is still empty, and the `APP3-B01-C1` hand-written decorator classes are replaced by `.meta()` on the Zod schema itself. Zero new paths, operations, capabilities, migrations or root scripts; **19 paths / 23 operations / 45 schemas** unchanged. `APP3-B03` and `APP3-B06` become `READY` ([`reports/APP3-P03-COMPLETION-REPORT.md`](./reports/APP3-P03-COMPLETION-REPORT.md)). `APP3-B06` stopped at `BLOCKED — ENTRY_ARCHITECTURE_PRESUPPOSITION_ABSENT`: its plan presupposed a server upload-intent operation, a browser presign capability and an upload-completion proof, and the repository has none — `ObjectStoragePort` has exactly six methods and excludes presign by design (`ADR-APP2-001` §4.7/§4.8), so the two-operation lane could not be built without inventing one. `APP3-G08` locks **`IMP-D048`**: the upload stays API-owned multipart streaming, `APP3-B06` becomes `REPLANNED — REPLACED_BY_APP3-B06A_AND_APP3-B06B`, Session bootstrap stays with its existing owner `APP3-B07` rather than creating a second issuer of the same credential, and the measured inspection/normalization ordering defect — a not-yet-`ACCEPTED` Asset is a **terminal** normalization verdict, so two events committed together would silently strand every lost race — is routed to the bounded predecessor `APP3-W01C` ([`reports/APP3-G08-COMPLETION-REPORT.md`](./reports/APP3-G08-COMPLETION-REPORT.md)). **`APP3-W01C`**, **`APP3-B06A`**, **`APP3-B06B`** (+`-C1`), **`APP3-B07`**, **`APP3-B08`** (+`-C1`) and **`APP3-P04`** are all `COMPLETE — REVIEW_ACCEPTED`, taking the surface to **23 paths / 27 operations / 66 schemas** and completing the anonymous Session backend: bootstrap/clone/resume, raster intake, and autosave under an `autosave_revision` CAS whose stale write is a `409` with nothing written, with the shared `DesignSessionSnapshotResponse` contract published for all three Session operations. **`APP3-ROADMAP-RECONCILIATION`** (docs-only) then corrected the roadmap itself after an execution-order audit. Autosave **cadence** ownership moves `APP3-S11` → **`APP3-S10`**: `S11` is *mobile controls and touch gestures* and had been proposed as the next checkpoint although eight of its predecessors had not started — an `OWNER =` line is not a queue position. `APP3-B04` is `BLOCKED_BY_APP3-B03`, not by the already-accepted `P02`/`B06B`. And the one `IMP-D044` PO-06 delivery class that had **no owner at all** — the *granted delivery* half of `APP3-B06`, lost when `APP3-G08` replanned it — is ruled into two dedicated one-operation checkpoints under `ASSET_DELIVERY_RULING = OPTION_2_DEDICATED_DELIVERY_CHECKPOINTS`: **`APP3-B05A`** (published Template asset, `DEFINED — BLOCKED_BY_APP3-B04_AND_APP3-B05`) and **`APP3-B06C`** (Session upload private delivery, `DEFINED — READY`), mirroring `APP3-B02`, so each of the three delivery classes is one checkpoint, one operation, one authorization context (phase plan §6.24). `APP3-B05` is **not** enlarged and stays two JSON reads. **No frontend checkpoint has started and none may**: `APP3-D01` = `READY — NOT STARTED` gates every `A*` and `S*` checkpoint, so `NEXT_ELIGIBLE_FRONTEND_CHECKPOINTS = NONE`. Eligible now: `APP3-B03`, `APP3-D01`, `APP3-B06C`; recommended next is **`APP3-B03`**, head of the `B03 → B03A → B04 → B05 → B05A → S01` critical path. **`APP3-B03` = `COMPLETE — REVIEW_DELIVERED`** — the first Admin Design Template surface, and the first half of the `B03_CONTRACT_RULING` split. Three operations (`adminDesignTemplate_create`, `_list`, `_detail`) take the surface to **25 paths / 30 operations / 72 schemas**. Create writes the header in `DRAFT` and **zero immutable versions** — the delivered `CreateDesignTemplateInput` already carried no document and `current_version` has no CHECK, so nothing had to be widened and no migration was needed — and the detail read reports an absent version as an absent field rather than a fabricated version 0. The placement scope is optional as a *triple*: `IMP-D042` PO-06 lets a draft be authored unscoped, but a Side without its Product is refused at the request boundary and a triple that names a retired row or an Area hanging from another Side is refused by the placement port `APP3-B08` already reads. The list is keyset-paginated, over-fetches by one and resolves no version per row. `TR-LC24-01` writes one bounded audit row (`design_template.created` on the new `DESIGN_TEMPLATE` target kind — open text, no migration, the same footing `APP2-B03` added `PRODUCT` on) and **no** outbox event: creating a private draft has no consumer. The document save, the immutable version, the Template Asset association and the `DESIGN_TEMPLATE_ASSET` normalization producer all stay `APP3-B03A`, which becomes `READY — NOT STARTED` and is the recommended next checkpoint. Six predecessor gates banned any Admin Template route as a proxy for "no backend checkpoint has run"; each now consults one shared `acceptedAdminTemplatePaths` list, and the three that pinned `APP3-B03 = READY — NOT STARTED` accept the ruled set instead. New gate `node tools/check-app3-b03.mjs` with 32 checker tests ([`reports/APP3-B03-COMPLETION-REPORT.md`](./reports/APP3-B03-COMPLETION-REPORT.md)). **`APP3-B03A` = `COMPLETE — REVIEW_DELIVERED`** — the Design Template draft document save, and the other half of the `B03_CONTRACT_RULING` split. One operation, `PUT /api/admin/design-templates/{templateId}/document` (`adminDesignTemplate_saveDocument`), taking the surface to **26 paths / 31 operations / 73 schemas**. A full canonical snapshot becomes the next immutable version under a compare-and-set that puts the caller’s `expectedCurrentVersion` **in the update predicate** — so two writers at the same token leave exactly one winner and one `409`, proved over ten iterations in one live invocation. The draft version is written with `published_at` null; publication stays `APP3-B04`’s and stamps it once, later. `APP3-P01` is the sole document authority — schema version, structure, complexity, quantization, canonicalization and contextual media — and **`APP3-P02` is deliberately absent**, because `IMP-D042` PO-06 lets a draft hold no scope at all and PO-07 makes placement and containment the publication guard `GRD-T01`, which *"no backend checkpoint may implement a reduced"* version of. A draft may therefore be saved out of bounds, and `APP3-B04` refuses to publish it. Placeable images come from one lane: `findScopedByIds` over `TEMPLATE_SOURCE` / `PRODUCTION_SENSITIVE`, so an Asset from any other lane is absent from the allowlist and indistinguishable from an id that does not exist. `APP3-B03A` is now the `DESIGN_TEMPLATE_ASSET` normalization producer `IMP-D046` PO-05 routed: each **newly inserted** association appends exactly one `asset.normalization.requested` after the association and inside the same transaction, with `schemaVersion` and `normalizationPolicyVersion` at 1 and the durable `designTemplateAssetId`; an association that already existed appends nothing, and one no later save removes, because an older immutable version may still need its provenance. One bounded `design_template.version_saved` audit row carries the version number and nothing else. `APP3-B04` becomes `READY — NOT STARTED` and is the recommended next checkpoint. **`FU-APP3-TEMPLATE-SOURCE-ASSET-INTAKE-01` = `OPEN — OWNER_NOT_YET_ASSIGNED`**: no accepted API creates a `TEMPLATE_SOURCE` Asset, so text-only Template authoring is production-reachable while production Template *image* authoring waits on that owner; the image path is implemented and proved against canonical seeded fixtures. New gate `node tools/check-app3-b03a.mjs` with 39 checker tests ([`reports/APP3-B03A-COMPLETION-REPORT.md`](./reports/APP3-B03A-COMPLETION-REPORT.md)). **`APP3-B03` stopped at `FAILED — MANUAL INTERVENTION REQUIRED`, `CAUSE = B03_HTTP_CONTRACT_AUTHORITY_CONFLICT`,** having written nothing: its canonical endpoint matrix is three operations (`POST /api/admin/design-templates` plus a list and a detail read) while `IMP-D042` PO-04, `APP3-A02` and `APP3-A03` between them require four — create, list, detail **and** the draft document save, which no checkpoint in the phase owned. The operator ruled **`B03_CONTRACT_RULING = OPTION_2_SPLIT_DRAFT_SAVE_INTO_APP3_B03A`**: `APP3-B03` stays exactly three operations and creates the Template **header only** in `DRAFT` (the delivered `CreateDesignTemplateInput` already carries no document and no version, and `current_version` has no CHECK, so a header with zero versions is storable and no migration is required); the new one-operation replan child **`APP3-B03A`** (`PUT /api/admin/design-templates/{templateId}/document`) carries the `IMP-D042` PO-04 behaviour that had no owner — a full canonical P01 document snapshot creating a new immutable version with the next monotonic number and `published_at = null`. `APP3-B03A` is authorized to add the draft-version repository seam the DB7 port lacks (`publishVersion` requires a non-null `publishedAt`), without schema change. Producer ownership of the `DESIGN_TEMPLATE_ASSET` `asset.normalization.requested` event moves `APP3-B03` → **`APP3-B03A`** by `IMP-D046` PO-05’s own principle — producer ownership follows the association write, and `APP3-B03` no longer performs one; the event contract is untouched. Dependencies become `APP3-B04` ← `APP3-B03A` + `APP3-P02` (publish needs an immutable version to publish) and `APP3-A03` ← `APP3-B03A`, while `APP3-A02` keeps `APP3-B03`. Critical path: `B03 → B03A → B04 → B05 → B05A`; surface 27 → **30** at `B03`, then **31** at `B03A`. `APP3-B03` = `READY — NOT STARTED` and remains the recommended next checkpoint; `APP3-B03A` = `BLOCKED_BY_APP3-B03 — NOT STARTED` (phase plan §6.25). **`APP3-B04` delivers the LC-24 lifecycle**: publish, unpublish and archive — four of the six transitions (`TR-LC24-02`…`TR-LC24-05`) — with restore split to the one-operation **`APP3-B04A`** under `B04_LIFECYCLE_ROUTING_RULING = RESTORE_SPLIT_TO_APP3_B04A`, which keeps the checkpoint at the three operations `IMP-D044` PO-09 allows rather than stopping on a fourth. Surface 26 → **29** paths / 34 operations / 76 schemas. Publish runs the **whole** `IMP-D042` PO-07 `GRD-T01` guard, not a reduced one: no immutable version, incomplete or unresolved scope, an invalid P01 document, a retired Side or an Area hanging from a different Side, a `APP3-P02` placement disagreement or an out-of-bounds document in `NEW_EDITING` mode, or an ineligible Asset each refuse it — and the guard is read-only, so a template that cannot be published is never quietly repaired. Every transition is a compare-and-set on `expectedCurrentVersion` **constrained to its source states in the same predicate**, so a stale token and a wrong source state are one refusal rather than a read-then-write race; archive accepts `DRAFT` and `PUBLISHED` in one predicate, which is why an unpublish racing an archive can legitimately answer `[200, 200]` — two valid transitions serialized, not a lost update. `published_at` is stamped by an `IS NULL` predicate, so a republication of the same version keeps its original timestamp: set once, never rewritten, expressed as a predicate rather than a read-and-branch. A reason is required and bounded on archive only. No version is created, no row is deleted, no status cascades into Product, Assets, Sessions or versions, no outbox event and **no migration** (34 unchanged). Three bounded audit rows — `design_template.published`, `design_template.unpublished`, `design_template.archived`. New gate `node tools/check-app3-b04.mjs` with **40** checker tests; `APP3-B03` and `APP3-B03A` had pinned the Admin Template operation count and the guarded-write count as literals, and both are re-derived from the shared surface authority so each accepted world explains its own numbers. `APP3-B05` = `READY — NOT STARTED` and becomes the recommended next checkpoint; `APP3-B04A` = `READY — NOT STARTED` (phase plan §6.26). **`APP3-B05` delivers the public published-Template reads**: `GET /api/public/design-templates` and `GET /api/public/design-templates/{slug}`, both anonymous, both JSON, with byte delivery staying `APP3-B05A`’s under §6.24.2 rather than becoming a third operation here. Surface 29 → **31** paths / 36 operations / 81 schemas. Visibility is two facts and both are in the predicate: the header is `PUBLISHED` **and** the Template holds a version whose `published_at` is not null, so a historical publication stamp can never make an archived or unpublished header public, and a `PUBLISHED` header whose versions were never published shows nothing. The version shown is the **highest whose `published_at` is set**, never the one `current_version` names — the two agree in every state the delivered lifecycle can reach, which is exactly why the public read must not depend on that agreement, and a live suite proves a hand-written higher *unpublished* version is not leaked even when the counter points at it. Compatibility is exact triple equality on `productId` + `productSideId` + `embroideryAreaId`, all three **required**: `IMP-D042` PO-06 admits no product-wide, side-wide or wildcard match, so a partial scope has no meaning the endpoint could honour and is refused at the boundary rather than resolved into a broader query. A `PUBLISHED` Template can become derived-ineligible without being touched — its Product unpublished or moved out of a public category, its Side or Area retired — so eligibility is re-asked of Catalog through a narrow provider-only port extension (`findPublicPlacementScope`) on **every** request and never written back; a live test proves unpublishing the Product hides its Templates while leaving every Template row unchanged. Unknown slug, `DRAFT`, `ARCHIVED`, unpublished, never-published and ineligible are one indistinguishable `PUBLIC_DESIGN_TEMPLATE_NOT_FOUND`; the error vocabulary has two codes and the absence of a third *is* the non-disclosure rule. Paging is bounded keyset (`created_at DESC, id DESC`) with the scope encoded inside the cursor, so a cursor replayed against another Area is refused rather than served, and a malformed one is a 400 rather than a silent restart. The detail reuses the `APP3-P01` `DesignDocument` component; no `previewDerivativeId`, storage key, bucket, internal id or `APP3-B05A` address is published, because that route is not locked yet. Read-only **by construction**: the public module binds a separate port on which no write method exists, and composes no Audit, Identity, Asset or object-storage module — no insert, update, delete, audit row, outbox event, normalization request or Session mutation, and **no migration** (34 unchanged). New gate `node tools/check-app3-b05.mjs` with **61** checker tests; the five predecessor gates whose APP3-shaped path allow-lists would have refused a public Template route now consult one shared authority, and the three that banned the route outright assert it in **both** directions instead. `APP3-B05A` = `READY — NOT STARTED`. **`APP3-D01` then delivers the phase-level Figma design package** into a new `APP_03` page (`592:3`): one section (`596:3`) holding 19 nested sections and **66 frames**, covering Admin `A01`–`A04` (placement authoring, Template list, Template editor, publish/unpublish/archive) and Studio `S01`–`S11` (shell and picker, stage, transforms, layers, text, customer image, zoom/safe area, undo/redo, watermark, autosave/conflict/resume, mobile touch), across Admin desktop/mobile and Studio desktop/tablet/mobile. Every frame is built from the accepted `DS – Core Components (WF06)` variables and text styles — no second visual language, and no token derived from a wireframe. Backend dependency is annotated per capability, and the two capabilities whose backend has **not** run are drawn disabled with the reason on the frame: restore (`APP3-B04A`) and Template preview media (`APP3-B05A`); customer image delivery names `APP3-B06C` and the still-open `FU-APP3-TEMPLATE-SOURCE-ASSET-INTAKE-01`. No endpoint is invented and no temporary asset URL is fabricated. All 66 registry rows enter as `REVIEW_REQUIRED` — `APP3-D01` does not self-approve — so `APP3-A01`/`A02`/`A03` become eligible **conditional on** human design approval flipping those rows to `APPROVED_FOR_IMPLEMENTATION`. `AUTOSAVE_CADENCE` stays `OPEN` (it is `APP3-S10`’s), and `APP3-S12`/`S13` stay `LATER_APP3` (phase plan §6.24.7). | `FU-APP2-CLOSURE-NEXTPHASE-GUARD-01` = **`COMPLETE — CLOSED_BY_APP2-X01-C2`** — the closure gate rejected any `reports/APP3-*.md` by filename prefix, blocking two mandated post-closure governance reports; `APP2-X01-C2` replaced the prefix ban and its exact-filename allowlist with a commit-graph chronology check (`checkNextPhaseChronology`), so an APP3 report added after the accepted closure commit `8b5f3b0` passes under its **canonical `APP3-*` name** while one added before it still fails; verdict text `APP3 NOT STARTED` → `APP3 chronology valid` ([`reports/APP2-X01-C2-COMPLETION-REPORT.md`](./reports/APP2-X01-C2-COMPLETION-REPORT.md)). |
| APP4 | `COMPLETE — PASS_WITH_FOLLOW_UPS — DELIVERED_FOR_REVIEW` (closed at `APP4-X01`) | Delivered: verified Customer + primary Contact Point, secure-grant lifecycle (`SecureGrantIssuer`, no HTTP surface), public secure-link resolution, notification intake/outbox/worker with manual replay, and Admin customer/grant/notification support. **11 feature endpoints** (B07 = 4; B08 manual operation is `/replay`). `NO_APP4_MIGRATION`. Cross-layer acceptance `APP4-E01 = PASS_AFTER_R01_C1` (`R01-C1` proved the replay is claimed, opened and delivered by real W01, plus expired-link rejection). Ships the recording notification adapter only — **no external provider is selected** (`IMP-O006` → APP12). Zero blocking follow-ups, nine owned nonblocking. Matrix [`reports/APP4-CLOSURE-MATRIX.md`](./reports/APP4-CLOSURE-MATRIX.md). |
| APP5 | `COMPLETE — PASS_WITH_FOLLOW_UPS` (closed at `APP5-X01`) | Delivered: a reachable Storefront request entry (`/yeu-cau/moi`) with the catalog-XOR-customer-owned subject chooser, embedded APP4 verification, challenge-scoped attachment intake, duplicate-safe submission, confirmation and grant-scoped single-request status; a public catalog variant read; and the Admin queue, detail, private request-asset delivery, moderation notes and guarded APP5 transitions with the internal reason never reaching the customer. Sixteen canonical checkpoints, ten HTTP operations, one migration (`0035_add_app5_intake_provenance`), 65 approved Figma rows. Runtime acceptance: `APP5-E01` — six serial tests, four journeys, 44 proofs. Closure: [`reports/APP5-X01-COMPLETION-REPORT.md`](./reports/APP5-X01-COMPLETION-REPORT.md); matrix [`reports/APP5-CLOSURE-MATRIX.md`](./reports/APP5-CLOSURE-MATRIX.md); plan [`phases/APP5-CUSTOM-REQUESTS.md`](./phases/APP5-CUSTOM-REQUESTS.md). Zero blocking follow-ups; ten nonblocking, all owned. |
| APP6 | `COMPLETE — PASS_WITH_FOLLOW_UPS` (closed at `APP6-X01`) | Delivered: the versioned quotation workflow (draft → history → send → supersede), secure grant-scoped quotation acceptance and rejection, the `GRD-005` digitizing gate, versioned design review with the revision loop, secure exact-version approval producing an immutable agreement-bound Approval Snapshot, Catalog **and** Customer-Owned Product support on both branches, two Admin workbenches and two customer secure screens. **16 operations** (OpenAPI 58→72 paths / 63→79 operations / 132→167 schemas, 0 removed), **1 migration** (`0036_add_app6_cop_design_context`, 35→36), **56 Figma rows** all `APPROVED_FOR_IMPLEMENTATION` under `FIG-APPROVAL-APP6-D01-PO-001`. Cross-layer acceptance `APP6-E01` = **PASS** (6 serial cases, 54 API acceptance tests; Catalog and COP both reach `APPROVED`), 0 blocking defects. **22 APP6-owned follow-ups dispositioned, 0 blocking**; `APP6_WATERMARK_RUNTIME_AUTHORITY = DELIVERED_APP3_S09`; `FU-APP6-E01-DESIGN-CASE-ORIGIN-01` = `CLOSED_FALSE_POSITIVE` (delivered owner `APP5-B01` / `TR-LC11-01`). APP6 ends at `APPROVED` and creates no order, deposit obligation or deposit attempt — that boundary is APP7's and is proved by `E01-06`. Plan: [`phases/APP6-DESIGN-REVIEW-AND-QUOTATION.md`](./phases/APP6-DESIGN-REVIEW-AND-QUOTATION.md); report: [`reports/APP6-X01-COMPLETION-REPORT.md`](./reports/APP6-X01-COMPLETION-REPORT.md); matrix: [`reports/APP6-CLOSURE-MATRIX.md`](./reports/APP6-CLOSURE-MATRIX.md). |
| APP7 | `COMPLETE — PASS_WITH_FOLLOW_UPS` (closed at `APP7-X01`) | **Delivered:** Admin SKU authoring that makes the Catalog `order_items` branch reachable at all; worker conversion of `design.approved` into exactly one Order at `AWAITING_DEPOSIT` with frozen items priced from the exact ACCEPTED quotation version and two distinct `DEPOSIT` / `REMAINING` obligations; the Admin order queue and detail; the customer secure deposit surface with exact bank instructions, a server-generated reference and a dynamic downloadable QR; an optional transfer-evidence lane that is `SUPPORTING_ONLY` and never decides payment; Admin exact manual verification and `REQUIRES_REVIEW` reconciliation; and Admin private evidence delivery. **13 operations** (OpenAPI 72→85 paths / 79→92 operations / 167→191 schemas, 0 removed), **1 migration** (`0037_add_app7_transfer_evidence_association`, 36→37; table `payment_transfer_evidence`, 78→79), **39 Figma rows** all `APPROVED_FOR_IMPLEMENTATION` under `FIG-APPROVAL-APP7-D01-PO-001`. Cross-layer acceptance `APP7-E01` = **COMPLETE** — 6/6 automated cases PASS, browser acceptance PASS, and the Product Owner's **real banking-application QR scan PASS** (bank/account, exact amount and exact transfer reference all pre-filled correctly; `TRANSFER_SUBMITTED = false`) — **0 blocking defects**. `PAYMENT_MVP = MANUAL_BANK_TRANSFER` (IMP-D052 / `PO-APP7-001`): no provider, no webhook, no automatic bank reconciliation, and `IMP-O007` stays `OPEN — DEFERRED_PROVIDER_INTEGRATION`, nonblocking. **All APP7 follow-ups classified, 0 blocking**; `FU-APP7-E01-DEV-COMPOSE-MERCHANT-01` = `CLOSED_RESOLVED_BY_APP7-E01-U01` and `FU-APP7-E01-BANK-SCAN-ENV-01` = `CLOSED_PASS`. APP7 publishes `DepositEligibilityPort(orderId)` to APP8 and writes **0** inventory and **0** production rows; remaining-payment execution is `DEFERRED_TO_APP9`. Plan: [`phases/APP7-DEPOSIT-PAYMENT-AND-ORDER-CREATION.md`](./phases/APP7-DEPOSIT-PAYMENT-AND-ORDER-CREATION.md); report: [`reports/APP7-X01-COMPLETION-REPORT.md`](./reports/APP7-X01-COMPLETION-REPORT.md); matrix: [`reports/APP7-CLOSURE-MATRIX.md`](./reports/APP7-CLOSURE-MATRIX.md). **Phase history, recorded during delivery and left unrewritten:** phase-entry audit and roadmap reconciliation complete: audit [`audits/APP7_PHASE_ENTRY_AUDIT.md`](./audits/APP7_PHASE_ENTRY_AUDIT.md), report [`reports/APP7-R00-COMPLETION-REPORT.md`](./reports/APP7-R00-COMPLETION-REPORT.md). Baseline recomputed with **no delta** (OpenAPI 72/79/167, 36 migrations, 56 `APP_06` Figma rows, 0 APP7 operations, 0 APP7 Figma rows). Dispositions: `APP7_SCHEMA_DISPOSITION = NO_MIGRATION_REQUIRED` (**revised by `APP7-G01` §9 to `MIGRATION_REQUIRED`, evidence association only; delivered by `APP7-DB01`**); `APP7_DESIGN_GATE = DESIGN_REQUIRED_BEFORE_UI_ONLY`; `APP7_ORDER_AGGREGATE_ENTRY_STATUS = STALE_TEST_EXPECTATIONS` (`order-fixture.ts` predates `0034`'s `product_sides.code` / `embroidery_areas.code` — a test fixture, no runtime defect); `APP7_PAYMENT_PROVIDER_DISPOSITION = MANUAL_VERIFICATION_MVP` (`IMP-O007` **stays open**); `APP7_INVENTORY_DISPOSITION = DEFERRED_TO_APP8`. Key finding: `TR-LC14-01` creates the Order **on the approval event** at `AWAITING_DEPOSIT` with both obligations in one transaction — not after the deposit — enforced physically by `payment_obligations.order_id NOT NULL`; the `design.approved` (SE-005) trigger is already emitted by APP6-B11 and has no consumer. Entry blocker: no application path creates a SKU, so the Catalog `order_items` branch is unreachable until `APP7-B01` delivers Admin SKU authoring (an inherited APP2 gap; APP2 is not reopened). 15 original candidates reconciled (3 `REMOVE`, 3 `REDEFINE`, 3 `KEEP`, 2 `DEFER`, 2 `MERGE`, 1 `SPLIT`+`REORDER`, 1 `REORDER`), 3 added — 12-checkpoint roadmap in [`phases/APP7-DEPOSIT-PAYMENT-AND-ORDER-CREATION.md`](./phases/APP7-DEPOSIT-PAYMENT-AND-ORDER-CREATION.md) §7. `TRUE_PO_DECISIONS = PO-APP7-001` (deposit collection method). **`APP7-G01` = `COMPLETE`** — the Product Owner locked `PO-APP7-001 = MANUAL_BANK_TRANSFER_WITH_DYNAMIC_QR_AND_OPTIONAL_EVIDENCE` (IMP-D052, authority package [`audits/APP7_G01_DEPOSIT_PAYMENT_AUTHORITY.md`](./audits/APP7_G01_DEPOSIT_PAYMENT_AUTHORITY.md), report [`reports/APP7-G01-COMPLETION-REPORT.md`](./reports/APP7-G01-COMPLETION-REPORT.md)): one merchant account, a server-generated non-persisted dynamic transfer QR, the locked reference format `ORD<10-char order-code body><DC|RM>` (`^[A-Z0-9]{15}$`, derived never stored), the exact frozen DEPOSIT obligation amount in VND, **optional** append-only customer transfer evidence (max 5 per attempt) on a fourth delivered intake lane, Admin evidence preview, and `PAYMENT_VERIFICATION = ADMIN_MANUAL_SERVER_SIDE` — no QR, download, customer assertion or evidence upload ever changes payment state. `IMP-O007` stays **open** as a deferred additive integration and is no longer a phase blocker. `APP7_SCHEMA_DISPOSITION` revised `NO_MIGRATION_REQUIRED` → **`MIGRATION_REQUIRED` for the evidence association alone**: `ADR-DB4-003` forbids a generic link table and CTX-PAY owns none, so `payment_transfer_evidence` is routed to a new `APP7-DB01`. Roadmap **12 → 15 checkpoints** (`APP7-DB01`, `APP7-B05` customer evidence upload, `APP7-B06` Admin evidence delivery), ~13 operations, backend slices 2/3/2/3/1. **`APP7-B01` = `COMPLETE`** — Admin SKU authoring delivered, the inherited APP2 gap that left the Catalog `order_items` branch unreachable (APP2 is not reopened). Exactly **two** operations, Catalog-owned: `POST /api/admin/products/{productId}/variants/{variantId}/skus` (`adminSku_create`) and `PATCH /api/admin/skus/{skuId}` (`adminSku_update`), both behind `AuthenticatedAdminGuard` + `StaffOriginGuard` + `StaffJsonBodyGuard`. Order-eligible is exactly `skus.is_active` (COL-TBL014-05, the "sellable flag"), the ACTIVE terminology `APP7-R00`/`G01` use; a variant may carry **at most one**, zero stays legal and reachable, and no SKU is ever nominated by recency, id order or any other tie-break — an ambiguous set is simply never allowed to exist. `skus.product_variant_id` has no uniqueness constraint, so the **owning `product_variants` row locked `FOR UPDATE`** is the concurrency arbiter (the product is locked `FOR SHARE` for its lifecycle state): every mutation locks, proves the hierarchy from locked rows, re-reads the variant's whole SKU set, writes, re-evaluates and refuses an ambiguous result by rolling the transaction back. Duplicate codes stay `CST-012`'s to decide — `23505` is mapped to `SKU_CODE_CONFLICT` with no SQLSTATE, constraint name or driver text reaching the client. SKU authoring is allowed on `DRAFT` **and** `PUBLISHED` (a Catalog variant only reaches an APP6 approval while published; a `DRAFT`-only rule would make the target state unreachable) and refused on `ARCHIVED`; the LC-04 publication lifecycle is untouched. Ownership is immutable: no body field can rebind a SKU to another variant. `SCHEMA_CHANGE = NONE`, `MIGRATION_CHANGE = NONE` — `APP7-DB01` is the evidence association and was not pulled forward. OpenAPI 72/79/167 → **74/81/170** (+2 paths, +2 operations, +3 schemas: `CreateSkuBody`, `UpdateSkuBody`, `AdminSkuResponse`; no existing path, operation or schema changed). Report [`reports/APP7-B01-COMPLETION-REPORT.md`](./reports/APP7-B01-COMPLETION-REPORT.md). **`APP7-B01-C1` = `COMPLETE`** — review found one defect, `INVENTED_SKU_CODE_CHARACTER_POLICY`: B01 narrowed `skus.code` to an ASCII alphabet that no accepted source establishes. The column is `text NOT NULL` with no CHECK, filed by `ADR-DB5-002` R1 under Population A and compared **bytewise** under `C` (R2) with global uniqueness (CST-012) — and "bytewise comparison" is a statement about equality, not about which bytes may be stored, which is exactly what B01 conflated. R3 names the pre-normalized columns (lowercase email, E.164 phone, slugified paths) and a SKU code is not among them. The consequence was that a legal Vietnamese code such as `ÁO-THUN-ĐEN-M` was refused by the API and by every generated client. The regex is **removed and deliberately not replaced** — no second alphabet, no `format`, no enum, no normalization; the only bounds left are payload limits documented as such. Everything else B01 delivered is frozen and untouched: the 2 operations, the eligibility invariant, the variant-row-lock arbiter, CST-012 as duplicate arbiter, the guard chain, and no schema or migration. OpenAPI stays 74/81/170 — the entire artifact delta is **2 deleted `pattern` lines** (one per body schema) and the client delta 2 deleted `@pattern` annotations. The race suite was **not** rerun: no concurrency or eligibility input changed. Report [`reports/APP7-B01-C1-CORRECTION-REPORT.md`](./reports/APP7-B01-C1-CORRECTION-REPORT.md). **`APP7-B01-FD1` = `COMPLETE`, and `APP7-B01-C1` is `SUPERSEDED_BY_FINAL_AUTHORITY_REPAIR`** — C1 produced the right audit (`max length = NONE`, `nonblank = NONE`) and then kept `min(1)`/`max(64)` anyway, defending them as payload bounds. The Product Owner ruled that a field-specific rule rejecting an otherwise authority-valid database value is a domain/API contract constraint whatever the comment calls it, and that transport abuse belongs to the delivered body-size controls rather than to a SKU-shaped number. Confirmed against physical truth before repairing: `0007_create_catalog_tables.sql:56-69` declares `"code" text NOT NULL` with `uq_skus__code` and **no CHECK on `code`** — its three CHECKs are the two money columns' — and the later migrations mentioning `skus` add only foreign keys to `skus.id`; the service and Drizzle adapter pass the code through with no rule of their own, so no second runtime layer existed. The SKU-code request schema is now **`z.string()`** and `SKU_CODE_MAX_LENGTH` is **deleted**, not re-tuned; published `code` is `{"type":"string"}` with no `pattern`, `format`, `enum`, `minLength` or `maxLength`, and the generated client carries no annotation on it (`priceOverrideAmount` keeps its `@pattern ^\d{1,12}$`, which `ck_skus__currency_scale` actually enforces). Empty, whitespace-only and 300-character codes are accepted and stored byte-for-byte; nothing trims, folds or normalizes. Surface unchanged at 74/81/170 — the whole artifact delta is 4 deleted lines. No schema, migration, concurrency, guard, audit or eligibility change, and the race, hierarchy and lifecycle suites were **not** rerun. `APP7-B01-C2` = `MUST_NOT_BE_CREATED`. Report [`reports/APP7-B01-FD1-FINAL-REPORT.md`](./reports/APP7-B01-FD1-FINAL-REPORT.md). **`APP7-W01` = `COMPLETE`** — the `design.approved` (SE-005) consumer APP6-B11 has had none of since it shipped; every approval had left a `PENDING` outbox row nothing claimed (`APP7-R00` §3.2). Delivered as the **fourth handler on the delivered Outbox/worker runtime** — same registry, same `WorkerJobQueueRepository` claim, same lease, same `background_job_attempts` ledger, same completion seam. **`HTTP_OPERATIONS_DELTA = 0`**: OpenAPI stays 74/81/170 (`sha256 44faf1fb…b549a80`), the generated client is untouched, and no route, decorator or request schema exists in the checkpoint. No queue, scheduler, poller, claim framework or Order-create HTTP route was created; no schema and no migration (36, unchanged). One business transaction produces `orders(status = AWAITING_DEPOSIT)` + frozen `order_items` + **both** `payment_obligations` (INV-04: `DEPOSIT` and `REMAINING`) + the canonical `order.created` (SE-006), all or nothing — proved by a refusal that leaves 0 orders, 0 items, 0 obligations, 0 events and no idempotency record. **No `PaymentAttempt`, no `DEPOSIT_PAID`, no bank/QR/reference/evidence/verification, no inventory or production row.** Authority is re-read from persisted rows, never taken from the event: the payload is reduced to `(approvalSnapshotId, customRequestId)` and everything else is discarded at the parse boundary. GRD-009 / G-DB7-05 is re-evaluated inside the conversion transaction with `OrderChainGuard`'s own refusal codes (restated in the worker's SQL repository because `apps/api` may not be imported — the delivered `APP4-W01` port precedent); a foreign Approval Snapshot paired with a real accepted quotation is refused with **zero** orders on either request. The **exact** `ACCEPTED` quotation version supplies the money — resolved by status from the request, never `current_quotation_id`, `current_version_id`, `MAX(version)` or "latest"; **zero and several are two different refusals and neither is settled by picking**. Money is copied as `numeric(14,2)` strings and never recomputed: the acceptance fixture prices a 35 % deposit (1,166,667 of 3,333,333 VND, the DB4 round-half-up of 1,166,666.55), so a hard-coded BR-005 40 % or a re-round is caught by assertion. Frozen Approval Snapshot evidence supplies branch, subject, display copy and quantity — proved by renaming the live `products` row after approval and watching the order still freeze the snapshot's copy. The **one** permitted live read is the foreign identity `order_items` demands and no snapshot column holds: a Catalog line resolves exactly one `is_active` SKU for the frozen variant — **0 refuses, >1 refuses**, and no SKU is created, synthesised, activated or chosen by id, code or recency (inactive SKUs are never counted); a COP line names the exact frozen `customer_owned_product_id` with `sku_id` NULL and fabricates no Product, SKU, Variant, Side or Area. `order.create` claims `(request, approval snapshot)` and stores a replayable result; **`uq_orders__request` (CST-030) is the final arbiter**, and CC-11 is proved with **two separately compiled `WorkerModule` instances on independent pools** against one database (queue-claimed and use-case-entered, plus 4 repeats) always converging on exactly one order, one item set, two obligations and one `order.created`. Duplicate delivery of two `design.approved` rows for one approval converts once and dead-letters neither: the effect key is the **approval**, not the outbox row. Failure classification uses the delivered closed taxonomy — a frozen-chain refusal is `JOB_INVARIANT_VIOLATION` and terminal (a variant with two active SKUs reaches an operator's dead-letter query instead of retrying forever), a held claim is transient; **the approval always stands** and nothing revokes or annotates it. Two inherited follow-ups closed: `FU-APP6-B03-ORDER-AGGREGATE-SUITE-RED-01` — the DB7-era `order-fixture.ts` predated `0034`'s `NOT NULL` `product_sides.code` / `embroidery_areas.code`; the two stale inserts were repaired with legal deterministic codes and **all three AGG-15 suites are green (36/36)**, no runtime change; and `FU-APP6-B01-CODE-GENERATOR-PROMOTION-01` — the `REQ-`/`QUO-`/`ORD-` mechanism is promoted once to `@embroidery/domain-types` with `REQ` and `QUO` behaviour byte-identical and no second alphabet (**IMP-D053**, which also records `BACKGROUND_JOB_KINDS += ORDER_CREATION`, a write-time guard with no CHECK and no migration, on the IMP-D030 precedent). Report [`reports/APP7-W01-COMPLETION-REPORT.md`](./reports/APP7-W01-COMPLETION-REPORT.md). **`APP7-W01-C1` = `COMPLETE`, and `APP7-W01` is `COMPLETE — CORRECTED (C1)`** — PO review found one architectural defect, `W01_DUPLICATED_ORDER_PERSISTENCE_AUTHORITY`: W01 had re-implemented canonical DB7 authority inside the worker — a second GRD-009 (`WorkerOrderChainGuard`), a second `orders`/`order_items` writer, a second `payment_obligations` writer and a second `order.created` append — because the worker may not import `apps/api`. `APP7-R00` had already ruled the opposite: *"`OrderRepository.createFromAcceptedQuotation` already carries GRD-009, G-DB7-05 and the SE-006 append; APP7 does not re-implement that."* The app boundary was real; the conclusion was wrong. **A runtime boundary is a reason to share authority, not to copy it** — every behavioural suite passed, which is exactly the danger: a second chain check does not fail a test, it fails years later when one copy is corrected and the other is not, and an order pairs one customer's approved artwork with another's accepted price while every foreign key stays satisfied (INV-19). The delivered DB7 implementations moved **unchanged** into `@embroidery/persistence` — `src/order/` (`OrderChainGuard`, `DrizzleOrderRepository`, `DrizzleOrderShippingRepository`, row mapper, transitions, contract, `ordering-identity`, `OrderPersistenceModule`) and `src/payment/` (`DrizzlePaymentObligationRepository`, `PaymentEvidenceRepository`, row mapper, deposit-eligibility port + adapter, contract, `PaymentPersistenceModule`) — with only import paths edited: no statement, guard, refusal code, mapping or transaction annotation changed. AGG-16 moved on the same rule (`C1` §6 permits a worker-local seam only for facts the canonical repositories do not already own, and `createForOrder` owns obligation creation). Both applications now import the **same Nest modules** and resolve the **same Symbol tokens** — asserted, not assumed — with **zero app-to-app edges and zero new workspace dependencies**; the shared home is the existing persistence package rather than a new one, since it already carries the DB7 runtime, `drizzle-orm`/`pg`, a Nest module and both apps as consumers. Three thin re-exports at the delivered contract paths keep ~10 consumers (3 AGG-15 suites, 2 payment suites, 3 DB9 benchmarks, the DB10 durability suite, the Inventory eligibility guard) importing exactly what they always did. `WorkerOrderChainGuard` is **deleted** — not wrapped, renamed or reduced to a helper — and the worker's remaining seam is reads-only, renamed `ConversionAuthorityRepository`, having also shed `findOrderByRequest`, the snapshot's now-unused `customerId` and the chain-only refusal `QUOTATION_BELONGS_TO_ANOTHER_REQUEST`. A focused structural suite (`canonical-order-authority.spec.ts`, 7 assertions, comments stripped, production source only) pins single-token identity and forbids recurrence, and was **verified to bite**: a probe file declaring a chain guard, an `INSERT INTO orders`, an `order.created` append and the guard's refusal code failed 4 of 7 and was then deleted. Everything accepted in W01 is unchanged and re-proved through the canonical path — worker conversion **64**, the three DB7 Order suites **36**, payment persistence **29**, CC-11 still one order / one item set / two obligations / one `order.created` on two independent pools. `FU-APP7-W01-ORDER-CHAIN-GUARD-DUPLICATION-01` = `CLOSED_BY_APP7_W01_C1` (it was not debt — it was the defect); `APP7-W01-C2` = `MUST_NOT_BE_CREATED`. OpenAPI unchanged at 74/81/170 (`44faf1fb…`), no client, schema or migration change. Report [`reports/APP7-W01-C1-CORRECTION-REPORT.md`](./reports/APP7-W01-C1-CORRECTION-REPORT.md). **`APP7-B02` = `COMPLETE`** — Admin order read, 2 operations: `adminOrder_list` (`GET /api/admin/orders`) and `adminOrder_detail` (`GET /api/admin/orders/{orderId}`), both behind the delivered `AuthenticatedAdminGuard`, both reads, and there is no third operation anywhere under the prefix. The whole checkpoint is one rule made structural: **an order is reported as `APP7-W01` froze it, never reconstructed from live state.** The Drizzle read adapter destructures exactly two tables from the schema — `orders` and `order_items` — and contains no `products`, `product_variants`, `product_sides`, `embroidery_areas`, `skus`, `quotations`, `quotation_versions`, `design_cases` or `customers` reference, no join, no `sum()`, no `count()` and no arithmetic, so `product_name`, `variant_label`, `size_label`, `unit_price_amount`, `line_total_amount` and `total_amount` are transported byte-for-byte and nothing recomputes `unit × quantity`, `sum(lines)`, a deposit percentage or a current SKU price. Proved behaviourally rather than by inspection: two real-database regressions mutate live Catalog **after** the order exists — one renames the `products` row the snapshot froze its copy from, the other reprices the SKU to 999999.00, deactivates it and rewrites the product base price to 1.00 — and assert the **entire** detail response is identical to the pre-mutation one, so a projection reading a live row for any single value fails them; a third proves the order total (2550000.00, the accepted version's total including its 50000.00 shipping fee) is **not** the line sum (2500000.00), which a re-summing projection would report. The queue is an opaque keyset page reusing the delivered `encodeCursor`/`decodeCursor`/`buildPage`/`resolveLimit` codec on `ix_orders__status_created_id` (IDX-074) — `(created_at DESC, id DESC)`, bounded 1..100 with a default of 20, over-fetched by one, no OFFSET and no COUNT — with the descending `id` tie-break proved across three orders sharing one instant (page 1 + 2 + 3 yield five distinct ids in the same order as a single page) and a malformed cursor refused as `ORDER_CURSOR_INVALID` rather than silently restarting at page one. Exactly **one** filter, `status`, justified by IDX-074's leading column; **no default triage subset was invented** — LC-14 defines none, so an omitted `status` means every state and no `CANCELLED` or `ON_HOLD` order is silently hidden — and no product, SKU, category, payment, evidence, inventory or production filter was added, with the closed key set asserted directly. Detail returns lines in frozen `position` order (inserted 3, 1, 2; returned 1, 2, 3) with the Catalog/COP branch derived **response-only** from the stored XOR under `ck_order_items__exactly_one_subject` — no column, enum or constraint was added for it, and it reuses the `CATALOG` / `CUSTOMER_OWNED` vocabulary `APP5-B04` already publishes. A COP line names its `customer_owned_product_id`, omits `skuId`, `variantLabel` and `sizeLabel` as **absent keys**, and the serialized response contains neither the fixture's SKU id nor its Product id; the COP fixture is a genuine customer-owned chain (a `customer_owned_products` row plus design-version and approval-snapshot rows on the COP branch of both `exactly_one_placement_branch` CHECKs), not a catalog one relabelled. `sizeLabel` is absent on every line and that is the truthful answer, not an omission: `approval_snapshots` has no size column and the only live source is `product_variants`, which B02 may not read. **`APP7-W01-C1` is untouched** — no file under `packages/persistence/src/order/` was modified, and `AdminOrderModule` imports `DatabaseModule` and `IdentityModule` and nothing else: not `OrderModule` (so `ORDER_REPOSITORY` and the one canonical `OrderChainGuard` are unreachable from both routes, the boundary `CustomRequestAdminModule` established), not `CatalogModule` and not `CustomerModule` (so live-state re-read is impossible by accident rather than merely forbidden). The read seam is three query methods with no create, transition, chain check, outbox append, lock or transaction. No payment attempt, obligation, transfer reference, evidence, provider or reconciliation field is published — `APP7-B04` owns that surface, and the contract suite asserts no property name contains any of them, while a runtime test repeats the check against a real `DEPOSIT_PAID` order — though the order's own `DEPOSIT_PAID` **state** is reported, because that is order state. `SCHEMA_CHANGE = NONE`, `MIGRATION_CHANGE = NONE`, no index added and none found missing. OpenAPI 74/81/170 → **76/83/174**: +2 paths, +2 operations, +4 schemas (`AdminOrderQueueResponse`, `AdminOrderQueueItemResponse`, `AdminOrderDetailResponse`, `AdminOrderItemResponse`), **600 inserted lines and 0 deleted** — no unrelated churn is representable in a diff with no deletions — and the generated client gains `adminOrderList` and `adminOrderDetail` (+193 lines, 0 deleted, tree hash `19e85e84…433044`). Both operation ids come from the repository's own `<domain>_<method>` policy with no `CONTROLLER_DOMAIN_KEYS` entry and no hand-written `operationId`. Focused evidence only — 5 suites / 48 tests (3 Docker-free: subject projection, request schemas, published contract; 2 real-database HTTP: queue and detail) — and `BROAD_REGRESSION = NOT_RUN_BY_DESIGN`, since B02 changed no input to the Order suites, the W01 worker, CC-11, the payment suites or any migration. Report [`reports/APP7-B02-COMPLETION-REPORT.md`](./reports/APP7-B02-COMPLETION-REPORT.md). **`APP7-B03` = `COMPLETE`** — the customer secure deposit surface, exactly **3** operations and all of them `POST` on one published `publicOrderDeposit` domain: `publicOrderDeposit_current` (`/api/public/orders/deposit`), `publicOrderDeposit_initiate` (`/api/public/orders/deposit/attempts`) and `publicOrderDeposit_qr` (`/api/public/orders/deposit/qr`). The verb and the absence of a locator are the same delivered rule twice over: `ADR-APP4-001` §11 makes the URL fragment the only browser carrier for a secure token and forbids a path or query carrier with no fallback, and a public route taking an order id would be an enumeration oracle for other customers' orders — so the binary QR is a `POST` returning `image/png` with `Content-Disposition: attachment` and a fixed filename, and the order is reached from the grant through `uq_orders__request`, never from the caller. The whole checkpoint is one rule made **structural**: nothing a customer can do moves money's state, and the write module injects no `ORDER_REPOSITORY`, no outbox store, no audit repository, no Admin guard and no provider client, so `SUCCEEDED`, `SATISFIED`, `DEPOSIT_PAID`, a reconciliation row and a provider event are unreachable from the injector rather than merely unwritten. The deposit amount and currency are the **DEPOSIT obligation's own frozen columns**, copied — no 40 % share is recomputed and no quotation repository or deposit-policy reader is reachable from either module — while the REMAINING obligation is never read, never projected, never made payable and byte-identical after an initiation. `APP7-G01` §4's reference format is used verbatim (`ORD` + the ten-character order-code body + `DC`, fifteen uppercase alphanumeric characters), derived on every read from `(order code, obligation kind)` and persisted in no column; the deriving function takes one argument and has **no kind parameter**, so no APP7 caller can ask for APP9's reserved `RM`. Initiation is one transaction — the grant re-authorized under its row lock (ADR-DB3-004 r9), the obligation required to be `PENDING`, `payment.initiate` claimed on `(obligation, caller attempt key)` with the raw `Idempotency-Key` hashed into the scope key and never stored, then GRD-003's step-up **derived from the grant's customer** (no challenge id is accepted anywhere, and naming one is a 400), then the canonical AGG-16 `openAttempt`, which re-reads the obligation `FOR UPDATE` and is the real arbiter. The claim precedes the step-up on `quotation.accept`'s recorded precedent and for its reason: a replay writes nothing, and gating it on a still-open window would tell a customer retrying after a dropped response to re-verify in order to be shown an attempt they already opened. All three request bodies carry **only `token`** and are `.strict()` — `amount`, `currency`, `method`, `providerKey`, `providerRef`, `stepUpChallengeId`, `grantId`, `orderId` and `paymentObligationId` have nowhere to be sent — and the attempt is written from server facts alone with both provider columns `NULL`. LC-16 is preserved by having no mutation path at all: the same key replays one attempt, a new key opens a second while the first stays `PENDING`, an attempt forced to `FAILED` is never reset, and a `SATISFIED` deposit refuses a new one with `DEPOSIT_NOT_PAYABLE`. **No active-attempt uniqueness was invented** — accepted payment authority has none, and the two arbiters that exist (the claim, and the obligation state predicate under the row lock) are the two used. The QR is generated locally and stored nowhere: an EMVCo/NAPAS `QRIBFTTA` payload built by repository-owned code including CRC-16/CCITT-FALSE, drawn by `qrcode@1.5.4` (MIT) whose entire package contains no `http`/`https`/`net`/`dns`/`tls`/`fetch`/`XMLHttpRequest` — the property `APP7-G01` §6 requires, because a hosted generator would put the merchant account number and the deposit amount into a third party's request log, which is why remote QR services are excluded by rule and not by preference. Conformance is proved **independently of the encoder**: every field is re-parsed by a TLV reader written from the grammar, the checksum is verified against the standard's own published vector (`"123456789"` yields `0x29B1`) and a one-character tamper is shown to fail it, and the delivered PNG is decoded back through `pngjs` + `jsQR` to a byte-identical payload — in a unit test and again over real HTTP. Tags 59/60 are deliberately omitted (for this service code the beneficiary is already identified inside tag 38, and the interoperable payloads Vietnamese banking apps accept omit both); the account-holder name still reaches the customer in the written instructions. `REAL_BANK_APP_SCAN = NOT_EXECUTABLE_IN_AUTOMATED_ENVIRONMENT` and is kept as `APP7-E01` item E01-12 rather than simulated. Both reads are zero-write, proved against a full before/after snapshot of order status, both obligations, every attempt and the reconciliation / provider-event / refund counts — `updated_at` included, so nothing moves merely because a QR was viewed — and the grant is not consumed. The duplicate-initiation race is real: two concurrent HTTP requests on **separate pool connections and separate transactions** leave exactly one `PENDING` attempt, unique completed claims, and the obligation and order untouched; the loser may replay or answer `DUPLICATE_OPERATION`, both accepted, neither creating a second attempt. Non-enumeration holds: an unusable token, a request whose order has not been created, and an order with no live DEPOSIT obligation all answer with the identical `404 / SECURE_LINK_UNAVAILABLE` — same status, code, message and shape — and the "foreign order" case is unreachable by construction, since no body carries an order identifier. The merchant account is a module-scoped fail-fast provider over `APP7-G01` §3's four exact variable names, and a test asserts **none of them matches** the `CLAUDE.md` §8a protected pattern — deliberately, because every one is printed on the customer's screen and encoded into the QR they scan, so naming one as a secret would falsely mark a customer-visible fact as a credential; no value is interpolated into any error message, `.env.example` carries structural placeholders only, **no write to `.env` was made** and no credential was read or rotated. `APP7-W01-C1` is untouched — no file under `packages/persistence/` was modified and **no second payment writer exists**; the single new persistence seam is a read-only Ordering port returning three columns, on the `CustomRequestQuotationPointerPort` precedent, so the deposit surface reaches the order without acquiring `ORDER_REPOSITORY` and its `transition()`. No evidence capability (`APP7-DB01`/`B05`/`B06`), no Admin verification (`APP7-B04`), no provider or webhook — `IMP-O007` stays **open** and `payment_provider_events` stays empty. `SCHEMA_CHANGE = NONE`, `MIGRATION_CHANGE = NONE` (36 migrations, unchanged). OpenAPI 76/83/174 to **79/86/180** (+3 paths, +3 operations, +6 schemas — `ReadDepositBody`, `InitiateDepositAttemptBody`, `DepositQrBody`, `CustomerDepositResponse`, `DepositBankInstructionsResponse`, `DepositAttemptResponse`; **816 inserted lines and 0 deleted**, so no unrelated churn is representable; `sha256 8c544826…35e27600`), and the generated client gains all three truthfully with the QR typed `Blob` (+221 lines, 0 deleted, tree hash `48ee591e…c99a224e6`). Focused evidence only — 7 suites / 145 tests (5 Docker-free: the reference, the QR payload standards proof, the optical decode, the merchant config, the published contract; 2 real-database HTTP: read/QR and initiation including the race) — and `BROAD_REGRESSION = NOT_RUN_BY_DESIGN`, since B03 changed no file under `packages/persistence/`, no migration, no worker code and no Admin surface. Report [`reports/APP7-B03-COMPLETION-REPORT.md`](./reports/APP7-B03-COMPLETION-REPORT.md). `APP7-DB01` then added the one schema fact `APP7-G01` §9 proved missing: `payment_transfer_evidence` (TBL-079, REL-109 ×2, migration `0037`), the CTX-PAY typed asset association, with exactly `payment_attempt_id` and `asset_id` — both `NOT NULL`, both `ON DELETE RESTRICT` — plus the convention `id`/`created_at` and `UNIQUE (payment_attempt_id, asset_id)`. No `role`, no `grant_id`/`step_up_challenge_id` (`payment_attempts` already holds both), no `submitted_at` (`created_at` **is** it), no `updated_at` (append-only), no media/storage metadata, no JSONB, and **no five-per-attempt CHECK or trigger** — a CHECK cannot count sibling rows, so the bound stays an application guard under the attempt row lock and the suite proves the database accepts a **sixth** row. Both `restrict` edges are proved by deleting the parent for real, and the 0036 → 0037 upgrade is executed against the committed baseline with a pre-seeded payment chain: `backfill = none`, every historical row unchanged. Live baseline 78 → **79** tables / 849 → **853** columns / 165 → **167** FK / 52 → **53** UNIQUE / 215 → **217** indexes (both constraint-created, no `IDX-*` slot), CHECK 204 and triggers 34 unchanged; fingerprint rebased to `56294cd8…` and gated on a third fresh database. `SCHEMA_CHANGE = payment_transfer_evidence only`, `OPENAPI_CHANGE = NONE` (79/86/180), `RUNTIME_CHANGE = NONE`; recorded as **IMP-D055**. Report [`reports/APP7-DB01-COMPLETION-REPORT.md`](./reports/APP7-DB01-COMPLETION-REPORT.md). `APP7-B05` then delivered the customer transfer-evidence capability on that table: **2** operations, both `POST` and both reached only through the APP5 `REQUEST_ACCESS` grant — `publicOrderDepositEvidence_upload` (`multipart/form-data`) and `publicOrderDepositEvidence_status`. The fourth `AssetIntakeLane` (`CUSTOMER_UPLOAD`/`CUSTOMER_PRIVATE`, JPEG/PNG/WebP, 10 MiB, namespace `public.order.deposit-evidence.upload`) reuses the delivered pipeline whole — Busboy parser, single-pass count/hash/signature check, `normalizeFilename`, server-derived object key, `IdempotencyAllocationStore`, `AssetRepository`, `ObjectStoragePort`, `OutboxEventStore`, Tx A / stream / Tx B — so no second upload or storage framework exists, and no presign, browser storage credential or "complete upload" call was added. Because `APP7-B03` defined no current-attempt selector, the attempt travels as an **opaque locator that is never authorization**: the server re-authorizes the grant under its row lock, walks request → order → live `DEPOSIT` obligation, locks the named attempt `FOR UPDATE`, and re-proves its obligation, that obligation’s kind and order, and `method = BANK_TRANSFER` — no `latest`/`newest`/`created_at` heuristic exists anywhere, and a foreign attempt id is byte-identical to a fictional one (`404 SECURE_LINK_UNAVAILABLE`, proved by comparing both responses). The attempt’s **own** `step_up_challenge_id` is re-verified and never re-issued or caller-supplied; no freshness window is re-applied, because `APP7-G01` §7.2 states the temporal bound as the attempt *state* and evidence changes no payment state (§7.6). `MAX_EVIDENCE_PER_ATTEMPT = 5` is an application guard under the attempt row lock inside Tx B, proved by `0→1`, `4→5`, a refused sixth, independent per-attempt quotas and a **real** `4 → 5` race of two concurrent HTTP requests on separate connections ending at exactly five. The association is bound in Tx B while the asset is still pre-inspection (`APP7-G01` §7.4’s deliberate divergence) beside exactly one `asset.inspection.requested` event; a replay writes one asset, one association and one dispatch. Append-only throughout: no delete, replace, detach or customer binary read exists in the contract or in `PaymentTransferEvidenceRepository`, the one canonical association writer, added to `@embroidery/persistence` so `APP7-B06` reuses it. Payment truth is snapshot-compared before and after every operation and is unchanged, including for a `REJECTED` image and a `REQUIRES_REVIEW` attempt. Two shared contracts widened minimally: `AssetIntakeLane` gained `credentialFields` (the secure-link token is a body-only carrier under `ADR-APP4-001` §11, so it must arrive as a multipart field before the file) with the three shipped lanes declaring `[]` and behaving identically; and the credential publishes as **`accessToken`**, because the artifact’s deterministic key sort makes the generated client append parts alphabetically and `token` would have been sent after the file part — a contract no generated client could use. `SCHEMA_CHANGE = NONE` (37 migrations / 79 tables untouched); OpenAPI 79/86/180 → **81/88/184** (632 insertions, **0 deletions**, `sha256 b2ab63ae…1a7c29160`); client +158 lines (tree hash `0d7fd92c…dfb1a78d`). Focused evidence only — 9 suites / 336 tests, including two real-PostgreSQL **and real-MinIO** HTTP suites — and `BROAD_REGRESSION = NOT_RUN_BY_DESIGN`. Report [`reports/APP7-B05-COMPLETION-REPORT.md`](./reports/APP7-B05-COMPLETION-REPORT.md). `APP7-B04` then delivered Admin manual verification — **3** operations (`adminOrderPayment_read`, `adminPaymentAttempt_verify`, `adminPaymentAttempt_review`), the first and only APP7 path with authority to move money state. One transaction proves attempt → `DEPOSIT` obligation → order under the attempt row lock, compares the operator observed amount and memo against `payment_obligations.amount` and `depositTransferReference(orders.code)` as exact `bigint` hundredths (no `numeric` value ever becomes a JS `number`), then settles `SUCCEEDED` → `SATISFIED` → `DEPOSIT_PAID` with one reconciliation, one audit row and one `payment.verified` (SE-007) — all through the shared `@embroidery/persistence` writers, with no API-local payment or order SQL. A mismatch is a committed `REQUIRES_REVIEW` (LC-16 `TR-LC16-05`), never a silent success and never a bare refusal; `REQUIRES_REVIEW → SUCCEEDED` resolution is `TR-LC16-06`, recorded as `RESOLVE_REVIEW`. Idempotency is the **state** — `APP7-G01` §10 declined a fabricated namespace for a provider-less flow — so a lost-response retry converges with `replayed: true` and writes nothing. Two concurrent-HTTP races (same attempt, and CC-10 across two eligible attempts) each apply exactly once, repeated 5× and 3×; a mid-transaction failure injected only through a testing-module override leaves no partial state. Verification succeeds with **zero** evidence; evidence stays supporting-only, is read association-first, and **no byte is served** (`APP7-B06`). `payment_provider_events` = 0 on every path, `IMP-O007` **OPEN**, `SCHEMA_CHANGE = NONE`. OpenAPI 81/88/184 → **84/91/191** (+3/+3/+7; 0 removed, 0 existing changed), client +313 lines, 0 deletions. Focused evidence: 5 suites / 77 tests plus 29 shared-persistence and 74 sibling-contract tests. Report [`reports/APP7-B04-COMPLETION-REPORT.md`](./reports/APP7-B04-COMPLETION-REPORT.md). **`APP7-B04-C1`** then corrected the one contract defect PO review found: the verify body validated `observedTransferReference` against `DEPOSIT_REFERENCE_PATTERN` — the shape of the reference the *server derives* — so a customer memo in lowercase, missing a character or carrying punctuation produced a `400` and the contradiction manual reconciliation exists to record was destroyed at the DTO boundary. The observed memo now carries a size ceiling and nothing else (`COL-TBL057-08` is nullable `text` with no CHECK, length or character set, so no bound existed to preserve and none was invented); no pattern, no `.trim()`, no uppercasing, no normalization. The expected reference and the exact string comparison are untouched, so `ord…dc` against `ORD…DC` is still **not** success — it is now a recorded `REQUIRES_REVIEW` whose `bank_reference` is byte-identical to what the bank returned. C1 also removed the 5×/3× race repetition loops B04 shipped, a validation-discipline defect: the suite is now one same-attempt race, one CC-10 race and one rollback proof, run once. **No runtime file changed** — no use case, repository, module, controller or recorder — and OpenAPI stays 84/91/191 with a 2-line diff. Report [`reports/APP7-B04-C1-CORRECTION-REPORT.md`](./reports/APP7-B04-C1-CORRECTION-REPORT.md). **`APP7-B04-FD1`** then removed the one constraint C1 had left behind: a `.max(2_000)` borrowed from the operator note beside it, while C1’s own audit had found `COL-TBL057-08` is nullable `text` with no CHECK, length or character set. The observed memo is now `z.string()` — no max, min, pattern, format, enum, trim or transform — and the rule is recorded in the schema header: a constraint is authorized by the thing being constrained, never by a neighbouring field. Source inspection proved Zod was the only narrowing layer, so one proof closes it: a 5000-character memo answers `200`, commits `REQUIRES_REVIEW` and persists byte-identically with its length asserted separately. The note/reason bound is retained and now pinned by its own test. Races were **not** rerun. OpenAPI stays 84/91/191, 2-line removal. Report [`reports/APP7-B04-FD1-FINAL-REPORT.md`](./reports/APP7-B04-FD1-FINAL-REPORT.md); `APP7-B04-C2 = MUST_NOT_BE_CREATED`. **`APP7-B06` = `COMPLETE`** — Admin private transfer-evidence delivery: **1** operation, `adminPaymentEvidence_get` (`GET /api/admin/payment-evidence/{evidenceId}/content`), the phase's only byte-serving route and the repository's third Admin binary. The locator is `payment_transfer_evidence.id` — the association id `APP7-B04` publishes instead of `asset_id`, which is the API design rather than an omission: an asset id addresses a file, an association id addresses *a file one payment attempt actually carries*, and only the second can be checked against anything. There is no `/api/admin/assets/{assetId}/content` and no customer evidence binary; the contract suite asserts the whole Admin binary inventory (three) and the whole Admin payment surface (four) by name. Authorization is six conjunctive terms in one order — association (inner-joined to `payment_attempts`, so "the association resolves one attempt" is a property of the SQL) → asset scoped to `CUSTOMER_UPLOAD`/`CUSTOMER_PRIVATE` → `ACCEPTED` **and** not tombstoned, checked separately because `recordInspection` and `tombstone` are different flows → persisted `mime_type` in the intake allowlist → complete descriptor → **only then** `getObjectStream`. Ten refusals collapse into one `404 PAYMENT_EVIDENCE_NOT_FOUND`, each proved to leave the call-counting object store at **zero**; the `401` is deliberately not collapsed into them, because an operator whose session died must be told so. The **source** is served: B05 dispatches inspection and no normalization, so there is no derivative and none is generated. Two independent authorities describe the bytes — the provider count and `assets.size_bytes` — and where they disagree the stream is destroyed and neither is sent, with no repair of the database from provider state. Provider absence and size contradiction are both `503`, never `404`: the association exists and the row says `ACCEPTED`, so a not-found would tell an operator the customer never submitted their screenshot. One test-side finding worth recording: the module refuses with `PAYMENT_EVIDENCE_UNAVAILABLE`, but the platform's `mapHttpException` replaces the code and message of **every** 5xx with the generic pair, so the wire carries the status alone — the feature code is internal vocabulary, and the first run's failed assertion was the test's, not the runtime's. Zero-write is structural, not behavioural: the module imports no `PaymentPersistenceModule`, no `AdminPaymentVerificationModule` and no `OrderModule`, so both canonical writers, the transaction manager, the outbox and `transition()` are unresolvable in its injector — confirmed by before/after snapshots of attempts, obligations, orders, assets, associations and five append-only row counts, on both the success path and a private miss. No feature-owned access log was added. The B04 handoff is end-to-end against the same booted application: `previewEligible: true` → `evidenceId` → exact bytes with B04's own `mediaType`/`byteSize`, and B04's evidence object still publishes no `assetId`; `INSPECTING` and `REJECTED` both report `previewEligible: false` and both answer the identical 404. Persistence reuses the **narrowest** seam rather than growing a second — B04's read-only `AdminPaymentReadRepository` gained one method and its Drizzle adapter one `select`; B05's canonical association writer is untouched and nothing here writes. `SCHEMA_CHANGE = NONE`, 37 migrations / 79 tables, no index. OpenAPI 84/91/191 → **85/92/191** (+1/+1/**+0** — a binary response mints no component; 0 removed, 0 existing changed); client +21 lines, 0 deletions, one `Blob` operation with `responseType: 'blob'` set by the generator. No curated `@embroidery/api-client` export, deferred to `APP7-A01` on the delivered `APP5-B06` → `APP5-A02` precedent. Focused evidence: 2 new suites / 32 tests plus 97 sibling-contract tests; B04's verify/review/race suites and B05's 336-test suite were **not** rerun. Report [`reports/APP7-B06-COMPLETION-REPORT.md`](./reports/APP7-B06-COMPLETION-REPORT.md). **`APP7-D01` = `COMPLETE — DELIVERED_FOR_PRODUCT_OWNER_REVIEW`:** the complete APP7 design package on the pre-existing but **empty** `APP_07` canvas (`726:3`, reused not re-created) — root section `728:3`, **11 sub-sections, 39 frames, 39 registry rows**, all `REVIEW_REQUIRED` with `Approval Evidence = —`, `SELF_APPROVAL = NO`; pre-draw audit `NO_EXISTING_APP7_DESIGN`. Admin 16 desktop frames, customer 12 desktop + 3 mobile-390, 7 shared specs. Drawn against the generated contract rather than planning prose, which corrected three assumptions: `publicOrderDeposit_current` returns **no attempt state** (attempt status reaches the customer only via the idempotent `publicOrderDeposit_initiate`/`replayed`), `assetStatus` publishes **four** values so `UPLOADED` renders exactly like `INSPECTING`, and `previewEligible` is Admin-only. `observedTransferReference` has no `maxLength` and no `pattern`, so the UI imposes no uppercase, punctuation-stripping, 15-character or invented 2000-character rule. The three facts never collapse (transfer ≠ evidence ≠ verification); mismatch is a durable business outcome, not an HTTP failure; `REQUIRES_REVIEW` resolves through the same verify action; zero-evidence verification is first-class. APP4 secure-access, APP6 step-up and APP1 shells are composed, not redrawn. All bank data and QR codes are synthetic and non-scannable. A layout defect — section-relative coordinates handled as absolute, plus a self-check that reproduced the same error and wrongly reported clean — was found and repaired inside the checkpoint, then re-verified by an independent `absoluteBoundingBox` pass and screenshots (0 overlaps, 0 escapes, rendered root height matching the root box). `node tools/check-figma-design-index.mjs` = **PASS** (373/373/19). `RUNTIME_CHANGE = NONE`. Report [`reports/APP7-D01-COMPLETION-REPORT.md`](./reports/APP7-D01-COMPLETION-REPORT.md). **Next: `APP7-A01`**, gated — `A01_UI_IMPLEMENTATION_GATE = BLOCKED_UNTIL_RELEVANT_D01_ROWS_ARE_PRODUCT_OWNER_APPROVED`. **`APP7-A01` (2026-08-24) — Admin order + deposit-payment workspace.** The checkpoint opened its own gate: the Product Owner passed the complete `APP7-D01` package and all **39** registry rows were promoted `REVIEW_REQUIRED` → `APPROVED_FOR_IMPLEMENTATION` under `FIG-APPROVAL-APP7-D01-PO-001`, as a registry edit only — no Figma node opened for mutation, `Last Verified` deliberately unchanged, the other 13 non-APP7 `REVIEW_REQUIRED` rows untouched, and the historical `SELF_APPROVAL = NO` preserved rather than rewritten. Two routes — `/orders` and `/orders/{orderId}` — consume the six accepted Admin operations with `BACKEND_CHANGE = NONE`, `SCHEMA_CHANGE = NONE`, `MIGRATION_CHANGE = NONE`, `OPENAPI_CHANGE = NONE`; the only package change is a curated `@embroidery/api-client` export (6 operations, 14 enums as values, 12 types) that `APP7-B02`/`B04`/`B06` deliberately deferred to their consumer on the `APP5-B06` → `APP5-A02` precedent. The rule the whole screen is built on is `751:3`: the order, obligation/attempt and evidence vocabularies live in three separate modules with no shared phrase and **no shared "Đã thanh toán" badge anywhere in the delivered code** — only attempt `SUCCEEDED` **and** obligation `SATISFIED` **and** order `DEPOSIT_PAID` together may read as paid, so a response where the attempt succeeded but the obligation did not reports neutrally instead of claiming a settled deposit, and a `200` carrying `REQUIRES_REVIEW` renders a durable business outcome rather than a failure (the phrase *xác nhận thất bại* appears exactly once in the UI, inside the approved note forbidding it, which is why the test asserts on the outcome title element rather than the document). `observedTransferReference` is submitted byte-for-byte — no pattern, length bound, uppercasing, trimming, punctuation stripping or normalization, and no HTML `pattern`/`maxLength` or CSS `text-transform` — proved by a space-padded mixed-case value arriving unchanged and by a 2400-character value submitting at full length; truncation exists only in read-only cells behind a keyboard-reachable reveal. The per-row *khớp*/*lệch* verdict the frames annotate is deliberately **not** computed: `740:48` matches an expected `ORDK7M2Q9XR4TDC` against a lower-case observed value embedded in a longer sentence, so a client-side comparison would have contradicted the outcome shown directly beneath it — both values are placed side by side and the server's verdict is the badge. Network ambiguity follows `741:51` literally: a transport failure with no status line, **and every 5xx** (the platform replaces a 5xx code and message with a generic pair, leaving nothing to separate "refused" from "committed then failed to answer"), holds the dialog in *đang kiểm tra lại* and re-reads the payment truth — the discriminating test rejects the mutation on the wire while the server's current read says `DEPOSIT_PAID` and asserts the UI renders committed success with exactly **one** write attempted; an unchanged state invites an explicit re-send whose body is asserted byte-identical to the first, never an automatic one. A `409` reconciles to the winner, and a test feeds it `could not obtain lock on relation payment_attempts` to prove no lock, transaction, version or relation language reaches an operator (`741:121`). Evidence is addressed by `evidenceId` and never an asset id (`RAW_ASSET_ID_USAGE = NONE`; no storage URL, download link or token), the preview is enabled exactly when `previewEligible` is true, `UPLOADED` renders identically to `INSPECTING`, `REJECTED` neither disables nor discourages verification, zero evidence is an ordinary verifiable deposit, and object URLs are asserted `revoked === created` on close and on unmount with `gcTime: 0` so no private bytes survive being unobserved. One real defect was caught by the tests and would have shipped: a successful verification satisfies the deposit, which removes the actionable attempt, and the action card's early return put the dialog at a different tree position — React unmounted and remounted it at the exact moment it had a result to show. Reuse held: one `QueryClient`, one Axios instance, TanStack Query for all server state, no Zustand mirror of the payment lifecycle, and one shared `AdminStatusBadge` tinted from the same `Color/Status/*` tokens the existing Admin badges use rather than a second colour system; `FU-ADMIN-SHARED-DIALOG-01` grew from six hand-rolled dialogs to seven. Focused evidence: **12 suites / 118 tests PASS**, including the two shared shell assertions that pin the navigation list — they exist to go red when an entry is added, and they did. `PLAYWRIGHT = NOT_RUN_BY_DESIGN`; no broad regression, backend, OpenAPI, database or worker gate was run, because none of their inputs changed. Report [`reports/APP7-A01-COMPLETION-REPORT.md`](./reports/APP7-A01-COMPLETION-REPORT.md). **Next = `APP7-S01`** (customer deposit, QR, evidence intake and confirmation at `/truy-cap/thanh-toan`). **`APP7-A01-C1` (2026-08-24) — oversized runtime stylesheet split, no behaviour change.** The A01 report flagged `order-detail.scss` at **869** lines as the Admin app's high-water mark; the Product Owner ruled that a runtime SCSS stylesheet is application source under the 400-line hard limit and that the pre-existing oversized Admin stylesheets are historical debt authorising nothing. The file is now a 33-line composition over six responsibility-owned partials — layout (154), cards (149), tables (100), payment workbench (176), payment dialogs (217), payment outcome (128) — every one ≤ 400, with `main.scss` unchanged so the feature still exposes exactly one entry and the styling architecture gains no new global import. The cut follows the file's existing banner sections and re-emits them in the order they were written, because that order *is* the cascade. One judgement is worth keeping: the frozen order lines, the reconciliation history and the expected-vs-observed comparison share a single base selector list, so a tidy-looking one-partial-per-table split would have had to duplicate that list across three files — the exact defect the correction exists to avoid — and the base therefore stays in `_order-tables.scss`, emitted before all three consumers, with the file's own header explaining why so it is not "fixed" later. `VISUAL_BEHAVIOR_CHANGE = NONE` is a measurement rather than a claim: the canonical Admin entry was compiled before and after and the emitted CSS is **byte-for-byte identical** (8669 lines both sides), which also proves no other feature's styles were disturbed. Two findings reported rather than actioned: `tools/check-file-size.mjs` scans only `.ts/.tsx/.js/.jsx/.mjs/.cjs`, so `.scss` is outside the gate that should have caught this — bringing it in line would immediately fail at least four historical stylesheets, so it is left as a Product Owner decision (§5 forbids a repository-wide cleanup); and `CMD-CHECK-STYLES` reports 14 pre-existing `apps/storefront` violations, identical before and after (verified by stashing C1) with **zero** in `apps/admin`. Focused evidence: 4 suites / 52 tests PASS as an import/render regression check only — `next/jest` stubs SCSS, so those tests observe no CSS at all and the visual claim rests entirely on the byte-identical compile diff. `apps/admin` typecheck and changed-file ESLint were not run and the report says why (no `.ts`/`.tsx` touched; ESLint does not match `.scss`). No backend, OpenAPI, schema, migration, Figma or generated-client change; the D01 approval registry did not change, so the Figma checker was not re-run. `APP7-A01-C2 = MUST_NOT_BE_CREATED`. Report [`reports/APP7-A01-C1-CORRECTION-REPORT.md`](./reports/APP7-A01-C1-CORRECTION-REPORT.md). **`APP7-E01` cross-layer acceptance: automated tier = PASS, 6/6 cases + browser acceptance, one canonical run of 23.2 s (43.8 s wall), 0 blocking runtime defects, `PRODUCTION_SOURCE_CHANGE = NONE`.** One serial Playwright project over the real topology (ephemeral PostgreSQL and MinIO, the built API behind the real Nginx gateway, both Next production builds, and the real `AppModule` + `WorkerModule` in the test process) with **no payment provider anywhere and nothing stubbed**; the fixture boundary is the APP6 hand-off only, and Order, OrderItems, both obligations, every attempt, every evidence row, every reconciliation and `DEPOSIT_PAID` are all produced by the delivered application. Proved: exact Catalog and COP conversion at `AWAITING_DEPOSIT` with the accepted 35 % split copied byte-for-byte; the customer deposit journey through the real B03 with the real `GRD-003` step-up run over the page; Admin exact verification moving the **same order id** to `DEPOSIT_PAID` with 1 transition, 1 reconciliation, 1 `payment.verified` (on the *attempt* aggregate) and 0 provider events; optional evidence reaching `ACCEPTED` while moving **no** payment state and B06 serving byte-identical content by `evidenceId`; a one-dong mismatch yielding a durable `REQUIRES_REVIEW` drawn as a business outcome; duplicate `design.approved` and verification replay both converging; and `DepositEligibilityPort` false until satisfaction with 0 rows in all seven APP8 tables. **`APP7-E01` is NOT COMPLETE and `APP7-X01` is NOT READY**: `REAL_BANK_APP_SCAN = PENDING_MANUAL_ENV` — no merchant bank configuration exists on this machine, §10 forbids simulating the gate, `HUMAN_PAUSES_SPENT = 0` and `TRANSFER_SUBMITTED = false`. Surfaced and deliberately not repaired (§16): the development Compose stack sets no `PAYMENT_MERCHANT_*` for the `api` or `staff-bootstrap` services, so `APP7-B03`'s fail-fast provider prevents the development API from starting. Report [`reports/APP7-E01-COMPLETION-REPORT.md`](./reports/APP7-E01-COMPLETION-REPORT.md) |
| APP8 | `COMPLETE — PASS_WITH_FOLLOW_UPS` (closed at `APP8-X01`) — all 13 canonical checkpoints COMPLETE, 0 blocking follow-ups (canonical status table: [`phases/APP8-INVENTORY-AND-PRODUCTION.md`](phases/APP8-INVENTORY-AND-PRODUCTION.md) §12; closure: [`APP8-X01-COMPLETION-REPORT.md`](reports/APP8-X01-COMPLETION-REPORT.md), [`APP8-CLOSURE-MATRIX.md`](reports/APP8-CLOSURE-MATRIX.md)) | `APP8 — Inventory Reservation and Production Operations`. Entry authority is `DepositEligibilityPort(orderId)`, published and proved by APP7: `true` only once the `DEPOSIT` obligation is SATISFIED — already consumed by the delivered `ReservationEligibilityGuard` (GRD-013), so APP8 adds no second implementation. `APP8-R00` audited the entry ([`APP8_PHASE_ENTRY_AUDIT.md`](audits/APP8_PHASE_ENTRY_AUDIT.md), [`APP8-R00-COMPLETION-REPORT.md`](reports/APP8-R00-COMPLETION-REPORT.md)) and found APP8 is **not greenfield**: the DB era already delivered the complete inventory and production **persistence** layer — 9 tables, both ports, both Drizzle adapters, the `sku_stocks` lock anchor, the LC-18 legality table, the frozen specification copy — with DB8 races `CC-15/16/17` PASS at P0; but **neither `InventoryModule` nor `ProductionModule` is composed into `AppModule`**, `APP8_HTTP_OPERATIONS_AT_ENTRY = 0`, `APP8_RUNTIME_WRITES_AT_ENTRY = 0` and the Figma registry holds **0** APP8 rows. Two real gaps were found and routed, not fixed in R00: **nothing produces a `sku_stocks` row** (`ensureStockRow` has no non-test caller, so every reservation fails on a missing stock record → `APP8-B01`), and **`releaseReservation`/`consumeReservation` take no reservation row lock**, so the DB3 `CC-21` release-vs-consume arbiter is absent and uncovered by DB8 (→ `APP8-B02`). Both schema dispositions are `NO_MIGRATION_REQUIRED`; notably there is **no `production_job_attempts` table**, so the candidate plan's worker-claims model was an invalid assumption — the phase's one real asynchronous owner is the official reservation on `payment.verified` (TR-LC17-04). `IMP-O003` is **closed** by `IMP-D029`, so no broker is introduced. All **14/14** original candidates were dispositioned into a **13-checkpoint** canonical roadmap `R00 → G01 → B01 → B02 → W01 → B03 → B04 → D01 → A01 → A02 → A03 → E01 → X01` (8 new HTTP operations, 0 migrations, 1 worker handler, 1 design package, 3 Admin screens). `CUSTOMER_UI_DISPOSITION = NO_CUSTOMER_UI_IN_APP8`; `DESIGN_GATE = DESIGN_REQUIRED_BEFORE_UI`. `APP8_TERMINAL_HANDOFF_STATE = order PRODUCTION_COMPLETED` (TR-LC14-04) with its job `COMPLETED`. The one true Product Owner decision R00 routed here — **`PO-APP8-001`**: whether a COP-only order, which physically cannot hold a reservation, satisfies GRD-015 — was **closed by `APP8-G01` as Option A** ([`APP8_G01_INVENTORY_AND_PRODUCTION_AUTHORITY.md`](audits/APP8_G01_INVENTORY_AND_PRODUCTION_AUTHORITY.md), [`APP8-G01-COMPLETION-REPORT.md`](reports/APP8-G01-COMPLETION-REPORT.md), `IMP-D056`): GRD-015's reservation clause binds **per reservable order subject**, so a COP-only order enters production with **no** reservation once every non-inventory guard passes, a mixed order needs coverage for its Catalog portions only, and reservation cardinality stays `CST-016`'s one active `RESERVED` row per `(order_id, sku_stock_id)`. `APP8-G01` also locked, with **no contradiction found against any stronger authority and no ADR required**: official reservations are **no-expiry** (`expires_at = NULL`, no sweep, `DEC-14` still deferred); production job creation is **Admin-initiated** and synchronous, `SYSTEM` remaining the actor only for the `payment.verified` reservation consumer; production artifact generation/management is **deferred, nonblocking and unassigned** — deliberately not routed to APP9 for symmetry; APP8's **successful production handoff** terminates at job `COMPLETED` + order `PRODUCTION_COMPLETED`, leaving `TR-LC14-05`, remaining payment, shipping, delivery, refund, final settlement and APP7's unsatisfied `REMAINING` obligation to APP9 — a boundary that (per `APP8-G01-C1`) **does not** exclude APP8-owned **production-job** cancellation, since `PLANNED`/`STARTED` → `CANCELLED` with a mandatory reason, releasing any still-active Catalog reservation, stays in `APP8-B04` exactly as the R00 audit §12.1 accepted, while the full **order** cancellation/refund saga (and `IMP-O008`) remains outside APP8; and `IMP-D054` applies **narrowly** — inventory persistence is promoted to `@embroidery/persistence` because two runtimes write it, while production persistence stays API-local. `APP8-B04` then delivered the guarded LC-18 transitions as **one** HTTP operation — `POST /api/admin/production-jobs/{jobId}/transitions` with a typed target (`STARTED`/`COMPLETED`/`CANCELLED`), OpenAPI 91→92 paths / 98→99 operations / 204→206 schemas, **0 migrations**, **0 worker behaviour**. A start enforces `GRD-015`/`GRD-022` against committed facts under the order and job row locks — order `DEPOSIT_PAID`, not `ON_HOLD`/`CANCELLING`, the DEPOSIT obligation satisfied through the **one** `DepositEligibilityPort`, and the job frozen from the approval the order still names — then consumes every Catalog reservation the **frozen order items** require and moves the order to `IN_PRODUCTION`, all in one transaction; a completion moves job and order to `COMPLETED` + `PRODUCTION_COMPLETED` and stops there; a cancellation requires a reason, releases only what is still `RESERVED`, never unconsumes issued stock and **does not move the order's commercial state**. The reservation requirement is re-derived through the one canonical `aggregateCatalogRequirements`, **promoted** out of `apps/worker` into `@embroidery/persistence` so `APP8-W01` reserves and `APP8-B04` consumes by the same aggregation (`IMP-D054`); `APP8-B03`’s display-only reservation summary is deliberately unreachable from the decision path (`FU-APP8-B02-02`, `FU-APP8-B03-02`). The new cross-aggregate lock order `orders → production_jobs → inventory_reservations → sku_stocks` is documented in [`DB8_LOCK_ORDER_MATRIX.md`](../database/DB8_LOCK_ORDER_MATRIX.md) and proved by a deterministic start-versus-order-hold race; isolation stays `READ COMMITTED` with explicit row locks. `APP8-D01` then delivered the phase design package on the pre-existing empty `APP_08` page (`766:3`, root section `771:3`): 9 sub-sections, **41 frames / 41 registry rows**, contract-derived rather than brief-derived, entering `REVIEW_REQUIRED` with no self-approval. The Product Owner reviewed and passed the **complete** package, and `APP8-A01` recorded that approval — all **41** rows promoted `REVIEW_REQUIRED` → `APPROVED_FOR_IMPLEMENTATION` under **`FIG-APPROVAL-APP8-D01-PO-001`**, a registry edit only, with no Figma node, node id, deep link, frame ownership or `Last Verified` value changed (`APP8-D01-C1` unused). `APP8-A01` then delivered the first Admin surface, the SKU stock workspace at **`/kho/skus/{skuId}`**, consuming the already-accepted `adminSkuStock_get` / `adminSkuStock_adjust` / `adminSkuStock_ledger` and adding **0** HTTP operations, **0** migrations, **0** worker changes and **0** OpenAPI/generated-client changes. Every figure on it is server truth — `available` is rendered as published rather than recomputed, a **negative** availability is a valid consequence of reservations exceeding on-hand rather than a data fault, `lowStockThreshold` is read-only because B01 publishes no authoring operation, and a never-counted SKU is the lazily-created zero anchor rather than a failure or a blank page. The audited adjustment sends exactly `{delta, reason}` — a signed non-zero integer and a mandatory reason, with no absolute overwrite — is guarded against duplicate submission, re-reads both queries instead of patching a metric from the delta, and answers `INVENTORY_STOCK_WOULD_GO_NEGATIVE` with the re-read on-hand figure while clamping nothing and retrying nothing. The bounded ledger renders only published fields and shows the truncation notice with **no** invented pagination. `APP8-A02` then delivered the second Admin surface, the production work queue at **`/san-xuat`**, consuming the already-accepted `adminProductionJob_list` and adding **0** HTTP operations, **0** migrations, **0** worker changes and **0** OpenAPI/generated-client changes. It is read-only by construction: the public api-client boundary publishes only the list operation to the Admin app, so job creation, the job detail read and the three LC-18 transitions are unreachable from this screen as a fact of the module graph rather than a convention. Every row is API-published truth — `jobId`, `orderId`, `approvalSnapshotId`, the LC-18 status and the lifecycle timestamps — with **no** `ORD-…` code and **no** frozen-specification label, because `AdminProductionJobQueueItemResponse` publishes neither and A02 opens no per-row Catalog or order read to invent one (the mismatch `APP8-D01` recorded on `788:179`, carried rather than resolved). The two filters are exactly the two the contract accepts: a repeatable `status[]` multi-select over the whole four-value LC-18 vocabulary where an empty selection sends **no** parameter rather than an `ALL` sentinel, and an `orderId` filter that operates on the id the schema accepts and never resolves an order code into one. Both live in the URL, and an unrecognised status token or a malformed order id is dropped rather than forwarded to a `.strict()` query schema that would refuse the whole request. Continuation is keyset on the server’s own `hasNext` / `nextCursor` with no page number, no total and no offset arithmetic; the server’s `(createdAt, id)` order is rendered unchanged with no client re-sort; a filter change addresses a different cache entry, so the cursor chain restarts rather than paging into a keyset the new filter never produced. The unfiltered and filtered empty states are deliberately distinct — the first offers the order list, **not** a standalone “create job” control, because `APP8-B03` nests creation under a specific order with that order’s exact approval and a satisfied deposit. The three approved read failures each get the only recovery that can succeed: a first-page reset for `PRODUCTION_CURSOR_INVALID`, a sign-in for `401` and a retry for the sanitised platform failure, with nothing retried automatically and no server message rendered as operator copy. The approved narrow reduction hides **only** the `approvalSnapshotId` column. `APP8-A02` also added the `Sản xuất` sidenav entry — `/san-xuat` is a real parameterless route, unlike the `Kho` entry `FU-APP8-A01-04` still carries. `APP8-A03` then delivered the third and final Admin surface, the production job detail at **`/san-xuat/{jobId}`**, consuming the already-accepted `adminProductionJob_get` and `adminProductionJob_transition` and adding **0** HTTP operations, **0** migrations, **0** worker changes and **0** OpenAPI/generated-client changes: the frozen specification, the reservation summary and the transition history are rendered from published fields only, action visibility is derived from `job.status`, and the three guarded transitions carry a mandatory cancellation reason with copy that names the **production job** and denies any order cancellation or refund. `APP8-E01` then closed the functional acceptance — **4 journeys / 14 cases / ACCEPTANCE PASS / 0 runtime changes**, delivered as three scoped commands (`CMD-TEST-APP8-E01-API`, `-WORKER`, `-ADMIN`) because `apps/api` may not import `apps/worker` and neither may be imported by `apps/admin`; every accepted lower-checkpoint proof was reused, none rerun. **`APP8-X01` closed the phase** on 2026-08-26 as documentation-only reconciliation — 0 source, test, schema, generated-artifact, Figma and tooling changes — recording the measured final baseline: OpenAPI **92 paths / 99 operations / 206 schemas** (+7/+7/+15 vs APP7, 0 removed); **7** APP8-owned HTTP operations (B01 = 3, B03 = 3, B04 = 1 — the phase plan predicted 8 because it expected two transition endpoints, and the measurement supersedes the prediction); **0 migrations and 0 new tables**, the database unchanged at **37 migrations / 79 tables**, APP8 being a composition phase over persistence the DB era had already shipped; **1** worker handler (`payment.verified` → official inventory reservation, the fifth outbox capability); **41/41** Figma rows `APPROVED_FOR_IMPLEMENTATION` under `FIG-APPROVAL-APP8-D01-PO-001`; and **3** new Admin routes with **one** new sidenav entry (`Sản xuất`; `Kho` stays unbuildable while no all-SKU route exists). Correction ledger: **`APP8-G01-C1` USED / PASS** (the `PO-APP8-005` wording), every other `C1` slot **UNUSED**. Shared-authority truth as closed: canonical inventory persistence under `@embroidery/persistence` with no API-local inventory persistence remaining, `CC-21` terminalization by row lock shared across the by-id and by-`(order, SKU)` entry points, one Catalog requirement aggregation shared by worker and API — and **production persistence deliberately NOT promoted**, remaining API-local. All follow-ups classified exactly once — 5 CLOSED, 2 CLOSED_FALSE_POSITIVE, 20 NONBLOCKING_OPEN, 2 ROUTED_TO_APP9 (`FU-APP8-W01-01` remaining-payment event routing, `FU-APP8-B04-02` the post-cancellation commercial saga), 3 NOT_RELEVANT — **0 blocking**, and none implemented to close. Terminal state: production job `COMPLETED` + order `PRODUCTION_COMPLETED`; `APP9_EXECUTION = NONE`. `NEXT_PHASE = APP9 — Remaining Payment, Fulfillment and Completion`. |
| APP9 | `IN_PROGRESS` — `APP9-R00` COMPLETE (`COMPLETE_WITH_PO_DECISIONS_REQUIRED`); **next is `APP9-G01`** | `APP9 — Remaining Payment, Fulfillment and Completion`. Entry state is an order at `PRODUCTION_COMPLETED` whose production job is `COMPLETED`, handed over by `APP8-X01`. APP9 begins at `TR-LC14-05` — the Admin move to `AWAITING_FINAL_PAYMENT` against the `REMAINING` obligation `APP7-W01` created alongside `DEPOSIT` and deliberately left unsatisfied — and owns remaining payment collection, fulfillment freeze, shipping, delivery and final settlement, none of which APP7 or APP8 implemented. `APP9-R00` audited the entry ([`APP9_PHASE_ENTRY_AUDIT.md`](audits/APP9_PHASE_ENTRY_AUDIT.md), [`APP9-R00-COMPLETION-REPORT.md`](reports/APP9-R00-COMPLETION-REPORT.md)) and found APP9 is **not greenfield**: the DB era already delivered the **complete** APP9 persistence layer — `shipping_details`/`shipping_snapshots`/`shipping_fee_acknowledgements` (migration `0023`) with their reject-mutation triggers (`0030`), `order_cancellation_requests`, `refunds`, `payment_reconciliations`, the full LC-14 legality table in `order-transitions.ts`, and `DrizzleOrderShippingRepository.saveShippingDetails`/`dispatch`/`acknowledgeShippingFee`/`openCancellationRequest`/`resolveCancellationRequest`/`loadShippingDetail` plus `openRefund`/`approveRefund`/`executeRefund`/`refundableAmount` — with **zero non-test callers**. `APP9_HTTP_OPERATIONS_AT_ENTRY = 0`, `APP9_RUNTIME_WRITES_AT_ENTRY = 0`, `APP9_FIGMA_ROWS_AT_ENTRY = 0`, `APP9_SCHEMA_DISPOSITION = NO_MIGRATION_REQUIRED`. Three provisional assumptions were **disproved** and removed rather than carried forward: there is **no payment provider, callback or webhook** (`IMP-D052`/`PO-APP7-001`, `PAYMENT_MVP = MANUAL_BANK_TRANSFER`), so the `B02` "callback/reconciliation" candidate was redefined into the `payment.verified` routing fix rather than inventing a provider; **fulfillment contracts and schema are already delivered**, so the `C02` contract candidate was removed; and there is **no "ready for handoff" or "fulfilled" state** — LC-14's canonical names are `AWAITING_FINAL_PAYMENT` → `READY_FOR_DELIVERY` → `DELIVERED` → `COMPLETED`, with `TR-LC14-05`/`07`/`08` **admin** and `TR-LC14-06` gated on `GRD-016`. `TR-LC14-06` will be implemented **synchronously inside the Admin verification transaction**, following the delivered `TR-LC14-02` precedent — which makes DB3 CC-14 structurally unreachable and is why APP9 adds **0 new worker handlers**. The `payment.verified` routing audit gave a concrete disposition: **D — a verified `REMAINING` obligation would dead-letter today** (the sole registrant is `InventoryReservationHandler`, the registry throws on a second handler for one event type, and its payload parser rejects any non-`'DEPOSIT'` kind with the terminal `JOB_PAYLOAD_INVALID`); **not** C, because that literal comparison is exactly what prevents a second reservation. `FU-APP8-W01-01` is therefore routed to `APP9-W01` (extend the one consumer to accept both kinds and no-op for `REMAINING`; 0 HTTP operations, 0 new handlers). Cancellation/refund is classified `PARTIALLY_GOVERNED_REQUIRES_PO_LOCK`: `ADR-DB3-002`'s S1–S9 stage matrix, the six-step saga, the Cancel-Order-vs-Cancel-Production-Job taxonomy and all persistence ship, but the per-stage default refund dispositions stay deferred to policy configuration (CON-144) and are carried as `IMP-O008` (owner APP9) — escalated as the phase's **one** genuine Product Owner decision **`PO-APP9-001`** (recommendation: **defer** the cancellation/refund branch, deliver the commerce-completion path first). `FU-APP8-B04-02` resolves without new APP8 work: the order stays where it is by design, `ON_HOLD` is already legal and delivered, and a replacement production job for the same approval snapshot is **impossible** (`uq_production_jobs__order_approval_snapshot` is a plain unique), so re-planning is the delivered `ADR-DB3-003` approval-revision path and only the S6 commercial branch is APP9's — conditional on `PO-APP9-001`. All **14/14** provisional candidates were dispositioned into a **13-checkpoint** canonical roadmap `R00 → G01 → B01 → B02 → B03 → W01 → B04 → B05 → D01 → A01 → S01 → E01 → X01` (**8–9** new HTTP operations, **0** migrations, **0** new worker handlers with 1 extended, 1 design package, **0** new Admin routes with `/orders/{orderId}` extended, **1** new Storefront route). No `Cxx` checkpoint survives — OpenAPI is a build product of the controllers (`APP0-B01`), so no contract-only deliverable exists. `APP9-A01`/`APP9-S02` were removed or merged rather than built to match the plan; `DESIGN_GATE = DESIGN_REQUIRED_BEFORE_UI` with `APP9-D01` on a new `APP_09` page. No carrier-tracking scope is added: `carrier_name`/`tracking_code` are static Admin fields recorded **internally** (`03-USER-JOURNEYS.md` J8). `NEXT_CHECKPOINT = APP9-G01`. |
| APP10–APP12 | `NOT_STARTED` | Dependency-ordered per §3. |

APP0 closure is a **foundation** milestone, not a release: R0 closes at APP1 (`09-RELEASE-AND-MILESTONE-POLICY.md` §2) and production readiness remains APP12's (IMP-D015).
