# Master Application Roadmap

## 1. Strategy

The implementation stage is ordered by dependency and business value. Admin authoring/operations lead the Storefront by one capability; both are completed inside the same business phase. Design is audited per phase and produced as a whole package only when needed.

## 2. Ordered phases

| Phase | Name | Primary outcome | Design expectation |
|---|---|---|---|
| APP0 | Application Delivery Foundation | Reviewable delivery system, Swagger/OpenAPI, generated client, SCSS foundation, testing and technical spikes. | `NONE` except technical UI/style verification. |
| APP1 | Staff Access and Application Shells | Secure Admin login, permission-aware shell, Storefront shell. | Audit; likely `SUPPLEMENT` for Admin shell. |
| APP2 | Assets and Catalog Publication | Admin publishes products/assets; Storefront lists and displays published products. | Audit existing public UI; Admin likely `NEW/SUPPLEMENT`. |
| APP3 | Design Templates and 2D Design Studio | Admin publishes compatible templates; customer creates watermark-protected 2D customization. | Audit existing studio design; package gaps as one phase. |
| APP4 | Customer Identity, Verification, Secure Access and Notification Core | Customer/contact foundation and secure links support later review/payment journeys. | Limited customer-facing package if missing; Admin operations may need design. |
| APP5 | Custom Requests and Customer-Owned Products | Customer submits embroidery request; Admin triages and manages it. | Whole request journey package when gaps exist. |
| APP6 | Design Review, Approval and Quotation | Versioned design review, secure approval and immutable quotation workflow. | Whole Admin/customer review and quotation package. |
| APP7 | Deposit Payment and Order Creation | Verified deposit converts approved quote into an order safely. | Payment/status package only after provider UX is known. |
| APP8 | Inventory Reservation and Production Operations | Order reserves inventory and moves through production with concurrency safety. | Mostly Admin; design package if production board absent. |
| APP9 | Remaining Payment, Fulfillment and Completion | Remaining obligation, fulfillment freeze, completion/cancellation/refund according to locked policy. | Customer order/payment and Admin fulfillment package. |
| APP10 | Customer Operations and Communication | Profiles, merge, notification operations, simple Zalo/Messenger handoff. | Admin/customer operations package as needed. |
| APP11 | Gallery, Content, SEO and Store Presentation | Public content/discovery and operational content management. | Reuse existing public designs; supplement CMS/Admin gaps. |
| APP12 | Hardening, UAT and Production Readiness | Security, resilience, performance, observability, runbooks and go-live evidence. | No broad redesign; only defect corrections. |

## 3. Dependency chain

```text
APP0
→ APP1
→ APP2
→ APP3
→ APP4
→ APP5
→ APP6
→ APP7
→ APP8
→ APP9
→ APP10
→ APP11
→ APP12
```

Some discovery/design work for the next phase may run while engineering implements the current phase, but implementation dependencies remain ordered unless an explicit re-plan proves independence.

## 4. First implementation target

After APP0 and APP1 foundations, the first full business vertical slice is:

```text
Admin login
→ upload/process asset
→ create product draft
→ publish product
→ Storefront server-renders product list/detail
→ unpublish removes public visibility
```

This slice proves authentication, authorization, application services, persistence, object storage, worker processing, Swagger/client, Admin, Storefront, SSR/SEO, SCSS, gateway, logging and E2E integration.

## 5. Phase planning depth

Detailed candidate checkpoints are in `phases/APP*.md`. They are a roadmap, not authorization to execute more than one checkpoint at a time.

Exact endpoints may be corrected during the phase contract audit when repository truth requires it, but corrections must preserve:

- Phase outcome.
- Five-endpoint maximum.
- Human review boundaries.
- Existing lifecycle/database semantics.
- Vertical delivery order.

## 6. Current phase status

This section is the canonical phase-status record for the application stage. Status values come from `11-TRACEABILITY-AND-STATUS-MATRIX.md` §3.

| Phase | Status | Evidence |
|---|---|---|
| APP0 | `COMPLETE` | Closed at `APP0-X01`; all 18 preceding checkpoints complete. Report: [`reports/APP0-X01-COMPLETION-REPORT.md`](./reports/APP0-X01-COMPLETION-REPORT.md); plan: [`phases/APP0-APPLICATION-DELIVERY-FOUNDATION.md`](./phases/APP0-APPLICATION-DELIVERY-FOUNDATION.md). |
| APP1 | `COMPLETE — PASS_WITH_FOLLOW_UPS` | **Closed at `APP1-X01`** (verdict `PASS_WITH_FOLLOW_UPS`; zero blocking follow-ups): report [`APP1-COMPLETION-REPORT.md`](./reports/APP1-COMPLETION-REPORT.md), evidence [`APP1-CLOSURE-EVIDENCE.md`](./reports/APP1-CLOSURE-EVIDENCE.md). Baselines frozen: OpenAPI `ae015dd6…`, API-client `89c1aace…`, DB 78 tables / 31 migrations / `4ca56a59…` (`NO_APP1_SCHEMA_OR_MIGRATION_CHANGE`); Figma D01 (13 rows) + D02 (7 rows) `APPROVED_FOR_IMPLEMENTATION`. Milestone **R0 achieved** (APP0 + APP1). Pre-implementation audit and `APP1-DEC-AUTH` decision complete. Plan: [`phases/APP1-STAFF-ACCESS-AND-SHELLS.md`](./phases/APP1-STAFF-ACCESS-AND-SHELLS.md); audit: [`audits/APP1_PRE_IMPLEMENTATION_AUDIT.md`](./audits/APP1_PRE_IMPLEMENTATION_AUDIT.md); reports: [`APP1-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md`](./reports/APP1-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md), [`APP1-DEC-AUTH-COMPLETION-REPORT.md`](./reports/APP1-DEC-AUTH-COMPLETION-REPORT.md). Staff auth locked by IMP-D027 / [`ADR-APP1-001`](../adr/backend/ADR-APP1-001-STAFF-AUTHENTICATION-AND-SESSIONS.md) (no migration). **`APP1-D01` = `COMPLETE — ADMIN DESIGN APPROVED`** (Product Owner approved the Admin login + shell designs; recorded at `APP1-A01` as `FIG-APPROVAL-APP1-D01-ADMIN-001`, 13 Admin registry rows promoted to `APPROVED_FOR_IMPLEMENTATION`; [`APP1-D01-COMPLETION-REPORT.md`](./reports/APP1-D01-COMPLETION-REPORT.md); registry [`FIGMA_DESIGN_INDEX.md`](../design/FIGMA_DESIGN_INDEX.md)). **`APP1-B01` = `COMPLETE — CORRECTED`** (staff session auth; two endpoints; `NO_MIGRATION_REQUIRED` — [`APP1-B01-COMPLETION-REPORT.md`](./reports/APP1-B01-COMPLETION-REPORT.md); validation corrected to the canonical Zod pipeline by `APP1-B01-C1` — [`APP1-B01-C1-CORRECTION-REPORT.md`](./reports/APP1-B01-C1-CORRECTION-REPORT.md)). **`APP1-B02` = `COMPLETE — CORRECTED`** (current staff `GET /api/staff/me`; one endpoint; `NO_MIGRATION_REQUIRED` — [`APP1-B02-COMPLETION-REPORT.md`](./reports/APP1-B02-COMPLETION-REPORT.md); success-data requirement corrected by `APP1-B02-C1` — [`APP1-B02-C1-CORRECTION-REPORT.md`](./reports/APP1-B02-C1-CORRECTION-REPORT.md)). **`APP1-A01` = `COMPLETE — CORRECTED, DELIVERED_FOR_PRODUCT_OWNER_REVIEW`** (Admin `/login` screen; approval recorded + 13 rows promoted; `staffSessionCreate`; no schema change — [`APP1-A01-COMPLETION-REPORT.md`](./reports/APP1-A01-COMPLETION-REPORT.md)). **`APP1-A01-C1` = `COMPLETE`** (login readiness correction — password-toggle hydration via the dev gateway, bidirectional route protection, automatic Compose admin bootstrap; no migration; live-tested through `admin.embroidery.local` — [`APP1-A01-C1-CORRECTION-REPORT.md`](./reports/APP1-A01-C1-CORRECTION-REPORT.md)). **`APP1-A01-C2` = `COMPLETE`** (real isolated-Compose evidence for the bootstrap environment policy — dev-fail readiness-block, create/reuse, prod-skip, unknown-env fail-closed, zero residue; a genuine result-line observability defect found and fixed — [`APP1-A01-C2-CORRECTION-REPORT.md`](./reports/APP1-A01-C2-CORRECTION-REPORT.md)). The Product Owner completed the A01 live test and accepted it, so **`APP1-A01` = `COMPLETE — CORRECTED — PRODUCT_OWNER_ACCEPTED`**. The Product Owner live-tested and accepted A02, so **`APP1-A02` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (authenticated Admin shell — protected-layout shell, single server-hydrated current-staff query, logout, client session-expiry modal, mobile drawer; no migration — [`APP1-A02-COMPLETION-REPORT.md`](./reports/APP1-A02-COMPLETION-REPORT.md)). The Product Owner reviewed and PASSED the Storefront shell & not-found design SUPPLEMENT, so **`APP1-D02` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (seven §4.2 rows `APPROVED_FOR_IMPLEMENTATION` under `FIG-APPROVAL-APP1-D02-STOREFRONT-001` — [`APP1-D02-COMPLETION-REPORT.md`](./reports/APP1-D02-COMPLETION-REPORT.md)). The Product Owner live-tested and accepted S01A, so **`APP1-S01A` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (shared Storefront shell + responsive navigation; server-first layout, responsive Full/Compact header, focus-trapped mobile drawer, non-interactive nav + honest search, `<main>` slot; SCSS-only — [`APP1-S01A-COMPLETION-REPORT.md`](./reports/APP1-S01A-COMPLETION-REPORT.md)). The Product Owner live-tested and accepted S01B, so **`APP1-S01B` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (Storefront not-found boundary — canonical `app/not-found.tsx` inside the accepted shell, unmatched URL → HTTP 404, safe Vietnamese copy with no path echo, `/` primary recovery, honestly-unavailable secondary; Server Component; SCSS-only; no API/schema change — [`APP1-S01B-COMPLETION-REPORT.md`](./reports/APP1-S01B-COMPLETION-REPORT.md)); with S01A accepted, the parent **`APP1-S01` = `COMPLETE — PRODUCT_OWNER_ACCEPTED`**. **`APP1-E01` = `COMPLETE — CORRECTED — REVIEW_ACCEPTED`** (staff access and shared-shell cross-layer acceptance; 17 tests; corrected by `APP1-E01-C1` — [`APP1-E01-COMPLETION-REPORT.md`](./reports/APP1-E01-COMPLETION-REPORT.md)). **`APP1-E01-C1` = `COMPLETE`** (the two missing cross-layer failure journeys — invalid/stale-cookie route matrix reaching authoritative 401, and initial protected-page API unavailability surfacing the safe framework 5xx boundary; test/harness only — [`APP1-E01-C1-CORRECTION-REPORT.md`](./reports/APP1-E01-C1-CORRECTION-REPORT.md)). **`APP1-X01` = `COMPLETE`** — phase closure, R0 evaluation, APP2 handoff. |
| APP2 | `STARTED_WITH_FOUNDATION_ONLY` (engineering) — `AUDITED — PASS_WITH_REQUIRED_DECISIONS` | Pre-implementation audit complete (`APP2-PRE-AUDIT`): audit [`audits/APP2_PRE_IMPLEMENTATION_AUDIT.md`](./audits/APP2_PRE_IMPLEMENTATION_AUDIT.md), report [`reports/APP2-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md`](./reports/APP2-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md). Verdict `NO_APP2_MIGRATION`; asset/catalog modules are persistence `SCAFFOLD` only. Two required decisions before code — `IMP-O002` object storage (`ADR_REQUIRED` → `APP2-DEC-STORAGE`) and `IMP-O003` job runtime (`ADR_REQUIRED`, DB claim path pre-designed by `ADR-DB5-003` → `APP2-DEC-JOBS`) — plus one design package (`APP2-D01`, Admin `NEW` + Storefront list/detail `SUPPLEMENT`). Corrected checkpoint map in [`phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md`](./phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md) §6.1. **`APP2-DEC-STORAGE` = `COMPLETE — CORRECTED, DELIVERED_FOR_REVIEW`** — object-storage/asset-intake architecture locked by IMP-D028 / [`ADR-APP2-001`](../adr/backend/ADR-APP2-001-OBJECT-STORAGE-AND-ASSET-INTAKE.md) (S3-compatible/MinIO dev, AWS SDK v3, API-proxied streaming upload, two private buckets, server SHA-256, SVG rejected, proxied publication-gated delivery, `NO_APP2_MIGRATION`), verdict `PASS_WITH_PRODUCT_PARAMETERS`; adds a narrow `APP2-I01` object-storage foundation checkpoint before `APP2-B01` (map now **18 checkpoints**). Report: [`reports/APP2-DEC-STORAGE-COMPLETION-REPORT.md`](./reports/APP2-DEC-STORAGE-COMPLETION-REPORT.md). **Corrected by `APP2-DEC-STORAGE-C1`** (post-object `UPLOADED` insert / storage-first two-transaction lifecycle; T1 `busboy`+`lib-storage` streaming multipart transport; route-scoped nginx `proxy_request_buffering off`; server-only SHA-256; `OBJECT_STORAGE_PUBLIC_BASE_URL` removed) — [`reports/APP2-DEC-STORAGE-C1-CORRECTION-REPORT.md`](./reports/APP2-DEC-STORAGE-C1-CORRECTION-REPORT.md). **Corrected by `APP2-DEC-STORAGE-C2`** (upload idempotency model I1 pre-stream durable allocation + pre-stream fingerprint, CW-01…CW-07; `@aws-sdk/s3-request-presigner` removed from the APP2 set; `lib-storage` memory-bound policy + C1 heap-claim corrected; repository implementation gap not schema gap, `NO_APP2_MIGRATION` re-confirmed) — [`reports/APP2-DEC-STORAGE-C2-CORRECTION-REPORT.md`](./reports/APP2-DEC-STORAGE-C2-CORRECTION-REPORT.md). **`APP2-DEC-STORAGE` accepted** as `ACCEPTED_WITH_BLOCKED_IMPLEMENTATION_GAPS` (no C3); `STORAGE-BLK-01..03` routed to the `APP2-B01` entry gate. **`APP2-DEC-JOBS` = `COMPLETE — CORRECTED, DELIVERED_FOR_REVIEW`** — asynchronous job runtime locked by IMP-D029 / [`ADR-APP2-002`](../adr/backend/ADR-APP2-002-ASYNCHRONOUS-JOB-RUNTIME.md) (PostgreSQL-backed claim queue on the existing persistence — `outbox_events` + `background_job_attempts` + `FOR UPDATE SKIP LOCKED` + visibility-timeout lease on `next_attempt_at`; J2 Redis/BullMQ + J3 RabbitMQ rejected; **no new runtime dependency, `NO_APP2_MIGRATION`**), verdict `PASS_WITH_CONFIG_PARAMETERS`; adds a narrow `APP2-I02` worker-runtime foundation (owns `FU-A07`) before `APP2-W01` (map now **19 checkpoints**). Report: [`reports/APP2-DEC-JOBS-COMPLETION-REPORT.md`](./reports/APP2-DEC-JOBS-COMPLETION-REPORT.md). **Corrected by `APP2-DEC-JOBS-C1`** ([`reports/APP2-DEC-JOBS-C1-CORRECTION-REPORT.md`](./reports/APP2-DEC-JOBS-C1-CORRECTION-REPORT.md)): **`PENDING` is the only automatic claim/retry state** (matches partial index IDX-088), retryable failure returns to `PENDING`+backoff (never `FAILED`; `FAILED` reserved/never emitted), ownership-guarded atomic completion transactions, expired-lease attempt evidence, no-heartbeat timeout invariant; spikes 23/23 + 25/25. Governance: a checkpoint may receive **at most one** correction — this is it. `APP2-DEC-JOBS` review **accepted**, which unblocked `APP2-D01` and `APP2-I02`. **`APP2-D01` = `DELIVERED_FOR_PRODUCT_OWNER_REVIEW`** — the Assets and Catalog Publication design package on the new Figma page **APP_02** (`419:3`), section `423:3`: 27 screen/state frames + 2 annotation frames + 1 DS supplement (`FIG-DS-INPUT-APP2`, closing `GAP-D01` for APP2 only; `GAP-D02` stays a local `ink/900 @45%` composition). Admin asset/product/catalog/publication `NEW`, Storefront list/detail `SUPPLEMENT` cloned from the approved APP1-D02 shells (APP1 frames unmodified, **no APP1 row superseded**). 30 new `REVIEW_REQUIRED` rows → registry now **69 IDs** ([`FIGMA_DESIGN_INDEX.md`](../design/FIGMA_DESIGN_INDEX.md) §4.3/§6.1b); handoff in [`phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md`](./phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md) §6.2; report [`reports/APP2-D01-COMPLETION-REPORT.md`](./reports/APP2-D01-COMPLETION-REPORT.md). **Product Owner review returned `PARTIAL_PRODUCT_OWNER_APPROVAL`, corrected by `APP2-D01-C1`** ([`reports/APP2-D01-C1-STOREFRONT-SOURCE-CORRECTION-COMPLETION-REPORT.md`](./reports/APP2-D01-C1-STOREFRONT-SOURCE-CORRECTION-COMPLETION-REPORT.md)): **Admin = `COMPLETE — PRODUCT_OWNER_APPROVED — FROZEN`**; **Storefront Product List = `REJECTED_NODE_SET_REMOVED` / `SOURCE_AUTHORITY_CORRECTED_TO_UI02`** — the four uniform card-grid frames (`444:204`, `445:204`, `445:210`, `446:223`) were deleted from Figma and their registry rows removed because the equal-height ecommerce grid conflicted with the established Pinterest-inspired image-led masonry of **UI02 – Discover Feed**, which is now the mandatory `APP2-S01` authority (`208:538`; desktop `208:2002`, tablet `224:871`, mobile `226:1038`; masonry 5/3/2); **Storefront Product Detail = `NOT_APPROVED` / `WITHHELD_PENDING_UI03_RECONCILIATION`** — frames unmodified but not implementation authority, requiring a separate UI03 reconciliation checkpoint. Registry stays **69 IDs** (−4 rejected, +4 UI02 authority rows); 31/31 frozen nodes verified unchanged. `APP2-A01..A04` = `BLOCKED_BY_D01_APPROVAL_AND_BACKEND`; `APP2-S01` = `BLOCKED_BY_BACKEND` (authority now UI02); `APP2-S02` = `BLOCKED_BY_UI03_RECONCILIATION_AND_BACKEND`; **`APP2-I01` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the S3-compatible object-storage foundation `@embroidery/object-storage` (six-operation port by `ORIGINALS`/`DERIVATIVES` alias with no presign and no list-all; streaming `lib-storage` upload 5 MiB × 2 with `leavePartsOnError: false`; PII-free UUIDv7 keys with SVG rejected; strict config with paired credentials and secret redaction; closed provider-error taxonomy), pinned `minio/minio:RELEASE.2025-04-08T15-41-24Z` development Compose service with in-package bucket bootstrap, and an **inactive** route-scoped nginx upload seam (`proxy_request_buffering off`, body-size/timeout hooks deliberately unset pending the open max-image-bytes parameter). Only `@aws-sdk/client-s3` + `@aws-sdk/lib-storage` `3.1095.0`; `NO_APP2_MIGRATION`, OpenAPI/API-client/DB/Figma baselines unchanged. 150 unit tests + 20/20 disposable-MinIO contract cases; contract case 16 found and fixed a real dangling-multipart defect (`Upload.abort()` lets `done()` reject before `AbortMultipartUpload` lands). Preflight recorded `PARTIAL` — the tree was not clean on entry. Report: [`reports/APP2-I01-COMPLETION-REPORT.md`](./reports/APP2-I01-COMPLETION-REPORT.md). **`APP2-I02` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the PostgreSQL worker-runtime foundation implementing IMP-D029 / [`ADR-APP2-002`](../adr/backend/ADR-APP2-002-ASYNCHRONOUS-JOB-RUNTIME.md) as corrected by `APP2-DEC-JOBS-C1`, closing `FU-A07`. `WorkerJobQueueRepository` (`packages/persistence`) provides the narrow typed seam — `claimRegisteredBatch`, `completeSucceededAttempt`, `completeRetryableAttempt`, `completeTerminalAttempt`, `probeWorkerDatabase` — with a single-statement database-time claim (`FOR UPDATE SKIP LOCKED`, conflict-safe `WORKER_LEASE_EXPIRED` evidence via a data-modifying CTE, lease on `next_attempt_at`, status stays `PENDING`) and ownership-guarded atomic completions returning typed `STALE_JOB_LEASE`; `FAILED` is never emitted. `apps/worker` gains the handler registry (one handler per event type, duplicate registration fails startup, an empty registry claims nothing), the closed error taxonomy, the exact capped backoff `min(base × 2^(n−1), max)`, worker identity + `AsyncLocalStorage` correlation with an allow-list log projection, a bounded poll loop with per-attempt `AbortController` timeout, graceful shutdown and readiness; the no-op keep-alive service is removed. Runtime policy is read from the canonical `policy_configurations` path under key `worker.runtime` — **no environment duplication, no production default, and no development bootstrap** (none exists to reuse), so an unconfigured worker reports `WORKER_POLICY_MISSING`, stays up and claims nothing. **No new dependency, `NO_APP2_MIGRATION`**, OpenAPI/API-client/DB/Figma baselines unchanged, production registry empty (no Asset handler). 115 worker + 107 persistence unit tests, all 27 required disposable-PostgreSQL cases (29 total) and a 6-assertion real-process smoke pass; two real defects were found by tests and fixed before Commit A. Report: [`reports/APP2-I02-COMPLETION-REPORT.md`](./reports/APP2-I02-COMPLETION-REPORT.md). **Review returned `CORRECTION_REQUIRED`; corrected by `APP2-I02-C1`** → **`APP2-I02` = `COMPLETE — CORRECTED — DELIVERED_FOR_REVIEW`**. Two blocking defects fixed. (1) A timed-out handler that ignored its `AbortSignal` was abandoned while the runtime recorded a retryable timeout and **released the lease**, so the still-running handler could overlap a retry of the same job with the same `effectKey` — Node cannot cancel a running promise, and the I02 report wrongly recorded this as a limitation rather than a defect. The handler promise is now never detached: at `handlerTimeoutMs` the runtime aborts and then waits on it until a hard stop of `min(timeoutInstant + leaseSafetyMarginMs, leaseExpiresAt − fatalExitSafetyMs)` (new internal constant `fatalExitSafetyMs = 250`, not an environment variable, enforced at policy startup by requiring `leaseSafetyMarginMs` to exceed it). A **cooperative** handler settles inside the deadline and completes through the existing atomic transaction as `JOB_HANDLER_TIMEOUT` — a late success can never become `SUCCEEDED`. An **uncooperative** handler writes no completion at all: the row keeps `PENDING`/`claimed_by`/`attempt_count`/`next_attempt_at` exactly as claimed, the runtime enters `FATAL_HANDLER_UNRESPONSIVE`, stops claiming, reports unready, emits one allow-list fatal log, closes context and pool once and exits `1` before the lease expires; a later worker reclaims after expiry and writes the single `WORKER_LEASE_EXPIRED` attempt. The process boundary is the only cancellation a JavaScript handler cannot ignore. (2) The acceptance smoke used `process.emit('SIGTERM')`, which proves a listener is attached and nothing more; a **real Linux SIGTERM** is now delivered by `docker kill --signal=TERM` to the shipped `runner` image (**exit 0 in 362 ms**), with `process.emit` demoted to a fast unit check. That smoke also exposed a pre-existing defect: the production image had never been executed (the dev stack uses the `dev` target) and could not start, because the `deps`/`prod-deps` stages never copied the `packages/database` and `packages/persistence` manifests, leaving their `node_modules` uninstalled — fixed in `worker.Dockerfile`. No dependency, no migration, no schema/status/outcome change, no persistence change (`leaseExpiresAt` was already projected), all frozen baselines unchanged. 130 worker + 107 persistence tests, 29 queue-integration cases, 3 timeout cases, 5 real-signal cases, zero Docker/database residue. Report: [`reports/APP2-I02-C1-CORRECTION-REPORT.md`](./reports/APP2-I02-C1-CORRECTION-REPORT.md). Governance: at most one correction per checkpoint — this is it; `APP2-I02-C2` must not be created. **Final review ruled C1 `IMPLEMENTATION_SUBSTANTIALLY_CORRECT BUT ACCEPTANCE_NOT_PROVEN`, resolved by the mandatory final directive `APP2-I02-FD1` → `APP2-I02` = `COMPLETE — CORRECTED — REVIEW_ACCEPTED`, `APP2-I02-C1` = `SUPERSEDED_BY_FINAL_PROCESS_PROOF`, `APP2-I02-FD1` = `COMPLETE`.** C1 had proven the uncooperative path with an injected process-exit seam inside Jest, so worker A's never-settling promise stayed alive in the test process and worker B started after an exit *record* rather than after worker A had actually terminated — ordering between two objects in one process cannot rule out overlap. FD1 replaces that with a **real Linux process boundary**: worker A runs the genuine Nest runtime as PID 1 in a container using the production exit seam, its handler ignores `AbortSignal` and never settles, and the real fatal path ends the real container; worker B is a second container started only after `docker inspect` reports worker A stopped **and** `clock_timestamp()` has passed the lease deadline. Proven externally over **three consecutive runs**: `ExitCode 1` / `Running false` ~17 s before the lease deadline; the row stays `PENDING`, owned, `attempt_count = 1`, `next_attempt_at` unchanged, `last_error` NULL; **zero** attempt rows before reclaim; worker B's handler starts ~18 s after worker A's `FinishedAt`; attempts become exactly `1/FAILED_RETRYABLE/WORKER_LEASE_EXPIRED` then `2/SUCCEEDED`; final `DISPATCHED`; worker B exits 0. All cross-worker ordering uses the database clock and Docker's `FinishedAt`, never a host clock or an in-process observer. **The production runtime needed no change** — the C1 state machine passed on the first complete run, and the only defects found were two of my own in the test (reading stdout hid Nest's stderr fatal line; one assertion targeted a warning that is unreachable once the fatal closer aborts the shutdown signal first). Test-only `process-test` Docker target; the production `runner` image copies nothing from it and normal Compose never references it. No dependency, no migration, no schema change. Report: [`reports/APP2-I02-FD1-FINAL-VERIFICATION-REPORT.md`](./reports/APP2-I02-FD1-FINAL-VERIFICATION-REPORT.md). **`APP2-B01-G01` = `COMPLETE — ENTRY_GATE_CLOSED`** — the asset-intake entry gate, executed under `APP2-B01` ownership and explicitly **not** a third storage correction (`APP2-DEC-STORAGE-C3` must not be created). It closes `STORAGE-BLK-01/02/03` (`RESOLVED_BY_APP2-B01-G01`) and `UPLOAD-POLICY-BLK-01` (`RESOLVED_BY_PRODUCT_OWNER`). Binding Product Owner policy: **25 MiB / 26 214 400 bytes** API-authoritative maximum raster product image; accepted media exactly `image/png`, `image/jpeg`, `image/webp`; **SVG rejected**; non-image embroidery source files excluded from APP2; originals retained while the asset exists (unpublish never deletes an original, no age-based deletion); a coarse **route-scoped 27 MiB / 28 311 552 bytes** gateway multipart ceiling that is not the file-size validator; **5-minute** API hard upload duration with strictly longer gateway timeouts; **15-minute** allocation TTL. Contract locked in [`ADR-APP2-001`](../adr/backend/ADR-APP2-001-OBJECT-STORAGE-AND-ASSET-INTAKE.md) §4.2f — a **two-stage identity** (immutable pre-stream request fingerprint in the `fingerprint` column + post-stream content fingerprint in the result, so a completed replay is content-complete only when it **resends and re-verifies the whole body** with zero object writes); a **versioned discriminated** `ASSET_UPLOAD_ALLOCATION`/`ASSET_UPLOAD_COMPLETED` result union with strict decoding and a state/result invariant (malformed → `IDEMPOTENCY_RESULT_INVALID`, safe 5xx, no object write, no transition); a **`claimToken`** rotated only on expired reclaim — preserving `assetId`/`objectKey` — and verified inside both Tx A and Tx B (stale → `STALE_UPLOAD_CLAIM`, nothing mutated), superseding the C2 delete-and-re-claim row that would have minted a second asset identity; and a strictly ordered reclaim that never deletes an existing durable asset and, when none exists, finishes cleanup and re-verifies the token **before** replacement streaming (cleanup failure accepts no object). `APP2-B01` is scoped to **exactly three operations** (Admin asset upload / detail / list), dropping the historical retry endpoint rather than padding a five-endpoint budget. Recorded reconciliations rather than silent fixes: the gate input's `PRODUCT_IMAGE` asset kind does not exist in the repository, so the locked `ASSET_KINDS` value **`CATALOG_MEDIA`** governs; and the http-level `client_max_body_size 20m` stays with the upload location overriding it upward to `27m` route-scoped. `NO_APP2_MIGRATION` re-confirmed — **9/9** disposable-PostgreSQL checks against the real 31-migration / 78-table schema, zero residue; all remaining shortfalls are **repository implementation gaps** (ADR §4.2f-7) owned by `APP2-B01`. Report: [`reports/APP2-B01-G01-COMPLETION-REPORT.md`](./reports/APP2-B01-G01-COMPLETION-REPORT.md). **`APP2-B01` = `READY — NOT STARTED`**; `APP2-W01` blocked by `B01`. **`APP2-B01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the three Admin asset operations (`adminAsset_upload` / `adminAsset_detail` / `adminAsset_list`): single-pass `busboy` streaming into the private `ORIGINALS` bucket with a bounded signature prefix settled before any byte reaches storage, an incremental authoritative 25 MiB counter and a server SHA-256 on the same bytes (no whole-file buffer anywhere); the pre-stream claim commits the allocation before a single file byte is read; a completed replay re-reads and re-verifies the whole resent body writing **zero** objects; an expired allocation is reclaimed by rotating the claim token in place and resuming from durable asset truth, never deleting an existing asset and always completing cleanup before a replacement stream; Tx A inserts-or-recovers as `UPLOADED` with no event, Tx B does the guarded `UPLOADED → INSPECTING` transition, appends **exactly one** `asset.inspection.requested` event and completes the record with its id, both verifying the claim token inside their own transaction; exactly-once not claimed. New `IdempotencyAllocationStore` (claim-with-allocation / locked read / expired renewal / guarded completion, expiry by database time) plus `AssetRepository` insert-or-recover, guarded transition and scoped keyset list. **No schema change, no migration**; only `busboy` `1.6.0` + `@types/busboy` `1.5.4` added. Nginx upload seam **activated** on one exact-match location (`27m`/`360s`/buffering off, values from the environment; global limits unchanged, MinIO unexposed). Three real streaming defects found by the live suite and fixed (errored sink destroyed without a listener crashed the process; `pipe` does not forward source errors so a disconnecting client emitted an unhandled `error`; a stalled client hung past its deadline because the reader only checked the abort signal on a chunk), plus a structural split of `AssetModule` into persistence and `AssetIntakeModule` after an existing DB7 suite exposed the coupling. 194 unit tests, **25 live PostgreSQL+MinIO cases covering all 30 required scenarios**, 27 API cases, 20 gateway cases including a real `nginx:1.27.3-alpine` render + `nginx -t`; `pnpm quality` `EXIT=0` (90/90 API suites, 1112 tests); OpenAPI `ae015dd6…`→`e19c2f76…`, client `89c1aace…`→`55de1cc1…` (+197/−0), DB 78/833/31 and Figma 69 unchanged — [`reports/APP2-B01-COMPLETION-REPORT.md`](./reports/APP2-B01-COMPLETION-REPORT.md). **`APP2-W01` = `READY — NOT STARTED`**. No upload, processing, publication or public product functionality yet. **`APP2-W01` entry gate returned `BLOCKED_BY_PROVEN_SCHEMA_OR_DOMAIN_GAP` (no commit, no file changed): `asset_derivatives.kind` had no non-watermarked catalog display kind — `PREVIEW_WATERMARKED` is bound to INV-22/BR-012 and the `ADR-APP2-001` §4.7 public preview contract, and `NORMALIZED` would redefine the artwork concept DB2 CON-042/CON-061 merged — so the map gains two prerequisites → **21 checkpoints**, order `APP2-DB01 → APP2-I03 → APP2-W01`.** **`APP2-DB01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — IMP-D030: adds the derivative kind **`CATALOG_PREVIEW`** (non-watermarked catalog display derivative of a `CATALOG_MEDIA` asset) and INV-22's first physical half `ck_asset_derivatives__watermark_by_kind` (**CST-126**: `PREVIEW_WATERMARKED → true`, `CATALOG_PREVIEW → false`; the other three kinds unconstrained) in forward-only migration **`0032`** with two preflight guards; aligns DB3 **LC-06 into two lanes** — the catalog-media lane prepares both rows at `PENDING`, transitions to `PROCESSING` through a guarded update (never inserted as `PROCESSING`), generates while the asset is `INSPECTING`, and writes both-`READY` + one inspection + `ACCEPTED` in one transaction, while the design/artwork lane keeps its after-`ACCEPTED` SE-013 timing; and locks the W01 job kind to **`ASSET_PROCESSING`** (the I02 seam already carries a handler-provided kind, so no runtime change). Migrations 31 → 32, CHECK 189 → 190, fingerprint `4ca56a59…` → `82864268…`; **78 tables / 833 columns unchanged**, no table/column added, uniqueness and READY/storage-key behaviour untouched, no historical row rewritten; OpenAPI `e19c2f76…`, client `55de1cc1…` and Figma 69 unchanged. 192 database + 112 persistence tests including a real upgrade run from the 0000–0031 baseline that preserves every seeded row and reproduces the fresh-install fingerprint — [`reports/APP2-DB01-COMPLETION-REPORT.md`](./reports/APP2-DB01-COMPLETION-REPORT.md). **`APP2-I03` = `READY — NOT STARTED`** (wire idempotent private-bucket bootstrap into the API and worker composition roots — `ensurePrivateBuckets` has no production caller); **`APP2-W01` = `BLOCKED_BY_APP2-I03`**; `APP2-A01` = `BLOCKED_BY_APP2-W01`. **`APP2-I03` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the private-bucket bootstrap is finally wired into both composition roots: `ensurePrivateBuckets` had existed since I01 but **no production caller** did, so a real deployment met its first upload with no bucket. The API resolves and awaits the bootstrap **before `app.listen`** (extracted into `start-api.ts` so the ordering is asserted, not described); on failure it never listens, closes the context exactly once and exits non-zero, with no retry loop. The worker gates its poll loop behind an abstract **`WORKER_STARTUP_GATE`** that the generic runtime injects but does **not** provide — the composition root binds it to the same bootstrap — awaited before `started`, so a closed gate issues no claim query at all; a closed gate is a *result*, not a throw, so the process stays up, reports `STARTUP_GATE_CLOSED` and `main.ts` closes the context once and exits non-zero. Bootstrap is memoized per process (concurrent callers share one attempt, never two `CreateBucket` calls), sticky on success, cleared on failure; four startup classes are mapped from the existing `ObjectStorageError` taxonomy, never from a message, and access-denied is never downgraded to unavailable. Logs carry only `component`/`operation`/`ready`/class. Readiness: the API adds **no** public field (listen *is* the storage gate, and a schema change would move the frozen OpenAPI artifact); worker readiness now requires policy + database + every gate + started. **No package correction was needed** — the existing `BucketAlreadyOwnedByYou` branch already makes a lost race a success. Proven live: a cross-process smoke starts the **built** API and worker back-to-back against an empty MinIO → both ready, both verified, **exactly two** private buckets, anonymous access refused, restart idempotent, zero residue (11/11); a worker-only smoke against a store with no buckets at all (9/9); plus 35 deterministic API cases and 11 new worker gate cases. All I02 regressions re-run green, including the **real Linux SIGTERM (exit 0 in 388 ms)** and the 16-case fatal-process isolation proof. Compose gives the worker the same storage configuration and orders both after a healthy MinIO (still no host port, still no Nginx route); the E2E edge gains an ephemeral tmpfs MinIO because the API no longer listens without a store. **Found and fixed a real production defect:** the shipped worker image never shipped `@embroidery/object-storage`, so it could not start — the same class of image defect I02-C1 and DB01 found before it. No migration, no schema change, no new external dependency (one workspace link), OpenAPI/client/Figma unchanged; `pnpm quality` `EXIT=0` (API 1136, worker 141, persistence 112, database 192, object-storage 150+20) — [`reports/APP2-I03-COMPLETION-REPORT.md`](./reports/APP2-I03-COMPLETION-REPORT.md). **`APP2-W01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the catalog asset inspection worker: one production handler for `asset.inspection.requested` filed under the domain kind **`ASSET_PROCESSING`** (not the transport `OUTBOX_DISPATCH`), effect key `asset-inspection:v1:{assetId}`, registered in `onModuleInit` so it is in the registry before the poll loop issues its first claim. `sharp@0.35.3` exact (Apache-2.0, libvips 8.18.3) added to `apps/worker` only — no `@types/sharp`, no second image library, nothing added to API/Admin/Storefront/object-storage. `ASSET_PROCESSING_POLICY_V1` is a frozen worker-owned object (PNG/JPEG/static WebP; `failOn: warning`, `unlimited: false`, `sequentialRead: true`, `animated: false`; 12,000 × 12,000, 40 MP, 4 channels, 1 page), and the two outputs are WebP `q82`/`alphaQuality 100`/`effort 4`/`smartSubsample` inside 480² and 1920² with `fit: inside` + `withoutEnlargement` — auto-oriented, sRGB, alpha preserved, every metadata block stripped, both `THUMBNAIL` and `CATALOG_PREVIEW` unwatermarked per DB01/CST-126. Durable preparation commits first (both rows inserted `PENDING`, then guarded `PENDING → PROCESSING`, never inserted as `PROCESSING`), external work happens outside every transaction, and one terminal transaction writes both `READY` rows + exactly one `ACCEPTED` inspection + the guarded `INSPECTING → ACCEPTED` together; an unpublishable image is a **business rejection** (both rows `FAILED`, one closed code, original retained), never a dead letter, and on the final attempt a retryable failure becomes `PROCESSING_RETRY_EXHAUSTED` so nothing sits in `INSPECTING` forever. Terminal replay writes zero objects and zero rows; a contradiction fails closed with `JOB_INVARIANT_VIOLATION` and is never repaired. **Nothing is buffered**: four bounded reads per attempt (integrity, metadata, one per output), each piped into the encoder and into managed multipart — no full-file `Buffer`, no `toBuffer`, no temp file. The V1 inspection detail is an allow-list in the existing nullable `text` column, bounded at 8 KiB, so no key/filename/EXIF/stack can reach it. **Five real defects were found by these tests and fixed before Commit A** — an upload that failed to open stalled the pipeline on backpressure; a decode failure was reported as the storage failure it cascaded into; contradictions raised outside the processing block escaped unclassified and were therefore *retried*; a cleanup failure bypassed the final-attempt rule; and `DrizzleRepository.run`'s error mapper flattened every contradiction into a generic `PersistenceError`, turning a fail-closed stop into a retry (only visible against a real database — the unit doubles are not wrapped by the mapper at all). 24 worker suites / **283** unit tests, **4 live PostgreSQL+MinIO suites / 52 tests covering all 40 required scenarios** with recovery states produced through real failure seams, and a **shipped-image Linux smoke** (12 tests: gate opens, Sharp musl binary loads, exactly 1 handler, claimed under `ASSET_PROCESSING`, asset `ACCEPTED`, both derivatives `READY`, both private objects, one inspection, outbox `DISPATCHED` with one `SUCCEEDED` attempt, no key/checksum/secret/`requestId` in the logs, real SIGTERM → **exit 0 in 309 ms**, zero residue). All I02/I03 regressions green (signal smoke exit 0 in 320 ms; 16-case fatal-process proof). **No migration, no schema change, no HTTP operation, no forbidden-area change**; OpenAPI `e19c2f76…`, client `55de1cc1…`, DB 78/833/190/`82864268…` and Figma 69 all unchanged; `pnpm quality` `EXIT=0` — [`reports/APP2-W01-COMPLETION-REPORT.md`](./reports/APP2-W01-COMPLETION-REPORT.md). **`APP2-A01` = `READY — NOT STARTED`**; `APP2-B02` = `BLOCKED_BY_APP2-A01`. **`APP2-A01` entry gate returned `BLOCKED_BY_MISSING_ASSET_LIST_CONTINUATION_DESIGN` (no commit, no file changed):** `adminAsset_list` returns `hasNext`+`nextCursor` (`limit` ≤ 100, no delete API), but none of the seven approved frames, the handoff-notes node `450:404`, the registry, the D01 reports or the phase plan defined a continuation interaction — proven from node metadata, since the Uploading/Processing/Rejected frames clip their grid at the 1024 px frame bottom; page-1-only would silently discard pages and inventing a control would be self-approved design. The same audit reproduced a second gap (every approved card/row displayed a **source filename** B01 neither persists nor exposes) and **cleared** two candidate blockers (thumbnails are honest placeholder glyphs, so no media-delivery contract is required; the side nav locks a dedicated Assets destination, so `/assets` is not invented). **`APP2-D02` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the narrow design-reconciliation checkpoint curing both gaps, **design and documentation only**: IMP-D031 locks an explicit user-triggered **`Tải thêm tài sản`** control shown only when `hasNext = true` (append, preserve items and scroll; loading = disabled + `Đang tải thêm…` + `aria-busy` + one polite announcement; failure keeps items and offers `Thử lại` on the **same** cursor; no control at `hasNext = false`; **no total-count copy** — the API exposes no total; infinite scroll, viewport-triggered loading, offset/page-number paging and silent truncation all rejected) and a **server-backed identity** (`image/png → Ảnh PNG`, `image/jpeg → Ảnh JPEG`, `image/webp → Ảnh WebP`, unknown → `Tài sản hình ảnh`; secondary line `{size} · {createdAt}` in `vi-VN`, `dd/MM/yyyy, HH:mm`) with **no backend field added and no filename fabricated** — `File.name` stays transient local upload state. Applied in place to the eight existing Admin Assets nodes (desktop Default grew 1024 → 1092 so the control is visible, not clipped; mobile rows 80 → 95 for the second line); the seven `FIG-ADMIN-ASSETS-*` rows are promoted to `APPROVED_FOR_IMPLEMENTATION` under **`FIG-APPROVAL-APP2-D01-ADMIN-001`** ([`../design/approvals/APP2-D01-ADMIN-ASSETS-DESIGN-APPROVAL.md`](../design/approvals/APP2-D01-ADMIN-ASSETS-DESIGN-APPROVAL.md)), closing the divergence between the frozen ruling and the rows, and one annotation node `FIG-ADMIN-ASSETS-CONTINUATION-IDENTITY` (`484:272`) is registered — **registry 69 → 70**; design authority is now `PRODUCT_OWNER_APPROVED — FROZEN — D02_RECONCILED`; map now **22 checkpoints**; OpenAPI `e19c2f76…`, client `55de1cc1…` and DB 78/833/190/`82864268…` unchanged — [`reports/APP2-D02-COMPLETION-REPORT.md`](./reports/APP2-D02-COMPLETION-REPORT.md). **`APP2-A01` = `READY — NOT STARTED`** (unblocked); `APP2-B02` = `BLOCKED_BY_APP2-A01`. **`APP2-A01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-28) — the Admin asset library at `/assets` inside the accepted APP1 shell, implementing IMP-D031 exactly (explicit `Tải thêm tài sản` only when `hasNext` and a usable cursor, append + same-cursor retry + one polite announcement, no total-count copy; identity from `mediaType` with a `{size} · {createdAt}` `vi-VN` line and **no filename anywhere**). Consumes only the three accepted B01 operations: generated multipart with ordered parts + `Idempotency-Key` + `AbortSignal` + real transferred-byte progress (never 100% before the HTTP result), honest ambiguous cancellation that reuses the same key, and detail-only processing reconciliation on one `ASSET_PROCESSING_POLL_INTERVAL_MS = 3000` constant that stops at any terminal or uninterpretable status. Thumbnails stay honest placeholders — APP2 exposes no media-delivery operation. Shell navigation gains its first real destination (links + pathname-derived current). 77 new tests (220 Admin total), `pnpm quality` `EXIT=0`; OpenAPI `e19c2f76…`, client `55de1cc1…`, DB 78/833/190/`82864268…` and Figma registry 70 all unchanged; **no new dependency** — [`reports/APP2-A01-COMPLETION-REPORT.md`](./reports/APP2-A01-COMPLETION-REPORT.md). Reviewer decision open: the approved Rejected-state action `Xoá khỏi danh sách` has **no backend capability** and is deliberately not implemented. **`APP2-B02` = `READY — NOT STARTED`**; `APP2-A02` = `BLOCKED_BY_APP2-B02`. **`APP2-B02` entry gate (2026-07-28):** the B02 pre-code audit blocked as **`BLOCKED_BY_PRODUCT_DRAFT_FIELD_DECISION`** (no commit, no tracked file changed) — `products` carries 10 NOT NULL columns with no default, and after DB7's four locked rules five mandatory values had none: `category_id`, `slug`, `base_price_amount`, `display_order`, `product_media.role`; `category_id` was unsatisfiable because zero categories existed, no category operation exists and B02 may not add a sixth. **`APP2-B02-G01` = `COMPLETE — ENTRY_GATE_CLOSED`** locks **IMP-D032** (fixed `thu-bong`/`khan`/`quan-ao`/`khac` taxonomy addressed by `categorySlug`, server-owned immutable product slug, `DRAFT` price sentinel `0` VND, draft `display_order = 0`, first media `THUMBNAIL` + rest `GALLERY`, `DETAIL` excluded) and provisions the taxonomy in **data-only migration `0033`**: 32 → 33 migrations with **78 tables / 833 columns / 190 CHECKs and fingerprint `82864268…` unchanged**, OpenAPI `e19c2f76…`, client `55de1cc1…`, Figma 70 and dependencies unchanged — [`reports/APP2-B02-G01-COMPLETION-REPORT.md`](./reports/APP2-B02-G01-COMPLETION-REPORT.md). **`APP2-B02` = `READY — NOT STARTED`** with its five-operation scope intact; `FU-APP2-CATEGORY-MANAGEMENT-01` = `DEFERRED_BEYOND_CATALOG_ALPHA` and `FU-APP2-THUMBNAIL-01` = `ROUTED_TO_APP2-T01`, both nonblocking. **`APP2-B02` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-31) — the Catalog Draft backend: exactly five Admin operations (`adminProduct_list/detail/create/update/archive`), no sixth and **no category endpoint**. Implements IMP-D032 as locked: `categorySlug` on the wire (closed enum, resolved in-transaction, **category UUID never exposed**), server-owned **immutable** slug (a rename never moves the public address), `DRAFT` price sentinel `0` VND rendered as whole đồng (`numeric(14,2)` returns `0.00`; VND has no minor unit), `display_order 0` with no `max+1`, and complete all-or-nothing ordered media replacement where the first Asset is the single `THUMBNAIL`, the rest `GALLERY` and `DETAIL` is never written. Concurrency uses `updated_at` **truncated to milliseconds** — the precision of the ISO token a client can echo; the raw microsecond comparison failed every guarded write. Archive is a guarded `DRAFT → ARCHIVED` that deletes nothing (`PUBLISHED` is a safe 409 because unpublication is B03's). **No ownership column exists** on `products`, so authentication is the scope and no audit actor is storable — recorded, not assumed. 88 new tests (53 unit, 21 live-PostgreSQL, 14 API, 6 gateway); `pnpm quality` `EXIT=0`; OpenAPI `e19c2f76…` → `b789cc99…` (7 → 10 paths, 8 → 13 operations) and client `55de1cc1…` → `66d1c991…`, additions only; **database baseline, Figma (70) and dependencies unchanged; no migration** — [`reports/APP2-B02-COMPLETION-REPORT.md`](./reports/APP2-B02-COMPLETION-REPORT.md). **`APP2-A02` and `APP2-A03` = `READY — NOT STARTED`**; `APP2-B03` = `BLOCKED_BY_APP2-B02`; `APP2-T01` stays `ROUTED — NOT PLANNED_FOR_EXECUTION`. **`APP2-B02-C1` (2026-07-31) — security & concurrency correction, `APP2-B02` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`.** Three reviewer defects, each reproduced before being fixed: mutations carried `StaffOriginGuard` but **not** `StaffJsonBodyGuard` (never exported from `IdentityModule`), so a `text/plain` post answered **400 instead of 415** — all three now carry the canonical `AuthenticatedAdminGuard + StaffOriginGuard + StaffJsonBodyGuard`; `updated_at` truncated to milliseconds was comparable but **not monotonic**, so two writes in one millisecond published the same token and a stale value still matched — the database now computes `GREATEST(date_trunc(ms, clock_timestamp()), date_trunc(ms, updated_at) + 1ms)` and returns it, with **no application time anywhere on the write path**; and the **invented 12-item media limit** is removed (it existed only to bound a per-item loop, had no product authority, and never reached OpenAPI). The fourth invariant is now proved: validation is one transaction-scoped `FOR SHARE` batch read, so a selected Asset **cannot leave `ACCEPTED` between validation and the media write** — two real transactions, rival transition fails with SQLSTATE `55P03` while the lock is held and succeeds after commit. Description semantics locked (absent = unchanged, `null`/blank = clear). 19 new tests (132 catalog tests total); `pnpm quality` `EXIT=0`; OpenAPI `b789cc99…` → `c4d1fef8…` and client `66d1c991…` → `3e3e267d…` — **paths and operation IDs unchanged**, the only diff being the description wording and the 403/415 the guards produce; DB baseline, Figma (70) and dependencies unchanged — [`reports/APP2-B02-C1-CORRECTION-REPORT.md`](./reports/APP2-B02-C1-CORRECTION-REPORT.md). **`APP2-B02-C2` = `MUST_NOT_BE_CREATED`**; `APP2-A02` and `APP2-A03` = `BLOCKED_BY_PRODUCT_OWNER_DESIGN_APPROVAL`. **`APP2-A02` blocked, then cured by `APP2-D03` (2026-07-31).** The A02 gate stopped before any code — no commit, no tracked change — as `BLOCKED_BY_CATALOG_LIST_DESIGN_CONTRADICTION`: the canonical A02 scope locks **filters**, but none of the three frozen Catalog nodes had a status or category filter, and their `Tạo sản phẩm` / `Chỉnh sửa` / `Xuất bản` / `Gỡ xuất bản` controls belong to `APP2-A03`/`APP2-A04`, whose routes do not exist yet. `APP2-D03` reconciles the three nodes in place (IMP-D033): `Trạng thái` and `Danh mục` filters added (desktop side-by-side, mobile stacked full-width), `Tải thêm sản phẩm` continuation reusing the `APP2-D02` cursor pattern, every create/edit/publication control removed so A02 is a truthful **read-only** list, and the empty state's dead CTA replaced. Price and search stay out of A02; A03 restores create/edit and A04 adds publish/unpublish as supplements. The A02/A03 label inversion in phase-plan §6.2 and annotation `450:404` is corrected. Three Catalog rows promoted under `FIG-APPROVAL-APP2-D03-CATALOG-LIST-001`, registry 70 → 71; OpenAPI, generated client and database unchanged — [`reports/APP2-D03-COMPLETION-REPORT.md`](./reports/APP2-D03-COMPLETION-REPORT.md). **`APP2-D03` = `COMPLETE — DELIVERED_FOR_REVIEW`**, so **`APP2-A02` = `READY — NOT STARTED`**. **`APP2-A02` delivers the read-only Admin Product List** at `/products`: `Trạng thái`/`Danh mục` filters with URL-owned state, `Tải thêm sản phẩm` cursor continuation with same-cursor retry, a semantic desktop table and a mobile card collection, and four truthful list states (loading, unavailable, unfiltered-empty, filtered-empty). `adminProduct_list` only — create/detail/update/archive are deliberately not re-exported from `@embroidery/api-client`, so no mutation is reachable before the checkpoint that owns it; no price, no search, no action control, honest media placeholder pending `APP2-T01`. 50 new component tests; production Admin reviewed behind the real gateway at 1440 and 390. OpenAPI, generated client, database and Figma registry unchanged; no dependency added — [`reports/APP2-A02-COMPLETION-REPORT.md`](./reports/APP2-A02-COMPLETION-REPORT.md). **`APP2-A02` = `COMPLETE — DELIVERED_FOR_REVIEW`**, so **`APP2-A03` = `READY — NOT STARTED`**. **`APP2-A03` then blocked on its mandatory pre-code audit** (`BLOCKED_BY_PRODUCT_FORM_DESIGN_CONTRACT_CONTRADICTION`, no source/commit/Figma edit): all five approved Product Draft nodes were one `Sản phẩm mới` create screen with a single atomic `Lưu bản nháp`, while `adminProduct_create` is `.strict()` and accepts only `categorySlug`/`name`/`description` — so media needed a second PATCH, `Phiên bản & SKU` matched no B02 field at all, there was **no edit/detail design**, and `basePriceAmount` had no approved surface. **`APP2-A03-G01`** — a narrow entry gate under A03 ownership, **not `APP2-D04`**, which must not be created — reconciles the five existing nodes in place into a two-mode capability: minimal create at `/products/new` (three POST fields, `Tạo bản nháp`, redirect to detail) and edit/detail at `/products/{productId}` (`Lưu thay đổi`, `Giá cơ bản`, read-only slug/status, ordered media via PATCH). Variants/SKU removed and deferred (`FU-APP2-PRODUCT-VARIANTS-SKU-01`); publication-readiness rail and `Tới bước xuất bản` removed (A04/B03); fabricated filenames replaced by B01/D02 server identity; keyboard media ordering added. IMP-D034 locked, five rows promoted under `FIG-APPROVAL-APP2-A03-G01-PRODUCT-FORM-001`, registry 71 → 72; OpenAPI, generated client, database and application source unchanged — [`reports/APP2-A03-G01-COMPLETION-REPORT.md`](./reports/APP2-A03-G01-COMPLETION-REPORT.md). **`APP2-A03-G01` = `COMPLETE — CORRECTED (C1) — REVIEW_ACCEPTED`** (the G01 promotion was spliced onto the document title instead of the canonical rows, leaving them `REVIEW_REQUIRED` behind a gate blind to rows outside a table; `APP2-A03-G01-C1` repaired the registry and added `document-title` / `row-placement` / `a03-approval` invariants — [`reports/APP2-A03-G01-C1-CORRECTION-REPORT.md`](./reports/APP2-A03-G01-C1-CORRECTION-REPORT.md)). **`APP2-A03` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** — Admin Product form/detail delivered against that authority: minimal POST create at `/products/new` redirecting to the authoritative `productId`, PATCH-capable edit/detail at `/products/{productId}` sending `expectedUpdatedAt` plus only changed fields (description absent-vs-`null`, whole-VND string price with the `"0"` sentinel, read-only slug/status), ordered `mediaAssetIds` with keyboard reordering and a cursor-paginated `ACCEPTED`-only asset picker, `PRODUCT_VERSION_CONFLICT` reload dialog with no force-save, and unsaved-change protection; A02 regains exactly `Tạo sản phẩm` and `Chỉnh sửa` while `adminProductArchive` stays off the client boundary. 403 admin tests green twice, production build reviewed live at 1440/390; OpenAPI, generated client, database and Figma unchanged — [`reports/APP2-A03-COMPLETION-REPORT.md`](./reports/APP2-A03-COMPLETION-REPORT.md). **Corrected by `APP2-A03-C1`** (plaintext development credential removed from evidence history and the credential rotated + reconciled to one authenticating credential; new canonical `pnpm check:secrets` gate; only the exact `PRODUCT_VERSION_CONFLICT` code opens the stale-version dialog, so a lifecycle or media-eligibility `409` no longer invites a reload that discards unsaved edits) — [`reports/APP2-A03-C1-CORRECTION-REPORT.md`](./reports/APP2-A03-C1-CORRECTION-REPORT.md). **`APP2-B03` blocked at its representability gate and was cured by the entry gate `APP2-B03-G01`** — accepted LC-04 authority had no exit from `PUBLISHED` except archive, so unpublish had no canonical transition; IMP-D035 adds **`TR-LC04-05` `PUBLISHED → DRAFT` (Unpublish Product)**, removing public visibility without archiving or deleting and returning the product to the editable draft state, with archive kept distinct as `PUBLISHED → ARCHIVED` (LC-04 4 TR → 5 TR; new `pnpm check:lifecycle` gate; no schema, source, OpenAPI, client or Figma change). **`APP2-B03-G01` = `COMPLETE — DELIVERED_FOR_REVIEW`**, **`APP2-B03` = `READY — NOT STARTED`** — [`reports/APP2-B03-G01-COMPLETION-REPORT.md`](./reports/APP2-B03-G01-COMPLETION-REPORT.md). **`APP2-B03` = `COMPLETE — DELIVERED_FOR_REVIEW`** — three Admin publication operations (`adminProduct_publicationReadiness`, `adminProduct_publish` `TR-LC04-01`, `adminProduct_unpublish` `TR-LC04-05`) with a closed seven-code readiness set, a single locked transaction that re-evaluates readiness rather than trusting the readiness GET, the B02-C1 monotonic `updated_at` token, and atomic `product.published`/`product.unpublished` audit + outbox evidence. No migration; OpenAPI 10 → 13 paths / 13 → 16 operations, generated-client additions only; database (33/78/833/190) and Figma (72/72) unchanged. Report: [`reports/APP2-B03-COMPLETION-REPORT.md`](./reports/APP2-B03-COMPLETION-REPORT.md). **`APP2-A04` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the Admin publication interaction at `/products/[productId]/publication`, consuming exactly those three operations through three new hand-written client exports (archive still withheld); a lifecycle action is offered only from a coherent detail+readiness snapshot and sends `{ expectedUpdatedAt }` from it, nothing is optimistic, all seven requirement codes render in server order with an unknown code never counted as satisfied, refusals are classified by exact domain code (only `PRODUCT_VERSION_CONFLICT` opens the reload dialog), unpublish is independent of readiness behind a `role="alertdialog"` confirmation, and the bare server slug is shown as read-only metadata because `450:404` marks `/san-pham/<slug>` `CHƯA CHỐT`; no API, OpenAPI, generated-client, database or Figma change ([`reports/APP2-A04-COMPLETION-REPORT.md`](./reports/APP2-A04-COMPLETION-REPORT.md)). **Corrected by `APP2-A04-C1` = `COMPLETE — DELIVERED_FOR_REVIEW`** — closes the missing production-runtime evidence: an ephemeral `runner`-stage image serves the real gateway as the `admin` upstream (zero bind mounts, `NODE_ENV=production`, `node apps/admin/server.js`), isolated from the active dev `.next` by `.dockerignore` and swapped through a temp-dir Compose override so no tracked Nginx/Compose file changes; direct route load, hard refresh, asset delivery, console cleanliness, a real `PRODUCT_VERSION_CONFLICT` and every desktop scenario pass, dev stack and mutable Product state restored exactly. **Retracts A04's `.next`-collision root cause** — `apps/admin/.next` is not bind-mounted and a host build provably does not disturb the dev container. The smoke's first run found a real defect A04 had recorded as passing — `a.product-publication__back` was a **16 px** touch target at 390 px — which the reviewer ruled in scope; it is repaired with the existing `$size-touch-target-min` token (label, `<Link>` semantics and route unchanged), and the mobile assertion now sweeps blocked DRAFT, ready DRAFT, PUBLISHED and the open dialog with a frozen single-entry exclusion list so it cannot be narrowed past a real failure. Final smoke **11/11 harness, 20/20 browser** ([`reports/APP2-A04-C1-CORRECTION-REPORT.md`](./reports/APP2-A04-C1-CORRECTION-REPORT.md)). **`APP2-A04-C2` = `MUST_NOT_BE_CREATED`**. **`APP2-B04` then blocked at its own entry gate — `BLOCKED_BY_PUBLIC_MEDIA_DELIVERY_CONTRACT_GAP`**: `Q-01`/`Q-02` require public product images, but no route in the repository served image bytes (16 operations, none public or binary; `apps/api` never called `getObjectStream`; `ADR-APP2-001` §4.7 designs the delivery it never built). `PUBLIC_CATALOG_SCHEMA_GATE` passed — only the transport was missing — so the Product Owner activated **`APP2-T01` as a mandatory predecessor of `B04`** (IMP-D036). **`APP2-T01` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW` (production-runtime evidence supplied by `APP2-T01-C1`, which also reproduced and fixed a production-only module-resolution defect that made the API production image unstartable — [`reports/APP2-T01-C1-CORRECTION-REPORT.md`](./reports/APP2-T01-C1-CORRECTION-REPORT.md); `APP2-T01-C2` = `MUST_NOT_BE_CREATED`)** — one anonymous binary operation `publicProductMedia_get`, publication re-proved per request in one bounded query, `getObjectStream` streaming with client-disconnect teardown, `image/webp`+`nosniff`+`inline`+`no-store`, and the shared `buildPublicProductMediaPath` helper for B04/S01/S02; OpenAPI 13→14 paths / 16→17 operations, no migration, no Figma, no Admin/Storefront/worker/gateway change ([`reports/APP2-T01-COMPLETION-REPORT.md`](./reports/APP2-T01-COMPLETION-REPORT.md)). **`APP2-B04` = `COMPLETE — DELIVERED_FOR_REVIEW` — two anonymous public operations `publicProduct_list` and `publicProduct_detail`, OpenAPI 14→16 paths / 17→19 operations, generated-client additions only, `NO_APP2_B04_MIGRATION`; `no-store` until a cache-invalidation consumer exists, and one recorded ADR-DB5-001 pagination conflict ([`reports/APP2-B04-COMPLETION-REPORT.md`](./reports/APP2-B04-COMPLETION-REPORT.md)) — **corrected by `APP2-B04-C1` = `COMPLETE — DELIVERED_FOR_REVIEW`**, so **`APP2-B04` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** and **`APP2-B04-C2` = `MUST_NOT_BE_CREATED`**: the Product Owner ruled **keyset** (IMP-D037), `ADR-DB5-001` gained **R10** and the DB5 query-shape/pagination/access-path/completeness/EXPLAIN/handoff documents were reconciled, with the prior offset classification preserved as dated history. The access path was measured on a disposable PostgreSQL 16.14 rather than asserted (`pnpm explain:q01`, [`../database/DB5_Q01_ACCESS_PATH_EVIDENCE.md`](../database/DB5_Q01_ACCESS_PATH_EVIDENCE.md)): both delivered forms sequential-scan and sort at MVP cardinality and are accepted, continuation pages eliminate rows at the scan and the thumbnail subquery is bounded by the page — and **IDX-065 orders the listing only from a `category_id`-constant form the public contract does not issue**, which is recorded precisely and routed to DB10 instead of being fixed inside frozen B04 source. New gate `pnpm check:pagination-authority`; no index, migration, application, OpenAPI, generated-client, Figma or dependency change ([`reports/APP2-B04-C1-CORRECTION-REPORT.md`](./reports/APP2-B04-C1-CORRECTION-REPORT.md))**, **`APP2-S01` = `READY — NOT STARTED`** (design authority **UI02 Discover Feed**, `REUSE_AND_SUPPLEMENT_ONLY`, 5/3/2-column masonry with linear DOM reading order) and **`APP2-S02` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** — Product Detail delivered at `/san-pham/[slug]` (server-first, `publicProductDetail` only, ordered catalog-preview gallery with an accessible lightbox, one 640px description section, browser-local share, S01 card upgraded to one semantic link; 99/99 production browser scenarios twice; corrected by `APP2-S02-C1`, which set the mobile content band to 24px/342px with a feature-local inset (the APP1 shell is untouched) and locked the streamed not-found transport as `SAFE_STREAMED_NOT_FOUND` — measured 200 with `noindex`, no Product canonical/metadata/data and five indistinguishable causes (IMP-D040); open non-blocking follow-ups `FU-APP2-DETAIL-NOT-FOUND-STATUS-01` (`ROUTED — FRAMEWORK_TRACKING — NONBLOCKING_AFTER_C1`), `FU-APP2-STOREFRONT-CONTENT-BAND-01` and `FU-APP1-SHELL-BRAND-TOUCH-TARGET-01`, [`APP2-S02-COMPLETION-REPORT.md`](./reports/APP2-S02-COMPLETION-REPORT.md), correction [`APP2-S02-C1-CORRECTION-REPORT.md`](./reports/APP2-S02-C1-CORRECTION-REPORT.md); `APP2-S02-C2` = `MUST_NOT_BE_CREATED`); it had been `READY — NOT STARTED` since `APP2-S02-G01`** — it was `BLOCKED_BY_UI03_RECONCILIATION` because `APP2-D01-STOREFRONT-PRODUCT-DETAIL` was `NOT_APPROVED — WITHHELD_PENDING_UI03_RECONCILIATION` with reuse policy `SEPARATE_RECONCILIATION_REQUIRED` against UI03 `261:1290` / `262:1291` / `273:1409` / `279:1504`, and backend completion does not lift a design blocker; **`APP2-S02-G01` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`** cured it by reconciling that draft against the delivered `publicProductDetail` contract — all twelve UI03 roots audited live and preserved as `HISTORICAL_DRAFT_SOURCE`, a new sibling section `529:2224` approved under `FIG-APPROVAL-APP2-S02-G01-PRODUCT-DETAIL-001`, the detail route locked to **`/san-pham/[slug]`** and the scope locked to gallery + lightbox, name/category identity, exactly one description section, browser-local share and Discover continuation, with **no price or stock display** and materials/process/related/save/commission deferred because the contract does not represent them (IMP-D039, new gate `pnpm check:storefront-product-detail-authority`, [`APP2-S02-G01-COMPLETION-REPORT.md`](./reports/APP2-S02-G01-COMPLETION-REPORT.md)); **`APP2-E01` = `BLOCKED_BY_APP2-S01_AND_APP2-S02`**; `FU-APP2-ADMIN-MEDIA-PLACEHOLDER-01` = `DEFERRED — NONBLOCKING_FOR_APP2-B04`; `FU-APP2-PRODUCT-ARCHIVE-LIFECYCLE-01` = `ROUTED — NONBLOCKING_FOR_A04`, `FU-APP2-PRODUCT-ARCHIVE-UI-01` = `DEFERRED_PENDING_PRODUCT_OWNER_SURFACE_DECISION`. **`APP2-S01-G01` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the first `APP2-S01` attempt blocked at its route gate with `BLOCKED_BY_STOREFRONT_DISCOVER_ROUTE_DECISION` **having changed no file** (the Storefront had exactly one browser route, `/`, Homepage-owned; the shell's `discover` item was deliberately `route: null`; the approved 404 tagged *Khám phá tác phẩm* `Sắp ra mắt`; and no accepted document named a Discover path — a Figma label is not a URL contract). The Product Owner ruling **IMP-D038** locks the Discover route **`/kham-pha`** (no alias, no redirect; `/`, `/discover`, `/catalog`, `/products`, `/san-pham` rejected), category state `?category=<slug>` over `thu-bong`/`khan`/`quan-ao`/`khac`, the narrow S01 shell and not-found activation, and **explicitly authorizes staged non-interactive Product cards** so the unresolved Product Detail route no longer blocks the feed; `/san-pham/<slug>` stays `CHƯA CHỐT`/`NOT_CANONICAL` and `APP2-S02` stays `BLOCKED_BY_UI03_RECONCILIATION`. New gate `pnpm check:storefront-route-authority`; no application, OpenAPI, generated-client, database, Figma, infrastructure or dependency change ([`reports/APP2-S01-G01-COMPLETION-REPORT.md`](./reports/APP2-S01-G01-COMPLETION-REPORT.md)); `APP2-S01-C1` = `NOT_APPLICABLE`. **`APP2-S01` = `COMPLETE — DELIVERED_FOR_REVIEW`** — the anonymous Discover feed at **`/kham-pha`**, server-first (`force-dynamic`; the first keyset page is in the HTML, only continuation is hydrated), consuming `publicProductList` **only**, with UI02's masonry preserved as CSS multi-column so **DOM reading order stays linear** while the browser paints 5 / 3 / 2 columns — audited live against `208:2002` / `224:871` / `226:1038` and then **measured** in a production browser at 1440 / 1024 / 390 with identical DOM order at each. Cards are staged non-interactive articles per IMP-D038; price and the out-of-stock flag are dropped at the projection boundary; unsupported UI02 modules (refine/layout controls, style-and-theme chips, trending, related, recently-completed, collection prompt, soft commission) are omitted rather than faked. Category state is `?category=<slug>` over the four contract slugs, with an unknown or repeated value taking the approved not-found boundary. Continuation is opaque-cursor keyset gated on `hasNext` **and** a usable cursor, deduped by slug, with no auto-retry and an exact-cursor `Thử lại`. Narrow shell activation only: nav `discover` → `/kham-pha` with `aria-current`, and the approved 404's *Khám phá tác phẩm* recovery becomes a real link; the other four areas stay unrouted. Isolated production evidence (`pnpm smoke:app2-s01-discover:production`, twice): canonical Storefront `runner` image (`node apps/storefront/server.js`, `NODE_ENV=production`, **zero** bind mounts) behind the real gateway with a production API and disposable TLS PostgreSQL, 24 synthetic published products against **real** THUMBNAIL bytes, 24/24 orchestration + 43/43 browser scenarios, zero console errors, and DRAFT removal proved (product gone, media 404, no route cache). Storefront tests 17 suites / 118; focused S01 6 suites / 65 twice. **No backend, Admin, worker, schema, OpenAPI, generated-client, object-storage, Figma, tracked-infrastructure or dependency change** ([`reports/APP2-S01-COMPLETION-REPORT.md`](./reports/APP2-S01-COMPLETION-REPORT.md)); `APP2-E01` = `BLOCKED_BY_APP2-S02`. |
| APP3–APP12 | `NOT_STARTED` | Dependency-ordered per §3. |

APP0 closure is a **foundation** milestone, not a release: R0 closes at APP1 (`09-RELEASE-AND-MILESTONE-POLICY.md` §2) and production readiness remains APP12's (IMP-D015).
