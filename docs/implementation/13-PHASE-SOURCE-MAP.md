# Phase Source Map

## 1. Purpose

This map identifies the minimum authoritative document families that each application phase must audit. Exact database filenames and completion commits must be resolved from repository truth at phase start.

Every phase also reads:

- `CLAUDE.md`.
- Root `README.md`.
- `docs/implementation/README.md`.
- `docs/implementation/10-MASTER-APPLICATION-ROADMAP.md`.
- Its detailed phase file in `docs/implementation/phases/`.
- `docs/design/FIGMA_DESIGN_INDEX.md` — the canonical Figma registry (query it before classifying design work; see §4).
- Relevant ADRs.
- Current completion reports/handoffs from prerequisite phases.
- `docs/database/DB_ROADMAP.md` only when confirming the database baseline or when the phase has a persistence dependency; do not require every checkpoint to read the full database corpus.

There is no root `ROADMAP.md`; the canonical roadmaps are `docs/database/DB_ROADMAP.md` and `docs/implementation/10-MASTER-APPLICATION-ROADMAP.md`.

## 2. Minimum sources by phase

| Phase | Product/business sources | Architecture/development sources | Database/design sources |
|---|---|---|---|
| APP0 | Project charter, scope, NFR, acceptance principles | System architecture, repository structure, backend/frontend/local-development conventions | Final DB application handoff; design vision/system/Figma architecture for tokens and editor spike |
| APP1 | Admin operations, security, NFR | System architecture, backend/frontend conventions | Identity/staff/role/permission/audit repository contracts; APP1 design audit |
| APP2 | Product requirements, user journeys, Admin operations, SEO/content, security | Backend/frontend conventions | Asset/catalog/publication tables, guards and query catalogs; public/Admin Figma audit |
| APP3 | Design Studio spec, product requirements, lifecycle, security/abuse prevention; **IMP-D041 placement rulings (`APP3-G01`), IMP-D042 Template lifecycle LC-24 + Product `TR-LC04-06` (`APP3-G02`) IMP-D043 anonymous Design Session ownership/transport/retention, closing `O-008` and the `design_sessions` half of `DP-RET-01` (`APP3-G03`), and IMP-D044 editor-safe media, asset eligibility, complexity and canonical derivative-metadata authority (`APP3-G04`)** | System architecture, frontend/backend conventions | Template/design-session/version/document guards and queries; **the APP2-owned Figma closure baseline (`reports/APP2-CLOSURE-FIGMA-BASELINE.json`) — later phases append to the shared registry, never mutate an owned record**; **catalog product-side and embroidery-area placement geometry (`px_per_mm`, bounds, physical maxima) and the side-background asset relation**; **`product_media.role`, `asset_derivatives.kind` and `secure_access_grants.scope_kind` closed sets, which bound what `APP3-G01`/`G02`/`G04` may rule without a migration — `APP3-G04` measured that `asset_derivatives` carries **no** intrinsic-dimension, media-type or byte-size column, so IMP-D044 contributes those four columns to `APP3-DB01` rather than reusing append-only `asset_inspections.detail` evidence — **delivered by migration `0034` (`APP3-DB01`), together with the placement `code`/`retired_at`/`superseded_by_id` authority and the referenced-row protection triggers**; **the controlled Inter v4.1 font assets vendored by `APP3-F01` at `packages/design-document/assets/fonts/inter/4.1/` — two unmodified WOFF2 binaries, the exact upstream OFL-1.1 `LICENSE.txt`, and the provenance and Vietnamese-coverage manifests bound to their SHA-256; the binaries are frozen evidence and may not be re-encoded, subset or renamed, and the `fontId = inter` runtime registry that consumes them is reserved for `APP3-P01`**; **the canonical Design Document contract owned by `packages/design-document` (`APP3-P01`) — schema v1, the nine IMP-D044 complexity constants, the APP0-R01 quantization scale of 10,000, RFC 8785 canonicalization, the document-migration registry, and the browser-safe root versus Node-only `/server` hashing split; every API that accepts or persists a Design Document consumes this package and none may re-implement serialization or hashing (ADR-DB1-012 §1)** — **`APP3-P01-C1` keys the decoded-pixel budget and the unique-media budget on `assetId`, allows exactly one `derivativeId` per Asset inside one document, and rejects a conflicting pair with `DERIVATIVE_METADATA_MISMATCH`, restoring the IMP-D044 rule rather than amending it**; **the geometry semantics locked as `IMP-D045` (`APP3-G05`) — top-left origin with y down, clockwise degrees, rotation and scale about the untransformed local-box centre with scale first, column-vector `Mlocal = T(centre) x R x S x T(-half)`, group-local child coordinates composed parent-outermost, `CONSERVATIVE_TRANSFORMED_AABB` bounds, blocking boundary-inclusive containment and `product_sides.px_per_mm` as the sole conversion authority. These are part of the semantic interpretation of `schemaVersion = 1`: a v1 document carries no pivot marker, so changing any of them is a schema-semantic event, never a refactor. The three APP0-R01 renderer adapters are comparative research evidence and contradict each other on pivot and group inheritance; they are not production authority** — **corrected by `APP3-G05-C1`: the stroke is drawable geometry for every kind storing `strokeWidthPx`, expanding the local envelope by `strokeWidthPx / 2` before it is transformed, and the v1 `line` path is locked to `(0,0)`-`(width,height)` with cap/join as version-level constants rather than document fields** — **implemented by `packages/design-engine` (`APP3-P02`), which consumes only the public root of `packages/design-document` and owns every geometric consequence: matrices, effective transforms, stroke-aware envelopes, transformed AABBs, placement reconciliation, px to mm conversion, containment and physical size. No other package may re-implement them** — **`APP3-P02-C1` adds the structural half of that authority: an element has at most one direct group parent, so a repeated or contested claim makes the graph invalid rather than order-dependent, and no bounds, containment or physical-size result is produced from it** — **`APP3-B01` consumes both: the Product/Catalog placement module authors Product Sides and Embroidery Areas through the `design-engine` scale and containment rules and exposes the public manifest whose `studioEligible` is derived from an editor-safe `NORMALIZED` derivative carrying the IMP-D044 metadata quartet, never from object storage** — **`APP3-B01-C1` makes the placement concurrency contract explicit in the published API: the Admin read returns `updatedAt`, the replace requires `expectedUpdatedAt` in the Catalog `datetime({ offset: true })` format, success returns the fresh token and the public manifest exposes neither** — **IMP-D046 normalization dispatch (`APP3-G06`): editor-safe `NORMALIZED` production is **association-bound**, dispatched by one new event `asset.normalization.requested` appended in the same transaction as the Product Side, Template or Session association write and consumed through the existing Outbox and worker registry with no new queue, scheduler or migration; the payload names the association, never the profile, which the worker re-derives at claim time; `APP3-W01` is replanned into raster-only `APP3-W01A` plus Template-SVG `APP3-W01B`, with sanitizer selection deferred to `APP3-G07` so SVG stays authorized by IMP-D044 while operationally unavailable** — **`APP3-W01A` implements the raster half of that authority: one `ASSET_PROCESSING` handler on the existing registry consumes `asset.normalization.requested` schema version 1, derives the profile from the named association by id (pointer, owner and asset lane all proven), admits JPEG/PNG/WebP only under 10 MiB / 4096 px / 16,777,216 decoded pixels, measures the IMP-D044 quartet from the encoder and the counted output stream rather than the source Asset, and writes `READY` plus all four fields in one claim-guarded statement keyed on Asset + policy version; Template SVG returns the staged capability and no sanitizer exists**  — **`APP3-B01N` closes the dispatch loop: the accepted placement replace transaction appends `asset.normalization.requested` for every created or repointed Product Side background, after the Side write and inside the compare-and-set transaction, through the existing transactional Outbox; the event vocabulary is owned by `@embroidery/domain-types` and imported by both applications, which never import each other; and the checkpoint adds no HTTP path, operation or schema, no generated-client change and no migration** — **IMP-D047 Template SVG sanitizer authority (`APP3-G07`): DOMPurify `3.4.13` on jsdom `29.1.1`, both exactly pinned and both verified against the locked `node:22.14.0` runtime (the newest jsdom is refused because it requires `^22.22.2`); DOMPurify runs in the SVG namespace with HTML and MathML disabled and XML parsing retained and its `removed` list is diagnostic only; nine allowed elements, a closed attribute set, total refusal of URLs, CSS, references and fonts, a closed value/transform/path grammar with real path parsing (numbers canonicalized by `APP3-W01B-C1` to the shortest **binary64 round-trip** token and never to a fixed decimal precision budget), `viewBox`-derived metadata, four complexity limits, whole-file rejection as `UNSAFE_OR_UNSUPPORTED_TEMPLATE_SVG`, and a thirteen-step pipeline whose second serialization must equal the first; SVGO is an optimizer and never runs after sanitization**; **the platform Zod→OpenAPI publication foundation owned by `apps/api/src/platform` (`APP3-P03`) — `z.toJSONSchema` as the single conversion authority, the class-name DTO registry, and the document augmentation that refuses to emit an empty request body; every schema-backed HTTP body in the API is published through it and no feature may hand-decorate its own**; **the Design Session upload architecture and authorization staging locked by `APP3-G08` (`IMP-D048`) — one API-owned multipart streaming operation with no presign, no upload-intent/completion pair and no `ObjectStoragePort` change, `APP3-B06` replanned into the `APP3-B06A` authorization foundation and the `APP3-B06B` intake producer, Session bootstrap left with its existing owner `APP3-B07`, and the terminal-verdict inspection/normalization ordering defect routed to `APP3-W01C`**; **the anonymous Session authorization foundation delivered by `APP3-B06A` (`apps/api/src/modules/design`) — per-session cookie-name derivation, peppered-HMAC verification with timing-safe comparison, the single non-enumerating refusal and its cookie-clearing rule, the Origin/Fetch-Metadata policy, the `IMP-D043` PO-07 limits on the platform sliding-window limiter, and the reusable expected-revision CAS seam, all with zero HTTP operations**; **the anonymous Session bootstrap/clone/resume surface delivered by `APP3-B07` — one discriminated request contract, P01/P02 document authority, Studio-eligibility scope resolution through the provider-only `CatalogPlacementReadModule`, CSPRNG secret minting with HMAC-only persistence, and guarded single-winner rotation****; **the anonymous Session raster intake delivered by `APP3-B06B` — one multipart operation streaming browser→API→private storage with no presign, upload-intent or completion proof, the APP2 pipeline reused through an intake lane so the Admin ceiling and vocabulary are untouched while a guest is capped at 10 MiB and lands `CUSTOMER_UPLOAD`/`CUSTOMER_PRIVATE`, and one bounded transaction that proves the Session live by expected-revision CAS, insert-or-confirms the association and appends exactly one inspection and one `asset.normalization.requested` event addressed by the returned `designSessionAssetId`**; **Design Session autosave delivered by `APP3-B08` — one `PUT /api/public/design-sessions/:sessionId/document` operation replacing the working document under a single atomic compare-and-set (ACTIVE, unexpired, at the caller revision, revision +1), judged by the P01 prepare pipeline and the P02 placement and containment authorities against the Session own persisted Side and Area including their retirement, with placeable images restricted to measured READY NORMALIZED derivatives of Assets this Session uploaded or already referenced, decided from canonical persistence and never from object storage; a stale revision is 409 with nothing written and the client refetches rather than replaying, and autosave never extends the 30-day lifetime, never issues or rotates the credential, records no idempotency and appends no event — cadence remains owned by `APP3-S10` (corrected from `APP3-S11` by `APP3-ROADMAP-RECONCILIATION`) — **`APP3-B08-C1` publishes the canonical document itself**: the structural JSON Schema is generated from P01 TypeScript types by an author-time, dev-only generator and registered as OpenAPI components, so the contract and the generated client carry the real shape instead of an unbounded map, while P01 imperative validators remain the sole acceptance authority for everything TypeScript cannot express (NFC, non-empty strings, non-zero scale factors, value ranges, point-count bounds); **`APP3-P04` completes the contract on the response side** — the three public Session operations (create, resume, autosave) all return the value of one function, so they publish one shared `DesignSessionSnapshotResponse` component whose document references the same generated P01 graph, replacing three success responses that carried no schema at all and generated as `void`**; Design System and Studio Figma artifacts |
| APP4 | Business rules, security, Admin operations, user journeys | Backend conventions, NFR | Customer/contact/verification/secure-grant/notification contracts and guards; customer secure-flow design audit |
| APP5 | User journeys, business rules, order/design lifecycle, Admin operations | Backend/frontend conventions | Customer-owned-product/request/asset/moderation/transition ownership and guards; request-flow Figma audit |
| APP6 | Business rules, order/design lifecycle, security | Backend/frontend conventions | Design version/current pointer/approval/quotation/money contracts and immutability guards; review/quotation Figma audit |
| APP7 | Payment business rules, lifecycle, security, NFR | Backend conventions, provider ADR | Payment obligation/attempt/idempotency/order conversion guards, race tests and query paths; payment UX package |
| APP8 | Inventory/production lifecycle, Admin operations, NFR | Backend conventions, worker/queue ADR | Hold/reservation/order/production guards, lock order, concurrency and performance evidence; Admin operations package |
| APP9 | Payment/fulfillment/cancellation/refund rules, lifecycle, Admin operations | Backend/frontend conventions | Remaining obligation, shipping freeze, completion/refund guards; fulfillment design package |
| APP10 | Customer/privacy/admin operations, notification scope | Backend/frontend conventions | Customer merge/agreement/notification attempt guards and retention obligations; customer operations design audit |
| APP11 | SEO/content requirements, Storefront journeys, scope | Frontend conventions, performance/NFR | Gallery/content/asset/publication query paths; approved public UI and Design System |
| APP12 | Security, NFR, acceptance principles, all critical journeys | All architecture/development/operations docs | DB correctness/concurrency/performance/durability completion evidence; all approved design packages and phase reports |

## 3. Database handoff rule

At APP0 preflight, identify the final database stage status and canonical handoff files. Do not hard-code obsolete DB checkpoint assumptions into application code.

Application phases must preserve:

- Physical schema and migration immutability.
- Repository ownership.
- Transaction/application guards.
- Concurrency/lock-order behavior.
- Measured query/index decisions.
- Backup/restore/retention obligations.

## 4. Design evidence rule

A chat summary that “screens were designed” is not enough. Each phase must record exact Figma file/page/section/node references and approval status before classifying design as `REUSE`.

The authoritative source for those references is `docs/design/FIGMA_DESIGN_INDEX.md`. Therefore:

- Every phase design audit **queries the registry** before classifying design as `NONE / REUSE / SUPPLEMENT / NEW`.
- Every `REUSE` classification **cites exact registry IDs / direct node links** (not file- or page-level links). A surface with no `APPROVED_FOR_IMPLEMENTATION` registry entry is not proven `REUSE`.
- Every `NEW` / `SUPPLEMENT` design checkpoint **updates the registry** in the same checkpoint.
- Every frontend checkpoint **records the registry IDs it implemented** in its completion report.
