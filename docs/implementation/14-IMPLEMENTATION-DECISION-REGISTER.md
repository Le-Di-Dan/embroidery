# Implementation Decision Register

## 1. Locked decisions

| ID | Decision | Status |
|---|---|---|
| IMP-D001 | Use a hybrid model: phase-level waterfall design plus checkpoint-based engineering. | LOCKED |
| IMP-D002 | A phase does not automatically contain design work; classify it as `NONE`, `REUSE`, `SUPPLEMENT`, or `NEW`. | LOCKED |
| IMP-D003 | Required design is completed and reviewed as one coherent phase package, not coding-style checkpoints. | LOCKED |
| IMP-D004 | One backend checkpoint normally has 1–3 APIs and never more than 5 tightly related APIs. | LOCKED |
| IMP-D005 | One frontend checkpoint normally owns one screen or one bounded capability of a complex screen. | LOCKED |
| IMP-D006 | One checkpoint is one human review boundary; an agent stops after it and does not continue automatically. | LOCKED |
| IMP-D007 | Admin operational capability leads Storefront by at most one capability inside the same business phase. | LOCKED |
| IMP-D008 | NestJS Swagger/OpenAPI is mandatory for backend APIs. | LOCKED |
| IMP-D009 | Generate TypeScript types and Axios client from OpenAPI; write TanStack Query hooks manually. | LOCKED |
| IMP-D010 | Frontend styling uses global SCSS only, with one `main.scss` import per Next.js app and shared Sass modules. | LOCKED |
| IMP-D011 | CSS Modules, inline style, CSS-in-JS, Tailwind and shadcn/ui styling baseline are prohibited. | LOCKED |
| IMP-D012 | Use approved Design System tokens; never derive production colors from grayscale wireframes. | LOCKED |
| IMP-D013 | Application-era schema changes require a dedicated approved database-change checkpoint. | LOCKED |
| IMP-D014 | Phase plans are not execution prompts; create one concise checkpoint specification at a time. | LOCKED |
| IMP-D015 | Production readiness may be claimed only after APP12 passes and explicit production approval is given. | LOCKED |
| IMP-D016 | Jest is the selected unit/integration test runner and already exists across `apps/*` and `packages/*`. Do not introduce a second unit/integration runner without an ADR. | LOCKED |
| IMP-D017 | The shared Sass token/foundation package lives at `packages/styles` (workspace name `@embroidery/styles`); apps consume tokens from it and do not define their own token source. Set by APP0-S01A. | LOCKED |
| IMP-D018 | Runtime-bearing workspace packages consumed by the Node backend (`@embroidery/database`, `@embroidery/persistence`) are compiled to JavaScript (`tsc` → `dist`, with declarations); their `main`/`types`/`exports` point to `dist`. The API and worker must never resolve a workspace package to `src/*.ts` at runtime, and bundlers/TS runtime loaders (webpack, esbuild, tsx, ts-node) are not used as a substitute for compilation. | LOCKED |
| IMP-D019 | The generated OpenAPI artifact is committed at `packages/contracts/openapi/openapi.generated.json` and produced by `pnpm openapi:generate`; `pnpm check:openapi` is a non-mutating drift gate in the root `quality` chain. Operation IDs follow `<domainKey>_<methodKey>` derived from the controller class and handler method, validated for presence, format and uniqueness. Documented paths carry the real `/api` global prefix and the document declares no server URL. Runtime Swagger UI is served at `/api/docs`, gated by `API_DOCS_ENABLED`, defaulting to enabled outside production and **disabled in production**. Set by APP0-B01. | LOCKED |
| IMP-D020 | Request correlation uses the single header `X-Request-ID` with the allowlist `^[A-Za-z0-9._-]{1,64}$`, shared byte-for-byte by the Nginx gateway and the API and asserted by a drift test. The gateway owns normalisation (valid incoming value reused, otherwise its native `$request_id`) **and** is the sole owner of the response header; the API consumes the effective value, preserves it unchanged, falls back to `crypto.randomUUID()` when it is absent or outside the allowlist, and never echoes an invalid value. Application request context uses the Node built-in `AsyncLocalStorage` behind a singleton provider — no third-party CLS package and no request-scoped provider cascade — and carries only `requestId`. No alias header (`X-Correlation-ID`, `X-Trace-ID`) may be introduced. Set by APP0-B02. | LOCKED |
| IMP-D021 | The request actor is provider-neutral. Its authenticated kinds are the canonical audit actor kinds `ADMIN` / `CUSTOMER` / `SYSTEM` (matching `audit_events.actor_kind`, CST-072); `ANONYMOUS` is a request-view fallback only and is never persisted. An unauthenticated request is anonymous — no id, role or privilege, never `SYSTEM`. The actor is bound **once** per request via `RequestContextService.bindActor()` (the seam APP1's authentication layer uses); a second bind fails, with no silent overwrite or merge, and the bound value is rebuilt through its factory and frozen so no credential/token/claim/contact detail rides along. Audit metadata is an immutable `{ requestId, actor, occurredAt }` snapshot from an injected `AuditClock`; it persists nothing, logs nothing and carries no request/response/header/body/token/PII. Records already-locked APP0-B04 semantics; not a new decision. | LOCKED |
| IMP-D023 | The OpenAPI→TypeScript/Axios generated-client codegen tool is **Orval** (npm `orval`, pinned `8.22.0`, MIT), run in **`axios-functions`** mode with a **`mutator`** that injects the repository's existing Axios instance. Input is the committed offline artifact `packages/contracts/openapi/openapi.generated.json` only (never a live Swagger URL); output is written under `packages/api-client/src/generated/` and is **never hand-edited**. Generated output is **types + thin per-operation functions only** — no TanStack Query/SWR/React hooks are generated (hooks stay handwritten at feature level, IMP-D009). The existing handwritten runtime (`packages/api-client/src/clients`, `/config`, `/errors`: the Axios instance, base-URL ownership, envelope error-normalization) is preserved and owns base URL/interceptors/retry; the `mutator` routes every generated call through it. A **non-mutating drift check** (generate to a temp dir, format, compare the tracked generated tree, fail with the regenerate command, clean up in `finally`) joins the root `quality` chain in APP0-C02. Rejected: OpenAPI Generator `typescript-axios` (Java/JAR runtime — supply-chain and toolchain cost in a Node/pnpm monorepo); Hey API `@hey-api/openapi-ts` (vendors a competing runtime client into the generated tree that fails the repo-locked `exactOptionalPropertyTypes` strict flag and duplicates the owned Axios runtime). Version policy: exact pin, no automatic major upgrade, generated-diff + strict-compile + determinism review on any upgrade. Set by APP0-DEC-CODEGEN; resolves IMP-O011. | LOCKED |
| IMP-D024 | The frontend component-test stack is **Jest** (already the sole unit/integration runner, IMP-D016 — **not reopened**) with the **`next/jest`** transformer (`nextJest({ dir })` → `createJestConfig`; SWC-based; auto-mocks `.css`/`.scss`, image imports and `next/font`, loads `.env`, ignores `node_modules`), the **`jsdom`** environment (`jest-environment-jsdom` `30.4.1`), **React Testing Library** (`@testing-library/react` `16.3.2` + peer `@testing-library/dom` `10.4.1`), **`@testing-library/user-event` `14.6.1`** for interactions, and **`@testing-library/jest-dom` `7.0.0`** matchers (all MIT; RTL peer `react ^18 || ^19` satisfied by React `19.2`). **Config ownership:** one **per-app** `jest.config.mjs` importing `next/jest` (each app binds its own `next.config`/`.env`; no shared config imports an app-specific Next config) plus a per-app **app-root** `jest.setup.ts` importing `@testing-library/jest-dom`; the shared render helper, provider wrappers (`QueryClient`), and `next/navigation` + `next/image` mocks live in a dedicated React-aware package **`@embroidery/frontend-testing`**, keeping `@embroidery/test-utils` backend-/package-neutral (per `REPOSITORY_STRUCTURE.md` §11). **Test placement:** component test files are **colocated** per the existing frontend convention (`*.test.tsx` beside the component, or a feature `tests/` folder — `REPOSITORY_STRUCTURE.md` §5/§6); test-support code is never under `src` (shared package + app-root config/setup); T02A proves the production `next build` (`.next`) excludes test code. **Server-component policy:** synchronous Server and Client components render in the harness; **async Server Components are not rendered in `jsdom`** (official Next guidance) — extract pure logic into unit tests and assert async-RSC composition through the browser E2E harness (APP0-DEC-E2E / T02B); the DOM component runner never claims full App Router coverage. **Network policy:** no live network — the generated Axios client is exercised by injecting a **mocked Axios instance** into the `apiRequest` mutator and/or `jest.mock` of the generated operation or feature service; **MSW is not adopted** without specific evidence. **Snapshot policy:** snapshots are **not** the primary assertion strategy (role/label/behavior queries are); narrow presentational snapshots only. **Coverage:** `v8` provider, collected from app `src` excluding generated/vendor/test-support; **no coverage threshold is invented in APP0**. **Commands (T02A):** per-app `test` / `test:watch` / `test:coverage` (Jest); component tests run inside the existing root `quality` → `pnpm test`. **Rejected:** Vitest (functionally capable in a spike but a *second* runner — overriding IMP-D016 needs an ADR — with a separate Vite/Sass config and no compelling advantage); Playwright Component Testing (requires browser binaries, still experimental, second runner; browser testing belongs to APP0-DEC-E2E). **Version policy:** caret ranges consistent with the repo, no automatic majors; re-verify RTL/jsdom/user-event compatibility on any Next/React/Jest major upgrade. Set by APP0-DEC-COMPONENT-TEST; resolves IMP-O005(a). | LOCKED |
| IMP-D025 | The browser end-to-end (E2E) test stack is **Playwright Test** (npm `@playwright/test`, pinned `1.61.1`, Apache-2.0, Node `>=18`). It is a **separate test tier** from the unit/component runner — Jest stays the sole unit/integration/component runner (IMP-D016, **not reopened**); Playwright drives real browsers only, so no ADR-gated runner conflict. **Ownership:** a dedicated private workspace package **`@embroidery/e2e-testing`** at `packages/e2e-testing/` (parallel to `@embroidery/frontend-testing` and the backend-neutral `@embroidery/test-utils`) owns `playwright.config.ts`, `specs/**`, and `support/**` (fixtures, orchestration, disposable-DB adapter); it depends on the canonical DB7/T01 owners (`@embroidery/database/testing`, `@embroidery/test-utils` `CleanupStack`) and never duplicates migration/database-lifecycle logic. No E2E code lives under any app `src/`. `@playwright/test` and browser binaries are devDependencies of that package only, never of an app; browsers install via `pnpm --filter @embroidery/e2e-testing exec playwright install` (Windows local) and the pinned official image `mcr.microsoft.com/playwright:v1.61.1-noble` (Linux/CI). **Browser matrix (tiered):** local smoke = Chromium; CI-required (per PR) = Chromium (admin + storefront projects); CI full/scheduled = Chromium + Firefox + WebKit. No mobile emulation in APP0-T02B; WebKit is a Safari **approximation**, not real iOS Safari; headed mode is debugging-only. **App/service startup:** one canonical orchestrator (Playwright `globalSetup` + a repository/package script; Playwright `webServer` may own the two Next apps in production `next start` mode) composes the real Nginx gateway + admin + storefront + API + a **disposable** PostgreSQL provisioned through the canonical T01/DB7 harness — never the persistent dev database — readiness-gated (`/api/health/readiness`, `/healthz`) with full teardown via `CleanupStack`; no per-spec process starting. **Test data:** disposable DB with a unique per-run name and the persistent-DB refusal guard; seeding only via approved API / application fixture-seed adapter / canonical repository helper — no raw ad-hoc business SQL in specs; cleanup on success and failure; worker parallelism bounded to DB/test-data isolation. **Auth (future-compatible, not implemented in APP0):** APP0-T02B tests public routes only; later, deterministic test users with Playwright `storageState` / API-based session setup, role isolation, secrets from env/CI store, no committed tokens/cookies. **Network:** real app HTTP + real owned API by default; route interception only for third-party/unavailable/failure-simulation of non-owned services; no live payment/shipping calls; no credentials in traces/reports. **Locators/flake:** `getByRole`/`getByLabel`/visible text, `data-testid` last; no arbitrary sleeps (rely on auto-waiting); retries only in CI and bounded, with trace on retry/failure; test independence, no cross-test order dependency. **Artifacts:** screenshot only-on-failure, trace retain-on-failure / on-first-retry, video off (or retain-on-failure), HTML report + `test-results/` output, all gitignored and never committed, CI-uploaded and redacted; visual regression is a **separate** concern — no broad screenshot-baseline assertions in T02B. **Accessibility:** semantic-locator policy plus optional keyboard/focus smoke; a targeted `@axe-core/playwright` scan is deferred to T02B (version locked only after official-source review); no full-WCAG claim. **CI/quality tier:** E2E is **not** in the fast default `pnpm quality`; it runs in a separate `quality:e2e`/CI job with browser-install caching and per-project sharding. **Commands (T02B):** root `e2e` / `e2e:headed` / `e2e:debug` / `e2e:report` / `e2e:install` / `e2e:smoke` / `check:e2e` delegating to the package. **T02B scope:** foundation + one Admin smoke + one Storefront smoke through the real gateway on a disposable environment, with artifact/debug proof — no broad feature E2E. **Rejected:** Cypress `15.19.0` (MIT) — no WebKit/real-Safari engine, heavier per-spec TS config (requires a separate `typescript` install + `tsconfig.json`, proven in the spike), and weaker separate-origin (admin+storefront) ergonomics; WebdriverIO `9.30.0` (MIT, reviewed not spiked) — capable (WebDriver BiDi) but heavier driver/config surface with no advantage for this repo. **Version policy:** exact pin, no automatic majors; re-verify browser/image tag and RTL-independent compatibility on any Next/React/Node major and re-pin the `-noble` image tag with the Playwright version. Set by APP0-DEC-E2E; resolves IMP-O005(b). | LOCKED |
| IMP-D026 | The 2D Design Studio renderer is **native SVG rendered by React 19 itself** — APP3 adds **no** rendering-engine dependency (no Konva, no Fabric, no PixiJS) and **no** interaction library (the platform Pointer Events API replaces the stale `interactjs 1.10.27`). **Architecture (mandatory even with one renderer):** the engine-neutral design document owned by `packages/design-document` (RFC 8785 JCS + SHA-256, ADR-DB1-012) is the single source of truth and the renderer is a pure projection of it; all rendering is reached through one narrow **renderer-adapter** contract (mount/load/select/transform/z-order/lock/hide/group/viewport/hit-test/serialize/destroy); engine-native serialization (`toJSON()`/`toObject()`) is never persisted or transported; selection, hover, handles, viewport zoom/pan and the watermark are runtime state and are never serialized; mutable runtime objects never enter Zustand or React state; geometry and pixel↔millimetre conversion live in `packages/design-engine`; undo/redo is a domain command/snapshot concern, never an engine history stack; transform handles are **DOM** elements outside the element box at ≥44 px and focusable; the Studio is client-only and lazily loaded behind a server-rendered route shell. **Watermark:** preview policy supplied at mount/load, non-selectable, non-deletable, topmost, regenerated on load, opaque session marker with no raw PII, never document content, never claimed as screenshot prevention (D-008/D-009). **Assets:** elements reference an asset id + approved derivative; no original or high-resolution preview in the customer editor; no export/download surface (D-007). **Browser code must not depend on `crypto.subtle`** (secure-context only); the API remains the hashing authority. **Provisional budgets** (until APP12 replaces them, `10 §3`): transform frame p95 ≤20 ms desktop / ≤33 ms mobile at 50 elements; no repeated >100 ms long task in a 2 s gesture; scene-load-to-usable ≤500 ms desktop / ≤1000 ms mobile at 50 elements; canonical serialize ≤100 ms / deserialize ≤300 ms at 150 elements; renderer gzip ≤200 KB; no monotonic heap growth over 20 mount/destroy cycles. **Rejected:** Fabric.js `7.4.0` (single-canvas full repaint fails the frozen budgets at product scale — desktop transform p95 33.4–42 ms, mobile 50.1 ms, long tasks to 179–203 ms — and attribution shows the *mandatory* repeated watermark is a dominant part of that cost; no React binding); Konva `10.3.0` + react-konva `19.2.5` (**runner-up and documented fallback**: passes every functional gate and every budget on desktop Chromium, but sits *on* the budget boundary rather than inside it: mobile transform p95 33.4 ms against a 33 ms budget in every run, desktop WebKit transform p95 20–23 ms against a 20 ms budget and mobile long tasks 96–109 ms against a 100 ms budget across repeated runs, so its verdict flips between runs; and adds ~92 KB gzip of which ~43 KB is the react-konva reconciler, cannot recolour an imported SVG, and is opaque to assistive technology); PixiJS `8.19.0` (WebGL/WebGPU graphics library with ten runtime dependencies, no editor accessibility story and GPU context-loss failure modes, for throughput this product does not need). **Upgrade policy:** no rendering dependency to upgrade; re-run `pnpm spike:editor:benchmark` on any React/Next major and re-open the ADR only with new measured evidence. Set by APP0-R01; resolves IMP-O004. ADR: `../adr/frontend/ADR-APP0-001-2D-RENDERING-ARCHITECTURE.md`. | LOCKED |
| IMP-D027 | **Staff authentication and session architecture** (single ADMIN operator, no role matrix — REQ-IDN-001/BR-016/D-018). Password hashing is the **Node built-in `crypto.scrypt`** (RFC 7914) at `N=2^17, r=8, p=1, keylen=32`, 16-byte per-hash salt, self-describing string `scrypt$N=…,r=…,p=…$salt$hash` in `admin_credentials.credential_reference` with `credential_kind='password_scrypt'`, `timingSafeEqual` verify, rehash-on-verify, min-12/max-4096-byte NFKC password policy — **no new dependency** (Argon2id via `@node-rs/argon2 2.0.2` MIT is the reviewed, benchmarked, no-migration upgrade path). Sessions are **server-side, revocable, opaque**: `randomBytes(32)` base64url token, stored as `sha256` in `admin_sessions.token_hash`, raw token never persisted/logged; **idle 30 min / absolute 12 h**, expiry-on-read, **sliding renewal in the guard (no renew endpoint)**, concurrent sessions allowed, logout revokes one, lock/disable revokes all. Cookie is **host-only** `__Host-adm_session` (prod, `Secure`+`SameSite=Strict`+`HttpOnly`+`Path=/`) / `adm_session` (dev HTTP, no `Secure`); production fails fast if `STAFF_SESSION_COOKIE_SECURE=false`; never in `localStorage`. **CSRF** is layered: `SameSite=Strict` host-only cookie + **Origin allowlist** (403) + **JSON-only** (415), not SameSite alone. **Rate limiting** is in-process/transient by identifier (5/15 min), IP (20/15 min) and global (100/5 min) → 429 `Retry-After`; **no persistent lockout, no migration** (distributed limiter deferred to APP12). **Bootstrap** is an out-of-band idempotent `staff:bootstrap` CLI (env secret, never argv/log, refuses an existing ACTIVE admin except `--rotate` recovery; audited SYSTEM); no self-service email/OTP reset in APP1. Guard order: cookie→hash→active-session→account-ACTIVE→bind `ADMIN` actor once (`bindActor`)→admit; failed login is enumeration/timing-safe (generic 401 + dummy verify) and audited as a **SYSTEM** event (never persisted ANONYMOUS). **Migration verdict: `NO_MIGRATION_REQUIRED`** (only code additions). Set by APP1-DEC-AUTH; **resolves IMP-O001 and DEC-29**. ADR: `../adr/backend/ADR-APP1-001-STAFF-AUTHENTICATION-AND-SESSIONS.md`. | LOCKED |
| IMP-D028 | **APP2 object-storage and asset-intake architecture** (resolves IMP-O002). Storage is **S3-compatible behind the application-owned `ObjectStoragePort`**; development = **MinIO** (version-pinned, network-internal, path-style), production = any S3-compatible store by config. Single SDK family **AWS SDK v3 S3** — `@aws-sdk/client-s3` + `@aws-sdk/lib-storage` (both `3.1095.0`) plus `busboy` `1.6.0` / `@types/busboy` `1.5.4` (streaming multipart parser; `endpoint` + `forcePathStyle`); no MinIO client, no broad `aws-sdk`, no repo-owned HTTP client, no Multer. **`@aws-sdk/s3-request-presigner` removed from the APP2 set (`APP2-DEC-STORAGE-C2`)** — no presigned browser flow under API-proxied delivery; reserved for a future ADR. Admin intake is **API-proxied streaming upload** (browser → nginx → Nest API → storage; reuses IMP-D027 cookie + Origin-allowlist CSRF, no browser credentials, no object-store CORS, server-streamed, no full-object buffering) via the **T1 single streaming multipart transport** (`busboy` → `lib-storage` `Upload` for the unknown-length part; metadata-before-file, one file, `busboy` size limit; JSON-body guard excluded from the stream); presigned direct upload is a reserved **portability seam**, not the canonical flow. **Two private buckets** (originals never public; derivatives private at the object level), env-named, no public-read/anonymous-list. Object key `{env}/{originals|derivatives}/{assetId-uuidv7}/{original|derivativeKind}.{ext}` (PII-free, prefix-cleanable, not authorization; DB stays metadata authority). **Checksum = server-computed SHA-256** persisted as `sha256:<hex>` (fits the existing column; ETag is never assumed to be a content hash). Validation split: synchronous intake (declared-MIME allowlist `image/png,image/jpeg,image/webp` + magic-byte sniff + extension consistency + max-bytes) vs async worker deep inspection (decode, dimension/decompression-bomb limits, metadata stripping). **SVG rejected at intake** in APP2 (raster product media only); archives/executables rejected; **malware scanning deferred** with explicit compensating controls (ADMIN-only, allowlist, private-by-default, rate limit, audit; future owner APP12). **Public delivery is gateway/API-proxied with a publication check** — never object-store public-read, never a stable public preview URL; portability seam reserved for a future CDN. Config contract is `OBJECT_STORAGE_*` env names read through the config layer (secrets fail-fast in prod); no values/credentials in docs and **no `.env.example` change here**. Package ownership: new **`packages/object-storage`** (port + S3 adapter over `client-s3`/`lib-storage`/presigner + key helpers + contract tests, runtime-loadable per IMP-D018), API owns the `busboy` transport + single upload use case, worker owns inspection/derivative wiring; acyclic. **Corrected by `APP2-DEC-STORAGE-C1`:** the `assets` row is created **only after** the object is stored/measured (`size_bytes NOT NULL > 0`; `UPLOADED` is a real post-upload state, never a pre-upload reservation; IDX-086 is recovery-only); consistency is **storage-first / DB-second / two idempotent transactions** (row+idempotency+audit, then guarded `UPLOADED→INSPECTING`+outbox); idempotency via `idempotency_records`, never a placeholder row/new table; orphan sweep by prefix + processing-state index; **unpublish never deletes originals**; two-phase tombstone (ADR-DB1-011); `OBJECT_STORAGE_PUBLIC_BASE_URL` **removed** (public URLs are gateway routes; CDN base is a future APP12 deployment decision). **Corrected by `APP2-DEC-STORAGE-C2`:** upload idempotency locked to **model I1 — pre-stream durable allocation** (UUIDv7 `assetId`+objectKey persisted in the `IN_PROGRESS` `idempotency_records.result`, mutable per DB5-A10; committed before streaming so a crash-retry recovers the same identity; retry overwrites the same key — no second object/asset); `idempotency_records.fingerprint` = **pre-stream** normalized request identity (no file content hash, so server-only SHA-256 Option A stands); full concurrency/crash matrix CW-01…CW-07; persisting the allocation at claim is a **repository implementation gap** (owner `APP2-B01`, `IdempotencyStore` claim-with-allocation), **not** a schema gap; `lib-storage` `partSize`/`queueSize`/`leavePartsOnError=false` memory bounds owned by I01/B01 and the C1 heap-only "constant memory" claim corrected (RSS/external verification deferred to I01/B01). **Migration verdict `NO_APP2_MIGRATION`** (re-confirmed by C2 against the real 31-migration schema). Handoff: new narrow **`APP2-I01` object-storage foundation** checkpoint (package + pinned MinIO Compose service + bucket bootstrap + config + `.env.example` keys + contract tests) inserted before `APP2-B01` → map is now 18 checkpoints. Job runtime (`APP2-DEC-JOBS`), image-processing library (`APP2-W01`), UI (`APP2-D01`), and production CDN vendor (APP12) are **not** decided here. Verdict `PASS_WITH_PRODUCT_PARAMETERS` (max upload size / non-image source-format scope / original retention / SVG need are PO parameters; only max-size blocks `APP2-B01`). **Entry gate `APP2-B01-G01` (2026-07-27) — not a third correction; `APP2-DEC-STORAGE-C3` must not be created.** Under `APP2-B01` ownership it closes `STORAGE-BLK-01/02/03` (`RESOLVED_BY_APP2-B01-G01`) and `UPLOAD-POLICY-BLK-01` (`RESOLVED_BY_PRODUCT_OWNER`), adding ADR §4.2f: Product Owner policy **25 MiB = 26 214 400 bytes** API-authoritative file limit, accepted media exactly `image/png`/`image/jpeg`/`image/webp`, **SVG rejected**, non-image embroidery source files excluded, originals retained while the asset exists (unpublish never deletes; no age-based deletion), coarse **route-scoped 27 MiB = 28 311 552 bytes** gateway multipart ceiling (not the file validator), **5-minute** API hard upload duration with strictly longer gateway timeouts, **15-minute** allocation TTL (> hard duration + cleanup margin). **Two-stage identity:** immutable pre-stream **request fingerprint V1** in the `fingerprint` column (`version`/`operationNamespace`/`scopeKey`/`assetKind`/`classification`/`declaredMediaType`/`normalizedFilename`, canonical sorted-key JSON, SHA-256 `sha256:<64 hex>`; excludes bytes, content hash, cookie, request id, boundary, ETag; the idempotency key lives inside `scopeKey`) plus a post-stream **content fingerprint V1** (`version`/`mediaType`/`byteSize`/`checksum`) stored **in the result** — a completed replay is content-complete only when it **resends the body**, which the API consumes through a hash-and-validate-only path with **zero object writes**, returning the stored result on match and `IDEMPOTENCY_CONFLICT` on mismatch; an active duplicate is `ASSET_UPLOAD_IN_PROGRESS` with no second stream processed. **Result union:** closed, versioned, discriminated `ASSET_UPLOAD_ALLOCATION` (written in the same tx as the first `IN_PROGRESS` claim) / `ASSET_UPLOAD_COMPLETED`, strict decoding, `IN_PROGRESS⇒ALLOCATION` and `COMPLETED⇒COMPLETED` invariant, malformed → `IDEMPOTENCY_RESULT_INVALID` (safe 5xx, no object write, no transition), no silent coercion of unversioned JSON. **`claimToken`:** generated on a new claim, unchanged on an active retry, **rotated atomically on expired reclaim while preserving `assetId`/`objectKey`**, verified inside Tx A and Tx B; stale → `STALE_UPLOAD_CLAIM` mutating nothing; never browser-visible or logged — this **supersedes** the C2 matrix row that reclaimed by **deleting** the record (which would have minted a second asset identity). **Reclaim ordering:** lock/verify/rotate/renew in one tx (single winner), then inspect durable asset truth — an existing asset is **never** deleted and resumes from its truthful lifecycle; a missing asset means the renewed claimant deletes the allocated original key and only the APP2-owned derivative prefix, verifies the prefixes are empty and the token still current, and **only then** streams the replacement; cleanup failure accepts no object, inserts no asset and leaves the allocation to expire. **Tx A** (token+fingerprint verified, insert-or-recover the same `assetId` as `UPLOADED`, **no** outbox event) / **Tx B** (token verified, guarded `UPLOADED→INSPECTING`, one outbox event, result replaced with `ASSET_UPLOAD_COMPLETED` carrying that event id, record `COMPLETED`); at-least-once, **exactly-once not claimed**. **`APP2-B01` = exactly three operations** (Admin asset upload / detail / list) — the historical "retry" endpoint is dropped rather than padding a five-endpoint budget. Recorded reconciliations: the gate input's `PRODUCT_IMAGE` asset kind **does not exist** in the repository, so the locked `ASSET_KINDS` value **`CATALOG_MEDIA`** governs (intake must never set classification `PUBLIC`, INV-09); the http-level `client_max_body_size 20m` stays and the upload location overrides it **upward** to `27m` route-scoped; and PostgreSQL `jsonb` normalises key order, so a stored result is decoded field-by-field and never re-serialised to reproduce a hash. `NO_APP2_MIGRATION` re-confirmed a third time — **9/9** disposable-PostgreSQL checks on the real 31-migration / 78-table schema (zero non-internal triggers on `idempotency_records`, both variants round-trip, in-place token rotation, `FOR UPDATE` single-winner reclaim, stale-token UPDATE matches zero rows), zero residue; the remainder are **repository implementation gaps** (ADR §4.2f-7), owner `APP2-B01`. Set by APP2-DEC-STORAGE; entry gate `APP2-B01-G01`; ADR: `../adr/backend/ADR-APP2-001-OBJECT-STORAGE-AND-ASSET-INTAKE.md`; report: `reports/APP2-B01-G01-COMPLETION-REPORT.md`. | LOCKED |
| IMP-D029 | **APP2 asynchronous job runtime** (resolves IMP-O003). Runtime is a **PostgreSQL-backed claim queue on the existing persistence** (J1) — **no new runtime dependency, `NO_APP2_MIGRATION`**. Rejected: **J2 Redis/BullMQ** and **J3 RabbitMQ** (both a second datastore + a new dependency, and job creation cannot share the domain Postgres transaction, so the transactional-outbox invariant INV-23 is lost or must be rebuilt as a Postgres→broker relay; over-built for single-node APP2 scale). **Model:** `outbox_events` (TBL-073) is the durable work signal — one logical job **per domain transition**, appended in the same transaction (`OutboxEventStore.append`); a **dedicated jobs table is out of scope** (per-event fan-out). Claim = the `CONTENDED_CLAIM` path (`ADR-DB5-003` R1) `FOR UPDATE SKIP LOCKED` on the partial `status='PENDING'` index, FIFO `(next_attempt_at NULLS FIRST, id)`. **Lease = a visibility timeout carried on `next_attempt_at`** (claim sets `claimed_by`/`claimed_at`/`attempt_count+1`/`next_attempt_at=now()+lease`), satisfying `ADR-DB5-003` R6 (exits the claimable set in the same tx) within the CST-099 mutable set and never touching `payload`/identity (INV-23); **crash reclaim is automatic** when the lease elapses; **heartbeat is out of scope** (single-node, bounded durations). Attempts append-only in `background_job_attempts` (TBL-075). **Corrected by `APP2-DEC-JOBS-C1`:** the **only** automatic claim/retry state is **`PENDING`** (three column-distinguished sub-states — fresh `next_attempt_at=NULL`/claimed NULL; backoff `next_attempt_at=due`/claimed NULL; leased `next_attempt_at=lease`/`claimed_by=worker`; `next_attempt_at` dual-purpose by `claimed_by` nullness), matching the partial claim index **IDX-088 (`WHERE status='PENDING'`)**. Claim keeps `status='PENDING'`; retryable failure returns to **`PENDING`** with backoff (never `FAILED`); success→`DISPATCHED`, terminal→`DEAD_LETTER`. **`FAILED` stays schema-valid but the APP2 automatic runtime never emits or claims it** (reserved for future/manual recovery). Success/retry/terminal each run **one ownership-guarded completion transaction** (`WHERE id AND status='PENDING' AND claimed_by=thisWorker AND attempt_count=thisAttempt`) that writes the attempt row **and** the outbox mutation atomically **after** the handler durable-effect tx commits; `job_key=outbox_events.id`; expired-lease recovery records the crashed attempt (`WORKER_LEASE_EXPIRED`, conflict-safe on CST-049) then reclaims the next attempt; no-heartbeat invariant `handler_timeout+lease_safety_margin<=lease_duration`, `shutdown_grace<=handler_timeout`, `poll_interval<lease_duration` with an `AbortSignal` handler seam; policy keys `worker.{concurrency,batch_size,poll_interval_ms,lease_duration_ms,handler_timeout_ms,lease_safety_margin_ms,shutdown_grace_ms,max_attempts,backoff_base_ms,backoff_max_ms}`. Backoff curve/max-attempts/lease/poll-interval/batch-size = **`policy_configurations`** (config parameters, never literals). **Delivery guarantee = at-least-once + idempotent creation + idempotent execution + durable effect; exactly-once is NOT claimed** (CC-25). **Worker contract:** `createApplicationContext`+`enableShutdownHooks`, own DB pool (DEC-DB7-003), poll loop → `event_type` handler registry → attempt record → terminal mark, bounded intra-process concurrency, SIGTERM/SIGINT graceful shutdown **replacing the no-op `WorkerLifecycleService` keep-alive**, DB-pool readiness. **FU-A07** (worker structured logging + out-of-request correlation via a `{job_kind}:{job_key}:{attempt_no}` `AsyncLocalStorage` seam over IMP-D022; `X-Request-ID`/IMP-D020 does not apply to jobs) is owned by the new **`APP2-I02`** worker-runtime foundation checkpoint (poll/claim-lease driver + registry + attempt seam + FU-A07 + shutdown), inserted between `APP2-DEC-JOBS`/`APP2-B01` and `APP2-W01` → map now **19 checkpoints**. **Repository gaps (owner `APP2-I02`, not schema gaps):** `OutboxEventStore.claimBatch` must add the `next_attempt_at` lease write (today it sets only `claimed_by`/`claimed_at`/`attempt_count`); the worker has no poll loop/registry/correlation/shutdown beyond the keep-alive. Reclaim needs no new method (lease-driven). **Finding:** the S24 trigger (CST-099) rejects app-level row `DELETE` on `outbox_events`, so worker cleanup never deletes outbox rows — `DISPATCHED` cleanup is the DB10 retention sweep (outside APP2). **Storage not reopened;** `STORAGE-BLK-01..03` (upload fingerprint / idempotency result JSON shape / expired-allocation cleanup ordering) are **routed** to the `APP2-B01` entry gate (block `B01` + downstream intake; do not block `APP2-DEC-JOBS`/`APP2-D01`/`APP2-I01`), not solved here. **Governance rule (recorded):** a checkpoint may receive at most **one** correction; further defects become named blockers with owner + activation gate. **Image-processing library** stays with `APP2-W01`; **priority/heartbeat** out of scope (re-openable by ADR). Verdict `PASS_WITH_CONFIG_PARAMETERS`; controlled spikes **23/23** (decision) + **25/25** (C1 correction) against the real 31-migration / 78-table schema, zero residue. Set by APP2-DEC-JOBS, corrected by `APP2-DEC-JOBS-C1` (retry/lease state machine); ADR: `../adr/backend/ADR-APP2-002-ASYNCHRONOUS-JOB-RUNTIME.md`. | LOCKED |
| IMP-D030 | **APP2 catalog derivative kind and catalog-media processing lane.** `asset_derivatives.kind` gains exactly one value — **`CATALOG_PREVIEW`**: the non-watermarked, display-oriented derivative generated from a `CATALOG_MEDIA` asset for catalog/Admin/Storefront presentation. It is **not** a customer design preview. Rejected: **`PREVIEW_WATERMARKED` with `is_watermarked=false`** (contradicts the kind name, INV-22, BR-012 and the `ADR-APP2-001` §4.7 public preview contract, which resolves that key for the *watermarked* preview); **`NORMALIZED`** (would silently redefine the artwork/design concept DB2 CON-042/CON-061 merged); **`MOCKUP`** (unrelated). `APP2-W01` produces exactly **`THUMBNAIL` + `CATALOG_PREVIEW`**, both `is_watermarked = false`. INV-22 gains its **first physical half** — `ck_asset_derivatives__watermark_by_kind` (**CST-126**): `PREVIEW_WATERMARKED → true`, `CATALOG_PREVIEW → false`; `MOCKUP`/`NORMALIZED`/`THUMBNAIL` stay application-governed, because no invariant constrains them. **Lifecycle (DB3 LC-06, two lanes):** the *catalog-media* lane prepares both derivative rows at **`PENDING`** and moves each to `PROCESSING` through a guarded transition (never inserted as `PROCESSING`), generates the binaries while the asset is still `INSPECTING`, and writes the terminal tuple — both rows `READY`, exactly one `ACCEPTED` inspection, asset `ACCEPTED` — in **one transaction**; a derivative never becomes `READY` for a `REJECTED` asset. The *design/artwork* lane keeps its existing after-`ACCEPTED` SE-013 timing unchanged. **Job kind:** `APP2-W01` files attempt evidence under the domain kind **`ASSET_PROCESSING`**, not the transport kind `OUTBOX_DISPATCH`; the I02 seam already carries a handler-provided kind (`RegisteredJobType`, `assertKnownJobKind`), so no runtime change was needed. **Migration `0032`** (forward-only, preflighted): 31 → 32 migrations, CHECK 189 → 190, fingerprint `4ca56a59…` → `82864268…`; **78 tables / 833 columns / 160 FKs / 50 UNIQUEs unchanged**, no table/column added, uniqueness and READY/storage-key behaviour untouched, no historical row rewritten, OpenAPI/client/Figma unchanged. Set by APP2-DB01 after the `APP2-W01` schema-representability gate blocked; ADR: `../adr/backend/ADR-APP2-001-OBJECT-STORAGE-AND-ASSET-INTAKE.md` (schema-change note, §4.7, §5, §8, §9). | LOCKED |
| IMP-D031 | **Admin Assets list continuation and server-backed asset identity** (cures `APP2-A01 = BLOCKED_BY_MISSING_ASSET_LIST_CONTINUATION_DESIGN`). **Continuation:** the Admin Assets list consumes B01 keyset pagination through an **explicit, user-triggered `Tải thêm tài sản` control**, rendered only when `hasNext = true`, after the collection, inside the main content flow (not floating, not inside a card). It requests `nextCursor` and **appends** the next page, preserving existing items and scroll position. While loading: button disabled, label `Đang tải thêm…`, `aria-busy="true"`, visible indicator, existing items retained (never a whole-list skeleton), announced politely **once**. On failure: existing items stay, an inline `Không thể tải thêm tài sản.` message with a `Thử lại` action adjacent to it, retrying the **same** cursor — never the full-page unavailable state, never a jump back to page 1. When `hasNext = false` there is no control and no decorative end-of-list copy. **Rejected:** infinite scroll, automatic viewport-triggered loading, offset pagination, page-number navigation, and silent page truncation. **No total-count copy** (`6 / 42`, `Tổng cộng 42`, `Trang 1 / 7`) — the API exposes no total. **Identity:** the server neither persists nor exposes the original filename, so every server-backed card/row is titled from the public `mediaType` — `image/png → Ảnh PNG`, `image/jpeg → Ảnh JPEG`, `image/webp → Ảnh WebP`, unknown/unsupported → `Tài sản hình ảnh` (never the raw MIME string) — with a secondary line `{formatted byte size} · {createdAt}` in `vi-VN` (`dd/MM/yyyy, HH:mm`, binary size units, one decimal when useful). **Rejected:** adding a backend filename field, fabricating a filename, and deriving identity from storage key, `assetId` fragment, checksum, object-key extension, inspection detail or worker logs. `File.name` is **transient local upload state only** — allowed in the pre-upload/uploading/processing banner of the current session, never persisted or cached as Asset metadata, and the view switches to server identity once it reconciles from B01 list/detail. Media delivery is unchanged: no authenticated or public media operation exists, the approved placeholder stays, and no fake server image URL is synthesised. Applied in place to the eight existing Admin Assets nodes; seven registry rows promoted to `APPROVED_FOR_IMPLEMENTATION` under `FIG-APPROVAL-APP2-D01-ADMIN-001`; one annotation node `FIG-ADMIN-ASSETS-CONTINUATION-IDENTITY` (`484:272`) added (registry 69 → 70). **No OpenAPI, generated-client, database or application-source change.** Set by APP2-D02; design authority `docs/design/FIGMA_DESIGN_INDEX.md` §4.3 + `../design/approvals/APP2-D01-ADMIN-ASSETS-DESIGN-APPROVAL.md`. | LOCKED |
| IMP-D032 | **APP2 product-draft required-field defaults and fixed category taxonomy** (cures `APP2-B02 = BLOCKED_BY_PRODUCT_DRAFT_FIELD_DECISION`). Every mandatory `products` column is NOT NULL **with no database default**, which collides with "a draft may be incomplete"; these are the locked resolutions. **Category taxonomy —** APP2 uses a closed, provisioned root set: `thu-bong` (`Thú bông`, 10), `khan` (`Khăn`, 20), `quan-ao` (`Quần áo`, 30), `khac` (`Khác`, 90), all `PUBLISHED` and `is_indexable = true` (the lifecycle has no `ACTIVE` literal, so "active" maps onto `PUBLISHED`; `ARCHIVED` is delisted, `DRAFT` is not yet active). `categories` is modelled **flat** (no parent column), so rootness needs no stored value. Slugs are **immutable APP2 reference keys**; `APP2-B02` accepts **`categorySlug`** on the wire and resolves it to `category_id` inside the transaction, and list/detail return `{ slug, name }` only — **the physical category UUID is never exposed** merely to make a form work. Unknown or inactive slug → `PRODUCT_CATEGORY_INVALID` (safe 400). **No category HTTP operation is added**: the `APP2-B02` plan owns exactly five product operations, so the taxonomy is a closed OpenAPI enum sourced from `APP2_CATEGORY_TAXONOMY`. **Product slug —** server-owned and **immutable in APP2-B02**; the client never supplies one and `PATCH name` never silently rewrites it. Derived from the name by: `đ/Đ → d/D`, NFD, drop combining marks, lowercase, non-`[a-z0-9]` runs → `-`, collapse/trim `-`, empty → `san-pham`, cap the base at 80 characters (`slug` is `text`, so the column imposes no limit — the cap is policy, chosen for URL readability and B-tree safety under `uq_products__slug`). On unique conflict, retry **once** as `{base}-{first 8 lowercase hex of the compact product UUID}`; a second conflict fails as `PRODUCT_SLUG_CONFLICT`. Rejected: operator-supplied slug, timestamp- or random-only slug. **Draft price —** create writes `base_price_amount = 0`, `currency_code = VND`; `0` under `status = DRAFT` means **"price not completed"**, a deliberate sentinel and never a sale price. `PATCH` may set a non-negative VND integer amount. **`APP2-B03` publication must require `> 0`** and no Admin surface may render it as a finished price (`APP2-A03` shows `Chưa đặt giá`). **Product display order —** create writes `display_order = 0` ("no curated public order yet"); `APP2-B02` computes **no `max+1`** and public ordering stays with `APP2-B03`/`B04`. **Product media role —** `product_media.role` is NOT NULL, has no default and is part of the row uniqueness key, so an ordered selection cannot be stored without deciding it: the **first** Asset is the single `THUMBNAIL` at `display_order = 0`, every following Asset is `GALLERY` at its zero-based request position; request order is authoritative, Asset ids are distinct, and **`DETAIL` is excluded from APP2-B02** (the physical closed set is unchanged — B02 simply never writes it). An empty media selection is allowed for an incomplete draft, and removing media removes only `product_media` links — never an Asset, derivative or stored object. **Migration `0033`** is **data only** (forward-only, idempotent per slug, and it fails loudly rather than overwriting a conflicting existing category): 32 → 33 migrations; **78 tables / 833 columns / 190 CHECKs unchanged and fingerprint `82864268…` unchanged**, proven on a fresh 33-migration database; OpenAPI, generated client and Figma unchanged; no dependency. Set by APP2-B02-G01 after the `APP2-B02` field gate blocked. | LOCKED |
| IMP-D033 | **Admin Product List is read-only in `APP2-A02`, with status and category filters** (cures `APP2-A02 = BLOCKED_BY_CATALOG_LIST_DESIGN_CONTRADICTION`). The frozen Catalog List nodes contained **no filter control** while the canonical A02 scope locks filters and pagination, and they carried `Tạo sản phẩm`, `Chỉnh sửa`, `Xuất bản` and `Gỡ xuất bản` — capabilities owned by `APP2-A03` and `APP2-A04`, whose routes do not exist at A02 delivery time. **Staged actions —** `APP2-A02` renders **none** of `Tạo sản phẩm` / `Chỉnh sửa` / `Xuất bản` / `Gỡ xuất bản` / `Lưu trữ` / `Xoá`; a control must not precede the capability behind it. The desktop `Hành động` column and header CTA, the mobile card action group, and the empty-state CTA are removed from the A02 base authority. `APP2-A03` later restores create and edit; `APP2-A04` adds publish/unpublish and readiness interactions — both as supplements that never redefine the base list layout, and A02 is **not** re-sequenced after them to preserve dead controls. **Filters —** exactly two, no search/sort/price/date/owner filter. `Trạng thái` defaults to `Tất cả trạng thái` (no `status` parameter) and maps `Bản nháp → DRAFT`, `Đã xuất bản → PUBLISHED`, `Đã lưu trữ → ARCHIVED`; the unfiltered list therefore carries all three states and never silently hides archived products. `Danh mục` defaults to `Tất cả danh mục` (no `categorySlug` parameter) over the closed IMP-D032 taxonomy `thu-bong`/`khan`/`quan-ao`/`khac` — no category endpoint, no category UUID on screen. Changing either filter resets pagination and refetches the first page; pages are never merged across filters; an unknown value normalizes safely to `Tất cả …` and is never rendered raw; filter controls stay visible while loading and the empty state is withheld until the request succeeds; the filter-specific empty state reads `Không có sản phẩm phù hợp` / `Hãy thử chọn trạng thái hoặc danh mục khác.`. **Continuation —** reuses the IMP-D031 `APP2-D02` cursor pattern with product copy `Tải thêm sản phẩm` (only when `hasNext = true`, appended after the collection, `Đang tải thêm…` while loading, `Không thể tải thêm sản phẩm.` + `Thử lại` on the same cursor); no infinite scroll, no total count, no page numbers. **Price —** not an A02 field; no price column is added and `basePriceAmount` is simply not rendered — the `APP2-B02` contract is unchanged and price belongs to A03 and publication readiness. **Search —** excluded entirely: no input, no client-side page search, no search parameter. **Ownership —** canonical mapping is A02 = Admin Product List (`439:100`, `440:102`, `440:191`), A03 = Admin Product Form/Detail (`434:20`, `436:37`, `436:140`, `438:90`, `437:73`), A04 = Publication Interaction (`441:106`, `442:110`, `442:205`, `443:121`); the earlier A02↔A03 inversion in phase-plan §6.2 and annotation `450:404` is stale and reconciled. Approval evidence `FIG-APPROVAL-APP2-D03-CATALOG-LIST-001`; registry 70 → 71 (one annotation node `498:272`); OpenAPI, generated client and database unchanged. Set by APP2-D03 after the `APP2-A02` design gate blocked. | LOCKED |
| IMP-D034 | **`APP2-A03` is one Admin Product capability with two explicit modes: minimal POST create, then PATCH-capable edit/detail** (cures `APP2-A03 = BLOCKED_BY_PRODUCT_FORM_DESIGN_CONTRACT_CONTRADICTION`; set by the `APP2-A03-G01` entry gate — **`APP2-D04` must not be created**). The five approved Product Draft nodes were a single `Sản phẩm mới` create screen with one atomic `Lưu bản nháp`, but `createProductBodySchema` is `.strict()` and accepts only `categorySlug`, `name` and optional `description` — so the screen's media selection required a second request, `Phiên bản & SKU` matched no field in create, update or the detail response, there was no edit/detail design at all, and `basePriceAmount` (PATCH-only) had no approved surface. **Create —** `/products/new`, exactly the three POST fields, `Tạo bản nháp` / `Huỷ`, one POST with no hidden PATCH, then redirect to `/products/{productId}` using the authoritative returned `productId`; price, media, slug, variants, SKU and publication readiness are absent before the product exists. **Edit/detail —** `/products/{productId}`, loaded by `adminProduct_detail` and saved by `adminProduct_update`; editable `name`/`categorySlug`/`description`/`basePriceAmount`/`mediaAssetIds`, every PATCH carrying `expectedUpdatedAt` from the loaded record, only changed fields sent, and the B02-C1 description contract preserved (absent = unchanged, null/blank = clear to NULL). **Price —** whole đồng as a string, non-negative, no separators or floating-point conversion; wire `"0"` means the price is not set and a blank input maps to that sentinel; `0 ₫` is never shown as a completed price; price stays an A03 draft field and does not move to A04. **Slug/status —** server-owned, immutable, read-only, edit-mode only; renaming never implies a slug rewrite; status is a read-only label, never a select, and A03 changes no lifecycle state. **Media —** edit-only (create cannot accept it), saved as the complete ordered `mediaAssetIds` replacement over assets with `kind = CATALOG_MEDIA`, `classification = PRODUCTION_SENSITIVE`, `status = ACCEPTED`; identity is the B01/D02 server identity (`mediaType` label + `{size} · {createdAt}`) because no original filename is stored or returned; honest placeholders only, since no thumbnail-delivery contract exists (`APP2-T01`); first selection is `THUMBNAIL` and the rest `GALLERY` with `DETAIL` excluded; keyboard `Di chuyển trước`/`Di chuyển sau` are mandatory and drag may never be the only ordering method; no arbitrary media maximum; removing media unlinks only. **Conflict —** `PRODUCT_VERSION_CONFLICT` (409) shows `Sản phẩm đã được cập nhật ở nơi khác` with `Tải lại dữ liệu`/`Đóng`; never a force-save, never an automatic retry with the stale token, and no timestamp, token value or request ID as the explanation. **Unsaved changes —** `Bỏ các thay đổi chưa lưu?` with `Rời khỏi trang`/`Tiếp tục chỉnh sửa`; pristine forms never prompt and a successful save clears the dirty state. **Variants/SKU —** removed from A03 authority and deferred as `FU-APP2-PRODUCT-VARIANTS-SKU-01`; no backend operation and no local-only fake persistence. **Publication —** `Điều kiện xuất bản` and `Tới bước xuất bản` are removed; readiness and lifecycle remain `APP2-B03`/`APP2-A04`. **A02 integration —** when A03 ships, the Product List restores exactly `Tạo sản phẩm` → `/products/new` and `Chỉnh sửa` → `/products/{productId}`, and nothing else. Approval evidence `FIG-APPROVAL-APP2-A03-G01-PRODUCT-FORM-001`; registry 71 → 72 (one annotation node `521:284`); OpenAPI, generated client, database and application source unchanged. | LOCKED |
| IMP-D035 | **Product unpublish is the canonical lifecycle transition `PUBLISHED → DRAFT` (`TR-LC04-05`)** (cures `APP2-B03 = BLOCKED_BY_PRODUCT_UNPUBLISH_LIFECYCLE_CONTRADICTION`). The mandatory `APP2-B03` representability audit proved the phase requires unpublish while accepted LC-04 authority had **no exit from `PUBLISHED` except archive**: LC-04 carried four transitions (`DRAFT→PUBLISHED`, `PUBLISHED→ARCHIVED`, `DRAFT→hard delete`, `ARCHIVED→PUBLISHED`), `DB0_LIFECYCLE_INVENTORY` described the machine as "create → publish → archive", `REQ-CAT-003` and `07 §3` listed only create/edit/archive, and `AGG-06` published no unpublished event. The `07 §4` "Publish/unpublish" line belongs to **Gallery Entry (AGG-18)**, a different aggregate. **Decision —** unpublish **removes public visibility without archiving or deleting**, returning the product to the same editable `DRAFT` state it was authored in; `slug`, category, name, description, price, `display_order`, SEO fields, ordered `product_media`, Assets and derivatives are all preserved, `archived_at` is never written and nothing is deleted; `updated_at` advances under the accepted monotonic mechanism. **No `UNPUBLISHED` state is created** — a second editable state would force every editing rule to name both. **Archive stays distinct** as `PUBLISHED → ARCHIVED` (TR-LC04-02) with its existing `ARCHIVED → PUBLISHED` exit (TR-LC04-04); archive is a durable retirement fact and is never the implementation of `Gỡ xuất bản`. **Readiness —** unpublish destroys no readiness fact, so a returned product may still be complete, but **no automatic re-publication is granted**: every publish re-evaluates readiness transactionally, because category state, Asset eligibility and derivative readiness are owned elsewhere and can change while the product sits in `DRAFT`. **Vocabulary (authority only; `APP2-B03` writes the code constants) —** Audit actions `product.published` / `product.unpublished` on `target_kind = PRODUCT` carrying before/after status, with **reason deliberately not required** (the `R` on the Product/catalog audit row applies to archive/unarchive only, and the approved publication command carries a concurrency token and nothing else); Outbox event types `product.published` / `product.unpublished` with `aggregateKind = PRODUCT` and the minimal versioned payload `{ schemaVersion, productId, slug }` — never a product snapshot, description, price, media, Asset ids, storage data, Admin identity, credentials or headers; lifecycle errors stay distinct — `PRODUCT_PUBLISH_NOT_ALLOWED` (publish from `PUBLISHED`/`ARCHIVED`), `PRODUCT_UNPUBLISH_NOT_ALLOWED` (unpublish from `DRAFT`/`ARCHIVED`), `PRODUCT_VERSION_CONFLICT` (stale `expectedUpdatedAt`), `PRODUCT_PUBLICATION_NOT_READY`, `PRODUCT_NOT_FOUND`, and none of them collapses into `PRODUCT_NOT_EDITABLE`. **Root cause recorded —** the APP2 pre-implementation audit judged "schema sufficient" from physical representability (`products.status` is a plain CHECK with no transition guard) and asserted `DRAFT↔PUBLISHED` without cross-checking LC-04; a new gate `pnpm check:lifecycle` now fails when APP2 claims a Product transition LC-04 does not define, when the LC-04 count drifts from `DB3_COMPLETENESS_MATRIX`, when unpublish is documented as targeting `ARCHIVED`, or when the unpublish transition is missing or duplicated. **LC-04 4 TR → 5 TR.** No migration, no schema, OpenAPI, generated-client, Figma, dependency or application-source change. Set by APP2-B03-G01; owner Product/domain authority. | LOCKED |
| IMP-D036 | **Public catalog media is delivered by one anonymous, publication-gated application route; `product_media.id` is an opaque delivery identity** (cures `APP2-B04 = BLOCKED_BY_PUBLIC_MEDIA_DELIVERY_CONTRACT_GAP`). The mandatory `APP2-B04` entry audit found that `Q-01` requires an image on every public product card and `Q-02` requires ordered media, while **no route anywhere in the repository served image bytes** — the built OpenAPI had 16 operations, none public and none binary; `apps/api` never called `getObjectStream`; the gateway declared no media location; and `admin-product.response.ts` already recorded the reason ("APP2 has no authenticated media-delivery contract, so an address here would be fabricated", `FU-APP2-THUMBNAIL-01`). `ADR-APP2-001` §4.7 fully *designs* proxied publication-gated delivery — **a design paragraph is not a route**. `PUBLIC_CATALOG_SCHEMA_GATE` passed: every fact was already representable in the 33 migrations, only the transport was missing. **Decision —** the already-routed `APP2-T01` is activated as a **mandatory predecessor of `APP2-B04`**, owning exactly one binary operation `publicProductMedia_get`: `GET /api/public/products/{slug}/media/{productMediaId}/{rendition}`, with the wire renditions `thumbnail` → `THUMBNAIL` and `catalog-preview` → `CATALOG_PREVIEW` (persistence vocabulary is mapped, never exposed). `B04` therefore stays exactly two JSON operations and projects media through the shared relative helper `buildPublicProductMediaPath` in `@embroidery/contracts`. **Identity —** `product_media.id` is authorised **only** as an opaque path segment: it is not a storage key, grants nothing on its own, must not become a standalone public DTO field, and is *not stable across a media edit* — `APP2-B02` replaces a product's associations wholesale, so a previously issued address stops resolving and safely reports not-found rather than serving a stale image. **Visibility —** one bounded query re-proves `products.status = PUBLISHED`, the category's published state, the attachment to *this* product, the `CATALOG_MEDIA`/`PRODUCTION_SENSITIVE`/`ACCEPTED` Asset lane, and a `READY` unwatermarked derivative with a durable key — reusing `product-publication.policy.ts` constants so delivery cannot drift from the publication that authorised it. Every miss collapses to one `PUBLIC_PRODUCT_MEDIA_NOT_FOUND`; unpublish (`TR-LC04-05`) revokes access on the next request and URL knowledge never bypasses it. **Transport —** `getObjectStream` only, never buffered, no transaction spanning the byte stream, client disconnect destroys the upstream stream, and **no object-storage call happens before visibility resolves**. **Content type —** `image/webp`, an application invariant rather than a stored fact (`asset_derivatives` has no media-type column); `APP2-W01` encodes both catalog derivatives through one frozen WebP policy, and a test reads the worker's declaration to hold the two together. **Content length** comes from the provider's length for the exact object being streamed, the only authoritative size available. **Cache —** **`no-store`**, because a cache that outlived an unpublish would keep serving a withdrawn product and APP2 has no dispatcher for the `product.unpublished` outbox backlog; immutable caching and CDN revalidation stay a later deployment decision. `inline` with **no filename** (the original upload name is never persisted) and `nosniff`. **Scope —** the delivered lane is public and `PUBLISHED`-only, so it cannot render a DRAFT product's images in Admin; replacing the Admin placeholders is deferred as `FU-APP2-ADMIN-MEDIA-PLACEHOLDER-01`, `NONBLOCKING_FOR_APP2-B04`. The earlier T01 wording ("authenticated Admin thumbnail delivery") named the first visible consumer, not the capability, and is superseded. No migration, no schema, Figma, dependency, Admin, Storefront, worker or tracked gateway change. Set by APP2-T01; owner backend/platform authority. | LOCKED |
| IMP-D037 | **Q-01 public catalog pagination is keyset over `(display_order ASC, id ASC)`, with `categorySlug` bound into cursor identity** (Product Owner ruling, `APP2-B04-C1`). `APP2-B04` delivered the first real public catalog contract as a cursor feed while `ADR-DB5-001` R5 and the DB5 matrices still classified Q-01 as offset; a checkpoint cannot close with its public API contradicting accepted authority. The ruling **supersedes only the prior Q-01 classification** — `ADR-DB5-001` gains **R10**, and the query-shape catalog, pagination/ordering matrix, access-path matrix, completeness matrix, EXPLAIN validation plan and test/operations handoff are reconciled to it. No other query is reclassified, the ordering tuple and tie-breaker are unchanged, page-size policy (R6) is unchanged, and **no schema, index, migration, application, OpenAPI, generated-client or Figma change is approved by this decision**. The access path was measured rather than asserted (`pnpm explain:q01`, [`../database/DB5_Q01_ACCESS_PATH_EVIDENCE.md`](../database/DB5_Q01_ACCESS_PATH_EVIDENCE.md)): both delivered forms sequential-scan and sort at MVP cardinality and are **accepted**, while IDX-065's ordering proves reachable only from a `category_id`-constant form the public contract does not issue — recorded precisely, routed to DB10 rather than fixed, and gated against regression by `pnpm check:pagination-authority`. | LOCKED |
| IMP-D038 | **The APP2 Storefront Discover / Product List route is `/kham-pha`, and `APP2-S01` Product cards are staged non-interactive articles until `APP2-S02` route authority exists** (Product Owner ruling, `APP2-S01-G01`). `APP2-S01` blocked at its route gate having changed no file: the Storefront carried exactly one browser route (`/`, Homepage-owned), the shell's `discover` item was deliberately `route: null`, the approved 404 tagged *Khám phá tác phẩm* `Sắp ra mắt`, and no accepted document named a Discover path — a Figma label is not a URL contract, so only the Product Owner could supply one. Locked: the Discover route is **`/kham-pha`** with no trailing slash; the Homepage keeps **`/`**; category state is **`?category=<slug>`** over exactly `thu-bong`, `khan`, `quan-ao`, `khac`, with "all" omitting the query and any unknown value using the approved public not-found policy; `APP2-S01` owns the narrow shell activation of nav id `discover` → `/kham-pha` (plus the not-found *Khám phá tác phẩm* recovery), while Collections, Studio, Commission, Journal and Search stay unrouted; and S01 Product cards are **explicitly authorized to be non-interactive** — semantic article, thumbnail or honest placeholder, Product name and Category name, with no `href`, click handler, button role, pointer cursor, hover affordance or *Xem chi tiết* — a deliberate truthful interim state rather than a missing acceptance item. `/`, `/discover`, `/catalog`, `/products` and `/san-pham` are **rejected** as Discover paths and no alias or redirect is approved. **This decision approves no Product Detail route**: `/san-pham/<slug>` remains `CHƯA CHỐT` / `NOT_CANONICAL` / `NOT_IMPLEMENTATION_AUTHORITY` and `APP2-S02` remains `BLOCKED_BY_UI03_RECONCILIATION` (UI03 `261:1290` / `262:1291` / `273:1409` / `279:1504`); S02 may later convert the S01 card wrapper into a link without changing the masonry architecture. UI02 masonry authority and the 72/72 Figma registry are unchanged, and **no application, OpenAPI, generated-client, database, infrastructure or dependency change is approved by this decision**. Gated against drift by `pnpm check:storefront-route-authority`; recorded in [`phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md`](./phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md) §6.2.2. | LOCKED |
| IMP-D039 | **The Storefront Product Detail browser route is `/san-pham/[slug]`, and `APP2-S02` is a studio Work Detail rather than an ecommerce PDP** (Product Owner ruling, `APP2-S02-G01`). `APP2-S02` was `BLOCKED_BY_UI03_RECONCILIATION`: UI03 was a Draft, the four `APP2-D01` Product Detail frames were `NOT_APPROVED — WITHHELD_PENDING_UI03_RECONCILIATION`, and IMP-D038 had confirmed `/san-pham/<slug>` was still only a Figma proposal. Locked: the route is **`/san-pham/[slug]`**, rendered `/san-pham/<server-owned-product-slug>`, whose slug comes only from `APP2-B04` and is immutable and server-owned — no Product UUID, no query-mode detail route and no trailing-slash authority; `/product/*`, `/products/*`, `/catalog/*`, `/tac-pham/*` and `/kham-pha/*` are **rejected** as detail aliases with no redirect approved, and `/kham-pha` remains Discover. **`APP2-S02` consumes only `publicProductDetail` (`GET /api/public/products/:slug`) and `APP2-T01` catalog-preview media**; there is no related-Product operation and none may be invented. Approved scope is the ordered gallery with an accessible lightbox, Product identity (`name` as H1 plus category identity), **exactly one** description section `Câu chuyện về tác phẩm` (omitted cleanly when `description` is absent, never split into invented sub-fields and never generated), a browser-local `Chia sẻ` action, and `Tiếp tục khám phá` links back to `/kham-pha` and `/kham-pha?category={slug}`. **Although `publicProductDetail` returns `price` and `isDisplayOutOfStock`, no price, stock or buying control is displayed.** Materials, techniques, craft taxonomy, the four-step process, related tabs/cards/recommendations, save/favourite, commission actions, the soft-commission CTA, the mobile sticky bar, year/technique/dimensions/collection metadata and a working global search are **deferred because the delivered contract does not represent them** — the UI03 draft retains them as historical content only. The contract publishes no image dimensions, so the artwork uses a neutral bounded stage with `contain`/natural-ratio behaviour and no locked 3:4 crop (`FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01`, routed non-blocking). Canonical URL `/san-pham/{slug}`; `title = seo.title ?? name`, `description = seo.description ?? description ?? approved generic`, `robots.index = seo.isIndexable`; no invented publish date, author, rating, availability or price schema, and no Product media as OG image until a social-image policy exists. Design authority is the reconciled Figma section `529:2224` under `FIG-APPROVAL-APP2-S02-G01-PRODUCT-DETAIL-001`; the UI03 roots `261:1290` / `262:1291` / `273:1409` / `279:1504` become `HISTORICAL_DRAFT_SOURCE` — `NOT_IMPLEMENTATION_AUTHORITY` and are preserved, never deleted. After `APP2-S02` is accepted the existing S01 card wrapper may become one link to `/san-pham/<slug>` without changing masonry, card content or DOM order; until then IMP-D038 keeps S01 cards non-interactive. **No application, OpenAPI, generated-client, database, infrastructure or dependency change is approved by this decision.** Gated by `pnpm check:storefront-product-detail-authority`; recorded in [`phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md`](./phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md) §6.2.3. | LOCKED |
| IMP-D040 | **Streamed Product Detail not-found transport, and the Product Detail mobile content band** (Product Owner ruling, `APP2-S02-C1`). **(1) `SAFE_STREAMED_NOT_FOUND`.** For the current streamed Next.js App Router Product Detail route, a data-driven `notFound()` answers **HTTP 200** with a `noindex` signal rather than 404 — framework-defined for a streamed response, and measured across six configurations in the running production stack (with and without `loading.tsx`/`error.tsx`, from `generateMetadata` and from the page, with a segment-local `not-found.tsx`, and with an unconditional `notFound()` and no awaits). That transport is **accepted** only while every safety condition holds and is measured per case: the approved indistinguishable surface renders; `<meta name="robots" content="noindex">` is present; **no** Product canonical, title, description, JSON or hidden Product data is emitted; unknown / `DRAFT` / `ARCHIVED` / non-public-category remain indistinguishable; no raw backend, domain or infrastructure reason appears; the response is not stored as a durable public Product page; a new authoritative request re-evaluates visibility; and the old media path still returns a **real** HTTP 404 after unpublish. A malformed slug takes the same surface with **no** Product API call. **Exact 404 must not be fabricated** through a proxy, middleware or custom-server preflight: every such workaround would duplicate the Product-detail read, create a second authorization path or bypass the request-scoped loader — the cure would be worse than the symptom. The wording `SAFE_STREAMED_NOT_FOUND` is canonical; a 200 response must never be called `HTTP_404`, `exact_404` or `transport_404`. Exact transport remains `FU-APP2-DETAIL-NOT-FOUND-STATUS-01` = `ROUTED — FRAMEWORK_TRACKING — NONBLOCKING_AFTER_C1`. **(2) Mobile content band.** Product Detail keeps a **24px gutter and a 342px content width at 390** independently of the global APP1 shell gutter (16px): the page insets itself by the missing 8px rather than widening a shell that also owns the Homepage, Discover and every other route. Desktop and tablet keep the 640px story measure and take their band from the shell unchanged. `FU-APP1-SHELL-BRAND-TOUCH-TARGET-01` = `ROUTED — NONBLOCKING_FOR_APP2-S02` records the shell brand link that misses 44px; the off-canvas skip link is a keyboard affordance and is out of the touch-target measurement. **No application contract, OpenAPI, generated-client, database, Figma, infrastructure or dependency change is approved by this decision.** Gated by `pnpm check:storefront-product-detail-authority`; recorded in [`phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md`](./phases/APP2-ASSETS-AND-CATALOG-PUBLICATION.md) §6.2.4. | LOCKED |
| IMP-D041 | **APP3 Product placement and side-media authority** (Product Owner ruling, `APP3-G01`). Seven locked rulings. **(PO-01) Placement authoring owner.** Product Side and Embroidery Area are **Product aggregate** placement authority; only authenticated **Admin** users may author them, through **`APP3-A01` — Admin Product Placement**. Placement application operations belong to the **Product/Catalog module**, never the Design module. Design Template, Design Session and Storefront code may *reference* placement authority but may never create or mutate it: no customer-authored side or area, and no Template-owned duplicate placement geometry. **(PO-02) Public placement read contract.** A published Product may expose one **read-only placement manifest** carrying only what Studio needs: product id/slug, `studioEligible`, and ordered active sides (id, stable code/name, display order, image width/height px, physical width/height mm, `px_per_mm`, a dedicated background-delivery reference) each with ordered active embroidery areas (id, stable code/name, display order, x/y origin, width/height, physical maxima when present). Public read is allowed only while the Product is eligible for public Storefront visibility. **No private original URL, no storage key, no customer-private data, no mutation field.** Ordering is deterministic; IDs are opaque identifiers and never authorization tokens; absent or incomplete placement returns **`studioEligible = false`**, never fabricated geometry. Operation owner: **`APP3-B01`**. **(PO-03) Side-background association and delivery.** The canonical association is and remains **`product_sides.background_asset_id`** (`NOT NULL`, FK → `assets`). Side backgrounds are **not** duplicated into `product_media`, which keeps its APP2 role set `GALLERY / THUMBNAIL / DETAIL`. Delivery is one dedicated public side-background operation owned by **`APP3-B02`**: published Product + active Product Side + `background_asset_id` → approved browser-safe derivative. Originals are never delivered; the operation must verify the side belongs to the addressed Product; archived/unpublished Products and retired sides are not publicly deliverable; delivery derives from `background_asset_id` and never from a second association row. **The derivative kind and processing lane remain owned by `APP3-G04`** — `APP3-G01` approves no kind, so `CATALOG_PREVIEW`, `NORMALIZED` and `PREVIEW_WATERMARKED` stay unapproved for this purpose and `APP3-B02` is blocked by **both** `APP3-G01` and `APP3-G04`. **(PO-04) Customer side and area selection.** The Storefront customer selects placement only where a real choice exists: exactly one active side selects automatically, multiple active sides are customer-selected; the same rule applies to areas on the selected side. The deterministic initial choice is the first active row by `display_order` with a stable tie-breaker. The chosen `product_side_id` and `embroidery_area_id` persist on the Design Session, and a customer may never submit identifiers outside the validated Product → Side → Area chain. **(PO-05) Canonical Studio route.** **`/san-pham/[slug]/thiet-ke`**, extending the accepted Product detail route `/san-pham/[slug]` (IMP-D039). Side and area identifiers are **not** path authority — current selection lives in bootstrap/session state. The route renders a server shell with a lazy client-only Studio. `/studio`, `/editor` and `/thiet-ke/[id]` are rejected as canonical routes. The existing Storefront route-authority mechanism is extended, never duplicated by a competing checker. **(PO-06) Publication versus Studio eligibility.** Global Product publication **does not** require placement — a Product may stay publicly published with no Product Side or Embroidery Area, and **no global publication guard requiring placement may be added**. Studio eligibility is a separate derived capability: published Product + ≥1 active Product Side + that side has ≥1 active Embroidery Area + an eligible approved side background. Product detail therefore stays public while the Studio CTA is absent or disabled, the public placement read reports `studioEligible = false`, existing published catalogs need **no forced grandfathering**, and Admin publication and placement authoring remain separate workflows. **(PO-07) Referenced placement mutability.** Placement identity and geometry are mutable **only before first reference**. Once a Product Side or Embroidery Area is referenced by a Design Template version or a non-terminal Design Session, the parent relationship, stable code, background asset, pixel dimensions, physical dimensions, `px_per_mm`, area origin, area extent and physical maxima all become **immutable**, and the row can **never be hard-deleted**. A changed placement is a **new replacement row**; the old row is retired from future selection and retained for historical Template and Session references. Display-only text and ordering may change only where that alters neither identity, geometry, nor historical rendering. **Database disposition.** `product_sides` and `embroidery_areas` currently carry **no** active/retired, supersession, stable-code or lock column, so PO-02 ("active" rows, stable code) and PO-07 (retirement without deletion) cannot be represented by the present schema. **`G01_DB_DISPOSITION = REQUIRES_APP3_DB01`.** `APP3-G01` implements no migration; `APP3-DB01` stays conditional at phase level until `APP3-G02` and `APP3-G04` also rule, after which one combined forward-only migration checkpoint is executed. Because G01 requires it, the collective disposition **cannot** be `NOT_REQUIRED — GATE_RESOLVED` unless this ruling is explicitly superseded by later human authority. Gate: `pnpm check:app3-g01`. Set by `APP3-G01`. | LOCKED |
| IMP-D042 | **APP3 Design Template lifecycle and Product archive authority** (Product Owner ruling, `APP3-G02`). Eight locked rulings. **(PO-01) Aggregate and state owner.** Design Template is a **Design-module** aggregate — ownership does not move to Catalog. The mutable header owns title/public metadata, placement scope, lifecycle status, the optimistic-concurrency token and version ordering; a **Design Template Version is an immutable store-authored design-document snapshot**; Asset owns binary metadata and storage while Template owns only the semantic association. **(PO-02) States.** Exactly `DRAFT | PUBLISHED | ARCHIVED`. No `UNPUBLISHED`, `EDITING`, `DISABLED` or `DELETED` may be introduced. **`DRAFT` is the only editable state**; `PUBLISHED` is public and immutable through normal editing commands; `ARCHIVED` is retained, non-public and non-editable. **(PO-03) Transition set — formalised as LC-24**, the next free canonical lifecycle (`DB3_LIFECYCLE_SPECIFICATIONS.md` §LC-24), with six stable ids: `TR-LC24-01` create → `DRAFT`; `TR-LC24-02` publish `DRAFT → PUBLISHED`; `TR-LC24-03` unpublish `PUBLISHED → DRAFT`; `TR-LC24-04` archive `DRAFT → ARCHIVED`; `TR-LC24-05` archive `PUBLISHED → ARCHIVED`; `TR-LC24-06` restore `ARCHIVED → DRAFT`. **No direct `ARCHIVED → PUBLISHED`** — restore always returns to `DRAFT` and republication is a separate guarded transition. **No hard-delete operation exists in APP3.** Archive and unpublish are distinct. Every transition is Admin-only, audited and concurrency-protected; an invalid source state fails **without mutation**; archive and restore require an audit reason while publish and unpublish do not; status mutation, Audit evidence and any future Outbox consequence are atomic. **(PO-04) Versions and publication.** Versions are **immutable from creation**. While the header is `DRAFT`, every successful save creates a new immutable version with the next monotonic number and `published_at` stays `null` until that exact version is first published. The *current draft version* is the highest version number belonging to the Template. Publish selects it, validates it, sets `published_at` **once** if still null, and flips the header atomically; once set, `published_at` is never cleared or rewritten. Unpublish changes **only** the header `PUBLISHED → DRAFT` and preserves every version and every `published_at`. Editing after unpublish creates a new immutable version and never overwrites a published one. Public read while the header is `PUBLISHED` selects **the highest version whose `published_at` is not null**. A Template update never mutates a Design Session or design artifact already cloned from an earlier version. **(PO-05) Clone authority.** A customer **never** receives a live mutable reference to a Template Version. Cloning copies a published version into a deep, independent working Design Session document, persisting the source Template and source Template Version **only** as lineage/audit. Later unpublish, archive, restore or new versions never mutate the clone (GRD-028). No customer export or download is created. **(PO-06) APP3 publication scope and compatibility.** APP3 publishes **area-scoped Templates only**: a publishable header carries `product_id`, a `product_side_id` belonging to it and an `embroidery_area_id` belonging to that side, all three **active** under IMP-D041. Drafts may hold an incomplete scope while being authored but cannot publish until the chain is complete. Public compatibility is **exact triple equality** between the selected product/side/area and the published header scope. Global, product-wide, side-wide, wildcard, tag-based and many-to-many compatibility are **not** APP3 scope; the existing nullable single-scope columns stay future-ready and unchanged. Therefore **`G02_DB_CONTRIBUTION = NONE`** — no template↔product many-to-many relation is added. `APP3-DB01` still runs, but only because `APP3-G01` already requires it. **(PO-07) Publication guards (GRD-T01).** Publish and republish require **all** of: header status `DRAFT`; at least one immutable version exists; the current highest version is valid under `packages/design-document`; the scope chain is complete and active; the document is in bounds for the exact Embroidery Area under `packages/design-engine`; all Template assets are eligible under `APP3-G04` authority; the actor is an authenticated Admin; and the expected concurrency token matches. **No backend checkpoint may implement a reduced publish guard.** Consequently `APP3-B03` may create Draft Templates only after `APP3-P01` and `DB-DISPOSITION-RESOLVED`, and `APP3-B04` requires `APP3-G02`, `APP3-P01`, `APP3-P02`, `APP3-G04` and `DB-DISPOSITION-RESOLVED`. **(PO-08) Product archive reconciliation.** LC-04 gains exactly one transition, **`TR-LC04-06` `DRAFT → ARCHIVED`**, authorising the archive-from-draft `APP2-B02` already shipped (`PRODUCT_ARCHIVABLE_STATES = [PRODUCT_DRAFT_STATE]`) against a lifecycle that permitted only `PUBLISHED → ARCHIVED`. LC-04 is now 6 transitions; `TR-LC04-01/02/03/04/05` are unchanged and not renumbered. Archive is durable catalog retirement and is **distinct from unpublish**; an archived Product is never public; archive hard-deletes **nothing** — Product, Product Media, Assets, placement, Templates, Design Sessions and historical records all persist. A published Template scoped to an archived Product becomes **derived-ineligible without any cascading Template status mutation**; relist re-evaluates current Product publication readiness. Product archive UI stays outside this gate. Closes **`FU-APP2-PRODUCT-ARCHIVE-LIFECYCLE-01`**; **`FU-APP2-PRODUCT-ARCHIVE-UI-01`** remains `DEFERRED_PENDING_PRODUCT_OWNER_SURFACE_DECISION`. No Product application code is modified. **Supersession.** LC-24 supersedes the informal "Additional lifecycles → Design Template" paragraph, which named states but assigned no transition ids, guards or concurrency rule. DB4's "lightweight versioning" reading is made exact by PO-04. Gate: `pnpm check:app3-g02`. Set by `APP3-G02`. | LOCKED |
| IMP-D043 | **APP3 anonymous Design Session ownership, transport and retention authority** (Product Owner ruling, `APP3-G03`). Ten locked rulings. **(PO-01) Anonymous ownership** requires **both** the public session id (`design_sessions.id`) and a high-entropy session secret verified against `session_secret_hash`; **neither authorizes alone**. The raw secret is never persisted, logged, placed in a URL, query or fragment, returned in JSON, stored in `localStorage`/`sessionStorage` or serialized into the document. No Customer, email, phone, name, device fingerprint or anonymous-user aggregate is created. Missing, expired and unauthorized sessions share **one** safe external error shape. `findActiveBySecretHash` is a persistence lookup, not an authorization decision. **(PO-02) Secret generation and verification.** At creation the server generates **32 CSPRNG bytes**, encodes them **unpadded base64url**, and persists only `HMAC-SHA-256(server pepper, raw secret)`; the pepper comes from runtime secret configuration. No browser `crypto.subtle`; no password hash for a uniformly random 256-bit secret; digests compared in **constant time**; secret and digest never enter logs, Audit payloads, telemetry or exceptions; a missing pepper **fails loudly** with no fallback. **(PO-03) Browser transport.** The raw secret travels **only** in a per-session cookie `__Host-nettheu_ds_<session-id>` with production attributes `Secure`, `HttpOnly`, `SameSite=Lax`, `Path=/`, **no** `Domain` and `Max-Age` never beyond `expires_at`; dev may omit `Secure` only under the explicit dev transport policy. The cookie carries only the raw secret; bootstrap returns the **non-secret** id, which is the only value the client may persist. Studio route stays `/san-pham/[slug]/thiet-ke`; APIs address the public id and the server selects the matching cookie; multiple sessions use separate cookies; the cookie is removed on expiry, invalidation and failed authorization. **Forbidden:** `Authorization: Bearer <raw secret>`, secret in path/query/fragment, one shared cookie for every session, the APP1 staff cookie, and browser storage of the raw secret. **(PO-04) Issuance and rotation.** Creation issues one secret and cookie; a **successful authenticated** resume/bootstrap rotates **atomically** — verify id + secret, generate a new 32-byte secret, replace the hash under **compare-and-swap**, `Set-Cookie`, invalidate the old secret **immediately**. Ordinary autosave does not rotate; exactly **one** concurrent resume rotation succeeds; rotation **never extends TTL**; there is **no previous-secret grace window** and no recovery email, code or account link in APP3. **(PO-05) CSRF, origin and enumeration controls.** Every state-changing request requires an exact allowed `Origin`, an allowed `Sec-Fetch-Site`, a matching host-only session cookie and the expected concurrency revision. Credentialed cross-origin CORS is **disabled**; a missing or disallowed `Origin` fails; `SameSite` alone is not sufficient. Session ids follow existing high-entropy identifier authority. Unauthorized, missing, expired and wrong-chain requests share **one** safe external status/code/message; no response reveals whether a session id exists; no secret, digest or raw document appears in an authorization error; repeated failures are rate-limited. **(PO-06) Retention and expiry.** `SESSION_TTL = 30 days` **absolute from `created_at`**; reads, resume, rotation and autosave never slide or extend it; `expires_at` is fixed at creation and cookie expiry never exceeds it. Expiry is `ACTIVE → EXPIRED` under **`TR-LC07-04`** and an expired session rejects all access; an **hourly** worker claims eligible rows safely; an `EXPIRED` session is hard-deleted under **`TR-LC07-05`** after a **24-hour purge grace**. Purge removes only the Session family owned **exclusively** by that session, including private associations/derivatives later defined by `APP3-G04`; shared Product, Template, Catalog Asset and historical authority are **never** deleted. `SUBMITTED` retention belongs to **APP5**. **No restore after expiry.** This closes `O-008` and the `design_sessions` half of `DP-RET-01` and **supersedes** the earlier sliding `last_activity_at + TTL` direction. **(PO-07) Rate and concurrency controls.** APP3 creates **no durable browser identity** and **no cross-session browser quota**. Locked: creation **5/hour** per ephemeral network key, burst **2/minute**; bootstrap/resume/read **60/minute** per ephemeral network key; authorization failures **10 per 15 minutes** per ephemeral network key **and** session id; authorized mutations **30/minute** per session id; **1** in-flight mutation per session. The network key is an **ephemeral HMAC of normalized source network data under a rotating runtime salt**; raw IP is **not** persisted in Design Session tables; the key is not ownership or customer identity; no "N active sessions per browser" rule; rate-limit responses reveal no session existence and never replace credential or revision checks. **(PO-08) Autosave conflict policy.** Every mutation carries the expected `autosave_revision`: a match applies **one atomic update and increments once**; a stale revision returns **`STALE_WRITE`** with current revision metadata only and **does not mutate**; network ambiguity means **refetch before retry**, never a blind replay loop. No idempotency-key table or write log is added — a duplicate retry with the old revision fails `STALE_WRITE` and cannot increment twice. Conflict/reload UX is owned by `APP3-S11`. **(PO-09) APP5 boundary.** APP3 anonymous sessions do not create Customer identity, collect email or phone, attach to staff auth, submit a custom request or transfer ownership. **APP5 exclusively owns `TR-LC07-03`**, contact collection, Customer association and anonymous-to-customer transfer. **(PO-10) Database contribution.** Measured on `design_sessions`: `session_secret_hash` (`text NOT NULL`, unique), `expires_at` (`timestamptz NOT NULL`), `created_at` (`timestamptz NOT NULL`), `status` (`text NOT NULL` + LC-07 CHECK) and `autosave_revision` (`integer NOT NULL`, `>= 0`) already exist, and no customer, email, phone, IP or fingerprint column exists. No durable browser id, previous-secret column, sliding-expiry field, idempotency table or recovery credential is required, so **`G03_DB_CONTRIBUTION = NONE`**. **Preserved.** LC-07 keeps its five transition ids and its four states; `ABANDONED` is not reintroduced. Gate: `pnpm check:app3-g03`. Set by `APP3-G03`. | LOCKED |
| IMP-D044 | **APP3 editor-safe media, asset eligibility and complexity authority** (Product Owner ruling, `APP3-G04`, delivered under a manual intervention directive after the first attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED`). Twelve locked rulings. **(PO-01) Canonical editor-safe derivative.** APP3 reuses the existing `NORMALIZED` derivative kind as its **sole** editor-safe kind and authorizes **no** new enum value (`EDITOR_SAFE`, `STUDIO_PREVIEW`, `DESIGN_PREVIEW`, `SESSION_PREVIEW` are rejected); it means browser-decodable, orientation-normalized, metadata-stripped, dimension-bounded, of known intrinsic size and approved MIME type, inspection-clean, safe for its exact source lane, never an original and never watermark-baked. `PREVIEW_WATERMARKED` (watermark baked into the bytes) and `CATALOG_PREVIEW` (store marketing media) are **never** Studio source. Derivative kind alone never grants access — lane, association, owner state and contextual operation all apply. **(PO-02) Processing profiles without a database enum.** The one kind is produced under three **application/worker policy** profiles — `SIDE_BACKGROUND`, `TEMPLATE_ASSET`, `SESSION_UPLOAD` — which are never persisted as an enum value or schema column and which control accepted source type, maximum size, output MIME, intrinsic-dimension rules and delivery eligibility. **(PO-03) Side background.** Lane `CATALOG_MEDIA`, association `product_sides.background_asset_id`, sources JPEG/PNG/WebP with SVG rejected, raster output; eligibility needs inspection CLEAN, derivative READY, intrinsic width and height, a published Product, an active Side belonging to the addressed Product and a matching `background_asset_id`; delivery is `APP3-B02` and public only through Product/Side context. **(PO-04) Template asset.** Authenticated **Admin** only, lane `TEMPLATE_SOURCE`, sources JPEG/PNG/WebP/SVG; SVG is accepted only here and only after **mandatory server-side sanitization** rejecting or removing at least `script`, event-handler attributes, `foreignObject`, external URLs, remote fonts, embedded HTML, `data:`/`blob:`/`javascript:` URLs, animation, filters outside the approved subset, unbounded path/node complexity and a missing or invalid `viewBox`; the sanitized derivative is self-contained, the original SVG never reaches the Studio, and a Template publishes only when every referenced asset has an eligible READY derivative. **(PO-05) Anonymous Session upload.** A valid anonymous Design Session credential (IMP-D043), lane `CUSTOMER_UPLOAD`/`CUSTOMER_PRIVATE`, sources JPEG/PNG/WebP with **SVG rejected in APP3**; owned by exactly one active Session, private, never reusable as Product/Template/Catalog media, expiring and purging with the Session family, never creating Customer identity. **(PO-06) Delivery boundaries.** Exactly three delivery classes (public contextual side background; public contextual published Template asset; private Session upload behind Session id plus matching per-session cookie). **No generic `GET /assets/:id`**, no object-storage key or private original URL in an API response, no direct object-storage URL as durable document authority; public delivery may use immutable validators, Session-private delivery is `private, no-store` and re-authenticates on every request; APP1 staff auth is never used for Storefront delivery; no `secure_access_grants` purpose or schema extension; a contextual reference is not permission to reach a sibling derivative. **(PO-07) Intrinsic dimensions.** Every Studio-eligible derivative must expose `width_px`, `height_px`, `media_type` and `byte_size` before a document may reference it; raster values come from the inspected decoded image and sanitized-SVG values from a valid bounded `viewBox` (`width`/`height` attributes alone are insufficient); missing, zero, negative or contradictory values make the derivative ineligible; the Studio never guesses; placement scale starts from these dimensions and the Product Side `px_per_mm`. **(PO-08) Upload limits.** Raster 10 MiB, 4096 × 4096 px, 16,777,216 decoded pixels per asset; Admin SVG 1 MiB source, 10,000 sanitized nodes, 1,000,000 path-data characters. Rejection happens before document eligibility, compressed size never overrides the decoded-pixel limit, and decoders are protected against decompression bombs. **(PO-09) Document limits.** 512 KiB serialized canonical document, 100 elements, 20 image elements, 80 text elements, 20 unique referenced assets, nesting depth 8, 500 characters per text element, 5,000 total text characters, 33,554,432 decoded pixels across unique referenced image assets. Rejected documents are not partially saved; enforcement is server-side in `design-document`/`design-engine` with client checks as early feedback only; hidden, locked and off-canvas elements count; a repeated Asset counts once toward asset and pixel budgets while each image element counts toward the image total; runtime overlays never count; no checkpoint may silently raise a value. **(PO-10) Fonts.** Documents store a server-owned `fontId`, never a CSS family, URL or font bytes; a versioned server registry maps it to family, styles/weights, a bundled WOFF2, SHA-256 integrity, licence metadata, Vietnamese glyph coverage and a fallback policy; no remote runtime font URL, no user-uploaded font, no Template-embedded bytes; an unknown `fontId` fails validation; `APP3-P01` delivers the initial registry before Template publication or Session write APIs and `APP3-D01` selects only from it; fonts do not consume the 20-asset budget; a missing font never silently substitutes a geometry-changing face. **(PO-11) Watermark separation.** An editor-safe derivative never contains the APP3 watermark; the Studio watermark is runtime-generated, topmost, non-selectable, non-deletable, non-serialized, opaque and PII-free, regenerated on load; `PREVIEW_WATERMARKED` stays outside Studio source eligibility; no export or download surface is created. **(PO-12) Canonical derivative metadata and database contribution** *(replaces the ruling issued with the failed first attempt, whose premise the measured schema contradicted).* A derivative row may carry canonical output metadata — `width_px`, `height_px`, `media_type`, `byte_size` — describing the output represented by that exact `asset_derivatives` row. `assets.mime_type` and `assets.size_bytes` remain **source-binary** metadata and are never substituted for it; `asset_inspections.detail` remains **append-only inspection evidence and never runtime state authority** (it is not uniquely linked to a derivative row, not indexed as current state, structurally limited to `THUMBNAIL`/`CATALOG_PREVIEW` evidence, unread by application code, and unfit for a per-write decoded-pixel budget check). The canonical owner is `asset_derivatives`. Therefore **`G04_DB_CONTRIBUTION = REQUIRES_APP3_DB01_ASSET_DERIVATIVE_METADATA`** and `APP3-DB01` = `REQUIRED — READY_FOR_EXECUTION` with two contribution groups (G01 placement retirement and stable code; G04 derivative metadata). `APP3-DB01` adds the four columns **nullable with an all-or-none invariant** and positive-value checks — not globally `NOT NULL`, because historical `THUMBNAIL`/`CATALOG_PREVIEW` rows may hold no canonical metadata, no editor-safe row exists yet, a migration must not call object storage, and missing metadata makes a derivative **ineligible rather than fabricated** — and must not add `inspection_detail_id`, `current_inspection_id`, a profile enum column, an editor derivative kind, a public-asset flag or a grant-purpose column. Metadata is written **atomically with the transition to READY**; document validation reads it from `asset_derivatives` and never queries object storage on a document write. A later bounded backfill may populate a historical row only from deterministically provable object metadata or exact inspection evidence linked to that derivative, and is not required for APP3 entry. `FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01` stays **OPEN — AUTHORITY_LOCKED_BY_APP3-G04**, final owner `APP3-B02`, blocked by `APP3-DB01` + `APP3-B06`. **No application, OpenAPI, generated-client, database, migration, infrastructure, Figma or dependency change is approved by this decision.** Gated by `node tools/check-app3-g04.mjs`; recorded in [`phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md`](./phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md) §6.7. | LOCKED |
| IMP-D045 | **APP3 geometry coordinate, transform and bounds semantics** (Product Owner ruling, `APP3-G05`, delivered under a manual intervention directive after the first `APP3-P02` attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED`). Twelve locked rulings. The stop was required because **no accepted document defined** rotation origin, rotation direction, scale origin, group coordinate semantics, parent/child composition order or a production bounds strategy, and the only executable source — the APP0-R01 spike — **contradicts itself**: the SVG adapter pivots on the box centre and nests children inside the group transform, Konva pivots top-left and zeroes the group's translation and rotation so only scale and opacity inherit, and Fabric pivots top-left and constructs an empty group so nothing inherits. The identical cross-engine canonical hash proved **serialization** agreement only — the hash is over the document, which stores `x`, `y`, `rotationDeg` whatever a renderer does with them — and APP0-R01 itself recorded that "the document box centre is not a reliable hit point. Hit testing must be asked of the engine, never derived from document geometry." The three adapters are therefore **comparative research evidence, not production authority**; `IMP-D026` keeps native SVG as the renderer architecture and the renderer now **implements** IMP-D045 rather than defining it. **(PO-01) Coordinate system.** Origin top-left, positive x right, positive y down, angles in degrees; no renderer may reinterpret v1 in a y-up or centre-origin space. **(PO-02) Persisted transform meaning.** `x`/`y` are the untransformed top-left of the element local box **in its parent frame**; `width`/`height` are its positive unscaled dimensions; local drawable coordinates run `(0,0)` to `(width,height)`; never centre coordinates, post-rotation or post-scale bounds, or renderer offsets. A root element parent frame is document space, a child parent frame is its direct group local space. **(PO-03) Rotation.** Positive `rotationDeg` is **clockwise** under y-down; the pivot is the **untransformed local-box centre** `(width/2, height/2)`, which for a root element is `(x + width/2, y + height/2)` in document space before rotation. Never the persisted corner, the document origin, the group origin, the transformed-AABB centre or a renderer default. **(PO-04) Scale.** `scaleX`/`scaleY` apply around the **same** centre pivot and are applied **before** rotation; negative scale stays valid only where P01 already accepts it and means reflection about that centre; scale never alters persisted `x`, `y`, `width`, `height`; `APP3-P02` never mutates a document. **(PO-05) Local matrix.** Column-vector semantics `p' = M x p` with `x' = a*x + c*y + e` and `y' = b*x + d*y + f`; `Mlocal = T(x + width/2, y + height/2) x R_clockwise(rotationDeg) x S(scaleX, scaleY) x T(-width/2, -height/2)`; under y-down the clockwise rotation is `a = cos, b = sin, c = -sin, d = cos, e = 0, f = 0`. Scale and rotation are never reversed, row-vector semantics are forbidden, and a library convention must be converted to this contract. **(PO-06) Group coordinate semantics.** A group defines a **real local frame** originating at the top-left of its untransformed box; every direct child `x`/`y` is expressed in it; effective transforms compose **parent outermost** — `Meffective(child) = Meffective(parent) x Mlocal(child)`. `child x parent`, document-space children inside a group, partial or scale-only inheritance and renderer-owned grouping are forbidden; a group applies translation, rotation, scale and any future v1 transform field uniformly to every descendant. **(PO-07) Group frame and bounds.** A group is **not visible geometry**; its persisted `width`/`height` define its local frame and centre pivot and are **never** replaced dynamically by descendant bounds, so a child edit cannot silently move the group pivot. Group **bounds** are the union of transformed descendant drawable bounds. A future grouping interaction must choose the frame, rebase children into it and persist that explicitly — a later Studio checkpoint, never `APP3-P02`, which must not rebase automatically. **(PO-08) Bounds strategy** *(corrected in place by `APP3-G05-C1`; no new decision id)*. `CONSERVATIVE_TRANSFORMED_AABB` — a production safety strategy, not path-accurate geometry. **The stroke is drawable geometry**: every kind storing `strokeWidthPx` — `rectangle`, `ellipse`, `line`, `freehand` — contributes it to bounds, containment, physical-size validation, group bounds and document bounds. The stroke is **centred on the local path**, so the local envelope expands by `strokeWidthPx / 2` on all four sides; a zero stroke expands nothing; stroke is never renderer decoration, never ignored because the element also has a fill, and never subtracted from the declared `width`/`height`. `text` and `image` carry no stroke and use their declared local box unexpanded, as does a filled `rectangle`/`ellipse` with `strokeWidthPx = 0`. **Local geometry per kind:** a **rectangle** path is the boundary of `(0,0)`–`(width,height)` with fixed v1 `lineJoin = miter`, `miterLimit = 4`, and for its four 90° corners the half-stroke envelope is the bounds authority; an **ellipse** is inscribed in the declared box with its stroke centred on the outline, giving the declared box expanded by half the stroke — still the conservative transformed AABB of that expanded box, never the tight rotated ellipse/stroke AABB; a **line** has no independent endpoint fields in v1, so its exact local path is locked as a **straight segment from `(0,0)` to `(width,height)`** with `lineCap = round`, `lineJoin = round`, and is never a horizontal or vertical centreline, an arbitrary renderer path, a renderer-chosen diagonal or a path with unstored endpoints; **freehand** points are in the element local frame and form an ordered polyline of straight segments with `lineCap = round`, `lineJoin = round`, no smoothing, interpolation, Bezier conversion or point simplification, a single stored point being a round dot of radius `strokeWidthPx / 2` and an empty sequence staying invalid under P01. **Transform order:** derive the exact local drawable envelope, transform **all four corners of that envelope** with the full effective matrix, take the document-space axis-aligned min/max, then quantize through P01 authority — transforming only the unexpanded path and adding an unscaled document-space half-stroke afterwards is forbidden because it ignores scale and rotation; for line and freehand this may overestimate the painted stroke under rotation or non-uniform scale, accepted for v1 containment safety. **Fixed cap/join are version-level constants, not document fields**: a renderer implementing `schemaVersion = 1` must reproduce miter join with `miterLimit 4` for rectangle and round cap/join for line and freehand (ellipse has no cap/join decision); these are **not** added to the v1 document and are not per-element customer choices, and changing one changes painted bounds and is therefore a schema-semantic change under PO-12, exposable only through a new decision and a migration. **Group** has no painted geometry of its own; group bounds are the union of descendant drawable AABBs **including every descendant stroke envelope**, and document bounds the union of all. Hidden and locked serialized elements still have geometry and z-order never affects bounds. **(PO-09) Containment.** **Blocking**, using the complete **stroke-aware** conservative transformed AABB (PO-08 as corrected): inside only when `minX >= area.minX`, `minY >= area.minY`, `maxX <= area.maxX`, `maxY <= area.maxY` after P01 quantization. Boundary equality is valid and **any** overhang invalid. Checking the untransformed box, the pivot/centre alone or sampled path points is forbidden, as are clamping, translating, rotating, scaling down, snapping and warn-but-persist. `APP3-P02` returns findings only, but APP3 document-write APIs must treat an out-of-bounds finding as a validation failure. A shape whose fill or path fits while part of its ruled stroke envelope overhangs fails, and physical-size validation uses the same stroke-aware bounds. The strategy may reject a path whose visible pixels fit while its declared box does not — accepted for v1 to prevent clipping and renderer disagreement. **(PO-10) Physical scale authority.** The sole px to mm source is **`product_sides.px_per_mm`**; never Embroidery Area width/height, CSS DPI, 96 DPI, image-metadata DPI or the APP0 spike `mmPerPx` helper. `embroidery_areas.max_width_mm`/`max_height_mm` are physical limits, not conversion authority. Consistency requires **both** `image_width_px / physical_width_mm` and `image_height_px / physical_height_mm` to equal `px_per_mm` after quantization; disagreeing axes are a typed mismatch and are **never averaged**. **(PO-11) Historical rows.** Retirement and geometry validity are separate: a retired Product Side or Embroidery Area is **not selectable** for new editing or new document creation, while an existing historical document referencing the same stable rows stays renderable and geometrically valid. `APP3-P02` exposes the distinction explicitly as `NEW_EDITING` versus `HISTORICAL_RENDER`; retirement is never deletion and a historical document is never silently moved to `superseded_by_id`. **(PO-12) Integrity consequence.** The v1 hash does not encode these semantics separately, so **IMP-D045 is part of the semantic interpretation of `schemaVersion = 1`** and approval, rendering and production systems must all run a geometry-engine version compatible with it. A change to pivot, rotation direction, scale origin, group frame, composition order, bounds strategy, containment strategy or `pxPerMm` source is a **schema-semantic change** requiring a new Product Owner decision, a schema-version increase, an explicit document migration and a new canonical approval/rendering contract — it can never ship as an internal refactor preserving the same v1 bytes and hash. The phase must later record the geometry-engine compatibility version in approval and rendering evidence. **`G05_DB_CONTRIBUTION = NONE`** — no database column is added here, and no application, OpenAPI, generated-client, migration, infrastructure, Figma or dependency change is approved by this decision. Gated by `node tools/check-app3-g05.mjs`; recorded in [`phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md`](./phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md) §6.11. | LOCKED |
| IMP-D046 | **APP3 association-bound normalization dispatch and staged SVG support** (Product Owner ruling, `APP3-G06`, delivered under a manual intervention directive after the first `APP3-W01` attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED`, primary cause `JOB_CONTRACT_INSUFFICIENT`, secondary cause `SVG_SANITIZER_NOT_SELECTED`). Twelve locked rulings. The stop was required and is confirmed by measurement: `asset.inspection.requested` is appended **inside the upload transaction** as an Asset moves `UPLOADED → INSPECTING`, its payload is deliberately only `{ schemaVersion, assetId }` with exact validation, and at that instant **no** APP3 association can exist — `product_sides.background_asset_id` requires an already `ACCEPTED` Asset, and `design_template_assets` / `design_session_assets` have no writer because `DesignModule` is not composed into `AppModule`. The worker therefore could not derive the IMP-D044 profile, and no approved SVG sanitizer exists anywhere in the repository. **(PO-01) One new event, same architecture.** Exactly one new event type is authorized — **`asset.normalization.requested`** — reusing the existing transactional Outbox, worker dispatcher, claim/heartbeat/retry mechanism, attempt evidence and dead-letter behaviour, and the existing `ASSET_PROCESSING` job kind. It is a new event type and **not** a second queue, second scheduler, polling sweep, second claim table, second retry framework or cron reconciler. `outbox_events.event_type` is open text with no CHECK and the worker dispatches through a registry keyed on event type whose claim filter is exactly the registered types, so this needs **no migration and no schema change**. `asset.inspection.requested` remains unchanged and is never repurposed. **(PO-02) The payload identifies the association, not the profile.** `schemaVersion = 1`; logical payload `schemaVersion` + `assetId` + `normalizationPolicyVersion` + `associationRef`, with `normalizationPolicyVersion = 1`. `associationRef` is a discriminated union over `PRODUCT_SIDE_BACKGROUND` (`productSideId`), `DESIGN_TEMPLATE_ASSET` (`designTemplateAssetId`) and `DESIGN_SESSION_ASSET` (`designSessionAssetId`) — each a stable opaque primary key proven present (`pk_product_sides`, `pk_design_template_assets`, `pk_design_session_assets`). The payload must **never** carry a profile, processing profile, source-classification claim, ownership claim, storage key, object URL, session secret, customer id or raw SVG. The event states which committed association triggered work; it does not assert that the association is still valid. **(PO-03) The profile is derived by the worker.** At claim time the worker re-reads the referenced association and the Asset and maps exactly `PRODUCT_SIDE_BACKGROUND` → `SIDE_BACKGROUND`, `DESIGN_TEMPLATE_ASSET` → `TEMPLATE_ASSET`, `DESIGN_SESSION_ASSET` → `SESSION_UPLOAD`. It must prove the association exists, points to the payload `assetId`, belongs to the expected owner/context, is still active where lifecycle authority requires it, and that the Asset exists, matches the profile's classification/kind rules and is in the required processable state. A payload discriminator is a **lookup key, not authorization**: no caller-supplied profile flag is accepted, the profile is never derived from MIME type alone, and the worker never scans an Asset's associations and picks one. **(PO-04) Association-bound producer timing.** The request is appended by the write that creates or changes the association, in the **same database transaction**, ordered association write → outbox append → commit, so a rolled-back association produces no visible event. No event is appended on a read, on a no-op save of the same Asset and association, when an association is removed or retired without a replacement Asset, or for an original upload before any association exists. An event **is** appended when a new active association is created, when an existing active association changes to another Asset, and on an explicitly authorized future retry/reprocess command. A derivative is Asset-owned and is never deleted merely because an association moves away from it. **(PO-05) Producer ownership follows the association write.** A new bounded checkpoint **`APP3-B01N`** appends the event after a new or changed `product_sides.background_asset_id`, modifying only the accepted B01 placement replacement transaction and adding **zero HTTP paths, operations and OpenAPI schemas**; it is not a B01 correction and does not reopen B01 acceptance. `APP3-B03` appends it in the transaction that creates or changes a `design_template_assets` association, and until the SVG extension is accepted it must reject SVG Template intake with a stable capability-unavailable error while accepting raster Template assets after W01A. `APP3-B06` appends it in the transaction that creates the `design_session_assets` association and never during the earlier raw-upload inspection transaction. **No generic API endpoint exists merely to enqueue normalization.** **(PO-06) The worker is replanned, not resumed.** `APP3-W01` becomes `REPLANNED — REPLACED_BY_APP3-W01A_AND_APP3-W01B` and is never marked complete. **`APP3-W01A`** is raster-only (JPEG/PNG/WebP for all three profiles), consumes `asset.normalization.requested` schema version 1, produces a `READY` unwatermarked `NORMALIZED` derivative with the canonical metadata quartet in a private object, reuses the APP2 worker architecture, and may be tested by inserting the ruled Outbox event directly into the disposable harness before any producer exists. **`APP3-W01B`** supports only `TEMPLATE_ASSET` SVG, is blocked by `APP3-W01A` and `APP3-G07`, and must not change raster behaviour. **(PO-07) SVG is staged, not dropped.** A later authority checkpoint **`APP3-G07`** selects and locks the sanitizer implementation/dependency, the allowed element and attribute sets, namespace handling, URL/reference policy, CSS/style policy, font/text policy, `viewBox` and dimension authority, node and path-data counting, deterministic serialization/canonicalization, output MIME and encoding, dependency version/licence/update policy and a security regression corpus. Until G07 and W01B are accepted, Template raster assets are supported after W01A while Template **SVG remains authorized by IMP-D044 but operationally unavailable** and is rejected safely. No checkpoint may silently rasterize unsanitized SVG through Sharp, use regex sanitization, use a browser DOM as an implicit sanitizer, choose a package inside W01A, or reinterpret SVG as raster by extension. This is staged implementation, **not** a product-scope deletion, and SVG is never removed from IMP-D044. **(PO-08) Event idempotency.** Logical normalization identity is `assetId` + `normalizationPolicyVersion` + derivative kind `NORMALIZED`. The association reference proves authorization and context for the request; it does **not** create a derivative per association. Duplicate delivery, different eligible associations and concurrent claims all converge on one authoritative `READY NORMALIZED` derivative under existing database uniqueness and worker claim rules, and the worker still validates each triggering association before accepting that an existing result satisfies the request. Neither profile nor association id is ever persisted on `asset_derivatives`. **(PO-09) Stale association behaviour.** When a claimed event's association no longer exists, no longer points to the payload `assetId`, is retired or inactive, belongs to another owner, or fails profile classification, the event completes as a bounded **non-retryable** contextual rejection — `NORMALIZATION_CONTEXT_NO_LONGER_ELIGIBLE` or the repository's canonical equivalent — producing no derivative. An absent association is never treated as an infrastructure retry, and no private owner data appears in the error detail. **(PO-10) Deployment order.** `APP3-W01A` → `APP3-B01N` → `APP3-G07` → `APP3-W01B`, never combined into one checkpoint: the consumer comes first so no producer emits events nothing can handle, B01N then enables SIDE_BACKGROUND normalization without reopening B01, G07 is security authority rather than worker implementation, and W01B extends only Template SVG after sanitizer selection. `APP3-B03` and `APP3-B06` may start only when their other dependencies and `FU-PLATFORM-ZOD-DTO-OPENAPI-METADATA-01` are resolved, and each must include its own producer append. **(PO-11) B02 dependency correction.** Once W01A and B01N are accepted, `APP3-B02` has the SIDE_BACKGROUND processing and trigger dependencies it needs and its blocker becomes **`BLOCKED_BY_APP3-W01A_AND_APP3-B01N`** plus any platform HTTP-schema blocker its concrete contract carries. `APP3-B06` is **not** the SIDE_BACKGROUND trigger owner — it owns Session Asset association only. `FU-APP2-PUBLIC-MEDIA-DIMENSIONS-01` stays `OPEN — AUTHORITY_LOCKED_BY_APP3-G04`, final owner `APP3-B02`, blocked by `APP3-W01A` + `APP3-B01N`, and is not closed here. **(PO-12) The platform OpenAPI follow-up stays separate.** `FU-PLATFORM-ZOD-DTO-OPENAPI-METADATA-01` remains `OPEN — BLOCKS_NEXT_SCHEMA_BACKED_HTTP_CHECKPOINT`. G06, W01A, G07 and W01B add no HTTP request-body schema and are not blocked by it; `APP3-B01N` adds no operation or body and is not blocked; `APP3-B03` and `APP3-B06` remain blocked when they introduce schema-backed HTTP bodies unless the follow-up is closed or they use an approved bounded publication strategy. `createZodDto` is **not** repaired here. **No application, worker, OpenAPI, generated-client, database, migration, infrastructure, dependency or Figma change is approved by this decision.** Gated by `node tools/check-app3-g06.mjs`; recorded in [`phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md`](./phases/APP3-DESIGN-TEMPLATES-AND-STUDIO.md) §6.16. | LOCKED |
| IMP-D047 | **Template SVG sanitizer and deterministic normalization authority** (`APP3-G07`). Fifteen locked rulings supplying the authority `APP3-G06` PO-06/PO-09 deferred and `APP3-W01A` stopped short of, so that `APP3-W01B` can be implemented without choosing a security boundary in passing. Every upstream value below was fetched from the official npm registry metadata and the official project repositories on 2026-08-04; none is remembered. **(PO-01) The sanitizer is DOMPurify on server-side Node with jsdom.** `dompurify@3.4.13` (`MPL-2.0 OR Apache-2.0`, zero runtime dependencies, `https://github.com/cure53/DOMPurify`) is the active-content and XSS defence layer, and `jsdom@29.1.1` (`MIT`, `https://github.com/jsdom/jsdom`) supplies the DOM it operates on. This is an explicitly approved server-side sanitizer architecture, not an implicit browser DOM. **The newest jsdom is deliberately not selected:** `jsdom@30.0.1` declares `engines.node = ^22.22.2 \|\| ^24.15.0 \|\| >=26.0.0` and every application image is `node:22.14.0-alpine`, which does not satisfy `^22.22.2`; `29.1.1` (`^20.19.0 \|\| ^22.13.0 \|\| >=24.0.0`) is the newest release the locked runtime can start, and raising the base image is an infrastructure decision this gate has no standing to make. `APP3-W01B` must pin both **exactly** — no `*`, `latest`, caret or tilde — and must not select `isomorphic-dompurify`, a `sanitize-svg` wrapper, regex-only sanitization, a headless browser, SVGO or Sharp in the sanitizer role; Sharp never rasterizes a Template SVG, because a raster fallback would silently change what an Admin approved. **(PO-02) DOMPurify's defaults are not the APP3 policy.** DOMPurify permits HTML, SVG and MathML by default and is a *removal* engine; APP3 is narrower on both counts and requires explicit allowlists, value validation, namespace and URL rejection, complexity limits, deterministic serialization and a fixed-point second pass. It runs with the SVG namespace, HTML and MathML disabled, and with XML parsing (`PARSER_MEDIA_TYPE` set to an XML media type) so XML safety is retained. `DOMPurify.removed` is **diagnostic only** and is never the security decision. **(PO-03) SVGO is an optimizer and is not run in v1.** SVGO documents itself as "a Node.js library and command-line application for optimizing SVG files"; optimization is not a security boundary, and DOMPurify's own documentation warns that if you "first sanitize HTML and then modify it afterwards, you might easily void the effects of sanitization". No optimizer runs after the sanitizer, and none runs at all in v1. **(PO-04) Input form.** UTF-8; source bytes at most 1 MiB; strict XML parsing and never HTML error recovery; exactly one root `<svg>`; root namespace `http://www.w3.org/2000/svg`; no foreign namespace; no DOCTYPE, entity declaration or processing instruction; no external entity resolution of any kind. **(PO-05) Reject the whole file.** Any unsupported element, attribute or value rejects the complete file with the stable non-retryable outcome `UNSAFE_OR_UNSUPPORTED_TEMPLATE_SVG`; content is never silently removed, because a Template that renders differently from what an Admin approved is worse than a refused upload — nobody is told. **(PO-06) Element allowlist.** Exactly `svg`, `g`, `path`, `rect`, `circle`, `ellipse`, `line`, `polyline`, `polygon`; everything else rejects, explicitly including `script`, `foreignObject`, `image`, `a`, `style`, all text and font elements, `use`, `symbol`, `defs`, gradients, `marker`, `pattern`, `mask`, `clipPath`, filters, `metadata`, `title`, `desc` and every animation element. **(PO-07) Attribute allowlist.** Root attributes are exactly `xmlns` and `viewBox`; elsewhere only, and only where semantically valid, `transform`, `fill`, `fill-opacity`, `fill-rule`, `stroke`, `stroke-width`, `stroke-opacity`, `stroke-linecap`, `stroke-linejoin`, `stroke-miterlimit`, `stroke-dasharray`, `stroke-dashoffset`, `opacity`, `x`, `y`, `width`, `height`, `rx`, `ry`, `cx`, `cy`, `r`, `x1`, `y1`, `x2`, `y2`, `points`, `d`. Always rejected: `id`, `class`, `style`, `href`, `xlink:href`, `src`, every `on*`, every `data-*`, every `aria-*`, `tabindex`, `role`, `xml:base`, `xml:space`, every `xmlns:*` beyond the root `xmlns`, and `vector-effect`. No custom element and no custom attribute; rejecting `id` is also the DOM-clobbering defence. **(PO-08) No URL, CSS, reference or font.** `url(...)`, `var(...)`, `javascript:`, `data:`, `blob:`, `http:`, `https:`, `ftp:`, `file:`, `cid:`, relative and protocol-relative URLs and `#fragment` references are rejected wherever they appear; no CSS, `style` element or attribute, class, external resource, remote or embedded font, text rendering, `currentColor`, `context-fill` or `context-stroke`. Paint is explicit presentation attributes only. **(PO-09) Value grammar.** All numbers are finite base-10 with no unit, percentage, `calc()`, `NaN` or `Infinity`, canonicalized so `-0` becomes `0`, with no leading `+`, no unnecessary zeros and one representation per value. **Clarified by `APP3-W01B-C1`: "one representation per value" is the shortest decimal token that parses back to the identical IEEE-754 binary64 value, never a fixed decimal precision budget — a budget rounds, and rounding a valid finite coordinate is the silent visual modification PO-05 forbids; lowercase exponent notation with no `+` and no leading exponent zeros is canonical wherever shortest-round-trip printing needs it.** Paints are `none`, `#rgb`, `#rgba`, `#rrggbb`, `#rrggbbaa`, `rgb(integer integer integer)` or `rgb(integer integer integer / alpha)`, canonicalized to lowercase `#rrggbb`, `#rrggbbaa` or `none`; named colours, `currentColor`, `hsl`, `lab`, `lch`, `color`, `device-cmyk`, `var` and `url` are rejected. Enumerations are exactly `fill-rule: nonzero \| evenodd`, `stroke-linecap: butt \| round \| square` and `stroke-linejoin: miter \| round \| bevel`; opacities lie within `0..1`; stroke widths and dash values are non-negative; `stroke-miterlimit` is positive. **(PO-10) Transform, viewBox, points and path.** Transforms are exactly `matrix(a b c d e f)`, `translate(tx \| tx ty)`, `scale(s \| sx sy)`, `rotate(angle \| angle cx cy)`, `skewX(angle)` and `skewY(angle)` with finite arguments, exact arity and degrees — no CSS transform syntax, no 3D, no `transform-origin` — with function order preserved and canonically serialized. `viewBox` is exactly four finite numbers with positive integer width and height, width at most 4096, height at most 4096 and width × height at most 16,777,216; derivative metadata is `width_px = viewBox width` and `height_px = viewBox height` with no rounding, no DPI and no fallback to a root `width`/`height`. `points` requires a valid, even coordinate count. `d` is parsed as real SVG path grammar for `M/L/H/V/C/S/Q/T/A/Z` including exact arity and `0`/`1` arc flags; a permissive character regex is forbidden. The parser is **package-owned and deterministic**: the repository already owns its geometry semantics under `IMP-D045`, so no audited parser dependency is required and `APP3-W01B` adds none. **(PO-11) Complexity limits.** Source bytes at most 1 MiB; sanitized and canonical nodes at most 10,000; total canonical path-data characters at most 1,000,000; maximum element depth 64. The boundary passes and boundary plus one rejects; nothing is ever truncated into compliance. **(PO-12) The deterministic pipeline.** `APP3-W01B` runs exactly, in order: byte limit; forbidden XML form rejection; strict XML parse; namespace, allowlist, value and complexity validation; DOMPurify with the exact APP3 policy; rejection on any structural removal or change; construction of the package-owned canonical representation; deterministic serialization; reparse, revalidation and a second sanitize; reserialization; a requirement that the second bytes equal the first; metadata derived from the final bytes; and completion through the `APP3-W01A` object, `READY` and quartet protocol. No mutation and no optimizer after the final pass — the fixed point is what makes the structural-change rejection meaningful. **(PO-13) Canonical output.** `media_type = image/svg+xml`, UTF-8, no BOM, XML declaration omitted, LF line endings, no trailing newline; serialization lowercases allowed names, emits the root `xmlns` first and `viewBox` second and sorts remaining attributes lexicographically, uses double quotes with correct XML escaping, emits no comment or insignificant text node, uses one locked empty-element form consistently, and gives `svg` and `g` explicit start and end tags. Determinism means identical accepted source bytes plus the exact policy and dependency versions produce byte-identical output; it does **not** mean that two semantically equivalent but textually different sources converge. **(PO-14) Policy version and supply chain.** `TEMPLATE_SVG_SANITIZATION_POLICY_VERSION = 1` is worker policy and never a database column or a derivative kind. Changing the sanitizer, jsdom or path parser version, the allowlists, the value grammar, the limits, the serializer or the `viewBox` rule requires security review, a corpus rerun and a policy version increase when behaviour changes. `APP3-W01B` must install with exact lockfile integrity, make no runtime network call, download no native binary and run no unapproved install script — neither `dompurify@3.4.13` nor `jsdom@29.1.1` declares an `install` or `postinstall` script, and `dompurify` has zero runtime dependencies. **`APP3-G07` installs nothing.** **(PO-15) Sanitization does not authorize delivery.** A sanitized Template SVG remains private, unwatermarked, `NORMALIZED` and Template-owned; `APP3-G07` authorizes no generic or public delivery, no inline HTML embedding, no data URL, no public ACL and no download endpoint. Passing a sanitizer is not the same as being safe to embed anywhere. | LOCKED |
| IMP-D048 | **Design Session upload architecture and authorization staging** (Product Owner ruling, `APP3-G08`). Nine locked rulings issued after `APP3-B06` stopped cleanly at `BLOCKED — ENTRY_ARCHITECTURE_PRESUPPOSITION_ABSENT`: the original B06 plan presupposed a server upload-intent operation, a browser presign capability and an upload-completion proof, and the repository has none of the three. **(PO-01) The upload stays API-owned and streaming** — `SESSION_UPLOAD_ARCHITECTURE = API_OWNED_MULTIPART_STREAMING`. One multipart operation receives the bytes, streams them to private object storage, validates the bounded upload, commits Asset and association state and appends the accepted events — the architecture `APP2-B01` actually delivered. **No** browser direct-to-storage upload, presigned URL, upload-intent/completion pair, upload token issued to a browser, `ObjectStoragePort` presign method or exposed storage endpoint or credential. `ObjectStoragePort` keeps exactly its six methods; `ADR-APP2-001` §4.7/§4.8 is **not** reopened, and its stated reason stands — a presign capability would create a delivery path that bypasses the publication check. **(PO-02) `APP3-B06` is replanned, not failed.** `APP3-B06 = REPLANNED — REPLACED_BY_APP3-B06A_AND_APP3-B06B`; the first attempt remains historical evidence as `BLOCKED — ENTRY_ARCHITECTURE_PRESUPPOSITION_ABSENT`, which was a correct refusal to invent an architecture, not an implementation failure. Order is `B06A → B06B` and the two are never combined. **(PO-03) `APP3-B06A` owns the anonymous-session authorization foundation only** — per-session cookie extraction and cookie-name derivation, `HMAC-SHA-256` verification under the runtime pepper, constant-time comparison, `ACTIVE` and expiry checks, one non-enumerating authorization failure, cookie clearing on invalid or expired authorization, `Origin` and `Sec-Fetch-Site` validation, credentialed cross-origin refusal, `IMP-D043` PO-07 rate-limit integration, the expected-revision/CAS mutation guard, the `DesignModule` composition seam and focused disposable-PostgreSQL tests. B06A uploads no bytes, creates no Asset, appends no normalization event and adds no generic Session read, list or autosave behaviour. **Session bootstrap is *not* assigned to B06A**: `APP3-B07` (`session bootstrap (blank + clone) and resume`) already owns creation and the `__Host-nettheu_ds_<session-id>` cookie in the accepted phase plan, and `APP3-G08` preserves that owner rather than duplicating it. **(PO-04) `APP3-B06B` publishes exactly one operation** — `POST /api/public/design-sessions/:sessionId/assets`, `multipart/form-data` — which streams one JPEG/PNG/WebP through the API, creates the private Session Asset, associates it to the authorized Design Session and appends inspection and normalization work. No JSON upload body, no second completion operation, no upload intent, no generic Asset endpoint, no SVG, no Template upload and no original-delivery route. Expected surface delta from the accepted `APP3-P03` baseline is `paths 19 → 20` and `operations 23 → 24`; the schema count is **measured at delivery, never pre-invented**. **(PO-05) Media and limits.** Exactly `image/jpeg`, `image/png`, `image/webp`; `image/svg+xml`, GIF, animated or multi-page input, unknown media and `application/octet-stream` are rejected. The source limit is **10 MiB**, enforced by the `APP2-B01` streaming byte counter, and the complete upload is never buffered; an oversized input is refused and never resized into compliance. The Asset lane is `CUSTOMER_UPLOAD` + `CUSTOMER_PRIVATE` — both already exist under `ck_assets__kind_allowed` and `ck_assets__classification_allowed`, so **no migration is required**. **(PO-06) Transaction staging follows the delivered APP2 pattern.** Outside the database transaction: authorize the Session, validate multipart metadata, allocate and claim the Asset through the accepted `IdempotencyAllocationStore` behaviour, stream the bytes to private storage and verify the transfer result. Inside the transaction: verify the Session expected revision under CAS, verify the allocated Asset and upload claim, transition the Asset through the accepted APP2 state, create or confirm the `design_session_assets` association, append `asset.inspection.requested` as APP2 requires, append `asset.normalization.requested` v1, advance the Session revision exactly once, commit. **No object-storage network call occurs inside the database transaction**, and failure uses the accepted object cleanup and reconciliation path. No second queue, scheduler or sweep. **(PO-07) The normalization event is exact.** `event_type = asset.normalization.requested`, `schemaVersion = 1`, `normalizationPolicyVersion = 1`, `associationRef.kind = DESIGN_SESSION_ASSET`, `associationRef.designSessionAssetId = ` the committed association primary key. The payload carries no profile, ownership, storage key, secret, URL, customer identity, session secret or raw bytes; the worker derives `DESIGN_SESSION_ASSET → SESSION_UPLOAD` at claim time from `IMP-D046` PO-03. No association scan. The association write and the event append are atomic. **(PO-08) Inspection/normalization ordering is not yet provable, and is routed to a bounded predecessor.** `APP3-G08` measured the accepted worker: `AssociationResolutionService` requires `REQUIRED_ASSET_STATUS = 'ACCEPTED'` and raises `NORMALIZATION_CONTEXT_NO_LONGER_ELIGIBLE` — a **terminal, non-retryable** `NormalizationRejection` recorded as a completed verdict — for an Asset still `INSPECTING`, stating that such an Asset is "a stale context, not something to wait for". Because B06B commits both events in one transaction, they become visible together, so **mechanism A is false** (nothing orders the two claims; created-at order is not a concurrency guarantee) **and mechanism B is false** (a not-yet-`ACCEPTED` Asset is killed, not retried). This works for `APP3-B01N` only because a Product Side background associates an Asset whose inspection completed long before. `APP3-G08` therefore routes a bounded predecessor, **`APP3-W01C`**, which must make a `DESIGN_SESSION_ASSET` request whose Asset is still `UPLOADED` or `INSPECTING` a **retryable** outcome that converges under the existing lease, backoff and dead-letter policy, while `REJECTED`, deleted, tombstoned and missing Assets stay terminal. `APP3-W01C` adds no polling loop, cron, scheduler, second normalization event, second queue or blind normalization before inspection, and `APP3-B06B` must prove the converged behaviour rather than assume it. **(PO-09) The association repository returns a stable identity.** `APP3-B06A` or `APP3-B06B` may extend the Design Session Asset repository so the producer receives the association primary key: insert-or-confirm on the canonical `uq_design_session_assets__session_asset` uniqueness, returning `designSessionAssetId`, idempotent for the same Session and Asset, refusing an Asset already attached to a foreign Session, and never a bare `void` result for producer use. No migration; the existing primary key and unique constraint are the authority; no duplicate active association is created. **`APP3-G08` implements nothing** — no application, package, schema, migration, OpenAPI, client or dependency change. Gate: `node tools/check-app3-g08.mjs`. Set by `APP3-G08`. | LOCKED |
| IMP-D049 | **APP4 secure-access, verification and notification authority** (`APP4-G01`, ADR `../adr/backend/ADR-APP4-001-SECURE-ACCESS-VERIFICATION-AND-NOTIFICATION-AUTHORITY.md`). Twelve rulings turning the closed `APP4-P00` architecture into values later checkpoints may not invent. **(PO-01) Policy values are versioned configuration, not literals** — four keys published through the existing `policy_configurations` / `policy_configuration_versions` mechanism at value-schema version 1, grouped one key per cohesive object exactly as `worker.runtime` is: `verification.challenge` (`ttlSeconds` 600, `codeLength` 6, `codeAlphabet` `DECIMAL_DIGITS`, `maxAttempts` 5, `resendCooldownSeconds` 60, `rateWindowSeconds` 900, `maxIssuesPerTargetPerWindow` 5), `secure_grant` (`standardTtlSeconds` 604800 = 7 days, `stepUpWindowSeconds` 900 = 15 minutes), `notification.delivery` (`maxAttempts` 3, `retryDelaysSeconds` `[60, 300]`, exhaustion → `DEAD_LETTER`, **no fourth automatic attempt**) and `secure_link.resolve` (`maxRequestsPerIpPerMinute` 30, counting requests never outcomes so it cannot become a token-validity oracle). Dataset: `packages/database/seed/app4-policy-configuration.seed.json`, tier `SYSTEM_REFERENCE` per ADR-DB1-015; the publishing caller is `APP4-B01` because DB9's seed runner never shipped. **No schema change, no migration.** **(PO-02) Contact normalization** — email is trim-then-lowercase of the whole address with **no** provider-specific rule, **no** dot removal and **no** plus-tag stripping (each "helpful" variant is a silent identity merge under ADR-DB2-001); phone is E.164 with default region `VN` / `+84`, explicit country codes preserved. No phone parser exists in the repository; `APP4-P01` selects one and must not add a second. **(PO-03) Masking is deterministic** — email exposes the first Unicode **code point** of the local part then `***`, domain preserved; phone preserves the country code and the final 4 digits only; `notification_intents.recipient_masked` **never** stores the normalized destination. **(PO-04) One non-enumerating public failure** — unknown token, expired, revoked, superseded, wrong target and wrong purpose all answer `404` / `SECURE_LINK_UNAVAILABLE` in the standard envelope, externally identical in body, status and classification; internal audit keeps a bounded non-secret reason class; there is deliberately no `…_EXPIRED`, `…_REVOKED` or `…_WRONG_TARGET`, following the delivered `PUBLIC_DESIGN_TEMPLATE_NOT_FOUND` shape. **(PO-05) Routes** — Storefront `/xac-minh-lien-he` (verification) and `/truy-cap` (secure-link landing), Admin `/support/customer-access`; checked against IMP-D038/IMP-D039 and the delivered route trees, none collides. No APP5/APP6/APP7 business-action route. **(PO-06) Opaque secrets and pepper separation** — verification code exactly 6 CSPRNG decimal digits with **modulo-biased selection prohibited**; secure-link token ≥ **256 bits** CSPRNG in unpadded base64url; both persisted only as peppered HMAC-SHA-256 digests compared in constant time, mirroring `DesignSessionSecretIssuer`/`Verifier`. Dedicated names `VERIFICATION_CODE_SECRET_PEPPER` and `SECURE_LINK_TOKEN_SECRET_PEPPER` (min 32 characters, fail-closed, no unpeppered fallback), **neither of which may be the AEAD key**. **(PO-07) Encrypted delivery envelope** — sole owner `@embroidery/notification-delivery` at `packages/notification-delivery`, **created by `APP4-B01` not by G01**, sealed by `apps/api` and opened by `apps/worker`, no app-to-app import, **no third-party crypto dependency**. `AES-256-GCM` from `node:crypto`, envelope version `1`, fresh random 96-bit IV per seal never reused, binary fields base64url, outer shape `version`/`algorithm`/`iv`/`ciphertext`/`authTag` only, plaintext limited to `secretKind` (`VERIFICATION_CODE` | `SECURE_LINK_TOKEN`), `originNotificationIntentId`, `channel`, `normalizedRecipient`, `secret`, `issuedAt`, `expiresAt`. Persisted to `outbox_events.payload` in the same transaction as the business write — permitted because ADR-DB4-004 rule 4 scopes redaction-by-construction to columns 6 and 8, **not** column 4 — and **never** to `notification_intents.params`. Key `NOTIFICATION_DELIVERY_ENVELOPE_KEY`, base64, **exactly 32 bytes decoded**, no default, **fail closed**, never logged, never committed. Authentication failure is terminal for that attempt and recorded as a bounded `error_class`. **(PO-08) `originNotificationIntentId` is lineage-only** — it is **not** the execution identity; the current intent is the outbox event's aggregate linkage. The two coincide on first delivery, which is exactly why a consumer can use the wrong one and never notice, and diverge on replay. A worker using it as the mutable lifecycle target would force replay to re-seal, which would force the API to decrypt. **(PO-09) Outbox linkage** — `aggregate_kind = NOTIFICATION_INTENT`, `aggregate_id =` the current `notification_intent.id`; `APP4-B01` extends the `OUTBOX_AGGREGATE_KINDS` write-time guard exactly as `APP2-B03` added `PRODUCT` (REL-104 is polymorphic, the column has no CHECK) — **no column, no CHECK, no migration**, and **no ciphertext is ever queried** to find an intent id. **(PO-10) Three delivery contracts, three names, never interchangeable** — *automatic transport retry* (`APP4-W01`: same intent, same outbox event, same envelope, same secret, fresh attempt number, mints nothing); *Admin manual transport replay* (`APP4-B08`: origin `FAILED` intent and origin `DEAD_LETTER` row both remain terminal, one new `PENDING` intent and one new `PENDING` outbox event, byte-identical ciphertext, fresh attempt budget, **the API never decrypts**); *business resend/reissue* (`APP4-B03`/`APP4-B05`: new secret, new challenge or reissued grant, new intent, new outbox event, new envelope). The old row is never reset because `job_key` **is** the outbox event id, so reuse collides with CST-049 and destroys `(job_kind, job_key, attempt_no)` monotonicity; a new intent rather than a reopen is required by DB3 §1/§3 rule 2 and is the only form that functions, since the retry budget derives from the attempt count for that intent. **(PO-11) Deterministic replay key and refusal** — replay `intent_key` is the SHA-256 hex digest of `app4-manual-replay:v1:<originNotificationIntentId>:<deadLetterOutboxEventId>`, non-secret, collapsing duplicate and concurrent Admin replays onto one intent through **existing** `intent_key` uniqueness with **no new idempotency framework**, and never substituting for the existing lock-and-guarded-write convention. Replay is refused as `409` / `REISSUE_REQUIRED` when the source challenge is expired, completed, invalidated, superseded or unanswerable, or the source grant is expired, revoked, superseded or inactive; the operator is routed to the business path and **no plaintext is ever reconstructed from a hash**. **(PO-12) Fragment transport, provider neutrality and one queue** — the secure link is `https://<storefront-origin>/truy-cap#t=<opaque-token>`; the token never appears in a server-visible path or query and therefore never in an access log, `APP4-S02` reads `#t=` locally and strips it with `history.replaceState` **before** any analytics, beacon or third-party activity, holds it in an ephemeral local variable, POSTs it in the body of `/public/secure-links/resolve`, and stores it in **none** of Zustand, TanStack Query cache data, `localStorage`, `sessionStorage`, cookies, persisted React/Next state, analytics or logs; **no query-parameter or path-segment carrier and no fallback**. The `09-SECURITY` §9 "never in a … fragment" sentence is scoped to the anonymous Design Session cookie credential (IMP-D043) and governs no other secret. `EMAIL` contact → `EMAIL` channel, `PHONE` contact → `SMS` channel, **no provider, vendor, sender identity or credential selected** (`IMP-O006` stays open as `APP4-PO-001`), `NotificationChannelPort` plus the recording dev adapter remain the boundary. The APP2 outbox runtime is the **only** queue; `NotificationIntentRepository.claimBatch` has no production caller and gains none. **`APP4-G01` implements nothing** — no customer, verification or notification service, no `packages/notification-delivery`, no AES-GCM runtime, no worker handler, no Admin replay, no UI, no OpenAPI operation, no generated client, no schema, no migration, no provider. `NO_APP4_MIGRATION` holds. Gate: `node tools/check-app4-g01.mjs`. Set by `APP4-G01`. | LOCKED |
| IMP-D050 | **Canonical Storefront public origin for absolute APP4 secure links** (Product Owner ruling — an approved narrow authority unblock granted before `APP4-B05` implementation, after that checkpoint's mandatory pre-implementation audit proved no canonical Storefront-origin authority existed anywhere in the repository; recorded as the addendum `../adr/backend/ADR-APP4-001-SECURE-ACCESS-VERIFICATION-AND-NOTIFICATION-AUTHORITY.md` §11.0, deliberately **not** a separate G-checkpoint). The locked rule: **`STOREFRONT_PUBLIC_ORIGIN` is the canonical public browser origin for absolute APP4 secure links. It is fail-closed, has no default, and secure tokens appear only in URL fragments.** Its meaning is **the absolute public browser origin customers use to reach the Storefront**, and it is explicitly **not** `STOREFRONT_HOST` (a bare gateway hostname carrying no scheme), **not** `INTERNAL_API_BASE_URL` (the API's compose-network address), **not** a CSRF allowlist (`DESIGN_SESSION_ALLOWED_ORIGINS`, `STAFF_ALLOWED_ORIGINS`), **not** a staff origin and **not** a gateway upstream address — promoting any of those into the role would let an unrelated operational change silently repoint every outbound secure link. Validation: absolute URL; `https:` or `http:` only; scheme and host present; no username or password; no query; no fragment; no application path other than `/`; normalized without a trailing `/` before composition. **No default and no fallback**; a missing or malformed value fails closed at the secure-link rendering boundary, and the delivery stays retryable so an operator omission cannot dead-letter a committed grant. No production domain appears in source and example values exist in tests only. It is **public configuration, not credential material**, so it is absent from `.env-ignore`. It is wired **only** into the process that renders — the worker — which composes `<origin>/truy-cap#t=<token>` immediately before the outbound channel sink from the already-decrypted worker-local token; the API never mints the final URL, and the rendered URL is never persisted and never logged. Set by `APP4-B05`. | LOCKED |
| IMP-D051 | **APP6 design review, approval and quotation authority** (`APP6-G01`, ADR `../adr/backend/ADR-APP6-001-CUSTOMER-OWNED-PRODUCT-DESIGN-CONTEXT.md`, authority package `audits/APP6_G01_DESIGN_REVIEW_AND_QUOTATION_AUTHORITY.md`). Six grouped rulings turning the `APP6-R00` audit into values later checkpoints may not invent. **(PO-01) Catalog-XOR-COP design context** — `design_versions` and `approval_snapshots` each carry exactly one branch, the complete Catalog placement quartet **or** `customer_owned_product_id`, on the delivered `order_items` shape; a customer-owned product is **never** given a fabricated or borrowed product, variant, side or embroidery area (INV-13). **(PO-02) COP placement-envelope semantics** — a COP version validates containment against its own frozen positive `physical_width_mm`/`physical_height_mm` **placement envelope**, never against an `embroidery_area_id` it does not have and never against the nullable `customer_owned_products.physical_*_mm`, which describe the item rather than the embroidery placement area; geometry and branch identity freeze at `TR-LC08-02` and change only by a new version. The one field pair existing persistence cannot hold is `design_versions.placement_side_label`/`placement_area_label`, because `approval_snapshots.side_name`/`area_name` and `production_specifications` are `NOT NULL` downstream and the COP branch has no FK to read them through — hence `DB01_SCHEMA_CONTRACT = CORE_XOR_PLUS_DESIGN_VERSION_PLACEMENT_LABELS`, and the `packages/design-document` placement widening (absence as `null`, no existing document hash changed) is routed to `APP6-B08`. **(PO-03) Effective quote expiry is independent of the sweep** — `effective_expired = now >= valid_until` evaluated in transaction, matching the delivered `now >= expiresAt` convention; `TR-LC12-05`/`SE-015` may normalise persistence later but is never the correctness arbiter; validity is **7 calendar days** from the committed send instant, and money stays exact decimal/string from `numeric(14,2)` to the UI. **(PO-04) Approval agreement required type set** — `[PAYMENT_POLICY, RETURN_POLICY]` as policy configuration, preserving the `DB3_AGREEMENT_ACCEPTANCE_SPEC` §2.1 baseline; the customer is shown the exact effective versions the approval binds, GRD-008 verifies the submitted set in transaction, and `approval_snapshot_agreement_acceptances` freezes them. **Corrected by `APP6-G01-C1`:** the exact-design approval confirmation is approval-action semantics enforced by GRD-007, **not** a substitute agreement, and `DESIGN_APPROVAL_TERMS` is not reintroduced; the content of each required type is a minimal, sentence-by-sentence source-traceable normalization of accepted authority — `PAYMENT_POLICY` from BR-004/005/006/008/010, D-012/013/014 and ADR-DB3-001 r2/r3/r4/r7/r8, `RETURN_POLICY` from the ADR-DB3-002 S1–S9 stage matrix and its locked mechanics — published verbatim from `audits/APP6_G01_DESIGN_REVIEW_AND_QUOTATION_AUTHORITY.md` §5.6, so no APP6 checkpoint drafts legal text and no source is `TBD`. **40 %/60 %** (BR-005, D-013/D-014) is preserved as policy authority and APP6 creates no Order, obligation, attempt or collection. Values ship as data in `packages/database/seed/app6-policy-configuration.seed.json`; the reader and publisher are `APP6-B01`. **(PO-05) Client-side review rendering** — safe formal document → delivered APP3 native-SVG renderer → `APP3-S09` runtime watermark → review UI; **no** APP6 server raster pipeline, **no** export or download path, **no** generic private-asset endpoint, and `preview_derivative_id`/`preview_hash` may stay NULL. **(PO-06) Projection-versus-command transition rule** — `QUOTED`, `QUOTE_ACCEPTED`, `DESIGN_REVIEW` and `APPROVED` are reached **only** inside the owning quotation or design transaction, with no sync-state API and no generic set-state use case; `QUOTE_ACCEPTED → DIGITIZING` under `GRD-005` is the only directly commanded APP6 transition. Secure access reuses `REQUEST_ACCESS` and the delivered grant/step-up architecture with no new grant kind, token format or customer account, customer identity is derived from the grant, and every grant-validity failure answers with the delivered non-enumerating `SECURE_LINK_UNAVAILABLE`. | LOCKED |
| IMP-D052 | **APP7 deposit payment authority** (`APP7-G01`, authority package `audits/APP7_G01_DEPOSIT_PAYMENT_AUTHORITY.md`). The Product Owner ruling `PO-APP7-001 = MANUAL_BANK_TRANSFER_WITH_DYNAMIC_QR_AND_OPTIONAL_EVIDENCE`, turned into values later APP7 checkpoints may not invent. **(PO-01) Manual bank transfer, no provider** — exactly one merchant account receives deposits; `PAYMENT_MODEL = MANUAL_BANK_TRANSFER_WITH_DYNAMIC_QR`, `ADMIN_VERIFICATION = REQUIRED`, and `IMP-O007` stays **open** as a deferred, additive integration with no provider ADR created. **(PO-02) Merchant bank configuration is operational config, not a secret** — `PAYMENT_MERCHANT_BANK_BIN`, `PAYMENT_MERCHANT_ACCOUNT_NUMBER`, `PAYMENT_MERCHANT_ACCOUNT_NAME`, `PAYMENT_MERCHANT_BANK_DISPLAY_NAME`, loaded by a module-scoped fail-fast provider, declared in `.env.example` with placeholders, never hard-coded in frontend source or generated-client constants; the names deliberately avoid the `CLAUDE.md` §8a protected pattern because these values are printed on the customer’s own screen, and a later checkpoint must not rename them into it. No bank API credential, webhook secret or signing key exists. **(PO-03) Payment reference format locked** — `ORD` + the 10-character order-code body + a 2-character obligation kind (`DC` deposit, `RM` remaining), `^[A-Z0-9]{15}$`: uppercase alphanumeric only so it survives bank memo normalization, parsed by position rather than by a delimiter, 15 characters against the 25-character EMVCo purpose field, unique through `uq_orders__code`, deterministic from `(order code, kind)` and therefore **derived, never persisted**; the customer never composes it and free-form customer text is never reconciliation authority. **(PO-04) Amount and QR** — the QR amount is the DEPOSIT obligation’s own frozen amount in VND, never recomputed or rounded in the frontend and never editable; the QR is generated **locally on the server** from merchant config + amount + reference, is downloadable, and is **not persisted** because every input is deterministic; a remote QR service is prohibited, and if standards-compliant local encoding proves impossible the owning checkpoint reports the constraint instead of falling back. **(PO-05) Optional transfer evidence** — `TRANSFER_EVIDENCE_REQUIRED = false`, `SUPPORTED = true`, `AUTHORITY = SUPPORTING_RECONCILIATION_ONLY`: a fourth delivered intake lane (`CUSTOMER_UPLOAD` / `CUSTOMER_PRIVATE`, 10 MiB, `image/png|jpeg|webp` by content signature, namespace `public.order.deposit-evidence.upload`), bound **server-side** to the payment attempt, at most **5 per attempt**, **append-only** with no customer delete or replace, retained through the delivered G4 tombstone flow; the association is written in intake Tx B while the asset is still `UPLOADED` and Admin preview refuses anything not `ACCEPTED` — a deliberate divergence from `custom_request_assets`, which binds at a later event. Neither QR generation, QR download, a customer asserting payment, nor an evidence upload changes payment state: only Admin verification moves `attempt -> SUCCEEDED`, `obligation -> SATISFIED`, `order -> DEPOSIT_PAID`, and a correct payment with no evidence verifies normally. **(PO-06) Schema and roadmap consequence** — `ADR-DB4-003` forbids a generic `asset_links` table and requires a context-owned association with `NOT NULL` `restrict` FKs on both ends; CTX-PAY has none and no delivered table can truthfully hold it, so `APP7_SCHEMA_DISPOSITION` is revised from `NO_MIGRATION_REQUIRED` to **`MIGRATION_REQUIRED` for the evidence association alone** (`payment_transfer_evidence`, no `role` column, the 5-per-attempt bound enforced in the application under the attempt row lock). Manual verification claims **no** idempotency namespace, because DB3 scopes `payment.callback` per provider event id and no provider event exists — the arbiter is the obligation state predicate under row lock (CC-10). Roadmap revised 12 → 15 checkpoints, adding `APP7-DB01`, `APP7-B05` and `APP7-B06`; secure access reuses `REQUEST_ACCESS` + the attempt's own step-up challenge with no new grant scope or token format. | LOCKED |
| IMP-D053 | **Shared human business-code mechanism and the `ORDER_CREATION` job kind** (`APP7-W01`). Two narrow implementation values later checkpoints may not re-decide. **(a) One code mechanism, one home.** `REQ-`, `QUO-` and `ORD-` are the same code — a fixed prefix plus ten characters drawn from a CSPRNG over the `G01-D12` alphabet `23456789ABCDEFGHJKMNPQRSTVWXYZ` with rejection sampling at 240 — and that mechanism now lives once, in `@embroidery/domain-types` (`codes/human-code.ts`: `HUMAN_CODE_ALPHABET`, `HUMAN_CODE_BODY_LENGTH`, `humanCodePattern`, `generateHumanCode`). `quotation-code.ts` deferred the promotion explicitly to "whoever needed the third code"; `APP7-W01` is that consumer and is the first outside `apps/api`, which is what makes an app-shared home insufficient. **Prefixes stay at their narrowest scope** — `REQUEST_CODE_*` in the order module, `QUOTATION_CODE_*` in the quotation module, `ORDER_CODE_*` in the worker's order-conversion domain — and each keeps its published constant names, so `FU-APP6-B01-CODE-GENERATOR-PROMOTION-01` is **closed** with `REQ`/`QUO` behaviour byte-identical. No historical code is migrated or rewritten; the uniqueness arbiter stays the database (`uq_custom_requests__code`, `uq_quotations__code`, `uq_orders__code`); a code is never an authorization input (CST-026); **no second alphabet or mechanism may be introduced** for a future code. The Storefront's deliberate transcription in `custom-request-confirmation/model/request-code.ts` is unchanged — a browser bundle may not reach into a backend application. **(b) `BACKGROUND_JOB_KINDS` gains `ORDER_CREATION`.** Order conversion files its attempt evidence under the **domain** kind, not the transport kind `OUTBOX_DISPATCH`, on the exact IMP-D030 precedent (`ASSET_PROCESSING`): filing domain work under the transport kind hides it from an operator's dead-letter query. `background_job_attempts.job_kind` is `text` with **no CHECK**, so this is the G-DB7-51 write-time guard and a source change only — **no migration**, on the same terms `OUTBOX_AGGREGATE_KINDS` gained `PRODUCT` and `NOTIFICATION_INTENT`. Set by `APP7-W01`. | LOCKED |
| IMP-D054 | **Shared canonical persistence for the two aggregates both applications write** (`APP7-W01-C1`). AGG-15 Order and AGG-16 Payment persistence live **once**, in `@embroidery/persistence` (`src/order/`, `src/payment/`), and are consumed by `apps/api` and `apps/worker` through `OrderPersistenceModule` / `PaymentPersistenceModule`. The rule this locks: **a runtime application boundary is a reason to share persistence authority, not to duplicate it.** `APP7-W01` reached the opposite conclusion — it restated GRD-009 as a worker-local `WorkerOrderChainGuard` and wrote `orders`, `order_items`, `payment_obligations` and `order.created` with its own SQL, because the worker may not import `apps/api` — and every behavioural suite still passed, which is precisely why the defect had to be removed rather than tested around: a second chain check fails years later, when one copy is corrected and the other is not, and an order pairs one customer's approved artwork with another customer's accepted price while every foreign key stays satisfied (G-DB7-05 / INV-19). **Locked consequences:** `OrderRepository.createFromAcceptedQuotation` is the sole creator of an Order and the sole producer of `order.created` (SE-006 / G-DB7-54); `OrderChainGuard` is the sole implementation of GRD-009 and is invoked as an object, never replaced by a caller-supplied pre-validated result; `PaymentObligationRepository.createForOrder` is the sole creator of a payment obligation. A consuming application may keep a local **read** seam for facts no canonical repository owns (APP7's is the frozen Approval Snapshot projection, the accepted quotation's priced lines, the active-SKU lookup and the customer-owned-product lookup) and may not write a canonical row. **Home selection is settled and is not reopened per phase:** the existing persistence package, because it already carries the DB7 runtime, `drizzle-orm`/`pg`, a Nest module and both applications as consumers — not a new top-level package, and never `@embroidery/domain-types`, which stays free of Drizzle/PostgreSQL. `CustomRequestId` and `RequestActor` moved with the Order contract that names them and are re-exported by `custom-request.repository.ts`; three thin re-exports at the delivered contract paths keep every existing consumer's import and, critically, the **same Symbol instances**, so no injection token was split. Recurrence is pinned by a focused structural suite rather than by convention. **No schema, migration, OpenAPI or client change**; delivered DB7 behaviour is byte-identical apart from import paths. Set by `APP7-W01-C1`. | LOCKED |
| IMP-D055 | **`payment_transfer_evidence` — the CTX-PAY transfer-evidence association** (`APP7-DB01`, migration `0037`, TBL-079, REL-109 ×2). The first application-era *table* added after the DB4 catalogue closed, authorized by `PO-APP7-001` / `APP7-G01` §9. Exactly two business columns — `payment_attempt_id` and `asset_id`, both `NOT NULL` and both `ON DELETE RESTRICT` — plus the convention `id` and `created_at`, and `UNIQUE (payment_attempt_id, asset_id)` as the CST-043 association rule. **What the shape locks:** `ADR-DB4-003`'s prohibition on a generic `asset_links` table is not relaxed — each consuming context still owns a typed association, and Payment now owns its own instead of borrowing Ordering's `custom_request_assets` (whose `role` set is a closed CHECK and which binds to the request, not the attempt, so a retry's evidence could not be told from the previous attempt's). No `role` column, because the table has exactly one meaning. No `grant_id`/`step_up_challenge_id`, because `payment_attempts` already carries both and evidence never migrates between attempts (LC-16). No `submitted_at`, because `created_at` *is* the submission instant. No media type, byte size, filename or object key, because Asset owns the binary. No `updated_at`, because the association is append-only. **`MAX_EVIDENCE_PER_ATTEMPT = 5` is deliberately not physical**: a CHECK cannot count sibling rows, so it stays an application guard evaluated under the payment-attempt row lock (`APP7-B05`), exactly as `MAX_ACCEPTED_UPLOADS_PER_CHALLENGE` is enforced today — the database must accept a sixth row. No trigger family is invented for append-only either; no sibling association table has one. **This narrows `APP7-G01` §9's proposed column list** (which also listed `grant_id`, `step_up_challenge_id` and `submitted_at`) to the minimum the invariant needs, on the PO's explicit `APP7-DB01` instruction; the owner context, table name, both FK behaviours and the uniqueness rule are unchanged from G01. **No new asset kind or classification, no payment or order state change, no OpenAPI/client change, no runtime code.** Set by `APP7-DB01`. | LOCKED |
| IMP-D056 | **APP8 inventory-reservation and production-operations authority** (`APP8-G01`, authority package `audits/APP8_G01_INVENTORY_AND_PRODUCTION_AUTHORITY.md`). Six Product Owner rulings turning the `APP8-R00` audit into values later APP8 checkpoints may not invent. No ADR: every ruling resolves inside an already-accepted architecture and three of them make a business decision the accepted documents explicitly deferred. **(PO-APP8-001 = APPROVED_OPTION_A) `GRD-015`'s reservation clause binds per *reservable order subject*, not per order** — a Catalog-only order starts production only on a valid exact approval snapshot + DEPOSIT `SATISFIED` via `DepositEligibilityPort` + order `DEPOSIT_PAID` + not `ON_HOLD`/`CANCELLING` + every inventory-reservable Catalog quantity covered by a reservation that is `RESERVED` when the start transaction consumes it; a **COP-only order may enter production with no reservation** once those non-inventory guards pass, because a COP `order_item` has `sku_id IS NULL` and `inventory_reservations.sku_stock_id` is `NOT NULL` — APP8 fabricates no SKU, no `sku_stocks` row and no reservation, and the absence is expected behaviour, not an error; a mixed order needs coverage for its Catalog portions only. Cardinality stays the repository's: `CST-016` permits one active `RESERVED` row per `(order_id, sku_stock_id)`, so several Catalog items on one SKU share **one** reservation whose quantity covers the order's frozen Catalog quantity for that SKU. One `GRD-015` implementation, one deposit predicate, no COP fake-inventory path. **(PO-APP8-002) APP8 official reservations are no-expiry** — `expires_at = NULL`, no sweep, no TTL value chosen, soft-hold expiry untouched; authorised by `ADR-DB1-018` r3 ("an explicit no-expiry marker where the business rules say so") and its Deferred Details, `DB4_SCHEMA_CATALOG_INVENTORY_ASSET.md` §37–38 and `TR-LC17-07`; `DEC-14` stays deferred/nonblocking. **(PO-APP8-003) Production job creation is Admin-initiated** — synchronous through the Admin API, post-deposit, against the exact approval snapshot, one job per `(order_id, approval_snapshot_id)` with the immutable specification frozen in the same transaction; `TR-LC18-01` permits "system/admin" and APP8 selects admin; the worker gains no job-creation duty and `SYSTEM` stays the actor for the `payment.verified` reservation consumer. **(PO-APP8-004) Production artifact generation and management are deferred/nonblocking** — no DST/PES/EXP writer, machine runner, new object storage, customer export or APP8 artifact surface; `attachArtifact` stays untouched and unused; `TR-LC18-03` carries no guard so nothing is bypassed; owner **UNASSIGNED**, deliberately not routed to APP9 for symmetry. APP8's authoritative production input is the frozen `production_specifications` row plus the approved design evidence already reachable through APP6. **(PO-APP8-005) APP8 ends at job `COMPLETED` + order `PRODUCTION_COMPLETED`** (`TR-LC14-04`); `TR-LC14-05`, remaining payment, shipping freeze, dispatch, delivery, cancellation execution, refund and final settlement are APP9's, and APP7's unsatisfied `REMAINING` obligation is preserved. **(PO-APP8-006) `IMP-D054` applies narrowly** — inventory persistence is promoted/reused through `@embroidery/persistence` because `apps/worker` and `apps/api` both write it, while **production persistence stays API-local** and is promoted later only if a concrete accepted cross-runtime consumer is proven; shared by demonstrated cross-runtime need, never by symmetry. Inherited and restated: `DepositEligibilityPort` is the sole deposit authority; both schema dispositions are `NO_MIGRATION_REQUIRED` and no `production_job_attempts` table is created; the existing PostgreSQL outbox/claim runtime (`IMP-D029`) is reused with no broker, queue table, second datastore or alternate idempotency subsystem; `CUSTOMER_UI_DISPOSITION = NO_CUSTOMER_UI_IN_APP8`. Roadmap unchanged. Set by `APP8-G01`. | LOCKED |
| IMP-D022 | Application logging is repository-owned, API-local structured logging: one-line JSON per record with a fixed platform schema (`schemaVersion`, `timestamp`, `level`, `service`, `event`, `message`, plus request/actor correlation and optional `context`/`http`/`error`/`attributes`), levels `debug`/`info`/`warn`/`error`, and a locked platform event taxonomy (`application.log`, `http.request.completed`, `platform.error`) — no business/audit event names. **No third-party logging framework and no remote/file transport**; the sink is newline-delimited JSON on stdout/stderr. Redaction runs **before** serialization (sensitive-key denylist plus conservative value patterns), with bounded depth/size and circular/throwing-value safety. Request/actor correlation is read from the B02 context and B04 actor; the query string, request/response body and raw headers/cookies are never logged; the 5xx internal-error log is redacted and separate from the completion record. A structured log is **not** an audit event. The shared `@embroidery/observability` package stays empty until it is made runtime-loadable (IMP-D018); no observability vendor is selected (no ADR). Set by APP0-B05. | LOCKED |

## 2. Decisions owned by future phases

These remain unresolved until the owning phase audits current repository ADRs and evidence. They must not be invented inside implementation.

| ID | Decision | Owner | Blocking point |
|---|---|---|---|
| IMP-O001 | Concrete staff authentication/session mechanism. **RESOLVED by IMP-D027** (APP1-DEC-AUTH): built-in `crypto.scrypt` hashing, server-side revocable opaque sessions, host-only `Strict` cookie, layered CSRF, in-process rate limiting, out-of-band bootstrap CLI; no migration; also resolves DEC-29. Customer authentication remains APP4 (IMP-O006 provider scope). ADR: `../adr/backend/ADR-APP1-001-STAFF-AUTHENTICATION-AND-SESSIONS.md`. | APP1 | Resolved — unblocks APP1-B01 |
| IMP-O002 | Concrete object-storage product/adapter configuration. **RESOLVED by IMP-D028** (APP2-DEC-STORAGE, corrected by `APP2-DEC-STORAGE-C1` and `APP2-DEC-STORAGE-C2`): S3-compatible behind `ObjectStoragePort`, MinIO dev / S3-compatible prod, AWS SDK v3 S3 (`client-s3`+`lib-storage`; presigner removed) + `busboy` T1 single streaming multipart, post-object `assets` insert (storage-first / two idempotent transactions), **idempotency model I1 pre-stream durable allocation + pre-stream fingerprint (repository implementation gap, no schema gap)**, two private buckets, UUIDv7 key scheme, server SHA-256, raster-only allowlist (SVG rejected), proxied publication-gated public delivery, `NO_APP2_MIGRATION`; new `APP2-I01` foundation checkpoint. The four intake gate blockers routed out of the decision — `STORAGE-BLK-01/02/03` and `UPLOAD-POLICY-BLK-01` — are **closed by the entry gate `APP2-B01-G01` (2026-07-27)**, so `APP2-B01` is `READY — NOT STARTED`. ADR: `../adr/backend/ADR-APP2-001-OBJECT-STORAGE-AND-ASSET-INTAKE.md` (§4.2f). | APP2 | Resolved — `APP2-I01` complete; `APP2-B01` unblocked by `APP2-B01-G01` |
| IMP-O003 | Concrete queue/broker and job runtime. **Owner assigned at APP0 closure (APP0-X01): APP2.** The APP0 verification required by this row was performed against the canonical APP2 plan, which contains `APP2-W01 — Asset inspection and derivatives: one idempotent worker job family with attempt records, bounded retry, terminal failure, and object-storage safety` — real asynchronous work, and the earliest of it. APP2 therefore decides the concrete mechanism (a database-backed claim queue on the existing persistence, which already has worker-claim indexing under `ADR-DB5-003`, versus an external broker) by ADR before `APP2-W01`. APP0 implemented no queue: no APP0 requirement for one appeared, and the worker application emits no jobs yet. **`APP2-PRE-AUDIT` classified this `ADR_REQUIRED`**: the DB-backed claim path is fully pre-designed (`ADR-DB5-003` skip-locked exclusive claim; `outbox_events` `FOR UPDATE SKIP LOCKED`; `background_job_attempts`; `idempotency_records`), but `.env.example` records "Queue/broker configuration is an open decision" and no runtime mechanism is ADR-locked. Decision checkpoint `APP2-DEC-JOBS` (DB-backed claim queue vs external broker, + worker correlation seam) precedes `APP2-W01`. **RESOLVED by IMP-D029** (APP2-DEC-JOBS, delivered for review): PostgreSQL-backed claim queue on the existing persistence (J1) — `outbox_events` durable work signal + `background_job_attempts` + `FOR UPDATE SKIP LOCKED` claim + visibility-timeout lease on `next_attempt_at`; J2 Redis/BullMQ and J3 RabbitMQ rejected; **no new runtime dependency, `NO_APP2_MIGRATION`**; adds `APP2-I02` worker-runtime foundation (owns FU-A07) → 19 checkpoints; `STORAGE-BLK-01..03` routed to `APP2-B01`. ADR: `../adr/backend/ADR-APP2-002-ASYNCHRONOUS-JOB-RUNTIME.md`. | **APP2** (verified in APP0, not blindly APP4) | Resolved — unblocks `APP2-I02` / `APP2-W01` (subject to review) |
| IMP-O004 | Canvas/SVG library and rendering architecture. **RESOLVED by IMP-D026** (APP0-R01): native SVG rendered by React 19, no rendering-engine dependency, behind a mandatory engine-neutral document + renderer-adapter boundary; ADR `../adr/frontend/ADR-APP0-001-2D-RENDERING-ARCHITECTURE.md`. | APP0 (spike APP0-R01) | Resolved — unblocks APP3 |
| IMP-O005 | Two separate open tool decisions kept under one parent ID: (a) frontend component test tool (`APP0-DEC-COMPONENT-TEST`) — **RESOLVED by IMP-D024** (Jest + `next/jest` + `jsdom` + React Testing Library + `user-event` + `jest-dom`); (b) browser E2E test tool (`APP0-DEC-E2E`) — **RESOLVED by IMP-D025** (Playwright Test `1.61.1`, dedicated `@embroidery/e2e-testing` package, disposable-DB reuse of T01/DB7, tiered Chromium/Firefox/WebKit matrix). Each requires its own ADR. Jest is already selected for unit/integration (see IMP-D016) and is not reopened. | APP0 | Both resolved — (a) unblocked APP0-T02A; (b) unblocks APP0-T02B |
| IMP-O011 | Generated-client codegen tool (OpenAPI → TypeScript/Axios). **RESOLVED by IMP-D023** (APP0-DEC-CODEGEN): Orval `8.22.0`, `axios-functions` + `mutator`, generated types/functions under `packages/api-client/src/generated/`, existing handwritten Axios/error-normalization runtime preserved. | APP0 | Resolved — unblocks APP0-C02 |
| IMP-O006 | Contact/notification providers and delivery channels. **Still open after APP4 closure, by design**: APP4 ships `NotificationChannelPort` + the in-memory recording adapter and delivers to nobody. Routed to **APP12 — Hardening, UAT and Production Readiness**. | APP12 (routed from APP4 at `APP4-X01`) | Before production notification delivery |
| IMP-O007 | Payment provider, checkout, webhook signature and sandbox. **Still open after `APP7-G01`, by design, and no longer a phase blocker.** `PO-APP7-001` (IMP-D052) locked `MANUAL_BANK_TRANSFER_WITH_DYNAMIC_QR_AND_OPTIONAL_EVIDENCE`: APP7 collects the deposit by bank transfer with a server-generated QR and Admin manual server-side verification, so no provider, checkout session, webhook, signature scheme, sandbox or provider credential is needed or created, and **no provider ADR is written for this flow**. The deferral is additive by construction — `payment_provider_events` with its `(provider_key, provider_event_ref)` uniqueness, `recordProviderEvent`, the `PROVIDER_REDIRECT` attempt method and the nullable `provider_key`/`provider_ref` columns all already exist, so a future provider phase adds a webhook checkpoint and a checkout screen without reworking order conversion, obligations, verification or transfer evidence. Re-opened by whichever phase first requires automatic payment confirmation. | APP7 (deferred) → re-open owner: first phase requiring automated payment confirmation | Not an APP7 blocker; before any provider-dependent implementation |
| IMP-O008 | Cancellation/refund policy parameters | APP9 | Before cancellation/refund checkpoints |
| IMP-O009 | Exact design classification and Figma references for every phase | Each phase | Before frontend engineering |
| IMP-O010 | Whether Storybook is worth its maintenance cost. **Closed by APP0-X01 as NOT ADOPTED for APP0** — APP0 built no component library, so there was nothing to catalogue and no maintenance case; the locked component-test stack (IMP-D024) already covers interaction and accessibility assertions. Not a stage blocker, as this row always stated. Re-openable by the first phase that builds a reusable UI component set (`@embroidery/ui`, earliest APP1/APP2) and only through an ADR. | APP0 (closed) → re-open owner: first phase building `@embroidery/ui` | Not adopted; no blocking point |

## 3. Decision procedure

For an open consequential decision:

1. Audit existing repository ADRs and implementation.
2. Compare options against locked product/architecture constraints.
3. Run a bounded spike only when evidence is required.
4. Create or update an ADR.
5. Record the decision here or link the authoritative ADR.
6. Only then unblock dependent checkpoints.

Do not use a checkpoint implementation as an implicit ADR.
