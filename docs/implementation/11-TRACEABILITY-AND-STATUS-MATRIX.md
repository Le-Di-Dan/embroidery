# Traceability and Status Matrix

## 1. Purpose

This matrix provides stage-level traceability. Exact requirement, lifecycle, table, query, design node, endpoint, and test IDs are added during each phase audit.

The canonical backend module/bounded-context ownership map (reconciled at APP0-C01) is owned by [`../architecture/SYSTEM_ARCHITECTURE.md`](../architecture/SYSTEM_ARCHITECTURE.md) §8 (logical ownership + dependency direction) and [`../architecture/REPOSITORY_STRUCTURE.md`](../architecture/REPOSITORY_STRUCTURE.md) §11/§11a (physical placement + packages). This matrix does not duplicate that map.

## 2. Phase matrix

| Phase | Backend modules/capabilities | Admin surface | Storefront/customer surface | Worker/integration | Milestone |
|---|---|---|---|---|---|
| APP0 | Cross-cutting application foundation | Shell foundation only | Shell foundation only | Harness/observability/spikes | R0 |
| APP1 | Staff identity, sessions, roles, permissions, audit actor | Login, shell, navigation, forbidden/session states | Public shell | Session/security support | R0 |
| APP2 | Asset, catalog, publication | Asset library, product list/form/detail/publication | Product list at `/kham-pha` (IMP-D038; `?category=<slug>`, staged non-interactive cards); product detail at `/san-pham/[slug]` (IMP-D039; `publicProductDetail` only, no price/stock display; mobile band 24px/342px and `SAFE_STREAMED_NOT_FOUND` per IMP-D040) | Asset inspection/derivatives | R1 |
| APP3 | Template, design session/version/document (every schema-backed HTTP request body publishes through the `APP3-P03` platform Zod→OpenAPI foundation; no module hand-decorates its own) | Template list/editor/publication | 2D Design Studio | Autosave/processing as required; editor-safe normalization is **association-bound** — dispatched by `asset.normalization.requested` from the transaction that creates or changes a Product Side, Template or Session association, never by the upload-time inspection event (`IMP-D046`); the raster consumer is delivered by `APP3-W01A` (JPEG/PNG/WebP → one `NORMALIZED` WebP derivative per Asset and policy version), Template SVG stays authorized and operationally unavailable until `APP3-G07` selects a sanitizer and `APP3-W01B` implements it; the Product Side trigger is delivered by `APP3-B01N`, which appends the event in the placement replace transaction itself and shares the event contract with the consumer through `@embroidery/domain-types`; Template SVG is sanitized by DOMPurify on server-side jsdom under a closed allowlist with whole-file rejection and a fixed-point deterministic serialization (`IMP-D047`), and stays operationally unavailable until `APP3-W01B` implements it; the anonymous Session upload lane is **API-owned multipart streaming** with no presign, no upload-intent/completion pair and no browser upload token (`IMP-D048`), split into the `APP3-B06A` authorization foundation and the `APP3-B06B` single upload operation, with Session bootstrap remaining owned by `APP3-B07` and the inspection→normalization ordering guarantee owned by `APP3-W01C`; anonymous Session mutations are authorized by the reusable `APP3-B06A` foundation — ownership is the pair (public id from the path plus the secret in the cookie that id derives), verified as a peppered HMAC in constant time, with one non-enumerating refusal, Origin and Sec-Fetch-Site both required, the `IMP-D043` PO-07 limits on the shared sliding window, and a reusable expected-revision CAS seam; `APP3-B07` remains the sole issuer of the session cookie and delivers the two anonymous Session operations — a blank or Template-cloned bootstrap on an exactly-resolved, Studio-eligible public placement, and a resume that rotates the secret under an old-digest guard without extending the 30-day TTL or advancing the document revision | R2 |
| APP4 | Customer, contact verification, secure grant, notification core | Customer/contact/secure-link operational views | Verification/secure entry | Notification intent/attempt delivery | R3 prerequisite (contributes to R3; `APP3-B06B` then delivers the single anonymous raster intake operation on that same Session credential — API-owned multipart streaming, 10 MiB, `CUSTOMER_UPLOAD`/`CUSTOMER_PRIVATE`, insert-or-confirm association and exactly one inspection and one normalization event per intake; R3 closes at APP6) |
| APP5 | Customer-owned product, request, request asset, moderation/transition | Request queue/detail/moderation | Request creation/status | Asset consequences/notifications | R3 |
| APP6 | Design case/version/review/approval, quotation | Review workbench, quote editor/history | Secure review, approval, quote response | Notification delivery | R3 |
| APP7 | Payment obligation/attempt/callback, order conversion | Payment/reconciliation/order view | Deposit checkout/status | Provider webhook/reconciliation/outbox | R4 |
| APP8 | Inventory hold/reservation, production job/attempt | Inventory and production board | Bounded order status | Worker claims/retries | R4 |
| APP9 | Remaining payment, fulfillment/shipping freeze, completion | Fulfillment/payment/completion operations | Remaining payment/order completion status | Provider/notification consequences | R4 |
| APP10 | Profile, merge, notification operations, agreements | Customer ops, merge, delivery attempts | Profile/contact preferences where in scope | Zalo/Messenger simple handoff | R5 |
| APP11 | Gallery, content, SEO metadata | Content/gallery management | Homepage/discovery/gallery/content | Asset derivatives/revalidation | R5 |
| APP12 | Cross-cutting hardening | UAT and operational tooling | UAT/accessibility/performance | Monitoring, backup/restore/deployment | R6 |

## 3. Status values

Use:

- `NOT_STARTED`
- `DESIGN_AUDIT`
- `DESIGN_IN_PROGRESS`
- `READY_FOR_ENGINEERING`
- `ENGINEERING_IN_PROGRESS`
- `REVIEW_REQUIRED`
- `BLOCKED`
- `COMPLETE`

Do not mark a phase `COMPLETE` based only on design, backend, or frontend completion.

Current values are recorded once, in [`10-MASTER-APPLICATION-ROADMAP.md`](./10-MASTER-APPLICATION-ROADMAP.md) §6, and are not duplicated here. As of APP1-X01: **APP0 = `COMPLETE`** (evidence [`reports/APP0-X01-COMPLETION-REPORT.md`](./reports/APP0-X01-COMPLETION-REPORT.md)), **APP1 = `COMPLETE — PASS_WITH_FOLLOW_UPS`** (closed at `APP1-X01`, zero blocking follow-ups — [`reports/APP1-COMPLETION-REPORT.md`](./reports/APP1-COMPLETION-REPORT.md), evidence [`reports/APP1-CLOSURE-EVIDENCE.md`](./reports/APP1-CLOSURE-EVIDENCE.md); milestone **R0 achieved**) (`APP1-DEC-AUTH` complete — staff auth IMP-D027; `APP1-D01` `COMPLETE — ADMIN DESIGN APPROVED` (Admin login+shell approved for implementation, `FIG-APPROVAL-APP1-D01-ADMIN-001`, 13 rows promoted) — [`APP1-D01-COMPLETION-REPORT.md`](./reports/APP1-D01-COMPLETION-REPORT.md), registry [`FIGMA_DESIGN_INDEX.md`](../design/FIGMA_DESIGN_INDEX.md); **`APP1-B01` `COMPLETE — CORRECTED`** — staff session authentication, two endpoints, no migration, [`APP1-B01-COMPLETION-REPORT.md`](./reports/APP1-B01-COMPLETION-REPORT.md); validation corrected to the canonical Zod pipeline by `APP1-B01-C1` ([`APP1-B01-C1-CORRECTION-REPORT.md`](./reports/APP1-B01-C1-CORRECTION-REPORT.md)); **`APP1-B02` `COMPLETE — CORRECTED`** — current staff `GET /api/staff/me`, one endpoint, no migration, [`APP1-B02-COMPLETION-REPORT.md`](./reports/APP1-B02-COMPLETION-REPORT.md); success-data requirement corrected by `APP1-B02-C1` ([`APP1-B02-C1-CORRECTION-REPORT.md`](./reports/APP1-B02-C1-CORRECTION-REPORT.md)); **`APP1-A01` `COMPLETE — CORRECTED, DELIVERED_FOR_PRODUCT_OWNER_REVIEW`** — Admin `/login` screen, no migration, [`APP1-A01-COMPLETION-REPORT.md`](./reports/APP1-A01-COMPLETION-REPORT.md); **`APP1-A01-C1` `COMPLETE`** — login readiness correction (toggle hydration via dev gateway, route protection, automatic Compose bootstrap), [`APP1-A01-C1-CORRECTION-REPORT.md`](./reports/APP1-A01-C1-CORRECTION-REPORT.md); **`APP1-A01-C2` `COMPLETE`** — real isolated-Compose bootstrap-policy evidence (dev-fail/create/reuse/prod-skip/unknown-env) + result-line fix, [`APP1-A01-C2-CORRECTION-REPORT.md`](./reports/APP1-A01-C2-CORRECTION-REPORT.md); Product Owner completed the A01 live test → **`APP1-A01` `COMPLETE — CORRECTED — PRODUCT_OWNER_ACCEPTED`**; Product Owner live-tested and accepted A02 → **`APP1-A02` `COMPLETE — PRODUCT_OWNER_ACCEPTED`** — authenticated Admin shell (protected-layout shell, one server-hydrated current-staff query, logout, client session-expiry modal, mobile drawer; no migration; `staffSelfGet`/`staffSessionDelete` — [`APP1-A02-COMPLETION-REPORT.md`](./reports/APP1-A02-COMPLETION-REPORT.md)); Product Owner reviewed and PASSED the D02 package → **`APP1-D02` `COMPLETE — PRODUCT_OWNER_ACCEPTED`** — Storefront shell & not-found design SUPPLEMENT (seven §4.2 rows `APPROVED_FOR_IMPLEMENTATION`, `FIG-APPROVAL-APP1-D02-STOREFRONT-001` — [`APP1-D02-COMPLETION-REPORT.md`](./reports/APP1-D02-COMPLETION-REPORT.md)); the Product Owner live-tested and accepted S01A → **`APP1-S01A` `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (shared shell + responsive navigation, focus-trapped mobile drawer, honest non-interactive nav/search, SCSS-only — [`APP1-S01A-COMPLETION-REPORT.md`](./reports/APP1-S01A-COMPLETION-REPORT.md)); the Product Owner live-tested and accepted S01B → **`APP1-S01B` `COMPLETE — PRODUCT_OWNER_ACCEPTED`** (Storefront not-found boundary — canonical `app/not-found.tsx` inside the accepted shell, HTTP 404, safe Vietnamese copy, `/` primary recovery + honestly-unavailable secondary, SCSS-only, no API/schema change — [`APP1-S01B-COMPLETION-REPORT.md`](./reports/APP1-S01B-COMPLETION-REPORT.md)), so the parent **`APP1-S01` `COMPLETE — PRODUCT_OWNER_ACCEPTED`**; **`APP1-E01` `COMPLETE — CORRECTED — REVIEW_ACCEPTED`** (staff access & shared-shell cross-layer acceptance; 17 tests; corrected by **`APP1-E01-C1` `COMPLETE`** — invalid/stale-cookie route matrix and initial protected-page API-unavailability journeys, test/harness only — [`APP1-E01-C1-CORRECTION-REPORT.md`](./reports/APP1-E01-C1-CORRECTION-REPORT.md)), **`APP1-X01` `COMPLETE`** (phase closure `PASS_WITH_FOLLOW_UPS`)), APP2–APP12 = `NOT_STARTED` (**APP2 `APP2-PRE-AUDIT` complete** — `AUDITED — PASS_WITH_REQUIRED_DECISIONS`, `NO_APP2_MIGRATION`; [`audits/APP2_PRE_IMPLEMENTATION_AUDIT.md`](./audits/APP2_PRE_IMPLEMENTATION_AUDIT.md); **`APP2-DEC-STORAGE` `COMPLETE — CORRECTED, DELIVERED_FOR_REVIEW`** — object-storage/asset-intake locked by IMP-D028 / `ADR-APP2-001` (`PASS_WITH_PRODUCT_PARAMETERS`, `NO_APP2_MIGRATION`, adds `APP2-I01` foundation → 18 checkpoints), **corrected by `APP2-DEC-STORAGE-C1`** (post-object `UPLOADED` insert / storage-first two-transaction lifecycle; T1 `busboy`+`lib-storage` streaming multipart; route-scoped nginx `proxy_request_buffering off`; server-only SHA-256; `OBJECT_STORAGE_PUBLIC_BASE_URL` removed — [`reports/APP2-DEC-STORAGE-C1-CORRECTION-REPORT.md`](./reports/APP2-DEC-STORAGE-C1-CORRECTION-REPORT.md)) **and `APP2-DEC-STORAGE-C2`** (upload idempotency model I1 pre-stream durable allocation + pre-stream fingerprint / CW-01…CW-07; presigner removed from the APP2 set; `lib-storage` memory bounds + C1 heap-claim corrected; repository implementation gap not schema gap — [`reports/APP2-DEC-STORAGE-C2-CORRECTION-REPORT.md`](./reports/APP2-DEC-STORAGE-C2-CORRECTION-REPORT.md)); **`APP2-DEC-STORAGE` accepted** as `ACCEPTED_WITH_BLOCKED_IMPLEMENTATION_GAPS` (no C3; `STORAGE-BLK-01..03` routed to the `APP2-B01` entry gate); **`APP2-DEC-JOBS` `COMPLETE — CORRECTED, DELIVERED_FOR_REVIEW`** — asynchronous job runtime locked by IMP-D029 / `ADR-APP2-002` (PostgreSQL-backed claim queue on the existing persistence — `outbox_events` + `background_job_attempts` + `FOR UPDATE SKIP LOCKED` + visibility-timeout lease on `next_attempt_at`; Redis/BullMQ and RabbitMQ rejected; `NO_APP2_MIGRATION`, no new dependency; `PASS_WITH_CONFIG_PARAMETERS`; adds narrow `APP2-I02` worker-runtime foundation owning `FU-A07` → map 19 checkpoints — [`reports/APP2-DEC-JOBS-COMPLETION-REPORT.md`](./reports/APP2-DEC-JOBS-COMPLETION-REPORT.md)); **corrected by `APP2-DEC-JOBS-C1`** (`PENDING` is the only automatic claim/retry state matching partial index IDX-088; retryable failure returns to `PENDING`+backoff, `FAILED` reserved/never emitted; ownership-guarded atomic completion transactions; expired-lease evidence; no-heartbeat invariant; spikes 23/23 + 25/25 — [`reports/APP2-DEC-JOBS-C1-CORRECTION-REPORT.md`](./reports/APP2-DEC-JOBS-C1-CORRECTION-REPORT.md)); `APP2-DEC-JOBS` review accepted, unblocking `APP2-D01`/`APP2-I02`; **`APP2-D01` `DELIVERED_FOR_PRODUCT_OWNER_REVIEW`** — Assets & Catalog Publication design package on Figma page **APP_02** (`419:3`) section `423:3`, 27 screen/state frames + 2 annotations + 1 DS supplement (`FIG-DS-INPUT-APP2`); Admin `NEW` + Storefront list/detail `SUPPLEMENT` cloned from the approved APP1-D02 shells with **no APP1 row superseded**; 30 new `REVIEW_REQUIRED` rows → registry **69 IDs**; handoff §6.2 of the phase plan — [`reports/APP2-D01-COMPLETION-REPORT.md`](./reports/APP2-D01-COMPLETION-REPORT.md); **Product Owner returned `PARTIAL_PRODUCT_OWNER_APPROVAL`; corrected by `APP2-D01-C1`** — Admin `COMPLETE — PRODUCT_OWNER_APPROVED — FROZEN`; Storefront Product List `REJECTED_NODE_SET_REMOVED` / `SOURCE_AUTHORITY_CORRECTED_TO_UI02` (four card-grid nodes `444:204`/`445:204`/`445:210`/`446:223` deleted, registry rows removed, UI02 `208:538` + `208:2002`/`224:871`/`226:1038` now mandatory authority with masonry 5/3/2); Storefront Product Detail `NOT_APPROVED` / `WITHHELD_PENDING_UI03_RECONCILIATION` (frames unmodified, not implementation authority); registry stays 69 IDs; 31/31 frozen nodes verified unchanged — [`reports/APP2-D01-C1-STOREFRONT-SOURCE-CORRECTION-COMPLETION-REPORT.md`](./reports/APP2-D01-C1-STOREFRONT-SOURCE-CORRECTION-COMPLETION-REPORT.md); `APP2-A01..A04` `BLOCKED_BY_D01_APPROVAL_AND_BACKEND`, `APP2-S01` `BLOCKED_BY_BACKEND`, `APP2-S02` `BLOCKED_BY_UI03_RECONCILIATION_AND_BACKEND`, **`APP2-I01` `COMPLETE — DELIVERED_FOR_REVIEW`** — S3-compatible object-storage foundation `@embroidery/object-storage`: six-operation port by `ORIGINALS`/`DERIVATIVES` alias (no presign, no unrestricted list-all), streaming `lib-storage` upload (5 MiB × 2, `leavePartsOnError: false`, no whole-file buffering), PII-free UUIDv7 keys from validated MIME with SVG rejected, strict fail-fast config with paired credentials + secret redaction, closed provider-error taxonomy; pinned `minio/minio:RELEASE.2025-04-08T15-41-24Z` dev Compose service with in-package private-bucket bootstrap (no `mc`, no second SDK, no bucket policy) and no published host port; **inactive** route-scoped nginx upload seam (`proxy_request_buffering off`; `client_max_body_size`/timeout hooks deliberately unset — the max-image-bytes parameter is still open). Direct deps exactly `@aws-sdk/client-s3` + `@aws-sdk/lib-storage` `3.1095.0` (no presigner, no `busboy`); `NO_APP2_MIGRATION`; OpenAPI `ae015dd6…`, API-client `89c1aace…`, DB 78/31/`4ca56a59…` and the Figma registry all unchanged. 150 unit tests + **20/20** disposable-MinIO contract cases with zero residue; case 16 exposed a real dangling-multipart defect (`Upload.done()` races an abort watcher, so `Upload.abort()` lets it reject before `AbortMultipartUpload` lands) — fixed by aborting through the source stream. Preflight `PARTIAL`: the tree was **not** clean on entry (pre-existing untracked I01 work) — disclosed, not reported as a pass — [`reports/APP2-I01-COMPLETION-REPORT.md`](./reports/APP2-I01-COMPLETION-REPORT.md); **`APP2-I02` `COMPLETE — DELIVERED_FOR_REVIEW`** — PostgreSQL worker-runtime foundation implementing IMP-D029 / `ADR-APP2-002` as corrected by `APP2-DEC-JOBS-C1`, closing `FU-A07`: `WorkerJobQueueRepository` (`claimRegisteredBatch` / `completeSucceededAttempt` / `completeRetryableAttempt` / `completeTerminalAttempt` / `probeWorkerDatabase`) with a single-statement database-time claim (`FOR UPDATE SKIP LOCKED`, registered-event filter, empty registry claims nothing, conflict-safe `WORKER_LEASE_EXPIRED` evidence via a data-modifying CTE, lease on `next_attempt_at`, status stays `PENDING`) and ownership-guarded atomic completions returning typed `STALE_JOB_LEASE`; `FAILED` never emitted. Worker gains the handler registry (one per event type, duplicate registration fails startup), the closed error taxonomy, exact capped backoff `min(base × 2^(n−1), max)` with no jitter, worker identity + `AsyncLocalStorage` correlation with an allow-list log projection (no fabricated `X-Request-ID`), a bounded poll loop with per-attempt `AbortController` timeout, graceful shutdown and readiness; the no-op keep-alive is removed. Policy comes from the canonical `policy_configurations` path under `worker.runtime` — no environment duplication, **no production default and no development bootstrap** (none exists to reuse), so an unconfigured worker reports `WORKER_POLICY_MISSING`, stays up and claims nothing. **No new dependency, `NO_APP2_MIGRATION`**, all frozen baselines unchanged, production registry empty (no Asset handler; W01 owns those). 115 worker + 107 persistence unit tests, all 27 required disposable-PostgreSQL cases (29 total, real parallel-transaction concurrency evidence) and a 6-assertion real-process smoke pass; SIGTERM is raised in-process because Windows has no real SIGTERM — disclosed, not hidden. Two real defects found by tests and fixed before Commit A — [`reports/APP2-I02-COMPLETION-REPORT.md`](./reports/APP2-I02-COMPLETION-REPORT.md); **review returned `CORRECTION_REQUIRED`, corrected by `APP2-I02-C1`** → **`APP2-I02` `COMPLETE — CORRECTED — DELIVERED_FOR_REVIEW`**: (1) a timed-out handler ignoring its `AbortSignal` was abandoned while the runtime recorded a retryable timeout and released the lease, letting the still-running handler overlap a retry of the same job with the same `effectKey` — the handler promise is now never detached, and after the abort the runtime waits on it to a hard stop of `min(timeoutInstant + leaseSafetyMarginMs, leaseExpiresAt − fatalExitSafetyMs)` (`fatalExitSafetyMs = 250`, internal, enforced at policy startup); cooperative handlers complete normally as `JOB_HANDLER_TIMEOUT` (a late success never becomes `SUCCEEDED`), uncooperative ones write **no** completion, keep the lease untouched, enter `FATAL_HANDLER_UNRESPONSIVE`, stop claiming, report unready, log once, close context/pool once and exit `1` before lease expiry, with the reclaimer writing the single `WORKER_LEASE_EXPIRED` attempt; (2) `process.emit('SIGTERM')` is no longer acceptance evidence — a real Linux SIGTERM via `docker kill --signal=TERM` against the shipped `runner` image exits **0 in 362 ms**, and that smoke also uncovered a pre-existing production-image defect (the `deps`/`prod-deps` stages never copied the `packages/database`/`packages/persistence` manifests, so the container could not resolve `@nestjs/common`), fixed in `worker.Dockerfile`. No dependency, migration, schema/status/outcome or persistence change; frozen baselines unchanged — [`reports/APP2-I02-C1-CORRECTION-REPORT.md`](./reports/APP2-I02-C1-CORRECTION-REPORT.md); **final review ruled C1 `ACCEPTANCE_NOT_PROVEN` — C1 had used an injected process-exit seam inside Jest, so worker A's never-settling promise stayed alive and worker B started after an exit *record*, not after worker A terminated — resolved by the mandatory final directive `APP2-I02-FD1`** → **`APP2-I02` `COMPLETE — CORRECTED — REVIEW_ACCEPTED`**, `APP2-I02-C1` `SUPERSEDED_BY_FINAL_PROCESS_PROOF`, `APP2-I02-FD1` `COMPLETE`: a real Linux process-boundary test runs worker A as PID 1 in a container with the production exit seam and a handler that ignores `AbortSignal` and never settles, then starts worker B in a second container only after `docker inspect` reports A stopped and `clock_timestamp()` has passed the lease deadline; over **three consecutive runs** `ExitCode 1`/`Running false` ~17 s before the deadline, the row stays `PENDING`/owned/`attempt_count 1`/`next_attempt_at` unchanged/`last_error` NULL with **zero** attempt rows before reclaim, worker B's handler starts ~18 s after A's `FinishedAt`, attempts become exactly `1/FAILED_RETRYABLE/WORKER_LEASE_EXPIRED` then `2/SUCCEEDED`, final `DISPATCHED`, worker B exits 0, zero Docker/database residue. **Production runtime unchanged** (the C1 state machine passed first time; both defects found were mine in the test). Test-only `process-test` Docker target excluded from the production image and from Compose; no dependency, migration or schema change; frozen baselines unchanged — [`reports/APP2-I02-FD1-FINAL-VERIFICATION-REPORT.md`](./reports/APP2-I02-FD1-FINAL-VERIFICATION-REPORT.md); a checkpoint may receive at most one correction — that was C1, and `APP2-I02-C2` must not be created; **`APP2-B01-G01` `COMPLETE — ENTRY_GATE_CLOSED`** — the asset-intake entry gate under `APP2-B01` ownership (not a third storage correction; `APP2-DEC-STORAGE-C3` must not be created) closing **`STORAGE-BLK-01/02/03` = `RESOLVED_BY_APP2-B01-G01`** and **`UPLOAD-POLICY-BLK-01` = `RESOLVED_BY_PRODUCT_OWNER`**: binding policy 25 MiB / 26 214 400 bytes API-authoritative file limit, `image/png`+`image/jpeg`+`image/webp` only, SVG rejected, non-image source files excluded, originals retained while the asset exists, coarse route-scoped 27 MiB / 28 311 552 bytes gateway ceiling, 5-minute API hard upload duration, 15-minute allocation TTL; `ADR-APP2-001` §4.2f locks the two-stage request+content fingerprint (a completed replay resends and re-verifies the whole body with zero object writes), the versioned discriminated `ASSET_UPLOAD_ALLOCATION`/`ASSET_UPLOAD_COMPLETED` result union with strict decoding and `IDEMPOTENCY_RESULT_INVALID` safe failure, the `claimToken` rotated only on expired reclaim while preserving `assetId`/`objectKey` and verified inside Tx A and Tx B (`STALE_UPLOAD_CLAIM`), and the ordered reclaim that never deletes an existing durable asset and completes cleanup before any replacement stream; `APP2-B01` scoped to **exactly three operations**; recorded reconciliations — `PRODUCT_IMAGE` does not exist so the locked `CATALOG_MEDIA` governs, and the http-level `client_max_body_size 20m` is overridden upward to `27m` route-scoped only; `NO_APP2_MIGRATION` re-confirmed by 9/9 disposable-PostgreSQL checks with zero residue, remaining shortfalls are repository implementation gaps (ADR §4.2f-7) owned by B01 — [`reports/APP2-B01-G01-COMPLETION-REPORT.md`](./reports/APP2-B01-G01-COMPLETION-REPORT.md); **`APP2-B01` = `READY — NOT STARTED`**, `APP2-W01` blocked by `B01`). **`APP2-B01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the three Admin asset operations (`adminAsset_upload` / `adminAsset_detail` / `adminAsset_list`): single-pass `busboy` streaming into the private `ORIGINALS` bucket with a bounded signature prefix settled before any byte reaches storage, an incremental authoritative 25 MiB counter and a server SHA-256 on the same bytes (no whole-file buffer anywhere); the pre-stream claim commits the allocation before a single file byte is read; a completed replay re-reads and re-verifies the whole resent body writing **zero** objects; an expired allocation is reclaimed by rotating the claim token in place and resuming from durable asset truth, never deleting an existing asset and always completing cleanup before a replacement stream; Tx A inserts-or-recovers as `UPLOADED` with no event, Tx B does the guarded `UPLOADED → INSPECTING` transition, appends **exactly one** `asset.inspection.requested` event and completes the record with its id, both verifying the claim token inside their own transaction; exactly-once not claimed. New `IdempotencyAllocationStore` (claim-with-allocation / locked read / expired renewal / guarded completion, expiry by database time) plus `AssetRepository` insert-or-recover, guarded transition and scoped keyset list. **No schema change, no migration**; only `busboy` `1.6.0` + `@types/busboy` `1.5.4` added. Nginx upload seam **activated** on one exact-match location (`27m`/`360s`/buffering off, values from the environment; global limits unchanged, MinIO unexposed). Three real streaming defects found by the live suite and fixed (errored sink destroyed without a listener crashed the process; `pipe` does not forward source errors so a disconnecting client emitted an unhandled `error`; a stalled client hung past its deadline because the reader only checked the abort signal on a chunk), plus a structural split of `AssetModule` into persistence and `AssetIntakeModule` after an existing DB7 suite exposed the coupling. 194 unit tests, **25 live PostgreSQL+MinIO cases covering all 30 required scenarios**, 27 API cases, 20 gateway cases including a real `nginx:1.27.3-alpine` render + `nginx -t`; `pnpm quality` `EXIT=0` (90/90 API suites, 1112 tests); OpenAPI `ae015dd6…`→`e19c2f76…`, client `89c1aace…`→`55de1cc1…` (+197/−0), DB 78/833/31 and Figma 69 unchanged — [`reports/APP2-B01-COMPLETION-REPORT.md`](./reports/APP2-B01-COMPLETION-REPORT.md). **`APP2-W01` = `READY — NOT STARTED`**. **`APP2-W01` entry gate returned `BLOCKED_BY_PROVEN_SCHEMA_OR_DOMAIN_GAP` (no commit, no file changed): `asset_derivatives.kind` had no non-watermarked catalog display kind — `PREVIEW_WATERMARKED` is bound to INV-22/BR-012 and the `ADR-APP2-001` §4.7 public preview contract, and `NORMALIZED` would redefine the artwork concept DB2 CON-042/CON-061 merged — so the map gains two prerequisites → **21 checkpoints**, order `APP2-DB01 → APP2-I03 → APP2-W01`.** **`APP2-DB01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — IMP-D030: adds the derivative kind **`CATALOG_PREVIEW`** (non-watermarked catalog display derivative of a `CATALOG_MEDIA` asset) and INV-22's first physical half `ck_asset_derivatives__watermark_by_kind` (**CST-126**: `PREVIEW_WATERMARKED → true`, `CATALOG_PREVIEW → false`; the other three kinds unconstrained) in forward-only migration **`0032`** with two preflight guards; aligns DB3 **LC-06 into two lanes** — the catalog-media lane prepares both rows at `PENDING`, transitions to `PROCESSING` through a guarded update (never inserted as `PROCESSING`), generates while the asset is `INSPECTING`, and writes both-`READY` + one inspection + `ACCEPTED` in one transaction, while the design/artwork lane keeps its after-`ACCEPTED` SE-013 timing; and locks the W01 job kind to **`ASSET_PROCESSING`** (the I02 seam already carries a handler-provided kind, so no runtime change). Migrations 31 → 32, CHECK 189 → 190, fingerprint `4ca56a59…` → `82864268…`; **78 tables / 833 columns unchanged**, no table/column added, uniqueness and READY/storage-key behaviour untouched, no historical row rewritten; OpenAPI `e19c2f76…`, client `55de1cc1…` and Figma 69 unchanged. 192 database + 112 persistence tests including a real upgrade run from the 0000–0031 baseline that preserves every seeded row and reproduces the fresh-install fingerprint — [`reports/APP2-DB01-COMPLETION-REPORT.md`](./reports/APP2-DB01-COMPLETION-REPORT.md). **`APP2-I03` = `READY — NOT STARTED`** (wire idempotent private-bucket bootstrap into the API and worker composition roots — `ensurePrivateBuckets` has no production caller); **`APP2-W01` = `BLOCKED_BY_APP2-I03`**; `APP2-A01` = `BLOCKED_BY_APP2-W01`. **`APP2-I03` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the private-bucket bootstrap is finally wired into both composition roots: `ensurePrivateBuckets` had existed since I01 but **no production caller** did, so a real deployment met its first upload with no bucket. The API resolves and awaits the bootstrap **before `app.listen`** (extracted into `start-api.ts` so the ordering is asserted, not described); on failure it never listens, closes the context exactly once and exits non-zero, with no retry loop. The worker gates its poll loop behind an abstract **`WORKER_STARTUP_GATE`** that the generic runtime injects but does **not** provide — the composition root binds it to the same bootstrap — awaited before `started`, so a closed gate issues no claim query at all; a closed gate is a *result*, not a throw, so the process stays up, reports `STARTUP_GATE_CLOSED` and `main.ts` closes the context once and exits non-zero. Bootstrap is memoized per process (concurrent callers share one attempt, never two `CreateBucket` calls), sticky on success, cleared on failure; four startup classes are mapped from the existing `ObjectStorageError` taxonomy, never from a message, and access-denied is never downgraded to unavailable. Logs carry only `component`/`operation`/`ready`/class. Readiness: the API adds **no** public field (listen *is* the storage gate, and a schema change would move the frozen OpenAPI artifact); worker readiness now requires policy + database + every gate + started. **No package correction was needed** — the existing `BucketAlreadyOwnedByYou` branch already makes a lost race a success. Proven live: a cross-process smoke starts the **built** API and worker back-to-back against an empty MinIO → both ready, both verified, **exactly two** private buckets, anonymous access refused, restart idempotent, zero residue (11/11); a worker-only smoke against a store with no buckets at all (9/9); plus 35 deterministic API cases and 11 new worker gate cases. All I02 regressions re-run green, including the **real Linux SIGTERM (exit 0 in 388 ms)** and the 16-case fatal-process isolation proof. Compose gives the worker the same storage configuration and orders both after a healthy MinIO (still no host port, still no Nginx route); the E2E edge gains an ephemeral tmpfs MinIO because the API no longer listens without a store. **Found and fixed a real production defect:** the shipped worker image never shipped `@embroidery/object-storage`, so it could not start — the same class of image defect I02-C1 and DB01 found before it. No migration, no schema change, no new external dependency (one workspace link), OpenAPI/client/Figma unchanged; `pnpm quality` `EXIT=0` (API 1136, worker 141, persistence 112, database 192, object-storage 150+20) — [`reports/APP2-I03-COMPLETION-REPORT.md`](./reports/APP2-I03-COMPLETION-REPORT.md). **`APP2-W01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the catalog asset inspection worker: one production handler for `asset.inspection.requested` filed under the domain kind **`ASSET_PROCESSING`** (not the transport `OUTBOX_DISPATCH`), effect key `asset-inspection:v1:{assetId}`, registered in `onModuleInit` so it is in the registry before the poll loop issues its first claim. `sharp@0.35.3` exact (Apache-2.0, libvips 8.18.3) added to `apps/worker` only — no `@types/sharp`, no second image library, nothing added to API/Admin/Storefront/object-storage. `ASSET_PROCESSING_POLICY_V1` is a frozen worker-owned object (PNG/JPEG/static WebP; `failOn: warning`, `unlimited: false`, `sequentialRead: true`, `animated: false`; 12,000 × 12,000, 40 MP, 4 channels, 1 page), and the two outputs are WebP `q82`/`alphaQuality 100`/`effort 4`/`smartSubsample` inside 480² and 1920² with `fit: inside` + `withoutEnlargement` — auto-oriented, sRGB, alpha preserved, every metadata block stripped, both `THUMBNAIL` and `CATALOG_PREVIEW` unwatermarked per DB01/CST-126. Durable preparation commits first (both rows inserted `PENDING`, then guarded `PENDING → PROCESSING`, never inserted as `PROCESSING`), external work happens outside every transaction, and one terminal transaction writes both `READY` rows + exactly one `ACCEPTED` inspection + the guarded `INSPECTING → ACCEPTED` together; an unpublishable image is a **business rejection** (both rows `FAILED`, one closed code, original retained), never a dead letter, and on the final attempt a retryable failure becomes `PROCESSING_RETRY_EXHAUSTED` so nothing sits in `INSPECTING` forever. Terminal replay writes zero objects and zero rows; a contradiction fails closed with `JOB_INVARIANT_VIOLATION` and is never repaired. **Nothing is buffered**: four bounded reads per attempt (integrity, metadata, one per output), each piped into the encoder and into managed multipart — no full-file `Buffer`, no `toBuffer`, no temp file. The V1 inspection detail is an allow-list in the existing nullable `text` column, bounded at 8 KiB, so no key/filename/EXIF/stack can reach it. **Five real defects were found by these tests and fixed before Commit A** — an upload that failed to open stalled the pipeline on backpressure; a decode failure was reported as the storage failure it cascaded into; contradictions raised outside the processing block escaped unclassified and were therefore *retried*; a cleanup failure bypassed the final-attempt rule; and `DrizzleRepository.run`'s error mapper flattened every contradiction into a generic `PersistenceError`, turning a fail-closed stop into a retry (only visible against a real database — the unit doubles are not wrapped by the mapper at all). 24 worker suites / **283** unit tests, **4 live PostgreSQL+MinIO suites / 52 tests covering all 40 required scenarios** with recovery states produced through real failure seams, and a **shipped-image Linux smoke** (12 tests: gate opens, Sharp musl binary loads, exactly 1 handler, claimed under `ASSET_PROCESSING`, asset `ACCEPTED`, both derivatives `READY`, both private objects, one inspection, outbox `DISPATCHED` with one `SUCCEEDED` attempt, no key/checksum/secret/`requestId` in the logs, real SIGTERM → **exit 0 in 309 ms**, zero residue). All I02/I03 regressions green (signal smoke exit 0 in 320 ms; 16-case fatal-process proof). **No migration, no schema change, no HTTP operation, no forbidden-area change**; OpenAPI `e19c2f76…`, client `55de1cc1…`, DB 78/833/190/`82864268…` and Figma 69 all unchanged; `pnpm quality` `EXIT=0` — [`reports/APP2-W01-COMPLETION-REPORT.md`](./reports/APP2-W01-COMPLETION-REPORT.md). **`APP2-A01` = `READY — NOT STARTED`**; `APP2-B02` = `BLOCKED_BY_APP2-A01`. **`APP2-A01` entry gate returned `BLOCKED_BY_MISSING_ASSET_LIST_CONTINUATION_DESIGN` (no commit, no file changed):** `adminAsset_list` returns `hasNext`+`nextCursor` (`limit` ≤ 100, no delete API), but none of the seven approved frames, the handoff-notes node `450:404`, the registry, the D01 reports or the phase plan defined a continuation interaction — proven from node metadata, since the Uploading/Processing/Rejected frames clip their grid at the 1024 px frame bottom; page-1-only would silently discard pages and inventing a control would be self-approved design. The same audit reproduced a second gap (every approved card/row displayed a **source filename** B01 neither persists nor exposes) and **cleared** two candidate blockers (thumbnails are honest placeholder glyphs, so no media-delivery contract is required; the side nav locks a dedicated Assets destination, so `/assets` is not invented). **`APP2-D02` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-27) — the narrow design-reconciliation checkpoint curing both gaps, **design and documentation only**: IMP-D031 locks an explicit user-triggered **`Tải thêm tài sản`** control shown only when `hasNext = true` (append, preserve items and scroll; loading = disabled + `Đang tải thêm…` + `aria-busy` + one polite announcement; failure keeps items and offers `Thử lại` on the **same** cursor; no control at `hasNext = false`; **no total-count copy** — the API exposes no total; infinite scroll, viewport-triggered loading, offset/page-number paging and silent truncation all rejected) and a **server-backed identity** (`image/png → Ảnh PNG`, `image/jpeg → Ảnh JPEG`, `image/webp → Ảnh WebP`, unknown → `Tài sản hình ảnh`; secondary line `{size} · {createdAt}` in `vi-VN`, `dd/MM/yyyy, HH:mm`) with **no backend field added and no filename fabricated** — `File.name` stays transient local upload state. Applied in place to the eight existing Admin Assets nodes (desktop Default grew 1024 → 1092 so the control is visible, not clipped; mobile rows 80 → 95 for the second line); the seven `FIG-ADMIN-ASSETS-*` rows are promoted to `APPROVED_FOR_IMPLEMENTATION` under **`FIG-APPROVAL-APP2-D01-ADMIN-001`** ([`../design/approvals/APP2-D01-ADMIN-ASSETS-DESIGN-APPROVAL.md`](../design/approvals/APP2-D01-ADMIN-ASSETS-DESIGN-APPROVAL.md)), closing the divergence between the frozen ruling and the rows, and one annotation node `FIG-ADMIN-ASSETS-CONTINUATION-IDENTITY` (`484:272`) is registered — **registry 69 → 70**; design authority is now `PRODUCT_OWNER_APPROVED — FROZEN — D02_RECONCILED`; map now **22 checkpoints**; OpenAPI `e19c2f76…`, client `55de1cc1…` and DB 78/833/190/`82864268…` unchanged — [`reports/APP2-D02-COMPLETION-REPORT.md`](./reports/APP2-D02-COMPLETION-REPORT.md). **`APP2-A01` = `READY — NOT STARTED`** (unblocked); `APP2-B02` = `BLOCKED_BY_APP2-A01`. **`APP2-A01` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-28) — the Admin asset library at `/assets` inside the accepted APP1 shell, implementing IMP-D031 exactly (explicit `Tải thêm tài sản` only when `hasNext` and a usable cursor, append + same-cursor retry + one polite announcement, no total-count copy; identity from `mediaType` with a `{size} · {createdAt}` `vi-VN` line and **no filename anywhere**). Consumes only the three accepted B01 operations: generated multipart with ordered parts + `Idempotency-Key` + `AbortSignal` + real transferred-byte progress (never 100% before the HTTP result), honest ambiguous cancellation that reuses the same key, and detail-only processing reconciliation on one `ASSET_PROCESSING_POLL_INTERVAL_MS = 3000` constant that stops at any terminal or uninterpretable status. Thumbnails stay honest placeholders — APP2 exposes no media-delivery operation. Shell navigation gains its first real destination (links + pathname-derived current). 77 new tests (220 Admin total), `pnpm quality` `EXIT=0`; OpenAPI `e19c2f76…`, client `55de1cc1…`, DB 78/833/190/`82864268…` and Figma registry 70 all unchanged; **no new dependency** — [`reports/APP2-A01-COMPLETION-REPORT.md`](./reports/APP2-A01-COMPLETION-REPORT.md). Reviewer decision open: the approved Rejected-state action `Xoá khỏi danh sách` has **no backend capability** and is deliberately not implemented. **`APP2-B02` = `READY — NOT STARTED`**; `APP2-A02` = `BLOCKED_BY_APP2-B02`. **`APP2-B02` entry gate (2026-07-28):** the B02 pre-code audit blocked as **`BLOCKED_BY_PRODUCT_DRAFT_FIELD_DECISION`** (no commit, no tracked file changed) — `products` carries 10 NOT NULL columns with no default, and after DB7's four locked rules five mandatory values had none: `category_id`, `slug`, `base_price_amount`, `display_order`, `product_media.role`; `category_id` was unsatisfiable because zero categories existed, no category operation exists and B02 may not add a sixth. **`APP2-B02-G01` = `COMPLETE — ENTRY_GATE_CLOSED`** locks **IMP-D032** (fixed `thu-bong`/`khan`/`quan-ao`/`khac` taxonomy addressed by `categorySlug`, server-owned immutable product slug, `DRAFT` price sentinel `0` VND, draft `display_order = 0`, first media `THUMBNAIL` + rest `GALLERY`, `DETAIL` excluded) and provisions the taxonomy in **data-only migration `0033`**: 32 → 33 migrations with **78 tables / 833 columns / 190 CHECKs and fingerprint `82864268…` unchanged**, OpenAPI `e19c2f76…`, client `55de1cc1…`, Figma 70 and dependencies unchanged — [`reports/APP2-B02-G01-COMPLETION-REPORT.md`](./reports/APP2-B02-G01-COMPLETION-REPORT.md). **`APP2-B02` = `READY — NOT STARTED`** with its five-operation scope intact; `FU-APP2-CATEGORY-MANAGEMENT-01` = `DEFERRED_BEYOND_CATALOG_ALPHA` and `FU-APP2-THUMBNAIL-01` = `ROUTED_TO_APP2-T01`, both nonblocking. **`APP2-B02` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-07-31) — the Catalog Draft backend: exactly five Admin operations (`adminProduct_list/detail/create/update/archive`), no sixth and **no category endpoint**. Implements IMP-D032 as locked: `categorySlug` on the wire (closed enum, resolved in-transaction, **category UUID never exposed**), server-owned **immutable** slug (a rename never moves the public address), `DRAFT` price sentinel `0` VND rendered as whole đồng (`numeric(14,2)` returns `0.00`; VND has no minor unit), `display_order 0` with no `max+1`, and complete all-or-nothing ordered media replacement where the first Asset is the single `THUMBNAIL`, the rest `GALLERY` and `DETAIL` is never written. Concurrency uses `updated_at` **truncated to milliseconds** — the precision of the ISO token a client can echo; the raw microsecond comparison failed every guarded write. Archive is a guarded `DRAFT → ARCHIVED` that deletes nothing (`PUBLISHED` is a safe 409 because unpublication is B03's). **No ownership column exists** on `products`, so authentication is the scope and no audit actor is storable — recorded, not assumed. 88 new tests (53 unit, 21 live-PostgreSQL, 14 API, 6 gateway); `pnpm quality` `EXIT=0`; OpenAPI `e19c2f76…` → `b789cc99…` (7 → 10 paths, 8 → 13 operations) and client `55de1cc1…` → `66d1c991…`, additions only; **database baseline, Figma (70) and dependencies unchanged; no migration** — [`reports/APP2-B02-COMPLETION-REPORT.md`](./reports/APP2-B02-COMPLETION-REPORT.md). **`APP2-A02` and `APP2-A03` = `READY — NOT STARTED`**; `APP2-B03` = `BLOCKED_BY_APP2-B02`; `APP2-T01` stays `ROUTED — NOT PLANNED_FOR_EXECUTION`. **SUPERSEDED (2026-08-01):** `APP2-T01` was **activated as a mandatory predecessor of `APP2-B04`** (IMP-D036) after the B04 entry audit returned `PUBLIC_MEDIA_DELIVERY_GATE = BLOCKED` — no route in the repository served image bytes, so `Q-01`/`Q-02` public product images were unrepresentable. Its canonical scope is the **public catalog media delivery foundation**, not the earlier "authenticated Admin thumbnail delivery" wording; the Admin placeholder replacement is deferred as `FU-APP2-ADMIN-MEDIA-PLACEHOLDER-01`. **`APP2-T01` = `COMPLETE — DELIVERED_FOR_REVIEW`** ([`reports/APP2-T01-COMPLETION-REPORT.md`](./reports/APP2-T01-COMPLETION-REPORT.md)). **`APP2-B02-C1` (2026-07-31) — security & concurrency correction, `APP2-B02` = `COMPLETE — CORRECTED (C1) — DELIVERED_FOR_REVIEW`.** Three reviewer defects, each reproduced before being fixed: mutations carried `StaffOriginGuard` but **not** `StaffJsonBodyGuard` (never exported from `IdentityModule`), so a `text/plain` post answered **400 instead of 415** — all three now carry the canonical `AuthenticatedAdminGuard + StaffOriginGuard + StaffJsonBodyGuard`; `updated_at` truncated to milliseconds was comparable but **not monotonic**, so two writes in one millisecond published the same token and a stale value still matched — the database now computes `GREATEST(date_trunc(ms, clock_timestamp()), date_trunc(ms, updated_at) + 1ms)` and returns it, with **no application time anywhere on the write path**; and the **invented 12-item media limit** is removed (it existed only to bound a per-item loop, had no product authority, and never reached OpenAPI). The fourth invariant is now proved: validation is one transaction-scoped `FOR SHARE` batch read, so a selected Asset **cannot leave `ACCEPTED` between validation and the media write** — two real transactions, rival transition fails with SQLSTATE `55P03` while the lock is held and succeeds after commit. Description semantics locked (absent = unchanged, `null`/blank = clear). 19 new tests (132 catalog tests total); `pnpm quality` `EXIT=0`; OpenAPI `b789cc99…` → `c4d1fef8…` and client `66d1c991…` → `3e3e267d…` — **paths and operation IDs unchanged**, the only diff being the description wording and the 403/415 the guards produce; DB baseline, Figma (70) and dependencies unchanged — [`reports/APP2-B02-C1-CORRECTION-REPORT.md`](./reports/APP2-B02-C1-CORRECTION-REPORT.md). **`APP2-B02-C2` = `MUST_NOT_BE_CREATED`**; `APP2-A02` and `APP2-A03` = `BLOCKED_BY_PRODUCT_OWNER_DESIGN_APPROVAL`. **As of `APP2-X01`: APP2 = `COMPLETE — PASS_WITH_FOLLOW_UPS — DELIVERED_FOR_REVIEW`** — closed against one canonical reconciliation, [`reports/APP2-CLOSURE-MATRIX.md`](./reports/APP2-CLOSURE-MATRIX.md), which is the current authority for APP2 checkpoint status, commit chain, frozen artifacts and routed follow-ups; the per-checkpoint prose above it remains historical evidence. 30 canonical checkpoints, all final with both commits and an existing report; 94 commits from `8643431b…` to `377321fd…`; 0 blocking checkpoints and 0 blocking follow-ups; 10 routed follow-ups, all nonblocking and owned. Frozen and unchanged: OpenAPI `c2c3b874…` 16/19/34, client `7524fc91…`, 33 migrations / 78 tables / 833 columns / 190 CHECKs, fingerprint `82864268…`, Figma 86/86/11. Public surface `/kham-pha` + `/san-pham/[slug]` with `SAFE_STREAMED_NOT_FOUND` current; publication Outbox events stay `PENDING`. Gate `pnpm check:app2-closure`. Milestone **R1 Catalog Alpha achieved**; **APP3 = `READY — NOT STARTED`** ([`reports/APP2-X01-COMPLETION-REPORT.md`](./reports/APP2-X01-COMPLETION-REPORT.md)). **`APP3-PRE-IMPLEMENTATION-AUDIT` = `COMPLETE — DELIVERED_FOR_REVIEW`** (2026-08-02) — verdict `PASS_WITH_REQUIRED_GATES`, migration verdict `NO_APP3_MIGRATION_UNLESS_G02_OR_G04_PROVES_ONE`; the Design Template and Design Session repositories exist with their DB7 guards and the DB9 CC-01 autosave proof but `DesignModule` is not composed, `design-document`/`design-engine` are empty, and **no operation can author or read `product_sides`/`embroidery_areas`** although `design_sessions` requires both ids `NOT NULL`; four authority gates and one phase-level design package precede code; corrected 37-row map — [`audits/APP3_PRE_IMPLEMENTATION_AUDIT.md`](./audits/APP3_PRE_IMPLEMENTATION_AUDIT.md), [`reports/APP3-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md`](./reports/APP3-PRE-IMPLEMENTATION-AUDIT-COMPLETION-REPORT.md). **`APP3-PRE-AUDIT-C1` (2026-08-03) = `COMPLETE — DELIVERED_FOR_REVIEW`** — human review returned `ACCEPTED_WITH_REQUIRED_CORRECTIONS` and five corrections were applied, documentation only: the evidence/governance classification now states that **Commit A was not docs-only** (two `tools/` files; disposition `APPROVED_NARROW_GOVERNANCE_DEVIATION`, not reverted) instead of claiming the two-docs-only requirement was met; the migration verdict becomes **`NO_APP3_MIGRATION_UNLESS_G01_OR_G02_OR_G04_PROVES_ONE`** because closed `product_media.role`/`asset_derivatives.kind` CHECK sets, a `secure_access_grants` table that cannot express an anonymous-session grant, and the absence of any side/area immutability column all sit in the path of what `APP3-G01` may rule; `APP3-DB01` gains the terminals `COMPLETE` and `NOT_REQUIRED — GATE_RESOLVED` with dependents waiting on `DB-DISPOSITION-RESOLVED`; `design-document` is mandatory before every document-accepting API while `design-engine` blocks only `B04`/`B07`/`B08` (the named out-of-bounds invariant); and `APP3-G01` now owns the existing-data/invariant rollout evidence (measured dev baseline: **0 `product_sides`, 0 `embroidery_areas`, 0 PUBLISHED products of 29**, with the queries to re-run against the authoritative environment). **`APP2-X01-C1` (2026-08-03) = `COMPLETE — REVIEW_DELIVERED`** — scopes the APP2 Figma closure baseline. The closure gate froze the **global total** of the shared, appendable Figma registry (`86 registry IDs / 86 node rows / 11 tables`), so the unrelated BRD0 commit `2a5d3bf` (96/96/13) failed it — while a global total is also blind to the attack that matters, an APP2 row deleted and replaced by an unrelated one at the same count. The gate now verifies the exact **86-record APP2-owned subset** transcribed from the closure commit `8b5f3b0` into [`reports/APP2-CLOSURE-FIGMA-BASELINE.json`](./reports/APP2-CLOSURE-FIGMA-BASELINE.json): each owned record must remain present, unique, in its original section and unchanged across eleven authority fields, and the registry must stay internally consistent — while unrelated rows may be appended freely. Proven mechanically: additive BRD0 growth passes; removal, authority-field mutation, duplication, section move, same-count substitution and node-link corruption each fail. Closure test suite 32 → 43 cases; the printed closure verdict no longer carries a global count. `FU-APP3-CLOSURE-FIGMA-BASELINE-01` = **`COMPLETE — CLOSED_BY_APP2-X01-C1`** (single owner). `pnpm quality` = `EXIT=0` — [`reports/APP2-X01-C1-COMPLETION-REPORT.md`](./reports/APP2-X01-C1-COMPLETION-REPORT.md). **`APP3-G01` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-03) — the first APP3 execution checkpoint. Authority, dependency reconciliation and a mechanical gate only: **no application source, schema, migration, OpenAPI operation, generated client or Figma node**, and the gate asserts that absence rather than assuming it. Seven Product Owner rulings are locked as **IMP-D041** and machine-checked by `pnpm check:app3-g01` against a 20-row fact table, an 8-row dependency table, the real Drizzle schema and the committed OpenAPI artifact: placement is **Admin-only Product** authority authored at `APP3-A01` (never Design-module, never customer-authored); the public read is a **read-only manifest** of ordered active sides/areas carrying `studioEligible`, with no original URL, storage key or mutation field; the canonical side-background association stays **`product_sides.background_asset_id`** and is **not** duplicated into `product_media`, with delivery owned by `APP3-B02` and the derivative kind still owned by `APP3-G04` — so `APP3-B02` is blocked by **both** gates; the customer selects a side or area only where more than one is active, defaulting to the first by `display_order`; the canonical Studio route is **`/san-pham/[slug]/thiet-ke`**, extending the accepted `/san-pham/[slug]` (IMP-D039) with `/studio`, `/editor` and `/thiet-ke/[id]` rejected; **global Product publication does not require placement** — Studio eligibility is separately derived, so no publication guard is added and no catalog is grandfathered; and referenced placement becomes **immutable with no hard delete**, changed only by a replacement row. Because `product_sides`/`embroidery_areas` carry no active/retired flag, supersession pointer, stable code or lock column, **`G01_DB_DISPOSITION = REQUIRES_APP3_DB01`** — the collective disposition can no longer terminate `NOT_REQUIRED — GATE_RESOLVED` without superseding that ruling. Measured dev baseline unchanged and untouched: 29 products (0 published), 0 `product_sides`, 0 `embroidery_areas`, 0 templates, 0 sessions — no row was inserted, updated or deleted. **APP3 = `IN PROGRESS — THIRD GATE DELIVERED_FOR_REVIEW`; `APP3-G02` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-03) — the second APP3 execution checkpoint. Authority, lifecycle formalisation, dependency reconciliation and a mechanical gate only: **no application source, schema, migration, OpenAPI operation, generated client or Figma node**. Eight rulings locked as **IMP-D042**, machine-checked by `pnpm check:app3-g02` against a 30-row fact table, a 12-row dependency table, both canonical transition tables and the real repository. The Design Template lifecycle becomes **LC-24** — the next free canonical id — with six stable transitions and **no** direct `ARCHIVED → PUBLISHED`, **no** hard delete, archive distinct from unpublish, every transition Admin-only, audited and token-guarded, and an invalid source state failing **without mutation**. It supersedes the informal "Additional lifecycles → Design Template" paragraph, which named states but assigned no transition ids. Versions are **immutable from creation**; every `DRAFT` save writes a new monotonic version; `published_at` is **set once and never cleared**, so unpublish is header-only and public read selects the highest version whose `published_at` is not null; clone yields a deep independent document with lineage only. APP3 publishes **area-scoped Templates only**, requiring the exact `product_id → product_side_id → embroidery_area_id` chain active under IMP-D041, with **exact triple equality** compatibility — no wildcard, tag-based or many-to-many relation — therefore **`G02_DB_CONTRIBUTION = NONE`** and `APP3-DB01` runs **only** because `APP3-G01` requires it. **`FU-APP2-PRODUCT-ARCHIVE-LIFECYCLE-01` = `COMPLETE — CLOSED_BY_APP3-G02`**: `APP2-B02` shipped `PRODUCT_ARCHIVABLE_STATES = [PRODUCT_DRAFT_STATE]` while LC-04 authorised only `PUBLISHED → ARCHIVED`, so LC-04 gains **`TR-LC04-06` `DRAFT → ARCHIVED`** (5 → 6 transitions, existing ids unchanged and not renumbered) and `check:lifecycle` now asserts it; `FU-APP2-PRODUCT-ARCHIVE-UI-01` stays `DEFERRED_PENDING_PRODUCT_OWNER_SURFACE_DECISION`. Measured dev baseline, read-only and unchanged: 0 templates, 0 template versions, 0 duplicate version numbers; 29 products (0 published, 26 draft, **3 archived — all from `DRAFT`**, the very transition now authorised). **`APP3-G03` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — the third APP3 execution checkpoint locked ten Product Owner rulings as **IMP-D043**, the anonymous Design Session ownership, transport and retention authority. Ownership requires **both** the public `design_sessions.id` **and** a high-entropy secret: 32 server CSPRNG bytes, unpadded base64url, persisted only as **`HMAC-SHA-256(runtime pepper, secret)`** and compared in constant time — **neither half authorizes alone**, and the raw secret is never persisted, logged, placed in a URL/query/fragment, returned in JSON or stored in browser storage. It travels **only** in the per-session cookie **`__Host-nettheu_ds_<session-id>`** (`Secure`, `HttpOnly`, `SameSite=Lax`, `Path=/`, no `Domain`, `Max-Age` never beyond `expires_at`); bootstrap returns the non-secret id, and the Studio route stays `/san-pham/[slug]/thiet-ke`. A successful authenticated resume rotates the secret **atomically under compare-and-swap** with exactly one winner, immediate old-secret invalidation, **no** previous-secret grace and **no TTL extension**; ordinary autosave never rotates. Every state-changing request needs an exact allowed `Origin`, an allowed `Sec-Fetch-Site`, the host-only cookie and the expected revision; credentialed cross-origin CORS is disabled and unauthorized, missing, expired and wrong-chain requests share **one** safe non-enumerating error shape. **`SESSION_TTL = 30 days`, absolute from `created_at`** — never slid by reads, resume, rotation or autosave — expiring `ACTIVE → EXPIRED` (`TR-LC07-04`) on an hourly sweep and hard-deleting (`TR-LC07-05`) after a **24-hour** purge grace, scoped to the exclusively-owned session family; `SUBMITTED` retention and `TR-LC07-03` stay **APP5-owned**. Locked quotas: creation 5/hour (burst 2/minute), read 60/minute, authorization failures 10/15 minutes, mutations 30/minute, **1** in-flight mutation — keyed by an **ephemeral network HMAC under a rotating salt**, with no durable browser identity, no browser session quota and no raw IP persisted. Autosave carries the expected `autosave_revision`; a stale revision returns **`STALE_WRITE` without mutating** and the client refetches before retrying, so no idempotency table is needed. This **closes `O-008`** and the `design_sessions` half of **`DP-RET-01`**, and **supersedes** the earlier sliding `last_activity_at + TTL` direction in `DB4_DELETE_ARCHIVE_RETENTION_MAPPING.md` and `DB10_DATA_DURABILITY_MATRIX.md`. LC-07 keeps its four states and five transition ids; `ABANDONED` is not reintroduced. **`G03_DB_CONTRIBUTION = NONE`** — `session_secret_hash` (NOT NULL, unique), `expires_at`, `created_at`, `status` (LC-07 CHECK) and `autosave_revision` (`>= 0`) were **measured** to already exist, with no customer, email, phone, IP or fingerprint column anywhere on the table. New gate `pnpm check:app3-g03`; no application source, schema, migration, OpenAPI operation, generated client or Figma change ([`reports/APP3-G03-COMPLETION-REPORT.md`](./reports/APP3-G03-COMPLETION-REPORT.md)). **`APP3-G04` = `READY — NOT STARTED`**; `APP3-P01` and `APP3-P02` = `READY — NOT STARTED`; `APP3-DB01` = `REQUIRED — AWAITING_G04_CONTRIBUTION` — [`reports/APP3-G02-COMPLETION-REPORT.md`](./reports/APP3-G02-COMPLETION-REPORT.md); `APP3-B01` = `READY — NOT STARTED`; `APP3-B02` = `BLOCKED_BY_APP3_G04`; `APP3-A01` = `PLANNED — BLOCKED_BY_APP3_D01_AND_APP3_B01`; `APP3-DB01` = `CONDITIONAL — AWAITING_G02_AND_G04_CONTRIBUTIONS`; every other APP3 checkpoint `NOT STARTED` — [`reports/APP3-G01-COMPLETION-REPORT.md`](./reports/APP3-G01-COMPLETION-REPORT.md). **`APP3-ENTRY-BRANCH-RECONCILIATION` (2026-08-03)** — the accepted baseline was delivered on `docs/brd0-logo-system-concept03` while canonical `production` sat at `5c0ba1f`. The graph was strictly linear with both `production` and `origin/production` already ancestors of the reviewed HEAD, so `production` was **fast-forwarded** `5c0ba1f` → **`82ed3f3`** in one `git merge --ff-only` — no merge commit, no rebase, no cherry-pick, no history rewrite, and the feature branch preserved. The reviewed range was exactly four commits (`2a5d3bf` BRD0, `1216e98` `APP3-PRE-AUDIT-C1`, `9da79bf` + `82ed3f3` `APP2-X01-C1`) with no unreviewed work mixed in. Every gate was re-run **on `production`**: `pnpm quality` = `EXIT=0`. **`APP2-X01-C1` = `COMPLETE — REVIEW_ACCEPTED`** with the `check-app2-closure-artifacts.mjs` departure accepted as an `APPROVED_NARROW_SCOPE_DEVIATION` (review disposition, not an `IMP-D###`). Nothing pushed — `origin/production` remains at `8b5f3b0` — [`reports/ENTRY-BRANCH-RECONCILIATION-REPORT.md`](./reports/ENTRY-BRANCH-RECONCILIATION-REPORT.md). **`APP3-F01` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — controlled font acquisition, delivered under a **manual intervention directive** after the first `APP3-P01` attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED`. `IMP-D044` PO-10 requires the Design Document font registry to be concrete and file-backed, and the repository controlled **zero** font binaries, no licence artifact, no integrity baseline and no glyph-coverage evidence — `packages/styles/src/settings/_typography.scss` names General Sans and Inter as browser-resolved CSS fallbacks, which is UI styling authority, not a controlled asset, and the only WOFF2 files on disk belonged to a Next dev-tool inside `node_modules`, subsetted `latin`/`latin-ext`. The Product Owner selected **Inter v4.1** from the official upstream `rsms/inter`, resolved commit `e3a3d4c57d5ecc01453a575621882a384c1995a3`, under **SIL Open Font License 1.1**. Two **unmodified** variable WOFF2 files — `InterVariable.woff2` (`normal`, 352 240 B, `sha256:693b77d4…`) and `InterVariable-Italic.woff2` (`italic`, 387 976 B, `sha256:e564f652…`), both carrying `wght 100..900` — are vendored with the exact upstream `LICENSE.txt` at `packages/design-document/assets/fonts/inter/4.1/`, beside a provenance manifest and a Vietnamese-coverage manifest. Coverage was **measured from the real `cmap` tables** of the committed bytes using pinned `fonttools 4.55.3` in a throwaway environment outside every tracked path: **156 / 156 required code points covered, zero missing, in both files** — the Vietnamese letters, the six vowel bases, `Đ`/`đ`, the eight combining marks canonical decomposition needs, and every assigned code point in `U+1EA0..U+1EF9`. Inter ships italic as a separate file rather than an `ital` axis, so italic identity rests on the `OS/2` `fsSelection` and `head` `macStyle` bits, measured set on the italic file and clear on the upright one. New gate `node tools/check-app3-f01-font-assets.mjs`, which hashes the committed bytes against the manifests **and** re-derives the mandated repertoire itself, so a coverage manifest cannot pass by quietly shrinking what it claims to require. The `fontId = inter` registry entry, its files, licence path, styles, weight range and `REJECT_IF_CONTROLLED_FONT_UNAVAILABLE` fallback policy are locked in §6.9.4 and **reserved for `APP3-P01`**; F01 writes no TypeScript. **`APP3-P01` = `BLOCKED_BY_APP3-F01_REVIEW_ACCEPTANCE`**, its first attempt recorded forward as `FAILED — MANUAL_INTERVENTION_REQUIRED`, cause `NO_CONTROLLED_FONT_ASSET_OR_LICENSE_EVIDENCE`, resolution `APP3-F01`. No package implementation, registry code, schema, migration, OpenAPI, generated client, worker, Figma or UI change ([`reports/APP3-F01-COMPLETION-REPORT.md`](./reports/APP3-F01-COMPLETION-REPORT.md)). **`APP3-P01` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — the production Design Document foundation, **resumed** after the manual intervention that `APP3-F01` resolved; `APP3-F01` = **`COMPLETE — REVIEW_ACCEPTED`**. `packages/design-document` stops being an empty stub and now owns the engine-neutral schema (version **1**; root `schemaVersion`/`placement`/`elements`, unknown root or element fields rejected), five element kinds (`text`, `image`, `shape`, `freehand`, `group` — **no** `svg`, which arrives as a sanitized Asset), structural validation with **no coercion** of any kind, complexity validation, contextual asset/font validation, quantization, RFC 8785 canonicalization, document migration and the controlled runtime font registry. Array order is z-order and elements are addressed by stable opaque id, never by index. **Quantization** uses the one precision in accepted authority — `10_000`, four decimal places, `-0 → 0` — traced to `ADR-APP0-001 §4` plus `APP0-R01` evidence and recorded in an exported `…_AUTHORITY` constant rather than a bare literal; integers, strings, ids and array order are never quantized. The pipeline is `version → structure → complexity → quantization → **revalidation** → canonical bytes → size`: the second pass is load-bearing, because a width of `0.00004` is a legal positive number that quantizes to `0`, which the schema forbids. **Canonicalization** is RFC 8785 implemented in-package per ADR-DB1-012 (key order by UTF-16 code unit, never locale collation; ECMAScript number serialization; JSON escaping including lone surrogates; arrays never reordered), with **NFC rejected-if-absent at the input boundary** as ADR-DB1-012 §7 requires. **SHA-256 is server-only**, behind the `@embroidery/design-document/server` subpath — a build-time boundary rather than a runtime branch, because APP0-R01 measured that `crypto.subtle` is undefined outside a secure context; the gate walks the whole module graph reachable from the browser-safe root and fails on any `node:` import. The nine IMP-D044 limits are exported constants asserted at boundary **and** boundary + 1: canonical size is measured on **UTF-8 bytes** after quantization, text length counts **code points**, hidden and locked elements count, and one Asset placed twenty times counts **once** toward the asset and decoded-pixel budgets but twenty times toward image elements. Contextual validation requires a `READY` `NORMALIZED` derivative whose `width_px`/`height_px`/`media_type`/`byte_size` (the `APP3-DB01` quartet) are present, usable and equal to the document's intrinsic dimensions — source-asset metadata is explicitly not a substitute — and it reads no database and no object storage. The **font registry** is version 1 with one entry (`fontId = inter`) exactly as §6.9.4 locked it, including both binary SHA-256 values and `REJECT_IF_CONTROLLED_FONT_UNAVAILABLE`; the metadata is transcribed rather than read at runtime, and a package test re-reads the committed manifests **and re-hashes the committed binaries** so the transcription cannot drift. General Sans is not registered and no font byte, path or URL is ever serialized into a document. New gate `node tools/check-app3-p01.mjs` (17 checks, chains F01 and DB01) with 39 checker tests; **151** package unit tests across 9 suites; the package now builds to `dist` with `test`/`build`/`typecheck`/`lint`. `APP3-P02` stays `READY — NOT STARTED` and owns all geometry, bounds and px↔mm — the gate and an architecture suite both assert none of it entered P01. No API, OpenAPI, generated client, worker, schema, migration, Figma or UI change ([`reports/APP3-P01-COMPLETION-REPORT.md`](./reports/APP3-P01-COMPLETION-REPORT.md)). **`APP3-P01-C1` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — human review returned **`APP3-P01 = COMPLETE — CORRECTION_REQUIRED`** on one contradiction inside the delivered evidence: `IMP-D044`/`APP3-G04` key repeated-reference counting on the **Asset** — once toward the 20-asset limit **and** once toward the 33,554,432 decoded-pixel total — while the P01 implementation summed decoded pixels over unique **`derivativeId`** values. The two are equivalent only while each Asset is reached through one derivative, and diverge the moment one `assetId` appears with two `derivativeId` values, at which point derivative-keying overcharges the budget for a document the Asset rule says costs one decode. Contextual validation now accumulates `Map<assetId, {derivativeId, decodedPixels}>`: the first sighting of an Asset records its derivative and charges its pixels once, a repeat through the **same** derivative charges nothing, and a repeat through a **different** derivative emits `DERIVATIVE_METADATA_MISMATCH` on the **later** element's `$.elements[n].derivativeId` path. The ambiguity is **rejected, never resolved** — counting the first, last, smallest or largest derivative makes the budget depend on element order or an arbitrary choice, summing them charges one Asset twice, and normalizing every reference to one derivative silently rewrites what the customer placed; each breaks one of the two G04 rules, and the ambiguity is equally unanswerable for canonical media identity, intrinsic-dimension validation and deterministic rendering. The restriction is one derivative **per Asset within one document**; an Asset may still have many derivatives globally. Complexity validation already keyed its 20-asset budget on `assetId` and is unchanged, as is the 20-image-element count. No new decision id was created: this **restores** `IMP-D044` rather than amending it. Package tests **151 → 162** (the regression reproduces the exact delivered ambiguity — one `assetId`, two eligible READY NORMALIZED derivatives, different ids — and asserts rejection); P01 checker tests **39 → 45**, with the gate now asserting Asset-keyed accumulation, absence of derivative-keyed accumulation, the conflict check, G04's surviving wording and the presence of the regression itself. `node tools/check-app3-g04.mjs` and `node tools/check-app3-f01-font-assets.mjs` both pass unchanged. **`APP3-P01` = `COMPLETE — CORRECTION_DELIVERED_FOR_REVIEW`**. No schema, canonicalization, hashing, quantization, font, migration, geometry, API, worker, database, Figma or UI change ([`reports/APP3-P01-C1-COMPLETION-REPORT.md`](./reports/APP3-P01-C1-COMPLETION-REPORT.md)). **`APP3-G05` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — geometry coordinate, transform and bounds authority locked as **`IMP-D045`** (twelve rulings, §6.11), delivered under a **manual intervention directive** after the first `APP3-P02` attempt stopped at `FAILED — MANUAL INTERVENTION REQUIRED`. **`APP3-P01` = `COMPLETE — REVIEW_ACCEPTED`** after its `C1` correction. The stop was required because **no accepted document defined** rotation origin, rotation direction, scale origin, group coordinate semantics, parent/child composition order or a production bounds strategy, and the only executable source — the APP0-R01 spike — **contradicts itself**: the SVG adapter pivots on the box centre and nests children inside the group transform, Konva pivots top-left and zeroes the group's translation and rotation so only scale and opacity inherit, and Fabric pivots top-left and constructs an empty group so nothing inherits. The identical cross-engine canonical hash proved **serialization** agreement only — it is taken over the document, which stores `x`, `y`, `rotationDeg` whatever a renderer does with them — and APP0-R01 itself recorded that "the document box centre is not a reliable hit point. Hit testing must be asked of the engine, never derived from document geometry." The three adapters are now classified as **comparative research evidence, not production authority**; `IMP-D026` keeps native SVG as the renderer architecture and the renderer **implements** IMP-D045 rather than defining it. The locked model: top-left origin, x right, **y down**, degrees, positive `rotationDeg` **clockwise**; persisted `x`/`y` are the untransformed local-box top-left **in the parent frame** and `width`/`height` its positive unscaled dimensions; rotation and scale share the **untransformed local-box centre** `(width/2, height/2)` with **scale applied before rotation**; column-vector `p' = M x p` with `Mlocal = T(x + width/2, y + height/2) x R_clockwise(rotationDeg) x S(scaleX, scaleY) x T(-width/2, -height/2)` and `a = cos, b = sin, c = -sin, d = cos` under y-down; a group defines a **real local frame** whose children carry group-local coordinates composed **parent-outermost**, with the group's persisted box as local frame and pivot but **never** as bounds and **never** recomputed from descendants; bounds are `CONSERVATIVE_TRANSFORMED_AABB` (four local corners through the effective matrix, ellipse deliberately using its declared box, line/freehand expanded by `strokeWidthPx / 2`, group and document as unions, hidden and locked elements still counting, z-order irrelevant); containment is **blocking** and boundary-inclusive over the complete transformed AABB with **no** clamping, snapping or warn-but-persist; **`product_sides.px_per_mm`** is the sole px↔mm authority with no DPI assumption, no Area-derived ratio and disagreeing axes reported rather than averaged; and retired placement is `NOT_SELECTABLE` for `NEW_EDITING` while remaining valid for `HISTORICAL_RENDER`. **PO-12** binds all of it to `schemaVersion = 1`: because a v1 document records **no pivot marker**, changing any of these would silently relocate every stored design while its SHA-256 stayed valid — the hash is over the bytes, not the interpretation, and `ADR-DB1-012` binds approvals to that hash — so such a change is a **schema-semantic** event requiring a new decision, a schema-version increase and an explicit migration, never an internal refactor. **`G05_DB_CONTRIBUTION = NONE`**. New gate `node tools/check-app3-g05.mjs` (+ `-transform.mjs`) with 50 tests, asserting every fact as an **exact string** and asserting the forbidden alternatives too. **`APP3-P02` = `BLOCKED_BY_APP3-G05_REVIEW_ACCEPTANCE`**, its first attempt recorded forward as `FAILED — MANUAL_INTERVENTION_REQUIRED`, cause `GEOMETRY_SEMANTICS_NOT_AUTHORIZED_AND_SPIKE_DIVERGENT`, resolution `APP3-G05`. No geometry implementation, package source, renderer, API, worker, schema, migration, Figma or UI change ([`reports/APP3-G05-COMPLETION-REPORT.md`](./reports/APP3-G05-COMPLETION-REPORT.md)). **`APP3-G05-C1` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — human review returned **`APP3-G05 = COMPLETE — CORRECTION_REQUIRED`**, accepting the coordinate, pivot, matrix, group, containment, physical-scale and integrity rulings and closing the one bounds-authority gap the delivered report had itself disclosed. **The stroke omission:** `rectangle` and `ellipse` carry `strokeWidthPx` in P01, but PO-08 as delivered expanded only `line` and `freehand` — so a stroked rectangle could paint outside the accepted bounds and still pass containment, making the strategy named `CONSERVATIVE_TRANSFORMED_AABB` optimistic for two of its four stroked kinds. **The unstated line path:** P01's `line` is a `ShapeKind` defined solely by its transform box and stores no endpoints, so P02 could not compute deterministic bounds and no two renderers were obliged to draw the same segment. `IMP-D045` PO-08 is corrected **in place** before review acceptance — no new decision id, no other ruling touched, and **no v1 schema change** (every corrected rule reads existing fields). The corrected model: the stroke is **drawable geometry** for every kind storing `strokeWidthPx` (`rectangle`, `ellipse`, `line`, `freehand`), **centred on the local path**, expanding the local envelope by `strokeWidthPx / 2` on all four sides and participating in bounds, containment, physical-size validation, group bounds and document bounds; a zero stroke expands nothing; stroke is never renderer decoration, never exempted by the presence of a fill and never subtracted from `width`/`height`; `text` and `image` stay unexpanded. Local geometry is now exact per kind — **rectangle** = the declared-box boundary with fixed `lineJoin = miter`, `miterLimit = 4`; **ellipse** = inscribed in the declared box, envelope = declared box + half stroke, still the conservative transformed AABB and never the tight rotated ellipse/stroke AABB; **line** = the straight segment `(0,0)` → `(width,height)` with round cap and join, never a horizontal or vertical centreline, an arbitrary renderer path, a renderer-chosen diagonal or a path with unstored endpoints; **freehand** = an ordered polyline of straight segments over element-local points with round cap and join, no smoothing/interpolation/Bezier/simplification, a single point being a round dot of radius `strokeWidthPx / 2`. **Transform order is locked**: expand the local envelope, transform all four of **its** corners through the effective matrix, then take the document-space AABB — adding an unscaled half-stroke after the transform is forbidden because it ignores scale and rotation; the resulting overestimation for rotated or non-uniformly scaled line/freehand is accepted for v1 containment safety. Containment and physical-size validation are now explicitly **stroke-aware**: a shape whose fill fits while part of its ruled stroke envelope overhangs fails. The fixed cap/join/miter values are **version-level constants, not v1 document fields** — a customer never chooses them, adding them would have been the schema change this correction exists to avoid, and changing one changes painted bounds and is therefore schema-semantic under PO-12. Gate tests **50 → 75**, covering every dropped-stroke, redefined-line, smoothed-freehand, post-transform-expansion and cap/join-as-field variant. **`APP3-G05` = `COMPLETE — CORRECTION_DELIVERED_FOR_REVIEW`**; **`APP3-P02` = `BLOCKED_BY_APP3-G05_CORRECTION_REVIEW`**. No geometry implementation, package source, schema, renderer, API, worker, database, Figma or UI change ([`reports/APP3-G05-C1-COMPLETION-REPORT.md`](./reports/APP3-G05-C1-COMPLETION-REPORT.md)). **`APP3-P02` = `COMPLETE — REVIEW_DELIVERED`** (2026-08-04) — the production geometry foundation, **resumed** after the manual intervention that `APP3-G05` and `APP3-G05-C1` resolved; both are **`COMPLETE — REVIEW_ACCEPTED`**, as are `APP3-P01`/`APP3-P01-C1`. `packages/design-engine` stops being an empty stub and now implements `IMP-D045` exactly: matrices with `a = cos, b = sin, c = -sin, d = cos` (clockwise under y-down), the local matrix `T(x + width/2, y + height/2) x R(rotationDeg) x S(scaleX, scaleY) x T(-width/2, -height/2)` with **scale before rotation**, effective transforms composed **parent-outermost** over a root-first chain, stroke-aware local envelopes (`rectangle` and `ellipse` expanded by `strokeWidthPx / 2` under miter/4, the v1 `line` path locked to `(0,0)`→`(width,height)` with round cap and join, `freehand` an element-local straight polyline with round cap and join and a single point as a round dot), `CONSERVATIVE_TRANSFORMED_AABB` bounds that transform **the envelope's** four corners rather than the declared box's, group bounds as the union of descendant drawable AABBs and never the group's persisted box, blocking boundary-inclusive containment, physical-size validation through **`product_sides.px_per_mm`** with disagreeing axes reported rather than averaged, and `NEW_EDITING` versus `HISTORICAL_RENDER` placement modes. The package **never mutates a document**, never rebases or flattens a group and never moves an element to make it fit; called directly with input P01 never validated it fails safely with a typed finding rather than a throw or an unbounded walk (parent chains are depth-bounded, so a cycle cannot recurse). Quantization **reuses** P01's `quantizeNumber` — no second precision, and no epsilon anywhere near a placement or containment comparison, which are exact on the grid; the one machine-level threshold (`1e-12`) guards matrix inversion only. Only the **public root** of `@embroidery/design-document` is imported: no deep import, no Node built-in, no renderer, no database, no spike, and no module-level mutable state. **118 package tests** across 6 suites (including the `APP3-G05-C1` regressions: a stroked rectangle whose fill fits and stroke does not now fails containment, and a 3× scaled 8px stroke reaches −112 rather than the −104 a post-transform expansion would have given); new gate `node tools/check-app3-p02.mjs` with **34** checker tests, asserting the *semantics* rather than the presence of a function. `APP3-B07` and `APP3-B08` become `READY_BY_P01_AND_P02`, `APP3-B04` is now blocked only by `APP3-B06`, and `APP3-S11` has its geometry foundation. No renderer, API, OpenAPI, generated client, worker, schema, migration, Figma or UI change, and no `packages/design-document` source change ([`reports/APP3-P02-COMPLETION-REPORT.md`](./reports/APP3-P02-COMPLETION-REPORT.md)).

## 4. Required phase traceability additions

Each phase completion report must map:

- Product requirement IDs.
- Business rule IDs.
- Lifecycle/transition IDs.
- Database tables/guards/queries used.
- Figma sections/nodes used.
- OpenAPI operation IDs.
- Frontend routes/screens.
- Worker job names.
- Test suites/E2E journeys.
- Commit hashes.
- Deferred work and owning phase.
