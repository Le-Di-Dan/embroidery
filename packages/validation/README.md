# @embroidery/validation

Shared validation schemas once the validation library is locked by ADR.

## Status

Approved package boundary with no implementation yet (created in CP0).
`src/index.ts` is intentionally empty — no placeholder business logic.

## Boundary

No validation library is installed yet (open decision O-001). This boundary exists so schemas have a home once the library is locked.
