# @embroidery/observability

Shared logging/correlation abstractions once observability tooling is locked by ADR.

## Status

Approved package boundary with no implementation yet (created in CP0).
`src/index.ts` is intentionally empty — no placeholder business logic.

## Boundary

Observability tooling/vendor is an open decision. No vendor SDK may be added here without an ADR.
