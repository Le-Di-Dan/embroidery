# @embroidery/domain-types

Stable, cross-application domain primitives.

## Status

Approved package boundary with no implementation yet (created in CP0).
`src/index.ts` is intentionally empty — no placeholder business logic.

## Boundary

Only stable primitives shared across applications. Backend ORM entities and feature-local types never move here.
