{
  "name": "@embroidery/design-document",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-independent design-document types, validation, serialization and migrations",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./server": {
      "types": "./dist/server/index.d.ts",
      "default": "./dist/server/index.js"
    }
  },
  "//typesVersions": "TypeScript's node10 `moduleResolution` (which apps/api uses, via @embroidery/typescript-config/nestjs.json) does not read `exports`, so `@embroidery/design-document/server` resolves at runtime but not at type-check time. This maps the one subpath to the declaration file `exports` already names. It is a resolution hint only: no source changes, the two entry points stay exactly what `exports` says, and nothing here makes the Node-only hasher reachable from the package root.",
  "typesVersions": {
    "*": {
      "server": [
        "./dist/server/index.d.ts"
      ]
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "schema:generate": "node scripts/generate-schema.mjs",
    "schema:check": "node scripts/generate-schema.mjs --check"
  },
  "devDependencies": {
    "@embroidery/eslint-config": "workspace:*",
    "@embroidery/typescript-config": "workspace:*",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "jest": "^30.4.2",
    "rimraf": "^6.1.3",
    "ts-jest": "^29.4.11",
    "ts-json-schema-generator": "^2.9.0",
    "typescript": "^5.9.3"
  }
}
