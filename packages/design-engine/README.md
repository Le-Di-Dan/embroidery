# @embroidery/design-engine

Framework-independent geometry and transformation logic for the Design Studio.

## Status

Approved package boundary with no implementation yet (created in CP0).
`src/index.ts` is intentionally empty — no placeholder business logic.

## Boundary

Must stay framework-independent: pure geometry/transform logic only. No React, no canvas library (open decision), no DOM coupling.
