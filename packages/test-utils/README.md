# @embroidery/test-utils

Shared test helpers once the full testing stack is locked by ADR.

## Status

Approved package boundary with no implementation yet (created in CP0).
`src/index.ts` is intentionally empty — no placeholder business logic.

## Boundary

Test framework selection beyond bootstrap defaults is an open decision. Keep helpers tool-agnostic until locked.
