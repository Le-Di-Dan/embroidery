# @embroidery/ui

React components genuinely shared between storefront and admin.

## Status

Approved package boundary with no implementation yet (created in CP0).
`src/index.ts` is intentionally empty — no placeholder business logic.

## Boundary

Only components with real cross-application reuse. Feature-specific components remain inside their application. The UI component system is an open decision.
