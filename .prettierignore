# Generated / third-party
node_modules
.next
dist
out
coverage
.turbo
pnpm-lock.yaml
next-env.d.ts
.scannerwork

# Product documentation is a locked baseline — do not reformat (CLAUDE.md §19)
docs/
README.md
CLAUDE.md
manifest.json

# drizzle-kit generated migration metadata — never hand-formatted (CLAUDE.md §5).
# Reformatting these changes bytes drizzle treats as migration identity.
packages/database/migrations/meta/

# The generated Design Document JSON Schema (APP3-B08-C1). Its serialization is
# the generator's, and `schema:check` compares bytes — reformatting it makes the
# drift gate fail against an artifact nobody edited.
packages/design-document/src/schema/generated/design-document.schema.json

# The generated OpenAPI artifact and API client, for the same reason: both are
# compared by digest, and both are produced by their own writers.
packages/contracts/openapi/openapi.generated.json
packages/api-client/src/generated/

# Generated OpenAPI contract — byte identity is owned by `pnpm check:openapi`
# (deterministic serializer), not by Prettier. Never hand-formatted.
packages/contracts/openapi/

# Browser-tier (Playwright) run artifacts. These are already git-ignored, but
# Prettier does not read .gitignore, so without these entries a `pnpm quality`
# run that follows `pnpm quality:e2e` or `pnpm spike:editor:benchmark` fails in
# `format:check` on a machine-written artifact. Found by APP0-X01 closure
# validation. Committed benchmark results under `spikes/*/results/` are NOT
# ignored — they stay Prettier-formatted.
test-results/
playwright-report/
spikes/app0-r01-design-studio/bench-results/
spikes/app0-r01-design-studio/.bench-container/

# Brand-icon artifacts written by `tools/generate-brand-icons.mjs` from the one
# approved geometry source. Prettier and the generator agree on their shape
# today, but a formatter that reflowed either would make the generator's own
# `--check` gate fail against a file nobody edited. The generator is the
# authority for these; it is itself Prettier-formatted like any other tool.
apps/*/src/app/icon.svg
apps/storefront/src/features/design-studio/styles/_brand-mark.generated.scss
